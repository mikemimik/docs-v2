---
description: Découvrez les pratiques exemplaires en matière de sécurité pour les connexions de base de données personnalisées.
title: Pratiques exemplaires en matière de sécurité pour les connexions de base de données personnalisées
---

<div id="access-legacy-identity-storage-via-custom-api">
  ## Accéder au stockage d’identités hérité au moyen d’une API personnalisée
</div>

Nous vous recommandons de mettre en œuvre une API afin d’appliquer le principe du moindre privilège à votre stockage d’identités hérité, plutôt que d’ouvrir simplement un accès général par Internet.

Protéger le stockage d’identités hérité contre un accès général constitue une bonne pratique recommandée. Exposer directement à Internet, par exemple, une base de données d’identités héritée peut être extrêmement problématique : les interfaces de bases de données SQL et autres du même genre offrent des fonctionnalités très étendues, ce qui contrevient au principe du moindre privilège en matière de sécurité.

L’autre possibilité consiste à créer une API personnalisée simple, protégée par l’utilisation d’un <Tooltip tip="Jeton d’accès : information d’autorisation, sous la forme d’une chaîne opaque ou d’un JWT, utilisée pour accéder à une API." cta="Voir le glossaire" href="/docs/fr-CA/glossary?term=access+token">jeton d’accès</Tooltip>, que chaque script d’action peut appeler. Celle-ci servirait alors d’interface vers le référentiel d’identités hérité. Le flux Client Credentials Grant Flow peut ensuite être utilisé pour obtenir un jeton d’accès à partir d’un script, puis ce jeton peut être mis en cache pour être réutilisé dans l’objet `global` afin d’améliorer les performances. L’API peut alors fournir un nombre limité de points de terminaison protégés qui n’exécutent que les fonctions de gestion des identités héritées nécessaires (par ex. : lecture d’un utilisateur, changement de mot de passe).

Par défaut, Auth0 vous donnera un jeton pour n’importe quelle API si vous vous authentifiez avec succès et incluez l’<Tooltip tip="Audience : identifiant unique de l’audience d’un jeton émis. Nommée aud dans un jeton, sa valeur contient l’ID soit d’une application (Client ID) pour un ID Token, soit d’une API (API Identifier) pour un Access Token." cta="Voir le glossaire" href="/docs/fr-CA/glossary?term=audience">audience</Tooltip> appropriée. Restreindre l’accès à l’API du référentiel d’identités hérité en limitant l’allocation des jetons d’accès au moyen d’une rule empêchera toute utilisation non autorisée et atténuera un certain nombre de scénarios de vecteurs d’attaque, notamment lorsqu’une redirection vers `/authorize` est interceptée et que l’audience de l’API est ajoutée.

<Warning>
  Restreindre l’accès à l’API au moyen d’une Rule atténuera les scénarios de vecteurs d’attaque, notamment lorsqu’une redirection vers `/authorize` est interceptée et que l’audience de l’API est ajoutée, et garantira que seuls des accès utilisant des identifiants client précis sont autorisés.
</Warning>

<div id="access-to-legacy-identity-storage">
  ## Accès au stockage d’identités hérité
</div>

Que vous gériez l’accès au stockage d’identités hérité au moyen d’une API personnalisée ou que vous utilisiez l’interface native fournie, vous devriez restreindre l’accès à la liste d’adresses IP associées à votre tenant Auth0. Pour voir la liste des adresses IP, consultez [Auth0 IP Addresses for AllowLists](/docs/fr-CA/secure/security-guidance/data-security/allowlist). L’ajout d’URL à l’AllowList limite l’accès au référentiel d’identités hérité et garantit que seuls les scripts Actions de base de données personnalisée définis dans Auth0 sont autorisés.

<Warning>
  L’AllowList d’adresses IP d’Auth0 est partagée entre tous les tenants Auth0 définis pour une région. N’utilisez jamais l’allow list comme seule méthode pour sécuriser l’accès à votre référentiel d’identités hérité; cela pourrait entraîner des vulnérabilités de sécurité et permettre un accès non autorisé à vos utilisateurs.
</Warning>

<div id="learn-more">
  ## En savoir plus
</div>

* [Sécurité](/docs/fr-CA/secure)
* [Bonnes pratiques pour les jetons](/docs/fr-CA/secure/tokens/token-best-practices)