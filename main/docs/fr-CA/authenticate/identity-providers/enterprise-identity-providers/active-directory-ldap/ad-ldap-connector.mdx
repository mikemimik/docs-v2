---
description: Découvrez comment l’AD/LDAP Connector d’Auth0 relie votre serveur Active Directory ou LDAP local à Auth0 au moyen d’une connexion sortante.
sidebarTitle: Aperçu
title: AD/LDAP Connector
---

Auth0 s’intègre à Active Directory (AD) à l’aide de Lightweight Directory Access Protocol (LDAP), par l’intermédiaire d’un AD/LDAP Connector Active Directory/LDAP que vous installez sur votre réseau.

<Warning>
  L’AD/LDAP Connector est conçu pour les scénarios où votre entreprise contrôle le serveur AD/LDAP. Le connecteur ne doit **pas** être installé sur les serveurs de votre client.

  Dans les scénarios B2B où vous voulez permettre aux utilisateurs de votre client d’accéder à vos applications à l’aide de leurs identifiants d’entreprise, connectez-vous au service de fédération de votre client (par exemple, son propre service Auth0, ADFS ou tout fournisseur d’identité SAML) en utilisant l’une des connexions d’entreprise disponibles.

  Si vous installez un AD/LDAP Connector sur les serveurs de votre client et qu’il est connecté directement à votre domaine Auth0, vous devrez gérer directement les mots de passe des utilisateurs de votre client. **Auth0 déconseille fortement ce type de déploiement et n’offre aucun soutien pour ceux-ci.**
</Warning>

L’AD/LDAP Connector (1) sert de pont entre votre Active Directory/LDAP (2) et le service Auth0 (3). Ce pont est nécessaire, car AD/LDAP est généralement limité à votre réseau interne, tandis qu’Auth0 est un service infonuagique qui fonctionne dans un contexte complètement différent.

<Frame>![AD/LDAP Connector - intégrer AD/LDAP à Auth0 - diagramme](/docs/images/cdy7uua7fh8z/1xMpdHrtor0TR7F1gZ96zL/130e13f59b728fe14e00f1815b90103f/ldap-connect.png)</Frame>

Lorsqu’un utilisateur s’authentifie avec Auth0, il est redirigé vers l’AD/LDAP Connector, qui valide l’utilisateur auprès de votre service AD, puis renvoie les résultats de la validation à Auth0. L’AD/LDAP Connector prend en charge l’authentification LDAP, Kerberos et les certificats client.

Pour en savoir plus, consultez [Configurer l’authentification du connecteur AD/LDAP avec Kerberos](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/configure-ad-ldap-connector-with-kerberos) et [Configurer l’authentification du connecteur AD/LDAP avec des certificats client](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/configure-ad-ldap-connector-client-certificates).

<div id="caches">
  ## Caches
</div>

Le connecteur AD/LDAP met en cache les profils d’utilisateur et les informations d’identification (Auth0 stocke un hachage du mot de passe de l’utilisateur) afin d’assurer une disponibilité et des performances optimales, et met à jour les données chaque fois qu’un utilisateur se connecte. Le cache n’est utilisé que lorsque le connecteur est en panne ou inaccessible. Les données mises en cache sont toujours conservées, sauf si vous désactivez la mise en cache des informations d’identification dans le Dashboard. Les valeurs du cache sont sensibles à la casse, ce qui signifie que les tentatives de connexion ne réussiront que si les utilisateurs fournissent exactement le nom d’utilisateur mis en cache.

<div id="high-availability-and-load-balancing">
  ## Haute disponibilité et équilibrage de charge
</div>

Pour assurer la haute disponibilité et l’équilibrage de charge, vous pouvez installer plusieurs instances du connecteur. Toutes les connexions sont sortantes, du connecteur vers le serveur Auth0; il n’est donc généralement pas nécessaire de modifier votre pare-feu.

Chaque instance du cluster haute disponibilité sera toujours en service et connectée à Auth0. Auth0 enverra les transactions de connexion et les autres requêtes à n’importe lequel des connecteurs disponibles. Si l’une des instances tombe en panne en raison d’un problème de réseau ou de matériel, Auth0 redirigera les transactions de connexion vers un autre connecteur. Le fait d’avoir un déploiement hautement disponible vous permet aussi de mettre à jour le connecteur sans interruption de service.

<div id="openldap-directories">
  ## Annuaires OpenLDAP
</div>

Par défaut, le connecteur est hautement optimisé pour Active Directory. Pour le configurer avec tout autre annuaire LDAP (comme OpenLDAP), vous devez personnaliser ces paramètres dans le fichier `config.json` :

```json config.json lines
"LDAP_USER_BY_NAME": "(cn={0})",
"LDAP_SEARCH_QUERY": "(&(objectClass=person)(cn={0}))",
"LDAP_SEARCH_ALL_QUERY": "(objectClass=person)",
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Dans certains cas, il peut être préférable d’utiliser `uid` comme attribut plutôt que `cn`.
</Callout>

<div id="opendj-example">
  ### Exemple OpenDJ
</div>

Avec le **panneau de configuration OpenDJ**, vous pouvez récupérer la liste des attributs de chaque utilisateur. Cette liste peut vous aider à déterminer quel attribut sera utilisé comme nom d’utilisateur pour s’authentifier avec Auth0.

<Frame>![Schéma de l’exemple OpenDJ AD/LDAP Connector](/docs/images/cdy7uua7fh8z/14fTMlPqtzUicW2VTZPLPG/3ffa90a44eb864455ad3b77406f8ad32/opendj-attributes.png)</Frame>

Dans cet exemple, le `cn` de John est **johndoe** et le champ `mail` est défini sur **johndoe@contoso.com**. Si votre organisation veut que les utilisateurs s’authentifient à l’aide de leur nom d’utilisateur (`cn`), vous pouvez définir le paramètre `LDAP_USER_BY_NAME` sur `(cn={0})`. Par contre, si les utilisateurs doivent s’authentifier à l’aide de leur adresse courriel, vous devez le définir sur `(mail={0})`.

<div id="learn-more">
  ## En savoir plus
</div>

* [Configuration système requise pour AD/LDAP Connector](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/ad-ldap-connector-requirements)
* [Installer et configurer AD/LDAP Connector](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/install-configure-ad-ldap-connector)
* [Configurer l’authentification d’AD/LDAP Connector avec des certificats clients](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/configure-ad-ldap-connector-client-certificates)
* [Configurer l’authentification d’AD/LDAP Connector avec Kerberos](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/configure-ad-ldap-connector-with-kerberos)
* [Importer et exporter des configurations d’AD/LDAP Connector](/docs/fr-CA/authenticate/identity-providers/enterprise-identity-providers/active-directory-ldap/ad-ldap-connector/import-export-ad-ldap-connector-configs)