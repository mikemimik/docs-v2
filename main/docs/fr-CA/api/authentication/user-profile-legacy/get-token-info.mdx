---
title: "Infos sur le jeton"
description: "Point de terminaison déprécié qui valide un JWT et renvoie le profil utilisateur associé ; utilisez plutôt /userinfo."
---

import { ResponseSchema } from '/snippets/ResponseSchema.jsx';

<div id="endpoint">
  ## Point de terminaison
</div>

`POST /tokeninfo`

<Note>
  Ce point de terminaison fait partie du pipeline d’authentification héritée et sera désactivé pour les utilisateurs de notre pipeline le plus récent, conforme à OIDC. Nous vous encourageons plutôt à utiliser le [point de terminaison /userinfo](#get-user-info). Pour en savoir plus sur le plus récent pipeline d’authentification, consultez [Introducing OIDC Conformant Authentication](/api-auth/intro).
</Note>

Ce point de terminaison valide un JSON Web Token (JWT) (signature et expiration) et renvoie les renseignements de l’utilisateur associés à l’ID utilisateur indiqué dans la propriété `sub` du jeton.

<div id="remarks">
  ### Remarques
</div>

* Ce point de terminaison retourne trois en-têtes de réponse HTTP qui fournissent des données pertinentes sur ses limites de débit :
  * `X-RateLimit-Limit` : Nombre de requêtes autorisées par minute.
  * `X-RateLimit-Remaining` : Nombre de requêtes disponibles. Chaque nouvelle requête réduit ce nombre de 1. Chaque minute qui passe ajoute de nouveau des requêtes; ce nombre augmente donc de 1 à chaque fois.
  * `X-RateLimit-Reset` : Temps restant avant la réinitialisation de la limite de débit (`X-RateLimit-Limit`). La valeur est exprimée en [secondes d’époque UTC](https://en.wikipedia.org/wiki/Unix_time).

<div id="learn-more">
  ### En savoir plus
</div>

* [Structure du profil utilisateur](https://auth0.com/docs/manage-users/user-accounts/user-profiles/user-profile-structure)
* [Politique de limitation du débit de l’API Auth0](https://auth0.com/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy)

<div id="response-schema">
  ## Schéma de réponse
</div>

<ResponseSchema>
  <ResponseField name="email_verified" type="boolean">
    Indique si l&#39;adresse courriel de l&#39;utilisateur a été vérifiée.
  </ResponseField>

  <ResponseField name="email" type="string">
    L&#39;adresse courriel de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="clientID" type="string">
    L&#39;ID client associé au jeton.
  </ResponseField>

  <ResponseField name="updated_at" type="string">
    Date et heure de la dernière mise à jour des renseignements de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="name" type="string">
    Le nom complet de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="picture" type="string">
    URL de la photo de profil de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="user_id" type="string">
    L&#39;identifiant unique de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="nickname" type="string">
    Le surnom de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="provider" type="string">
    Le fournisseur d&#39;identité de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="connection" type="string">
    La connexion utilisée pour authentifier l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="isSocial" type="boolean">
    Indique si l&#39;utilisateur s&#39;est authentifié au moyen d&#39;un fournisseur social.
  </ResponseField>

  <ResponseField name="created_at" type="string">
    Date et heure de création de l&#39;utilisateur.
  </ResponseField>

  <ResponseField name="global_client_id" type="string">
    L&#39;ID client global.
  </ResponseField>
</ResponseSchema>

<div id="response-messages">
  ## Messages de réponse
</div>

| Statut | Description                                                   |
| ------ | ------------------------------------------------------------- |
| 200    | Réponse réussie contenant les renseignements de l’utilisateur |