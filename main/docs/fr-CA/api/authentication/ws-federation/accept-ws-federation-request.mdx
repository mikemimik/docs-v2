---
title: "Accepter une requête WS-Federation"
description: "Accepter une requête WS-Federation pour lancer un processus de connexion."
---

<div id="endpoint">
  ## Endpoint
</div>

`GET /wsfed/{clientId}`

Cet endpoint accepte une requête WS-Federation pour amorcer un login.

<div id="remarks">
  ### Remarques
</div>

* Le paramètre `wtrealm` doit être dans l’un de ces formats :
  * `urn:clientID` (par exemple, `urn:${account.clientId}`)
  * Si ce paramètre ne commence pas par un URN, l’array `client.clientAliases` est utilisée pour la recherche. Cette valeur ne peut être définie qu’avec la Management API [/api/v2/clients](https://auth0.com/docs/api/management/v2/clients/get-clients).
* Le paramètre `whr` est associé à la connexion comme suit : `urn:CONNECTION_NAME`. Par exemple, `urn:google-oauth2` indique une connexion avec Google. Si aucun paramètre `whr` n’est inclus, l’utilisateur sera redirigé vers l’[Auth0 Login Page](https://auth0.com/docs/login_page).

<div id="learn-more">
  ### En savoir plus
</div>

* [WS-Federation](https://auth0.com/docs/authenticate/protocols/ws-fed-protocol)

<div id="path-parameters">
  ## Paramètres de chemin d’accès
</div>

<ParamField path="client-id" type="string" required>
  Le `client-id` de votre application.
</ParamField>

<div id="query-parameters">
  ## Paramètres de requête
</div>

<ParamField query="wtrealm" type="string">
  Peut être utilisé à la place de `client-id`.
</ParamField>

<ParamField query="whr" type="string">
  Le nom de la connexion (utilisé pour éviter d’afficher la page de connexion).
</ParamField>

<ParamField query="wctx" type="string">
  L’état de votre application.
</ParamField>

<ParamField query="wreply" type="string">
  L’URL de rappel.
</ParamField>

<div id="response-messages">
  ## Messages de réponse
</div>

| Statut | Description                                 |
| ------ | ------------------------------------------- |
| 200    | Réponse réussie à la requête WS-Federation. |