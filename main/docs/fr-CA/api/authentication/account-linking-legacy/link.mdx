---
title: "Lier"
description: "Point de terminaison obsolète pour lier des comptes d’utilisateurs; utilisez plutôt la Management API."
---

<div id="endpoint">
  ## Point de terminaison
</div>

`GET /authorize`

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Ce point de terminaison est **obsolète** pour la liaison de comptes. Utilisez plutôt [POST /api/v2/users/{id}/identities](/api/management/v2/users/post-identities). Pour en savoir plus, consultez l’[avis de migration](/migrations/past-migrations#account-linking-removal).
</Callout>

Appelez ce point de terminaison lorsqu’un utilisateur souhaite associer une deuxième méthode d’authentification (par exemple, une connexion de base de données avec nom d’utilisateur et mot de passe, à Facebook).

Ce point de terminaison déclenche le flux de connexion pour associer un compte existant à un nouveau compte. Il renvoie une redirection 302 vers la `connection` que l’utilisateur actuel souhaite ajouter. L’utilisateur est identifié par le jeton d’accès renvoyé après une connexion réussie.

<div id="remarks">
  ### Remarques
</div>

* La valeur `redirect_uri` doit être définie comme une URL de rappel valide dans les [paramètres de votre application](https://manage.auth0.com/dashboard).

<div id="learn-more">
  ### En savoir plus
</div>

* [Lier des comptes d’utilisateur](https://auth0.com/docs/manage-users/user-accounts/user-account-linking/link-user-accounts)
* [Scénario de liaison de comptes d’utilisateur déclenché par l’utilisateur](http://auth0.com/docs/users/references/link-accounts-user-initiated-scenario)
* [Scénario de liaison de comptes d’utilisateur côté serveur](https://auth0.com/docs/ja-jp/manage-users/user-accounts/user-account-linking/suggested-account-linking-server-side-implementation)

<div id="query-parameters">
  ## Paramètres de requête
</div>

<ParamField query="response_type" type="string" required>
  Utilisez `code` pour les flux côté serveur et `token` pour les flux côté client.
</ParamField>

<ParamField query="client_id" type="string" required>
  Le `client_id` de votre application.
</ParamField>

<ParamField query="connection" type="string">
  Le nom de la connexion configurée pour votre application. Si cette valeur est nulle, l’utilisateur sera redirigé vers [Auth0 Login Page](https://auth0.com/#/login_page) et le Login Widget s’affichera en utilisant la première connexion de base de données.
</ParamField>

<ParamField query="redirect_uri" type="string" required>
  L’URL vers laquelle Auth0 redirigera le navigateur une fois l’autorisation accordée par l’utilisateur.
</ParamField>

<ParamField query="access_token" type="string" required>
  Le jeton d’accès de l’utilisateur connecté.
</ParamField>

<div id="response-messages">
  ## Messages de réponse
</div>

| Statut | Description                                           |
| ------ | ----------------------------------------------------- |
| 302    | Redirection vers la connexion pour la liaison de comptes |