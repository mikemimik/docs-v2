---
mode: wide
sidebarTitle: .NET Android & iOS
title: Ajouter la connexion à votre application .NET pour Android et iOS
---

import {HowToSchema} from "/snippets/HowToSchema.jsx";

<HowToSchema />

<Accordion title="Utilisez l’IA pour intégrer Auth0" icon="microchip-ai" iconType="solid" defaultOpen>
  Si vous utilisez un assistant de programmation IA comme Claude Code, Cursor ou GitHub Copilot, vous pouvez ajouter l’authentification Auth0 automatiquement en quelques minutes à l’aide d’[Agent Skills](https://agentskills.io/home).

  **Installer :**

  ```bash
  npx skills add auth0/agent-skills --skill auth0-quickstart --skill auth0-net-android --skill auth0-net-ios
  ```

  **Ensuite, demandez à votre assistant IA :**

  ```text
  Add Auth0 authentication to my .NET Android & iOS app
  ```

  Votre assistant IA créera automatiquement votre application Auth0, récupérera les identifiants, installera le SDK Auth0 OidcClient, configurera les paramètres propres à la plateforme et implémentera les flux de connexion et de déconnexion. [Documentation complète sur Agent Skills →](/docs/fr-CA/quickstart/agent-skills)
</Accordion>

<div id="get-started">
  ## Premiers pas
</div>

<Steps>
  <Step title="Créer un nouveau projet .NET" stepNumber={1}>
    Créez un nouveau projet .NET MAUI ou .NET Android/iOS pour ce Quickstart.

    <Tabs>
      <Tab title=".NET MAUI">
        **Dans Visual Studio 2022 ou une version ultérieure :**

        1. **Fichier** → **Nouveau** → **Projet**
        2. Sélectionnez le modèle **.NET MAUI App**
        3. Configurez votre projet :
           * **Nom du projet** : `Auth0MauiSample`
           * **Emplacement** : Choisissez l’emplacement souhaité
           * **Framework** : **.NET 8.0 ou version ultérieure**
        4. Cliquez sur **Créer**
      </Tab>

      <Tab title=".NET Android">
        **Dans Visual Studio 2022 ou une version ultérieure :**

        1. **Fichier** → **Nouveau** → **Projet**
        2. Sélectionnez le modèle **Android App (.NET)**
        3. Configurez votre projet :
           * **Nom du projet** : `Auth0AndroidSample`
           * **Framework** : **.NET 8.0 ou version ultérieure**
        4. Cliquez sur **Créer**
      </Tab>

      <Tab title=".NET iOS">
        **Dans Visual Studio 2022 (Mac) ou Visual Studio for Mac :**

        1. **Fichier** → **Nouveau** → **Projet**
        2. Sélectionnez le modèle **iOS App (.NET)**
        3. Configurez votre projet :
           * **Nom du projet** : `Auth0iOSSample`
           * **Framework** : **.NET 8.0 ou version ultérieure**
        4. Cliquez sur **Créer**
      </Tab>
    </Tabs>

    <Info>
      Ce Quickstart porte sur .NET Android et iOS, la nouvelle génération de Xamarin.Android et Xamarin.iOS. Si vous utilisez encore Xamarin, vous pouvez suivre ce guide, car l’intégration est identique et les SDKs sont compatibles.
    </Info>
  </Step>

  <Step title="Installer le SDK Auth0" stepNumber={2}>
    Ajoutez le SDK Auth0 OIDC Client à votre projet.

    <Tabs>
      <Tab title="Console du gestionnaire de package">
        Ouvrez la console du gestionnaire de package (**Affichage** → **Autres fenêtres** → **Console du gestionnaire de package**) et installez le package approprié :

        **Pour .NET Android :**

        ```bash Package Manager Console
        Install-Package Auth0.OidcClient.AndroidX
        ```

        **Pour .NET iOS :**

        ```bash Package Manager Console
        Install-Package Auth0.OidcClient.iOS
        ```

        **Pour .NET MAUI (les deux plateformes) :**

        ```bash Package Manager Console
        Install-Package Auth0.OidcClient.MAUI
        ```
      </Tab>

      <Tab title="Visual Studio for Mac">
        1. Dans le **Solution Pad**, faites un Ctrl+clic (ou un clic droit) sur le dossier **Packages**
        2. Sélectionnez **Add Packages...**
        3. Recherchez :
           * `Auth0.OidcClient.AndroidX` pour Android
           * `Auth0.OidcClient.iOS` pour iOS
           * `Auth0.OidcClient.MAUI` pour les projets MAUI
        4. Sélectionnez le package, puis cliquez sur **Add Package**
      </Tab>

      <Tab title=".NET CLI">
        Exécutez la commande suivante dans le répertoire de votre projet :

        **Pour .NET Android :**

        ```bash Terminal
        dotnet add package Auth0.OidcClient.AndroidX
        ```

        **Pour .NET iOS :**

        ```bash Terminal
        dotnet add package Auth0.OidcClient.iOS
        ```

        **Pour .NET MAUI :**

        ```bash Terminal
        dotnet add package Auth0.OidcClient.MAUI
        ```
      </Tab>
    </Tabs>

    <Tip>
      Le SDK Auth0 OIDC Client gère tous les détails des protocoles OAuth 2.0 et OIDC et fournit une API simple pour l’authentification.
    </Tip>
  </Step>

  <Step title="Configurez votre Auth0 App" stepNumber={3}>
    Créez une nouvelle application dans votre tenant Auth0 et configurez-la pour mobile.

    1. Accédez au [Auth0 Dashboard](https://manage.auth0.com/dashboard/)
    2. Cliquez sur **Applications** → **Applications** → **Create Application**
    3. Saisissez un nom pour votre application, sélectionnez **Native** comme type d’application, puis cliquez sur **Create**
    4. Ouvrez l’onglet **Settings** dans la page Application Details
    5. Prenez en note votre **Domain** et votre **Client ID** - vous en aurez besoin à l’étape suivante

    **Configurer les URL de rappel :**

    Dans l’onglet **Settings**, ajoutez les URL suivantes :

    **Allowed Callback URLs :**

    <Tabs>
      <Tab title="Android">
        ```
        YOUR_ANDROID_PACKAGE_NAME://{yourDomain}/android/YOUR_ANDROID_PACKAGE_NAME/callback
        ```

        Remplacez :

        * `YOUR_ANDROID_PACKAGE_NAME` par le nom de package de votre application (p. ex. `com.mycompany.myapp`)
        * `{yourDomain}` par votre domaine Auth0 (p. ex. `dev-abc123.us.auth0.com`)

        Exemple : `com.mycompany.myapp://dev-abc123.us.auth0.com/android/com.mycompany.myapp/callback`
      </Tab>

      <Tab title="iOS">
        ```
        YOUR_BUNDLE_IDENTIFIER://{yourDomain}/ios/YOUR_BUNDLE_IDENTIFIER/callback
        ```

        Remplacez :

        * `YOUR_BUNDLE_IDENTIFIER` par l’identifiant de bundle de votre application (p. ex. `com.mycompany.myapp`)
        * `{yourDomain}` par votre domaine Auth0 (p. ex. `dev-abc123.us.auth0.com`)

        Exemple : `com.mycompany.myapp://dev-abc123.us.auth0.com/ios/com.mycompany.myapp/callback`
      </Tab>
    </Tabs>

    **Allowed Logout URLs :**

    Utilisez les mêmes URL que pour vos URL de rappel.

    <Warning>
      Assurez-vous que les URL de rappel et de logout sont en **minuscules**. Si les URL ne correspondent pas, l’authentification échouera.
    </Warning>

    <Info>
      Les **Allowed Callback URLs** sont essentielles pour la sécurité - elles garantissent que les utilisateurs sont redirigés de façon sécuritaire vers votre application après l’authentification. Sans URL correspondante, le processus de connexion échouera.

      Les **Allowed Logout URLs** offrent une expérience fluide lorsque les utilisateurs se déconnectent, en les redirigeant vers votre application au lieu de les laisser sur une page Auth0.
    </Info>
  </Step>

  <Step title="Initialiser le client Auth0" stepNumber={4}>
    Créez une instance d’`Auth0Client` pour communiquer avec Auth0.

    <Tabs>
      <Tab title="Android - MainActivity">
        ```cs MainActivity.cs lines
        using Auth0.OidcClient;
        using Android.App;
        using Android.Content;

        [Activity(Label = "Auth0Sample", MainLauncher = true, Icon = "@drawable/icon",
            LaunchMode = LaunchMode.SingleTask)]
        [IntentFilter(
            new[] { Intent.ActionView },
            Categories = new[] { Intent.CategoryDefault, Intent.CategoryBrowsable },
            DataScheme = "YOUR_ANDROID_PACKAGE_NAME",
            DataHost = "{yourDomain}",
            DataPathPrefix = "/android/YOUR_ANDROID_PACKAGE_NAME/callback")]
        public class MainActivity : Activity
        {
            private Auth0Client auth0Client;

            protected override void OnCreate(Bundle savedInstanceState)
            {
                base.OnCreate(savedInstanceState);

                // Initialiser le client Auth0
                auth0Client = new Auth0Client(new Auth0ClientOptions
                {
                    Domain = "{yourDomain}",
                    ClientId = "{yourClientId}"
                }, this);
            }

            protected override async void OnNewIntent(Intent intent)
            {
                base.OnNewIntent(intent);
                Auth0.OidcClient.ActivityMediator.Instance.Send(intent.DataString);
            }
        }
        ```

        <Warning>
          Remplacez `YOUR_ANDROID_PACKAGE_NAME`, `{yourDomain}` et `{yourClientId}` par vos valeurs réelles. Assurez-vous que tout le texte de `DataScheme`, `DataHost` et `DataPathPrefix` est en **minuscules**.
        </Warning>

        <Info>
          Le `IntentFilter` enregistre votre application pour qu’elle puisse gérer l’URL de rappel. `LaunchMode.SingleTask` garantit qu’Android ne crée pas de nouvelle instance de l’activité lorsque l’URL de rappel est appelée.
        </Info>
      </Tab>

      <Tab title="iOS - AppDelegate">
        Commencez par enregistrer le schéma d’URL dans votre `Info.plist` :

        ```xml Info.plist lines
        <key>CFBundleURLTypes</key>
        <array>
            <dict>
                <key>CFBundleTypeRole</key>
                <string>None</string>
                <key>CFBundleURLName</key>
                <string>Auth0</string>
                <key>CFBundleURLSchemes</key>
                <array>
                    <string>YOUR_BUNDLE_IDENTIFIER</string>
                </array>
            </dict>
        </array>
        ```

        Ensuite, configurez votre `AppDelegate.cs` :

        ```cs AppDelegate.cs lines
        using Auth0.OidcClient;
        using Foundation;
        using UIKit;

        [Register("AppDelegate")]
        public class AppDelegate : UIApplicationDelegate
        {
            private Auth0Client auth0Client;

            public override bool FinishedLaunching(UIApplication application, NSDictionary launchOptions)
            {
                // Initialiser le client Auth0
                auth0Client = new Auth0Client(new Auth0ClientOptions
                {
                    Domain = "{yourDomain}",
                    ClientId = "{yourClientId}"
                });

                return true;
            }

            public override bool OpenUrl(UIApplication application, NSUrl url,
                string sourceApplication, NSObject annotation)
            {
                ActivityMediator.Instance.Send(url.AbsoluteString);
                return true;
            }
        }
        ```

        <Warning>
          Remplacez `YOUR_BUNDLE_IDENTIFIER`, `{yourDomain}` et `{yourClientId}` par vos valeurs réelles.
        </Warning>
      </Tab>
    </Tabs>

    <Tip>
      Stockez votre domaine Auth0 et votre ID client dans un fichier de configuration ou dans les paramètres de l’application au lieu de les coder en dur afin d’en faciliter la maintenance.
    </Tip>
  </Step>

  <Step title="Implémenter la connexion et la déconnexion" stepNumber={5}>
    Ajoutez des méthodes pour prendre en charge l’authentification des utilisateurs.

    **Implement Login :**

    ```cs Authentication.cs lines
    public async Task LoginAsync()
    {
        var loginResult = await auth0Client.LoginAsync();

        if (!loginResult.IsError)
        {
            // Authentification réussie
            var accessToken = loginResult.AccessToken;
            var idToken = loginResult.IdentityToken;
            var user = loginResult.User;

            // Enregistrer les identifiants et mettre à jour l'interface
            Console.WriteLine($"Logged in as: {user.FindFirst("name")?.Value}");
        }
        else
        {
            // Gérer l'erreur d'authentification
            Console.WriteLine($"Login error: {loginResult.Error}");
        }
    }
    ```

    **Implement Déconnexion :**

    ```cs Authentication.cs lines
    public async Task LogoutAsync()
    {
        var logoutResult = await auth0Client.LogoutAsync();

        if (logoutResult == BrowserResultType.Success)
        {
            // Effacer les identifiants stockés
            // Mettre à jour l'interface utilisateur vers l'état déconnecté
            Console.WriteLine("Logged out successfully");
        }
    }
    ```

    <Info>
      La méthode `LoginAsync()` lance le navigateur du système (ou Chrome Custom Tabs sur Android) pour afficher la page Universal Login d’Auth0. Après l’authentification, l’utilisateur est redirigé vers votre application à l’aide de l’URL de rappel.
    </Info>

    <Tip>
      Ajoutez ces méthodes à votre MainActivity (Android) ou à un ViewController (iOS), puis appelez-les lorsque les utilisateurs appuient sur les boutons Login/Logout.
    </Tip>
  </Step>

  <Step title="Lancez votre application" stepNumber={6}>
    Compilez et exécutez votre application.

    <Tabs>
      <Tab title="Visual Studio (Windows)">
        **Pour Android :**

        1. Sélectionnez un émulateur Android ou un appareil connecté dans la liste déroulante des appareils
        2. Appuyez sur **F5** ou cliquez sur le bouton **Run**
        3. L’application est compilée, déployée et lancée

        **Pour iOS (nécessite un hôte de compilation Mac) :**

        1. Connectez-vous à votre hôte de compilation Mac
        2. Sélectionnez un simulateur ou un appareil iOS dans la liste déroulante des appareils
        3. Appuyez sur **F5** ou cliquez sur le bouton **Run**
      </Tab>

      <Tab title="Visual Studio pour Mac">
        1. Sélectionnez votre plateforme cible (Android/iOS) dans la liste déroulante des appareils
        2. Sélectionnez un émulateur/simulateur ou un appareil connecté
        3. Appuyez sur **⌘+Return** ou cliquez sur le bouton **Run**
        4. L’application est compilée et lancée
      </Tab>

      <Tab title=".NET CLI">
        **Pour Android :**

        ```bash Terminal
        dotnet build -f net8.0-android
        dotnet run -f net8.0-android
        ```

        **Pour iOS :**

        ```bash Terminal
        dotnet build -f net8.0-ios
        dotnet run -f net8.0-ios
        ```
      </Tab>
    </Tabs>

    **Déroulement attendu :**

    1. L’application s’ouvre avec un bouton **Se connecter**
    2. Touchez **Se connecter** → Le navigateur ou un onglet personnalisé Chrome s’ouvre → Terminez l’authentication
    3. Retour automatique à votre application
    4. L’utilisateur est authentifié avec succès

    <Tip>
      Au premier lancement, iOS peut vous demander de confirmer l’ouverture du navigateur pour l’authentication. Il s’agit d’un comportement normal et attendu.
    </Tip>
  </Step>
</Steps>

<Check>
  **Point de contrôle**

  Vous disposez maintenant d’une expérience de connexion avec Auth0 entièrement fonctionnelle dans votre application .NET Android ou iOS. L’application utilise le navigateur du système pour une authentification sécurisée et gère automatiquement le flux de rappel.
</Check>

***

<div id="access-user-information">
  ## Accéder aux informations sur l’utilisateur
</div>

Après une authentification réussie, vous pouvez accéder aux informations sur l’utilisateur à partir du résultat de la connexion.

<div id="authentication-result">
  ### Résultat d’authentification
</div>

La méthode `LoginAsync()` renvoie un objet `LoginResult` contenant :

```cs UserInfo.cs lines
var loginResult = await auth0Client.LoginAsync();

if (!loginResult.IsError)
{
    // Jetons d'accès
    var accessToken = loginResult.AccessToken;
    var idToken = loginResult.IdentityToken;
    var refreshToken = loginResult.RefreshToken;

    // Accéder aux claims de l'utilisateur
    var user = loginResult.User;
    var name = user.FindFirst("name")?.Value;
    var email = user.FindFirst("email")?.Value;
    var picture = user.FindFirst("picture")?.Value;

    Console.WriteLine($"Name: {name}");
    Console.WriteLine($"Email: {email}");
}
```

<div id="iterate-through-all-claims">
  ### Parcourir l’ensemble des claims
</div>

Pour voir tous les renseignements disponibles sur l’utilisateur :

```cs UserClaims.cs lines
if (!loginResult.IsError)
{
    foreach (var claim in loginResult.User.Claims)
    {
        Console.WriteLine($"{claim.Type}: {claim.Value}");
    }
}
```

<Info>
  Les claims exacts renvoyés dépendent des scopes demandés. Pour en savoir plus, consultez [Using Scopes](https://auth0.github.io/auth0-oidc-client-net/documentation/advanced-scenarios/scopes.html) dans la documentation d’Auth0 OIDC Client.
</Info>

<div id="request-custom-scopes">
  ### Demander des scopes personnalisés
</div>

Pour obtenir plus d’information sur l’utilisateur, indiquez les scopes lors de la création de l’Auth0Client :

```cs CustomScopes.cs lines
var auth0Client = new Auth0Client(new Auth0ClientOptions
{
    Domain = "{yourDomain}",
    ClientId = "{yourClientId}",
    Scope = "openid profile email offline_access read:posts"
});
```

***

<div id="troubleshooting-advanced">
  ## Dépannage et options avancées
</div>

<Accordion title="Problèmes courants et solutions">
  ### Le navigateur ne redirige pas vers l’application

  **Solutions :**

  1. Vérifiez que les URL de rappel dans Auth0 Dashboard correspondent exactement au nom du package ou au Bundle Identifier de votre application
  2. Assurez-vous que les URL de rappel sont en **minuscules**
  3. Vérifiez que `DataScheme`, `DataHost` et `DataPathPrefix` (Android) ou le schéma d’URL (iOS) correspondent à votre configuration
  4. Nettoyez et régénérez votre projet

  ### L’authentification échoue avec l’erreur &quot;Invalid Callback URL&quot;

  **Correctif :**

  * Vérifiez de nouveau que votre URL de rappel dans Auth0 Dashboard correspond au format suivant :
    * Android : `packagename://yourdomain/android/packagename/callback`
    * iOS : `bundleidentifier://yourdomain/ios/bundleidentifier/callback`
  * Assurez-vous que l’URL est en minuscules
  * Vérifiez que le Domain dans votre code correspond au Domain dans Auth0 Dashboard

  ### LoginAsync() se bloque ou ne se termine jamais

  **Solutions :**

  * Assurez-vous que le filtre d’intention (Android) ou le schéma d’URL (iOS) est correctement configuré
  * Vérifiez que `OnNewIntent()` (Android) ou `OpenUrl()` (iOS) appelle `ActivityMediator`
  * Vérifiez que votre application peut ouvrir le navigateur système
  * Vérifiez la connectivité réseau

  ### Erreur : &quot;Default App must use Token Endpoint Authentication Method &#39;None&#39;&quot;

  **Correctif :**

  1. Accédez aux Application Settings de votre Application Auth0 dans le Dashboard
  2. Faites défiler jusqu’à **Application Properties**
  3. Définissez **Application type** sur **Native**
  4. Définissez **Token Endpoint Authentication Method** sur **None**
  5. Cliquez sur **Save Changes**

  ### iOS : Le navigateur ne s’ouvre pas

  **Solutions :**

  * Vérifiez que `Info.plist` contient la bonne configuration de schéma d’URL
  * Vérifiez que `OpenUrl()` est implémenté dans `AppDelegate`
  * Assurez-vous que la cible de déploiement iOS est compatible avec votre version de l’Auth0 SDK
</Accordion>

<Accordion title="Considérations pour la production">
  ### Best Practices de sécurité

  * **Stockage sécurisé des jetons** : Utilisez le stockage sécurisé propre à la plateforme (Android Keystore, iOS Keychain) pour stocker les jetons
  * **Actualisation des jetons** : Implémentez la gestion du refresh token pour maintenir les sessions utilisateur
  * **Épinglage de certificat** : Envisagez l’épinglage de certificat pour renforcer la sécurité de l’API
  * **ProGuard/obfuscation du code** : Ajoutez les rules appropriées si vous utilisez l’obfuscation du code sur Android

  ### Exigences de l’App Store

  * **Politique de confidentialité** : Assurez-vous que votre application dispose d’une politique de confidentialité qui décrit l’utilisation d’Auth0
  * **Gestion des données utilisateur** : Suivez les lignes directrices de la plateforme pour le traitement des données d’authentification des utilisateurs
  * **Liens profonds** : Testez minutieusement la gestion des URL de rappel dans différents scénarios
  * **Exigences réseau** : Gérez les scénarios hors ligne de façon fluide

  ### Optimisation des performances

  * **Mettre Auth0Client en cache** : Créez une seule instance et réutilisez-la partout dans votre application
  * **Chargement paresseux** : Initialisez Auth0Client uniquement au besoin
  * **Actualisation en arrière-plan** : Implémentez l’actualisation des jetons en arrière-plan pour les sessions de longue durée
</Accordion>

<Accordion title="Configuration avancée">
  ### Scopes personnalisés et audience

  Demandez des scopes précises et définissez une audience pour votre API :

  ```cs AdvancedAuth.cs expandable lines
  var auth0Client = new Auth0Client(new Auth0ClientOptions
  {
      Domain = "{yourDomain}",
      ClientId = "{yourClientId}",
      Scope = "openid profile email offline_access read:posts write:posts",
      Audience = "https://myapi.example.com"
  });

  var loginResult = await auth0Client.LoginAsync();
  ```

  ### Paramètres supplémentaires

  Ajoutez des paramètres supplémentaires à la requête d’autorisation :

  ```cs ExtraParams.cs expandable lines
  var extraParameters = new Dictionary<string, string>
  {
      { "prompt", "login" },
      { "ui_locales", "es" },
      { "custom_param", "value" }
  };

  var loginResult = await auth0Client.LoginAsync(extraParameters);
  ```

  ### Refresh Tokens

  Utilisez des refresh tokens pour obtenir de nouveaux jetons d’accès sans intervention de l’utilisateur :

  ```cs RefreshToken.cs expandable lines
  var refreshResult = await auth0Client.RefreshTokenAsync(loginResult.RefreshToken);

  if (!refreshResult.IsError)
  {
      var newAccessToken = refreshResult.AccessToken;
      var newIdToken = refreshResult.IdentityToken;
      // Stocker les nouveaux jetons
  }
  ```

  <Info>
    Pour obtenir un refresh token, incluez la portée `offline_access` dans votre requête d’authentification.
  </Info>

  ### Configuration du navigateur selon la plateforme

  **Android - Utilisez Chrome Custom Tabs avec des couleurs personnalisées :**

  ```cs AndroidBrowser.cs expandable lines
  var auth0Client = new Auth0Client(new Auth0ClientOptions
  {
      Domain = "{yourDomain}",
      ClientId = "{yourClientId}",
      Browser = new AndroidBrowser
      {
          ToolbarColor = Android.Graphics.Color.ParseColor("#FF6B35")
      }
  }, this);
  ```

  **iOS - Utilisez SFSafariViewController avec une présentation personnalisée :**

  ```cs iOSBrowser.cs expandable lines
  var auth0Client = new Auth0Client(new Auth0ClientOptions
  {
      Domain = "{yourDomain}",
      ClientId = "{yourClientId}",
      Browser = new ASWebAuthenticationSessionBrowser
      {
          PrefersEphemeralWebBrowserSession = false
      }
  });
  ```
</Accordion>

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Configurer les fournisseurs d’identité" icon="key" href="/docs/fr-CA/authenticate/identity-providers" iconType="solid">
    Ajoutez des fournisseurs de connexion sociale comme Google, Facebook et GitHub
  </Card>

  <Card title="Activer l’authentification multifacteur" icon="shield-halved" href="/docs/fr-CA/secure/multi-factor-authentication" iconType="solid">
    Ajoutez une couche de sécurité supplémentaire avec la MFA
  </Card>

  <Card title="Protection contre les attaques" icon="shield" href="/docs/fr-CA/secure/attack-protection" iconType="solid">
    Découvrez comment vous protéger contre les attaques par force brute et les bots
  </Card>

  <Card title="Personnaliser l’expérience de connexion" icon="paintbrush" href="/docs/fr-CA/customize/universal-login-pages" iconType="solid">
    Personnalisez la page Universal Login pour qu’elle corresponde à votre image de marque
  </Card>
</CardGroup>