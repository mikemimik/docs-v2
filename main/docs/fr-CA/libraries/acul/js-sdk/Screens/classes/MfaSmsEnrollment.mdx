---
title: "MfaSmsEnrollment"
description: "Décrit toutes les propriétés et méthodes permettant de personnaliser l’écran `mfa-sms-enrollment` de Universal Login."
---

La classe MfaSmsEnrollment implémente les fonctionnalités de l’écran `mfa-sms-enrollment`. Cet écran s’affiche lorsque l’utilisateur doit enregistrer un numéro de téléphone pour l’AMF par SMS.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/5DG03Sm0FFKKt9JOxOl5WN/5df6ce785b080baa951b749b4c472793/Screenshot_2025-02-21_at_11.55.42.png" alt="MfaSmsEnrollment" />
</Frame>

<div id="constructors">
  ## Constructeurs
</div>

Créez une instance du gestionnaire de l’écran d’inscription SMS pour l’AMF :

```typescript Example
import MfaSmsEnrollment from '@auth0/auth0-acul-js/mfa-sms-enrollment';

const mfaSmsEnrollment = new MfaSmsEnrollment();
await mfaSmsEnrollment.continueEnrollment({ phone: '1234567890' });
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Fournit des paramètres liés à l’image de marque, comme le thème et les réglages d’image de marque.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Fournit des configurations liées au client, comme `id`, `name` et `logoUrl`, pour l’écran `mfa-sms-enrollment`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Fournit des renseignements sur l’Organisation de l’utilisateur, comme `id` et `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contient des données sur le prompt actuel dans le flux d’authentification.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaSmsEnrollment">ScreenMembersOnMfaSmsEnrollment</a></span>}>
  Contient des détails propres à l’écran `mfa-sms-enrollment`, y compris sa configuration et son contexte.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contient des données liées au tenant, comme `id` et les métadonnées associées.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Fournit des données propres à la transaction pour l’écran `mfa-sms-enrollment`, comme les identifiants actifs et les états du flux.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gère les données non fiables transmises au SDK, comme les saisies de l’utilisateur pendant l’inscription à l’AMF par SMS.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Contient les détails de l’utilisateur actif, y compris `username`, `email` et `roles`.
</ParamField>

<div id="methods">
  ## Méthodes
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Cette méthode change la langue d’affichage de la page Universal Login.

  ```typescript Example
  import MfaSmsEnrollment from '@auth0/auth0-acul-js/mfa-sms-enrollment';

  const mfaSmsEnrollment = new MfaSmsEnrollment();
  mfaSmsEnrollment.changeLanguage({
    language: 'fr',
  });
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      Le code de langue pour la langue souhaitée (par exemple, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Si elle est définie sur `'session'`, la langue sélectionnée est conservée pendant toute la durée de la session.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="continueEnrollment" type="Promise<void>">
  Cette méthode poursuit le processus d’inscription par SMS avec le numéro de téléphone fourni.

  ```typescript Example
  import MfaSmsEnrollment from '@auth0/auth0-acul-js/mfa-sms-enrollment';

  const mfaSmsEnrollment = new MfaSmsEnrollment();
  await mfaSmsEnrollment.continueEnrollment({ phone: '1234567890' });
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [MfaSmsEnrollmentOptions](/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/MfaSmsEnrollmentOptions).
    </ParamField>

    <ParamField body="captcha?" type="string">
      Le code CAPTCHA ou la réponse du fournisseur CAPTCHA. Obligatoire si Bot Detection est activé dans votre tenant Auth0.
    </ParamField>

    <ParamField body="phone?" type="string">
      Le numéro de téléphone de l’utilisateur.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Cette méthode récupère le tableau des erreurs de transaction dans le contexte, ou un tableau vide s’il n’y en a aucune.
</ParamField>

<ParamField body="pickCountryCode" type="Promise<void>">
  Cette méthode mène à l’écran où l’utilisateur peut sélectionner un indicatif de pays pour son numéro de téléphone.

  ```typescript Example
  import MfaSmsEnrollment from '@auth0/auth0-acul-js/mfa-sms-enrollment';

  const mfaSmsEnrollment = new MfaSmsEnrollment();
  await mfaSmsEnrollment.pickCountryCode();
  ```
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Cette méthode permet à l’utilisateur d’essayer une autre méthode d’AMF.

  ```typescript Example
  import MfaSmsEnrollment from '@auth0/auth0-acul-js/mfa-sms-enrollment';

  const mfaSmsEnrollment = new MfaSmsEnrollment();
  await mfaSmsEnrollment.tryAnotherMethod();
  ```
</ParamField>