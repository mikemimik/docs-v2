---
title: "MfaEmailList"
description: "Décrit toutes les propriétés et méthodes offertes pour personnaliser l’écran `mfa-email-list` de Universal Login."
---

La classe MfaEmailList met en œuvre la fonctionnalité de l’écran `mfa-email-list`. Cet écran permet aux utilisateurs de sélectionner une adresse courriel déjà enregistrée pour la MFA.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/7d8tt9q970YJ4gT5C0kNhx/dfbaefcd9d913b41addc7b652d3bf63d/Screenshot_2025-02-20_at_15.43.19.png" alt="ACUL MFA Email List" />
</Frame>

<div id="constructors">
  ## Constructeurs
</div>

Créez une instance du gestionnaire de l’écran MFA Email List :

```typescript Example
import MfaEmailList from '@auth0/auth0-acul-js/mfa-email-list';

const mfaEmailList = new MfaEmailList();
await mfaEmailList.selectMfaEmail({
  index: 0
});
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Fournit la configuration liée à l’image de marque, comme le thème et les paramètres.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Fournit la configuration liée au client, comme `id`, `name` et `logoUrl`, pour l’écran `mfa-email-list`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Fournit des renseignements sur l’organisation de l’utilisateur, comme `id` et `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contient des données sur l’étape actuelle du flux d’authentification.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaEmailList">ScreenMembersOnMfaEmailList</a></span>}>
  Contient des détails propres à l’écran `mfa-email-list`, y compris sa configuration et son contexte.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contient des données liées au tenant, comme `id` et les métadonnées associées.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Fournit des données propres à la transaction pour l’écran `mfa-email-list`, comme les identifiants actifs et l’état du flux.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gère les données non fiables transmises au SDK, comme la saisie de l’utilisateur lors de la sélection dans la liste d’adresses courriel MFA.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Détails de l’utilisateur actif, y compris `username`, `email` et `roles`.
</ParamField>

<div id="methods">
  ## Méthodes
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Cette méthode modifie la langue d’affichage de la page Universal Login.

  ```typescript Example
  import MfaEmailList from '@auth0/auth0-acul-js/mfa-email-list';
  const mfaEmailList = new MfaEmailList();
  mfaEmailList.changeLanguage({
    language: 'fr',
  });
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      Le code du paramètre régional de la langue souhaitée (par exemple, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Lorsqu’elle est définie à `'session'`, la langue sélectionnée est conservée pendant toute la durée de la session.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Cette méthode récupère dans le contexte le tableau des erreurs de transaction, ou un tableau vide s’il n’y en a aucune.
</ParamField>

<ParamField body="goBack" type="Promise<void>">
  Cette méthode retourne à l’écran précédent.

  ```typescript Example
  import MfaEmailList from '@auth0/auth0-acul-js/mfa-email-list';

  const mfaEmailList = new MfaEmailList();
  await mfaEmailList.goBack();
  ```
</ParamField>

<ParamField body="selectMfaEmail" type="Promise<void>">
  Cette méthode sélectionne dans la liste une adresse courriel déjà enregistrée.

  ```typescript Example
  import MfaEmailList from '@auth0/auth0-acul-js/mfa-email-list';

  const mfaEmailList = new MfaEmailList();
  await mfaEmailList.selectMfaEmail({
    index: 0
  });
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [SelectMfaEmailOptions](/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/SelectMfaEmailOptions).
    </ParamField>

    <ParamField body="index" type="number" required>
      L’indice du courriel à sélectionner.
    </ParamField>
  </Expandable>
</ParamField>