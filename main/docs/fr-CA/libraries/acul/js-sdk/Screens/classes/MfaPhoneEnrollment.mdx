---
title: "MfaPhoneEnrollment"
description: "Décrit toutes les propriétés et méthodes disponibles pour personnaliser l’écran `mfa-phone-enrollment` d’Universal Login."
---

La classe MfaPhoneEnrollment implémente la fonctionnalité de l’écran `mfa-phone-enrollment`. Cet écran permet aux utilisateurs d’enregistrer un numéro de téléphone pour la MFA.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/ja-jp/cdy7uua7fh8z/3o4ohXUXfkVaaZy0992sgL/a84b54fa31fcbf2b0695c9ff08357887/Screenshot_2025-04-23_at_17.36.28.png" alt="ACUL Inscription MFA par téléphone" />
</Frame>

<div id="constructors">
  ## Constructeurs
</div>

Créez une instance du gestionnaire de l’écran d’enrôlement par téléphone de la MFA :

```typescript Example
import MfaPhoneEnrollment from '@auth0/auth0-acul-js/mfa-phone-enrollment';

const mfaPhoneEnrollment = new MfaPhoneEnrollment();
await mfaPhoneEnrollment.continueEnrollment({ phone: '+1234567890', type: 'sms' });
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Fournit des configurations liées à l’image de marque, comme le thème et les paramètres.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Fournit des configurations liées au client, comme `id`, `name` et `logoUrl`, pour l’écran `mfa-phone-enrollment`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Fournit des renseignements sur l’Organization de l’utilisateur, comme `id` et `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contient des données sur le prompt actuel dans le flux d’authentification.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ScreenMembers">ScreenMembers</a></span>}>
  Contient les détails propres à l’écran `mfa-phone-enrollment`, y compris sa configuration et son contexte.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contient des données liées au tenant, comme `id` et les métadonnées associées.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Fournit des données propres à la transaction pour l’écran `mfa-phone-enrollment`, comme les identifiants actifs et les états du flux.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gère les données non fiables transmises au SDK, comme les saisies de l’utilisateur lors de l’enrôlement MFA par téléphone.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Détails de l’utilisateur actif, y compris `username`, `email` et `roles`.
</ParamField>

<div id="methods">
  ## Méthodes
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Cette méthode modifie la langue d’affichage de la page Universal Login.

  ```typescript Example
  import MfaPhoneEnrollment from '@auth0/auth0-acul-js/mfa-phone-enrollment';
  const mfaPhoneEnrollment = new MfaPhoneEnrollment();
  mfaPhoneEnrollment.changeLanguage({
    language: 'fr',
  });
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      Le code de langue de la langue souhaitée (par exemple, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Lorsque la valeur `'session'` est utilisée, la langue sélectionnée est conservée pendant toute la session.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="continueEnrollment" type="Promise<void>">
  Cette méthode poursuit le processus d’enrôlement avec le numéro de téléphone et le type fournis (SMS ou appel vocal).

  ```typescript Example
  import MfaPhoneEnrollment from '@auth0/auth0-acul-js/mfa-phone-enrollment';

  const mfaPhoneEnrollment = new MfaPhoneEnrollment();
  await mfaPhoneEnrollment.continueEnrollment({ phone: '+1234567890', type: 'sms' });
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [MfaPhoneEnrollmentContinueOptions](/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneEnrollmentContinueOptions).
    </ParamField>

    <ParamField body="phone" type="string" required>
      Le numéro de téléphone de l’utilisateur.
    </ParamField>

    <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;" required>
      Le mode d’envoi du code de vérification.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Cette méthode récupère le tableau des erreurs de transaction depuis le contexte, ou un tableau vide s’il n’y en a aucune.
</ParamField>

<ParamField body="pickCountryCode" type="Promise<void>">
  Cette méthode accède à l’écran de sélection de l’indicatif de pays.

  ```typescript Example
  import MfaPhoneEnrollment from '@auth0/auth0-acul-js/mfa-phone-enrollment';

  const mfaPhoneEnrollment = new MfaPhoneEnrollment();
  await mfaPhoneEnrollment.pickCountryCode();
  ```
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Cette méthode permet à l’utilisateur d’essayer une autre méthode MFA.

  ```typescript Example
  import MfaPhoneEnrollment from '@auth0/auth0-acul-js/mfa-phone-enrollment';

  const mfaPhoneEnrollment = new MfaPhoneEnrollment();
  await mfaPhoneEnrollment.tryAnotherMethod();
  ```
</ParamField>