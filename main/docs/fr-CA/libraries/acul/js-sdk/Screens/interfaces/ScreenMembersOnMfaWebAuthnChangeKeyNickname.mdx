---
title: "ScreenMembersOnMfaWebAuthnChangeKeyNickname"
---

ScreenMembersOnMfaWebAuthnChangeKeyNickname

```ts Example
export interface ScreenMembersOnMfaWebAuthnChangeKeyNickname extends ScreenMembers {
  /**
   * Données propres à l'écran contenant le surnom actuel de la clé WebAuthn.
   * @type {{ nickname: string; } | null}
   */
  data: {
    /**
     * Le surnom actuel de la clé de sécurité WebAuthn que l'utilisateur est sur le point de modifier.
     * Cette valeur doit être affichée à l'utilisateur pour le contexte.
     * @type {string}
     * @example "My YubiKey"
     * @example "Work Security Key"
     */
    nickname: string;
  } | null;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="captcha" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="{ nickname: string; }">
  Donnée propre à l’écran.

  * Déclaration de type : `{ nickname: string; }`

  <Expandable title="propriétés">
    <ParamField body="nickname" type="string">
      Le surnom actuel de la clé de sécurité WebAuthn que l’utilisateur s’apprête à modifier.
      Cette valeur devrait être affichée à l’utilisateur à titre indicatif.

      <CodeGroup>
        ```ts Example 1
        "My YubiKey"
        ```

        ```ts Example 2
        "Work Security Key"
        ```

        ```ts Example 3
        null
        ```
      </CodeGroup>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />