---
title: "MfaPhoneChallengeMembers"
---

MfaPhoneChallengeMembers

````ts Example
export interface MfaPhoneChallengeMembers extends BaseMembers {
  /**
   * @property {ScreenMembersOnMfaPhoneChallenge} screen - Informations spécifiques à l'écran pour le MFA Phone Challenge.
   */
  screen: ScreenMembersOnMfaPhoneChallenge;

  /**
   * continue
   * Soumet le choix de la méthode d’envoi AMF (SMS ou appel vocal) pour poursuivre le défi.
   * Cela déclenchera l'envoi du code de vérification au téléphone de l'utilisateur.
   *
   * @param {ContinueOptions} payload - Les options précisant le type de livraison ('sms' ou 'voice').
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';
   *
   * const mfaPhoneChallenge = new MfaPhoneChallenge();
   *
   * // Pour envoyer le code par SMS
   * await mfaPhoneChallenge.continue({ type: 'sms' });
   *
   * // Pour envoyer le code par appel vocal
   * await mfaPhoneChallenge.continue({ type: 'voice' });
   * ```
   */
  continue(payload: ContinueOptions): Promise<void>;

  /**
   * pickPhone
   * Déclenche l'action permettant de choisir un numéro de téléphone différent.
   * Cela redirige généralement l'utilisateur vers un écran où il peut sélectionner parmi plusieurs numéros de téléphone enregistrés, le cas échéant.
   *
   * @param {PickPhoneOptions} [payload] - Options personnalisées facultatives à inclure avec la requête.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';
   *
   * const mfaPhoneChallenge = new MfaPhoneChallenge();
   * await mfaPhoneChallenge.pickPhone();
   * ```
   */
  pickPhone(payload?: PickPhoneOptions): Promise<void>;

  /**
   * tryAnotherMethod
   * Déclenche l'action permettant de sélectionner un facteur AMF/authentificateur différent.
   * Cela redirige l'utilisateur vers un écran où il peut choisir une autre méthode AMF (p. ex., application OTP, code de récupération).
   *
   * @param {PickAuthenticatorOptions} [payload] - Options personnalisées facultatives à inclure avec la requête.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';
   *
   * const mfaPhoneChallenge = new MfaPhoneChallenge();
   * await mfaPhoneChallenge.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: PickAuthenticatorOptions): Promise<void>;
}
````

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaPhoneChallenge">ScreenMembersOnMfaPhoneChallenge</a></span>} />

<ParamField body="tenant" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Méthodes
</div>

<ParamField body="continue" type="Promise<void>">
  continue
  Soumet le choix de la méthode d’envoi de l’AMF (SMS ou appel vocal) pour passer au défi.
  Cela déclenche l’envoi du code de vérification au téléphone de l’utilisateur.

  Une promesse qui se résout lorsque l’action est soumise.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';

  const mfaPhoneChallenge = new MfaPhoneChallenge();

  // Pour envoyer le code par SMS
  await mfaPhoneChallenge.continue({ type: 'sms' });

  // Pour envoyer le code par appel vocal
  await mfaPhoneChallenge.continue({ type: 'voice' });
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneChallengeContinueOptions">MfaPhoneChallengeContinueOptions</a></span>}>
      Les options qui précisent le mode d’envoi (`sms` ou `voice`).

      **Propriétés**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        Le type de défi à effectuer.

        * `sms` : Envoyer le code par message texte.
        * `voice` : Envoyer le code par appel vocal.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="pickPhone" type="Promise<void>">
  pickPhone
  Lance l’action permettant de choisir un autre numéro de téléphone.
  Cela redirige généralement l’utilisateur vers un écran où il peut sélectionner, s’il y a lieu, parmi plusieurs numéros de téléphone enregistrés.

  Une promesse qui se résout lorsque l’action est soumise.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';

  const mfaPhoneChallenge = new MfaPhoneChallenge();
  await mfaPhoneChallenge.pickPhone();
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneChallengePickPhoneOptions">MfaPhoneChallengePickPhoneOptions</a></span>}>
      Options personnalisées facultatives à inclure dans la requête.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Lance l’action permettant de sélectionner un autre facteur ou authentificateur d’AMF.
  Cela redirige l’utilisateur vers un écran où il peut choisir une autre méthode d’AMF (par ex. une application OTP ou un code de récupération).

  Une promesse qui se résout lorsque l’action est soumise.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';

  const mfaPhoneChallenge = new MfaPhoneChallenge();
  await mfaPhoneChallenge.tryAnotherMethod();
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneChallengePickAuthenticatorOptions">MfaPhoneChallengePickAuthenticatorOptions</a></span>}>
      Options personnalisées facultatives à inclure dans la requête.
    </ParamField>
  </Expandable>
</ParamField>