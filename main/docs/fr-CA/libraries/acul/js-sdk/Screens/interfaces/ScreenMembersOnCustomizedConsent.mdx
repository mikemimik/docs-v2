---
title: "ScreenMembersOnCustomizedConsent"
---

ScreenMembersOnCustomizedConsent

```ts Example
export interface ScreenMembersOnCustomizedConsent extends ScreenMembers {
  /**
   * Un tableau d'objets `Scope` détaillant chaque permission (scope) demandée.
   * Il s'agit généralement de permissions de haut niveau comme 'read:profile' ou 'openid'.
   * Cette liste doit être affichée à l'utilisateur pour qu'il puisse la consulter.
   */
  scopes: Scope[];

  /**
   * Un tableau d'objets `AuthorizationDetail`.
   * Chaque objet fournit des détails granulaires sur des données spécifiques ou
   * des actions que l'application souhaite effectuer, potentiellement liées aux
   * Rich Authorization Requests (RAR) ou à d'autres modèle de permissions granulaires.
   * Cette liste doit être affichée à l'utilisateur pour qu'il puisse la consulter.
   */
  authorizationDetails: AuthorizationDetail[];
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="authorizationDetails" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/AuthorizationDetail">AuthorizationDetail</a></span>}>
  Un tableau d’objets `AuthorizationDetail`.
  Chaque objet fournit des détails granulaires sur des données ou des
  actions précises que l’application souhaite effectuer, possiblement liées à
  Rich Authorization Requests (RAR) ou à d’autres modèles de permissions granulaires.
  Cette liste doit être affichée à l’utilisateur afin qu’il puisse l’examiner.
</ParamField>

<ParamField body="captcha" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/PasskeyCreate">PasskeyCreate</a></span>}>
  Données propres à l’écran.
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="scopes" type={<span><a href="/docs/fr-CA/libraries/acul/js-sdk/Screens/interfaces/Scope">Scope</a></span>}>
  Un tableau d’objets `Scope` détaillant chaque permission (scope) demandée.
  Il s’agit généralement de permissions de haut niveau comme &#39;read:profile&#39; ou &#39;openid&#39;.
  Cette liste doit être affichée à l’utilisateur afin qu’il puisse l’examiner.
</ParamField>

<ParamField body="texts" type="Record" />