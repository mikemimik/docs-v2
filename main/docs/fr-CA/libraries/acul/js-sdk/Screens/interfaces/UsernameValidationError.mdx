---
title: "UsernameValidationError"
---

Représente une erreur unique de validation du nom d’utilisateur renvoyée au cours de la validation.

Chaque erreur comporte un `code` lisible par machine et un `message` lisible par l’humain.

```ts Example
export interface UsernameValidationError {
  /**
   * Un identifiant unique pour la règle de validation qui a échoué.
   * 
   * Exemple : `"username-too-short"`, `"username-invalid-characters"`
   */
  code: string;

  /**
   * Une description lisible par l’humain de l'erreur.
   * 
   * Exemple : `"Username must be at least 3 characters long."`
   */
  message: string;

  /**
   * Le champ associé à l'erreur de validation, généralement "username", "email" ou "phone".
   */
  field: string;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Un identifiant unique de la règle de validation ayant échoué.

  Exemple : `"username-too-short"`, `"username-invalid-characters"`
</ParamField>

<ParamField body="field" type="string">
  Le champ associé à l’erreur de validation, généralement &quot;username&quot;, &quot;email&quot; ou &quot;phone&quot;.
</ParamField>

<ParamField body="message" type="string">
  Une description de l’erreur lisible par l’humain.

  Exemple : `"Username must be at least 3 characters long."`
</ParamField>