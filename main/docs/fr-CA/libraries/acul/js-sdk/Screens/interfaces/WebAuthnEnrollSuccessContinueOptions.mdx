---
title: "WebAuthnEnrollSuccessContinueOptions"
---

ContinueOptions

```ts Example
export interface ContinueOptions extends CustomOptions {
  // Aucune propriété spécifique à l'écran n'est requise pour l'action 'continue' au-delà de CustomOptions.
  // Le serveur utilise `action: "default"` pour cette opération.
}
```