---
title: "MfaVoiceChallengeContinueOptions"
---

Options pour soumettre le code de vérification vocale.

```ts Example
export interface MfaVoiceChallengeContinueOptions {
  /**
   * Le code de vérification reçu par appel vocal.
   */
  code: string;

  /**
   * Indicateur optionnel pour mémoriser l'appareil pendant 30 jours, en ignorant les futurs défis AMF.
   */
  rememberDevice?: boolean;

  /**
   * Option personnalisée supplémentaire à transmettre avec la requête.
   */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Le code de vérification reçu par appel vocal.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indicateur facultatif pour mémoriser l’appareil pendant 30 jours et éviter de futurs défis MFA.
</ParamField>