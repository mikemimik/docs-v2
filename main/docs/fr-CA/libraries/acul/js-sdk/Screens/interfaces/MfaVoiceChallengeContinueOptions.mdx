---
title: "MfaVoiceChallengeContinueOptions"
---

Options pour soumettre le code de la vérification par appel vocal.

```ts Example
export interface MfaVoiceChallengeContinueOptions {
  /**
   * Le code de vérification reçu par appel vocal.
   */
  code: string;

  /**
   * Indicateur optionnel pour mémoriser l'appareil pendant 30 jours, évitant ainsi les futures vérifications AMF.
   */
  rememberDevice?: boolean;

  /**
   * Options personnalisées supplémentaires à transmettre avec la requête.
   */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Le code de vérification reçu par appel vocal.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indicateur facultatif permettant de mémoriser l’appareil pendant 30 jours et d’éviter les prochaines vérifications AMF.
</ParamField>