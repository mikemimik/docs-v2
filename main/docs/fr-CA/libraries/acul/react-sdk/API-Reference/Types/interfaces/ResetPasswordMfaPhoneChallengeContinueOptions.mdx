---
title: "ResetPasswordMfaPhoneChallengeContinueOptions"
---

ContinueOptions

```ts Example
export interface ContinueOptions extends CustomOptions {
  /**
   * La méthode de livraison sélectionnée par l'utilisateur pour recevoir le code de vérification.
   * - `sms`: Envoyer le code par message texte.
   * - `voice`: Envoyer le code par appel vocal.
   * @type {'sms' | 'voice'}
   */
  type: 'sms' | 'voice';
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
  Le mode de livraison choisi par l’utilisateur pour recevoir le code de vérification.

  * `sms` : Envoie le code par message texte.
  * `voice` : Envoie le code par appel téléphonique.
</ParamField>