---
title: "MfaWebAuthnErrorMembers"
---

MfaWebAuthnErrorMembers

````ts Example
export interface MfaWebAuthnErrorMembers extends BaseMembers {
  /**
   * L'objet d’écran avec la structure de données spécifique à MFA WebAuthn Error.
   */
  screen: ScreenMembersOnMfaWebAuthnError;
  
  /**
   * Permet à l'utilisateur de retenter l'opération WebAuthn précédente.
   * Cela redirige généralement l'utilisateur vers l'écran où l'opération WebAuthn a été lancée.
   *
   * @param {CustomOptions} [payload] - Options personnalisées facultatives à inclure avec la requête.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * // En supposant que 'sdk' est une instance de MfaWebAuthnError
   * await sdk.tryAgain();
   * ```
   */
  tryAgain(payload?: CustomOptions): Promise<void>;

  /**
   * Permet à l'utilisateur d'opter pour l'authentification par mot de passe comme solution de rechange, si cette option est configurée.
   * Cette option est généralement disponible si l'authentification principale ou une étape précédente faisait intervenir un mot de passe.
   *
   * @param {CustomOptions} [payload] - Options personnalisées facultatives à inclure avec la requête.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * // En supposant que 'sdk' est une instance de MfaWebAuthnError
   * await sdk.usePassword();
   * ```
   */
  usePassword(payload?: CustomOptions): Promise<void>;

  /**
   * Permet à l'utilisateur de choisir une méthode AMF différente si d'autres facteurs sont enrôlés ou disponibles.
   * Cela dirige l'utilisateur vers un écran de sélection d'authentificateur.
   *
   * @param {CustomOptions} [payload] - Options personnalisées facultatives à inclure avec la requête.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * // En supposant que 'sdk' est une instance de MfaWebAuthnError
   * await sdk.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: CustomOptions): Promise<void>;

  /**
   * Permet à l'utilisateur de refuser l'ajout d'un appareil ou l'exécution de l'étape WebAuthn, déclinant ainsi le flux WebAuthn en cours.
   * Cette action est généralement utilisée dans les scénarios d'enrôlement où l'utilisateur choisit de ne pas continuer.
   *
   * @param {CustomOptions} [payload] - Options personnalisées facultatives à inclure avec la requête.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * // En supposant que 'sdk' est une instance de MfaWebAuthnError
   * await sdk.noThanks();
   * ```
   */
  noThanks(payload?: CustomOptions): Promise<void>;
}
````

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnError">ScreenMembersOnMfaWebAuthnError</a></span>}>
  L’objet d’écran doté de la structure de données spécifique à MFA WebAuthn Error.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Méthodes
</div>

<ParamField body="noThanks" type="Promise<void>">
  Permet à l’utilisateur de refuser d’ajouter un appareil ou de terminer l’étape WebAuthn, ce qui revient à refuser le flux WebAuthn en cours.
  Cette action est généralement utilisée dans des scénarios d’enrôlement où l’utilisateur décide de ne pas poursuivre.

  Une promesse qui se résout lorsque l’action est soumise avec succès.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnError
  await sdk.noThanks();
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Options personnalisées facultatives à inclure dans la requête.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAgain" type="Promise<void>">
  Permet à l’utilisateur de réessayer l’opération WebAuthn précédente.
  Cela redirige généralement l’utilisateur vers l’écran où l’opération WebAuthn a été lancée.

  Une promesse qui se résout lorsque l’action est soumise avec succès.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnError
  await sdk.tryAgain();
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Options personnalisées facultatives à inclure dans la requête.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permet à l’utilisateur de choisir une autre méthode d’AMF si d’autres facteurs sont déjà enrôlés ou disponibles.
  Cela dirige l’utilisateur vers un écran de sélection de l’authentificateur.

  Une promesse qui se résout lorsque l’action est soumise avec succès.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnError
  await sdk.tryAnotherMethod();
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Options personnalisées facultatives à inclure dans la requête.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="usePassword" type="Promise<void>">
  Permet à l’utilisateur d’opter pour l’authentification par mot de passe comme solution de rechange, si elle est configurée.
  Cette option est généralement offerte si l’authentification principale ou une étape précédente impliquait un mot de passe.

  Une promesse qui se résout lorsque l’action est soumise avec succès.

  #### Lève

  Lève une erreur si la soumission échoue.

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnError
  await sdk.usePassword();
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Options personnalisées facultatives à inclure dans la requête.
    </ParamField>
  </Expandable>
</ParamField>