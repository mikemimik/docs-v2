---
title: "VerifyPlatformAuthenticatorOptions"
---

VerifyPlatformAuthenticatorOptions

```ts Example
export interface VerifyPlatformAuthenticatorOptions extends CustomOptions {
  /**
   * Facultatif. Si la valeur est true et que `screen.showRememberDevice` est true, cela indique que l'utilisateur
   * a choisi de mémoriser cet appareil, généralement pendant 30 jours, pour contourner l'AMF lors des connexions ultérieures.
   * @type {boolean}
   */
  rememberDevice?: boolean;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body="rememberDevice?" type="boolean">
  Facultatif. Si true, tente de mémoriser le navigateur pour les prochaines vérifications AMF.
  Correspond au champ de formulaire `rememberBrowser`. Cela s’applique uniquement si `screen.showRememberDevice` est true.
</ParamField>