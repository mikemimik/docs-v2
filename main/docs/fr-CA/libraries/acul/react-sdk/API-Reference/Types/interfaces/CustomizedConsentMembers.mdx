---
title: "ConsentementPersonnaliséMembres"
---

ConsentementPersonnaliséMembres

````ts Example
export interface CustomizedConsentMembers extends BaseMembers {
  /**
   * Provides access to the propriétés et données propres à l’écran of the Customized Consent screen,
   * including the list of `scopes` and `authorizationDetails` being requested.
   * @type {ScreenMembersOnCustomizedConsent}
   */
  screen: ScreenMembersOnCustomizedConsent;

  /**
   * Soumet la décision de l'utilisateur d'accepter (accorder) les permissions demandées et les détails d'autorisation.
   * Cette action envoie une requête à l'endpoint `/u/customized-consent` avec `action: "accept"`.
   * L'état de la transaction est automatiquement inclus.
   *
   * @param {CustomOptions} [payload] - Facultatif. Un objet pour toute paire clé-valeur personnalisée
   *                                    à envoyer avec la requête. Ces paramètres seront
   *                                    inclus dans les données du formulaire soumises au serveur.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action d'acceptation est soumise avec succès.
   *                          En cas de succès, Auth0 redirige généralement l'utilisateur vers l'application
   *                          ou vers l'étape suivante du flux d'authentification.
   * @throws {Error} Génère une erreur si la soumission du formulaire échoue (p. ex., problème réseau, état invalide).
   *                 Les erreurs côté serveur (comme « invalid_request ») seront reflétées dans `this.transaction.errors`
   *                 après l'opération, plutôt que d'être levées comme des erreurs JavaScript.
   *
   * @example
   * ```typescript
   * // Assuming 'customizedConsentManager' is an instance of the CustomizedConsent SDK class
   * try {
   *   await customizedConsentManager.accept();
   *   // If successful, the page will typically redirect.
   * } catch (error) {
   *   // Handle unexpected errors during the submission itself.
   *   console.error("Failed to submit consent acceptance:", error);
   * }
   * // After the await, check customizedConsentManager.transaction.errors for server-side validation issues.
   * ```
   */
  accept(payload?: CustomOptions): Promise<void>;

  /**
   * Submits the user's decision to rejeter (reject) the requested permissions and authorization details.
   * This action posts to the `/u/customized-consent` endpoint with `action: "deny"`.
   * The transaction state is automatically included.
   *
   * @param {CustomOptions} [payload] - Optional. An object for any custom key-value pairs
   *                                    to be sent with the request. These parameters will be
   *                                    included in the form data submitted to the server.
   * @returns {Promise<void>} A promesse that resolves when the rejeter action is successfully submitted.
   *                          On success, Auth0 typically redirects the user, potentially showing an
   *                          access denied message or returning an error to the application.
   * @throws {Error} Throws an error if the form submission fails (e.g., network issue, invalid state).
   *                 Server-side errors are reflected in `this.transaction.errors`.
   *
   * @example
   * ```typescript
   * // Assuming 'customizedConsentManager' is an instance of the CustomizedConsent SDK class
   * try {
   *   await customizedConsentManager.deny({ reason_code: "user_rejected_details" }); // Example custom option
   *   // If successful, the page will typically redirect.
   * } catch (error) {
   *   console.error("Failed to submit consent denial:", error);
   * }
   * // After the await, check customizedConsentManager.transaction.errors for server-side validation issues.
   * ```
   */
  deny(payload?: CustomOptions): Promise<void>;
}
````

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnCustomizedConsent">ScreenMembersOnCustomizedConsent</a></span>}>
  Donne accès aux propriétés et aux données propres à l’écran Customized Consent,
  y compris à la liste des `scopes` et des `authorizationDetails` demandés.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Méthodes
</div>

<ParamField body="accept" type="Promise<void>">
  Soumet la décision de l’utilisateur d’accepter (accorder) les permissions demandées et les détails d’autorisation.
  Cette action envoie une requête au endpoint `/u/customized-consent` avec `action: "accept"`.
  L’état de la transaction est automatiquement inclus.

  Une promesse qui est résolue lorsque l’action d’acceptation est soumise avec succès.
  En cas de succès, Auth0 redirige généralement l’utilisateur vers l’application
  ou vers l’étape suivante du flux d’authentification.

  #### Lance

  Lance une erreur si la soumission du formulaire échoue (p. ex., problème de réseau, état non valide).
  Les erreurs côté serveur (comme &quot;invalid&#95;request&quot;) seront reflétées dans `this.transaction.errors`
  après l’opération, plutôt que d’être lancées comme des erreurs JavaScript.

  ```typescript Example
  // En supposant que 'customizedConsentManager' est une instance de la classe SDK CustomizedConsent
  try {
    await customizedConsentManager.accept();
    // En cas de succès, la page sera généralement redirigée.
  } catch (error) {
    // Gérer les erreurs inattendues pendant la soumission elle-même.
    console.error("Failed to submit consent acceptance:", error);
  }
  // Après le await, vérifiez customizedConsentManager.transaction.errors pour les problèmes de validation côté serveur.
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Facultatif. Un objet contenant des paires clé-valeur personnalisées
      à envoyer avec la requête. Ces paramètres seront
      inclus dans les données du formulaire soumises au serveur.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="deny" type="Promise<void>">
  Soumet la décision de l’utilisateur de rejeter (refuser) les permissions demandées et les détails d’autorisation.
  Cette action envoie une requête au endpoint `/u/customized-consent` avec `action: "deny"`.
  L’état de la transaction est automatiquement inclus.

  Une promesse qui est résolue lorsque l’action de rejet est soumise avec succès.
  En cas de succès, Auth0 redirige généralement l’utilisateur, en affichant au besoin un
  message d’accès refusé ou en renvoyant une erreur à l’application.

  #### Lance

  Lance une erreur si la soumission du formulaire échoue (p. ex., problème de réseau, état non valide).
  Les erreurs côté serveur sont reflétées dans `this.transaction.errors`.

  ```typescript Example
  // En supposant que 'customizedConsentManager' est une instance de la classe SDK CustomizedConsent
  try {
    await customizedConsentManager.deny({ reason_code: "user_rejected_details" }); // Exemple d’option personnalisée
    // En cas de succès, la page sera généralement redirigée.
  } catch (error) {
    console.error("Failed to submit consent denial:", error);
  }
  // Après le await, vérifiez customizedConsentManager.transaction.errors pour les problèmes de validation côté serveur.
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload?" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Facultatif. Un objet contenant des paires clé-valeur personnalisées
      à envoyer avec la requête. Ces paramètres seront
      inclus dans les données du formulaire soumises au serveur.
    </ParamField>
  </Expandable>
</ParamField>