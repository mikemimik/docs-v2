---
title: "DeviceCodeActivationContinueOptions"
---

payload permettant de poursuivre l’activation du device code.

Cette interface étend `CustomOptions`, ce qui signifie que toutes les propriétés personnalisées
sont fusionnées au niveau supérieur de l’objet. Cela évite l’imbrication et offre
une structure plus simple pour les utilisateurs de cette interface.

```ts Example
export interface ContinueOptions extends CustomOptions {
  code: string;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string" />