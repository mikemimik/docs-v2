---
title: "ContinueOptions"
---

Options pour poursuivre avec la vérification par courriel.

```ts Example
export interface ContinueOptions {
  /** Le code saisi par l'utilisateur. */
  code: string;
  /** Indique si cet appareil doit être mémorisé. */
  rememberDevice?: boolean;
  /** Toute option personnalisée supplémentaire */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Toutes les options personnalisées supplémentaires

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Le code entré par l’utilisateur.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indique s’il faut mémoriser cet appareil.
</ParamField>