---
title: "ResetPasswordRequest"
description: "Décrit tous les hooks et toutes les méthodes disponibles pour personnaliser l’écran `reset-password-request` de Universal Login."
---

L’écran `reset-password-request` s’affiche au début du flux de réinitialisation du mot de passe. Il invite l’utilisateur à saisir son nom d’utilisateur ou son adresse courriel afin de recevoir un lien de réinitialisation du mot de passe.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/3sY6D83USU8smQ4ozQuNzP/6e856749303830f72b764f539918b8ab/Screenshot_2025-03-27_at_14.44.14.png" alt="ACUL Reset Password Request" />
</Frame>

<div id="import">
  ## Importation
</div>

Chaque écran possède son propre ensemble de hooks et de méthodes. Le SDK prend en charge **l’importation partielle** et **l’importation à la racine** pour chaque écran.

* L’importation partielle vous permet d’inclure uniquement le code nécessaire à votre cas d’utilisation.
* L’importation à la racine vous permet de charger tous les écrans à partir d’un seul bundle, ce qui est utile si vous voulez qu’une compilation unifiée prenne en charge tous les écrans possibles.

```jsx Import Example
// importation racine
import { useResetPasswordRequest } from '@auth0/auth0-acul-react';

// importation partielle
import {
  useResetPasswordRequest,
  // Hooks de contexte
  useUser,
  useTenant,
  useBranding,
  useClient,
  useOrganization,
  usePrompt,
  useScreen,
  useTransaction,
  useUntrustedData,
  // Hooks communs
  useCurrentScreen,
  useAuth0Themes,
  useErrors,
  useLoginIdentifiers,
  // Hooks utilitaires
  useChangeLanguage,
  // Méthodes
  backToLogin,
  resetPassword,
} from '@auth0/auth0-acul-react/reset-password-request';

function ResetPasswordRequestScreen() {
  const { resetPassword } = useResetPasswordRequest();
  return (
    <button onClick={() => resetPassword({ username: 'user@example.com' })}>
      Send Reset Link
    </button>
  );
}
```

<div id="context-hooks">
  ## Hooks de contexte
</div>

Hooks propres à l’écran qui fournissent un accès en lecture seule aux données de contexte Auth0 sur l’écran `reset-password-request`. Importez-les depuis `@auth0/auth0-acul-react/reset-password-request`.

<ParamField body="useBranding" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Ce hook fournit la configuration de l’image de marque, notamment le logo, les couleurs et les paramètres de thème affichés sur l’écran `reset-password-request`.

  ```jsx Example
  import { useBranding } from '@auth0/auth0-acul-react/reset-password-request';
  function CustomTheme() {
    const branding = useBranding();
  }
  ```
</ParamField>

<ParamField body="useClient" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>}>
  Ce hook fournit des renseignements liés au client, comme `id`, `name` et `logoUrl`, pour l’écran `reset-password-request`.

  ```jsx Example
  import { useClient } from '@auth0/auth0-acul-react/reset-password-request';
  function AppInfo() {
    const client = useClient();
  }
  ```
</ParamField>

<ParamField body="useOrganization" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Ce hook fournit des renseignements sur l’Organization de l’utilisateur si la réinitialisation du mot de passe est limitée à cette Organization. Renvoie `null` lorsqu’aucun contexte d’Organization n’est présent.

  ```jsx Example
  import { useOrganization } from '@auth0/auth0-acul-react/reset-password-request';
  function OrgSelector() {
    const organization = useOrganization();
    if (!organization) {
      return <p>No Organization context</p>;
    }
  }
  ```
</ParamField>

<ParamField body="usePrompt" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>}>
  Ce hook contient des données sur le prompt en cours dans le flux d’authentification.

  ```jsx Example
  import { usePrompt } from '@auth0/auth0-acul-react/reset-password-request';
  function FlowInfo() {
    const prompt = usePrompt();
  }
  ```
</ParamField>

<ParamField body="useScreen" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnResetPasswordRequest">ScreenMembersOnResetPasswordRequest</a></span>}>
  Ce hook contient des détails propres à l’écran `reset-password-request`, y compris sa configuration et son contexte.

  ```jsx Example
  import { useScreen } from '@auth0/auth0-acul-react/reset-password-request';
  function ScreenDebug() {
    const screen = useScreen();
  }
  ```
</ParamField>

<ParamField body="useTenant" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>}>
  Ce hook contient des données sur le tenant, comme son `id` et les métadonnées associées.

  ```jsx Example
  import { useTenant } from '@auth0/auth0-acul-react/reset-password-request';
  function TenantInfo() {
    const tenant = useTenant();
  }
  ```
</ParamField>

<ParamField body="useTransaction" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembersOnResetPasswordRequest">TransactionMembersOnResetPasswordRequest</a></span>}>
  Ce hook fournit des données sur la transaction propres à l’écran `reset-password-request`, comme les connexions actives et l’état actuel du flux.

  ```jsx Example
  import { useTransaction } from '@auth0/auth0-acul-react/reset-password-request';
  function TransactionInfo() {
    const transaction = useTransaction();
  }
  ```
</ParamField>

<ParamField body="useUntrustedData" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Ce hook traite les données non fiables transmises à l’écran, comme une adresse courriel ou un nom d’utilisateur prérempli à partir des paramètres d’URL.

  ```jsx Example
  import { useUntrustedData } from '@auth0/auth0-acul-react/reset-password-request';
  function PrefilledForm() {
    const untrustedData = useUntrustedData();
  }
  ```
</ParamField>

<ParamField body="useUser" type={<span>() =&gt; <a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>}>
  Ce hook fournit des renseignements sur l’utilisateur actif, y compris le `username`, l’`email` et les méthodes d’authentification disponibles.

  ```jsx Example
  import { useUser } from '@auth0/auth0-acul-react/reset-password-request';
  function UserProfile() {
    const user = useUser();
  }
  ```
</ParamField>

<ParamField body="useResetPasswordRequest" type={<a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordRequestMembers">ResetPasswordRequestMembers</a>}>
  Ce hook renvoie toutes les méthodes et le contexte disponibles sur l’écran `reset-password-request`.
</ParamField>

<div id="methods">
  ## Méthodes
</div>

<ParamField body="backToLogin" type="Promise<void>">
  Cette méthode redirige l’utilisateur vers l’écran de connexion et annule le flux de réinitialisation du mot de passe.

  ```jsx Example
  import { useResetPasswordRequest } from '@auth0/auth0-acul-react/reset-password-request';

  function BackToLoginButton() {
    const { backToLogin } = useResetPasswordRequest();
    return (
      <button onClick={() => backToLogin()}>
        Back to Login
      </button>
    );
  }
  ```
</ParamField>

<ParamField body="resetPassword" type="Promise<void>">
  Cette méthode envoie le nom d’utilisateur ou l’adresse courriel pour lancer le processus de réinitialisation du mot de passe.

  ```jsx Example
  import { useResetPasswordRequest } from '@auth0/auth0-acul-react/reset-password-request';

  function ResetPasswordForm() {
    const { resetPassword } = useResetPasswordRequest();
    return (
      <button onClick={() => resetPassword({ username: 'user@example.com' })}>
        Send Reset Link
      </button>
    );
  }
  ```

  **Paramètres de la méthode**

  <Expandable title="Paramètres">
    <ParamField body="options">
      [ResetPasswordRequestOptions](/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordRequestOptions).
    </ParamField>

    <ParamField body="username" type="string" required>
      Le nom d’utilisateur ou l’adresse courriel du compte à réinitialiser.
    </ParamField>

    <ParamField body="captcha?" type="string">
      Le jeton de réponse CAPTCHA, requis lorsque CAPTCHA est activé pour le tenant.
    </ParamField>
  </Expandable>
</ParamField>

<div id="commonutility-hooks">
  ## Hooks communs et utilitaires
</div>

<ParamField body={<a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Hooks/useLoginIdentifiers">useLoginIdentifiers</a>} type="Hooks">
  Ce hook renvoie les types d’identifiants de connexion (courriel, nom d’utilisateur, téléphone) configurés pour la connexion actuelle.
</ParamField>

<ParamField body={<a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Hooks/useAuth0Themes">useAuth0Themes</a>} type="Hooks">
  Ce hook récupère les options du thème actuel avec une configuration aplatie provenant du contexte d’image de marque.
</ParamField>

<ParamField body={<a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Hooks/useChangeLanguage">useChangeLanguage</a>} type="Hooks">
  Ce hook renvoie une fonction permettant de changer la langue d’affichage sur l’écran ACUL actuel.
</ParamField>

<ParamField body={<a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Hooks/useCurrentScreen">useCurrentScreen</a>} type="Hooks">
  Ce hook récupère le contexte et l’état de l’écran actuel.
</ParamField>

<ParamField body={<a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Hooks/useErrors">useErrors</a>} type="Hooks">
  Ce hook lit et gère les erreurs côté serveur, côté client et côté développeur à l’écran.
</ParamField>