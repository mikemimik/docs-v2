---
title: "useLoginIdentifiers"
---

<ParamField body="useLoginIdentifiers()" type={<span><a href="/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/type-aliases/IdentifierType">IdentifierType[]</a></span>}>
  Retourne une liste des types d’identifiant actifs (par exemple, `'email'`, `'phone'` ou `'username'`)
  actuellement utilisés dans le flux d’authentification ou le processus de connexion.

  ## Retourne

  [`IdentifierType`](/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/type-aliases/IdentifierType)[]

  Un tableau de [IdentifierType](/docs/fr-CA/libraries/acul/react-sdk/API-Reference/Types/type-aliases/IdentifierType) représentant les types d’identifiant actifs.

  ## Écrans pris en charge

  * `login`
  * `login-id`
  * `reset-password-request`

  ```tsx Example
  import { useLoginIdentifiers } from '@auth0/auth0-acul-react/login';

  const loginIdentifiers = useLoginIdentifiers();
  const hasEmail = loginIdentifiers.includes('email');
  const hasPhone = loginIdentifiers.includes('phone');
  const hasUsername = loginIdentifiers.includes('username');

  // loginIdentifiers pourrait être :
  // ['email', 'username']
  ```
</ParamField>