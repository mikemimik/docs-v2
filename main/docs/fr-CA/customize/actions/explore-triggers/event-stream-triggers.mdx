---
description: Découvrez les Actions Event Stream.
'og:image': https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
'og:title': Déclencheurs Event Stream
'og:url': https://auth0.com/docs/
permalink: event-stream-trigger
title: Déclencheurs Event Stream
sidebarTitle: Vue d’ensemble
'twitter:description': Découvrez les Actions Event Stream, qui s’exécutent dans le cadre du flux Event Stream.
'twitter:title': Déclencheurs Event Stream
---

Les Actions Event Stream sont des fonctions qui exécutent une logique de façon asynchrone lorsque des événements précis se produisent dans votre tenant Auth0. Il s’agit d’actions non bloquantes qui n’ont aucune incidence sur la latence de l’expérience utilisateur.

Chaque Action Event Stream est associée à un Event Stream et écoute un ensemble prédéfini de types d’événements (par exemple, les connexions réussies, les changements de mot de passe ou les suppressions d’utilisateurs). Lorsqu’un événement auquel l’Action est abonnée se produit, celle-ci est déclenchée. Contrairement aux Actions Login ou Pre-Registration, les Actions Event Stream s’exécutent en arrière-plan et n’affectent pas la transaction principale de l’utilisateur.

<Frame>![Schéma illustrant le TBD.](/docs/images/customize/actions/event-stream-triggers.svg)</Frame>

Ces Actions sont non bloquantes (asynchrones) et sont abonnées à un ensemble précis de [Types d’événements](/docs/fr-CA/customize/events/event-types).

<div id="triggers">
  ## Déclencheurs
</div>

<div id="event-stream">
  ### Event Stream
</div>

Les Actions `event-stream` sont des fonctions qui s’exécutent lorsque les types d’événements auxquels vous êtes abonné surviennent.

<div id="references">
  #### Références
</div>

* [Objet Event](/docs/fr-CA/customize/actions/explore-triggers/event-stream-triggers/event-stream-event-object) : fournit le contexte pour le message Event Stream ainsi que pour l’exécution de l’Action.
* [Objet API](/docs/fr-CA/customize/actions/explore-triggers/event-stream-triggers/event-stream-api-object) : fournit des méthodes permettant de modifier le comportement du flux.

<div id="use-cases">
  ## Cas d’usage
</div>

<div id="synchronization">
  ### Synchronisation
</div>

Une Action event-stream peut être utilisée pour transmettre un événement précis à un service externe selon une logique personnalisée. L’Action suivante montre comment transmettre de façon sécuritaire un message d’événement à un service externe à l’aide d’une API key enregistrée.

```javascript lines
/**
* Handler à exécuter lors du traitement des événements dans un flux d'événements.
* @param {Event} event - Détails sur l'événement entrant.
* @param {EventStreamAPI} api - Méthodes et utilitaires pour définir le traitement du flux d'événements.
*/
exports.onExecuteEventStream = async (event, api) => {
  const message = event.message;

  try {
    await fetch(event.secrets.URL, {
      method: 'POST',
      headers: {
        'X-API-Key': event.secrets.API_KEY,
      },
      body: JSON.stringify(message),
    });
  } catch (err) {
    throw new Error('External service failure');
  }

  return;
};
```

Pour en savoir plus sur la création d’Actions, consultez [Write Your First Action](/docs/fr-CA/customize/actions/write-your-first-action).

Pour en savoir plus sur la création d’un Event Stream, consultez [Create an Event Stream](/docs/fr-CA/customize/events/create-an-event-stream#create-an-event-stream-actions).