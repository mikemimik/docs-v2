---
description: Comment authentifier et autoriser un appareil Tessel à l’aide d’Auth0.
title: Authentifier et autoriser un appareil Tessel avec Auth0
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

[Tessel](https://tessel.io) est une carte formidable. Non seulement elle offre d’excellentes caractéristiques matérielles et une excellente extensibilité, mais vous pouvez aussi la programmer en Javascript ! Lorsqu’elle a été annoncée sur Kickstarter, nous l’avons immédiatement soutenue et avons attendu de longues semaines avant de pouvoir mettre la main sur un exemplaire.

<Frame>![Tessel - Diagramme des ports de Tessel](/docs/images/cdy7uua7fh8z/5mSnxvHvgWrHtCwk35z4Db/891c39e289c58d97c7a4047084d118a5/TM-00-04-ports.png)</Frame>

Elle est finalement arrivée, et nous pouvons écrire notre premier programme : obtenir un token d’Auth0 et appeler une API.

Tessel vise une compatibilité complète avec Javascript. La plupart des modules de base de Node.js fonctionnent aussi, mais pas tous. Consultez [la documentation de Tessle sur Github](https://github.com/tessel/docs/blob/master/compatibility.md) pour plus de détails.

<div id="the-sample">
  ## L’exemple
</div>

Cet exemple est simple :

1. Nous appelons le endpoint Resource Owner d’Auth0 avec des identifiants d’appareil
2. Nous obtenons un token en retour
3. Nous utilisons le token pour appeler une API

<Frame>![Tessel - Diagramme du flux de Tessel vers Auth0](/docs/images/cdy7uua7fh8z/3iyOfO564gkbyQSsvTJE9c/a981ca1166d1384ba2e5e7dd72f5a255/2023-09-22_13-14-21.png)</Frame>

export const codeExample = `var http = require('https');
var tessel = require('tessel');

tessel.syncClock(function () {

  var device_id = 'tessel-01';
  var password = 'LE MOT DE PASSE DE TESSEL';

  authenticate(device_id, password, function(e,token){

    if(e) return console.log("Erreur :" + e);

    getDeviceProfile(token.access_token, function(e, profile){
      console.log("Profil de l’appareil :");
      console.log(profile);
    });
  });

  function getDeviceProfile(token, done){
    request('{yourDomain}',
          '/userinfo',
          'GET',
          {
          "Content-type": "application/json",
          "Authorization": "Bearer " + token
        },
        null,
        function(e,response){
          if(e) return done(e);
          done(null, JSON.parse(response));
        });
  }

  function authenticate(device_id, password, done)
  {
    request('{yourDomain}',
          '/oauth/ro',
          'POST',
          {
          "Content-type": "application/json",
        },
        JSON.stringify({
            client_id:   '{yourClientId}',
            username:    device_id,
            password:    password,
            connection:  'devices',
            grant_type:  "password",
            scope: 'openid'
          }),
          function(e,response){
            if(e) return done(e);
            done(null, JSON.parse(response));
        });
  }

  function request(host, path, method, headers, body, done){
    var options = {
      hostname: host,
      path: path,
      method: method,
      headers: headers
    };

    var req = http.request(options, function(res) {
      res.setEncoding('utf8');

      var response = "";

      res.on('data', function (chunk) {
        response += chunk;
      });

      res.on('end', function(){
        done(null, response);
        });
    });

    req.on('error', function(e) {
      done(e);
    });

    if( body ) req.write(body);
    req.end();
  }
});`;

<AuthCodeBlock children={codeExample} language="javascript" />

Points saillants du code :

1. Le code est compatible à 99 % avec Node (le seul module propre à l’appareil est `tessel`, que nous utilisons uniquement pour nous assurer que tous les appels SSL s’effectuent avec des références temporelles adéquates).
2. La fonction `request` est un simple encapsuleur autour des fonctions du module `http`. Le module `request` ne fonctionne pas actuellement dans Tessel.

Le endpoint <Tooltip tip="Resource Owner : entité (comme un utilisateur ou une application) capable d’accorder l’accès à une ressource protégée." cta="Voir le glossaire" href="/docs/fr-CA/glossary?term=Resource+Owner">Resource Owner</Tooltip> exige des identifiants (comme un nom d’utilisateur et un mot de passe); le répertoire d’utilisateurs du backend connecté à Auth0 doit donc prendre cela en charge (comme une Database connection ou Active Directory).

<div id="tessel-setup">
  ## Configuration de Tessel
</div>

* Exécutez `tessel update` pour vous assurer d’installer le micrologiciel le plus récent compatible avec SSL.
* Vous aurez évidemment besoin d’une connexion au Web. Vous pouvez configurer le WiFi avec la commande `tessel wifi`.
* Envoyez toujours des identifiants (comme `username`/`password`) sur des réseaux sécurisés.

<div id="summary">
  ## Aperçu
</div>

Tessel est formidable. Nous lui voyons beaucoup de potentiel. Cet exemple montre à quel point il est facile de l’intégrer à Auth0.