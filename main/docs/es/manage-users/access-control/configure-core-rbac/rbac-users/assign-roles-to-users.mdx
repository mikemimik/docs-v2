---
description: Aprende a asignar roles a un usuario desde el Auth0 Dashboard o la Management API.
title: Asignar roles a usuarios
---

import {AuthCodeBlock} from "/snippets/es/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/es/AuthCodeGroup.jsx";

Puedes asignar [roles](/docs/es/manage-users/access-control/rbac) a un usuario mediante el <Tooltip tip="Auth0 Dashboard: el producto principal de Auth0 para configurar tus servicios." cta="Ver glosario" href="/docs/es/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip> o la <Tooltip tip="Auth0 Dashboard: el producto principal de Auth0 para configurar tus servicios." cta="Ver glosario" href="/docs/es/glossary?term=Management+API">Management API</Tooltip>. Los roles asignados pueden usarse con el conjunto de funcionalidades Authorization Core para API.

<div id="prerequisites">
  ## Prerrequisitos
</div>

* Para que el control de acceso basado en roles (RBAC) funcione correctamente, debe habilitarlo para su API mediante el Dashboard o la Management API. La funcionalidad de Authorization Core es diferente de la de Authorization Extension. Para ver una comparación, lea [Authorization Core vs. Authorization Extension](/docs/es/manage-users/access-control/authorization-core-vs-authorization-extension).
* Los roles se seleccionan de valores predefinidos. Si la lista de roles está vacía, asegúrese de [crear roles](/docs/es/manage-users/access-control/configure-core-rbac/roles/create-roles) primero.

<div id="dashboard">
  ## Dashboard
</div>

Hay dos formas de asignar un rol a un usuario. Puede elegir un usuario de la lista de **Users** y luego asignarle un rol, o bien ir a la página **User Details** (perfil del usuario) de un usuario concreto y elegir el rol que quiere asignar en la pestaña **Roles**.

Si tiene más de 50 roles, solo se muestran los primeros 50 en la pestaña **Roles**. Los demás roles pueden asignarse mediante la Management API.

<div id="assign-roles-in-user-list">
  ### Asignar roles en la lista de usuarios
</div>

1. Ve a [Dashboard &gt; User Management &gt; Users](https://manage.auth0.com/#/users).
2. Haz clic en `...` junto al usuario que quieres modificar y selecciona **Assign Roles**.
3. Elige los roles que deseas asignar y, a continuación, haz clic en **Assign**.

<div id="assign-roles-in-user-profile">
  ### Asignar roles en el perfil del usuario
</div>

También puede asignar roles a los usuarios desde su página de perfil individual.

1. Vaya a [Dashboard &gt; User Management &gt; Users](https://manage.auth0.com/#/users) y haga clic en el nombre del usuario.
2. Haga clic en la vista **Roles** y luego en **Asignar rol**.
3. Elija el rol que desea asignar y haga clic en **Asignar.**

<div id="management-api">
  ## Management API
</div>

Haz una llamada `POST` al [endpoint Assign User Roles](https://auth0.com/docs/api/management/v2#!/Users/post_user_roles). Asegúrate de sustituir los placeholder values `USER_ID`, `MGMT_API_ACCESS_TOKEN` y `ROLE_ID` por tu user ID, el <Tooltip tip="Access Token: credencial de autorización, en forma de una cadena opaca o JWT, utilizada para acceder a una API." cta="Ver glosario" href="/docs/es/glossary?term=access+token">access token</Tooltip> de Management API y los ID de rol correspondientes, respectivamente.

<AuthCodeGroup>
  ```bash cURL
  curl --request POST \
    --url 'https://{yourDomain}/api/v2/users/USER_ID/roles' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN' \
    --header 'cache-control: no-cache' \
    --header 'content-type: application/json' \
    --data '{ "roles": [ "ROLE_ID", "ROLE_ID" ] }'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/users/USER_ID/roles");
  var request = new RestRequest(Method.POST);
  request.AddHeader("content-type", "application/json");
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  request.AddHeader("cache-control", "no-cache");
  request.AddParameter("application/json", "{ "roles": [ "ROLE_ID", "ROLE_ID" ] }", ParameterType.RequestBody);
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"strings"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/users/USER_ID/roles"

  	payload := strings.NewReader("{ "roles": [ "ROLE_ID", "ROLE_ID" ] }")

  	req, _ := http.NewRequest("POST", url, payload)

  	req.Header.Add("content-type", "application/json")
  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
  	req.Header.Add("cache-control", "no-cache")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.post("https://{yourDomain}/api/v2/users/USER_ID/roles")
    .header("content-type", "application/json")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .header("cache-control", "no-cache")
    .body("{ "roles": [ "ROLE_ID", "ROLE_ID" ] }")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'POST',
    url: 'https://{yourDomain}/api/v2/users/USER_ID/roles',
    headers: {
      'content-type': 'application/json',
      authorization: 'Bearer MGMT_API_ACCESS_TOKEN',
      'cache-control': 'no-cache'
    },
    data: {roles: ['ROLE_ID', 'ROLE_ID']}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/users/USER_ID/roles",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "POST",
    CURLOPT_POSTFIELDS => "{ "roles": [ "ROLE_ID", "ROLE_ID" ] }",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN",
      "cache-control: no-cache",
      "content-type: application/json"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  payload = "{ "roles": [ "ROLE_ID", "ROLE_ID" ] }"

  headers = {
      'content-type': "application/json",
      'authorization': "Bearer MGMT_API_ACCESS_TOKEN",
      'cache-control': "no-cache"
      }

  conn.request("POST", "/{yourDomain}/api/v2/users/USER_ID/roles", payload, headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/users/USER_ID/roles")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Post.new(url)
  request["content-type"] = 'application/json'
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'
  request["cache-control"] = 'no-cache'
  request.body = "{ "roles": [ "ROLE_ID", "ROLE_ID" ] }"

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

| Valor                   | Descripción                                                                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `USER_ID`               | El ID del usuario que se va a actualizar.                                                                                            |
| `MGMT_API_ACCESS_TOKEN` | [Access Token for the Management API](https://auth0.com/docs/api/management/v2/tokens) con los scopes `read:roles` y `update:users`. |
| `ROLE_ID`               | El ID del rol o de los roles que desea agregar al usuario especificado.                                                              |

<div id="learn-more">
  ## Más información
</div>

* [Administrar roles del control de acceso basado en roles](/docs/es/manage-users/access-control/configure-core-rbac/roles)
* [Ver Roles asignados a Users](/docs/es/manage-users/access-control/configure-core-rbac/rbac-users/view-user-roles)
* [Crear Roles](/docs/es/manage-users/access-control/configure-core-rbac/roles/create-roles)
* [Eliminar Roles](/docs/es/manage-users/access-control/configure-core-rbac/roles/delete-roles)
* [Quitar Roles de Users](/docs/es/manage-users/access-control/configure-core-rbac/rbac-users/remove-roles-from-users)
* [Agregar permisos a Roles](/docs/es/manage-users/access-control/configure-core-rbac/roles/add-permissions-to-roles)