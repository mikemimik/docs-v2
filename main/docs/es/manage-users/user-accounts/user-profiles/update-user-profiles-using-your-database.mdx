---
description: Aprende a actualizar los perfiles de usuario al usar tu propia base de datos como proveedor de identidad.
title: Actualizar perfiles de usuario con tu base de datos
---

Actualiza los perfiles de usuario cuando [uses tu propia base de datos como proveedor de identidad](/docs/es/authenticate/database-connections/custom-db) realizando las siguientes tareas:

* Usa la [Management API](https://auth0.com/docs/api/management/v2#!/Users/patch_users_by_id).
* Actualiza al usuario en tu base de datos.
* [Configura la migración de usuario](/docs/es/manage-users/user-migration/configure-automatic-migration-from-your-database) desde tu base de datos a Auth0.

<div id="update-users-with-the-management-api">
  ## Actualizar usuarios con la Management API
</div>

Si usas tu propia base de datos para la autenticación, puedes usar la [Management API](https://auth0.com/docs/api/management/v2) para actualizar los siguientes campos:

* `app_metadata`
* `user_metadata`
* `blocked`

Si necesitas actualizar otros campos del usuario, tendrás que hacerlo directamente en tu base de datos.

<div id="update-users-in-your-database">
  ## Actualizar usuarios en su base de datos
</div>

Puede actualizar los perfiles de usuario en su base de datos como lo hace normalmente, y Auth0 actualizará el perfil de usuario almacenado en caché la próxima vez que ese usuario inicie sesión.

El perfil de usuario en la base de datos personalizada puede implementarse con cualquier estructura de perfil de usuario, pero debe mapearlo en la llamada Login a los atributos del perfil de usuario normalizado de Auth0, como se muestra en la plantilla de base de datos personalizada &quot;Login&quot;.

Puede acceder a las plantillas de la base de datos personalizada desde
Connections -&gt; Database -&gt; Custom Database. Asegúrese de activar el toggle &quot;Use my own database&quot; para habilitar la edición de los scripts.

Consulte la sección [Caché del perfil de usuario](#user-profile-cache) a continuación para ver un breve resumen de cómo Auth0 almacena en caché los perfiles de usuario.

<div id="update-users-through-migration">
  ## Actualizar usuarios mediante la migración
</div>

Si ha [habilitado la migración de usuario](/docs/es/manage-users/user-migration/configure-automatic-migration-from-your-database) y un usuario ya se ha migrado a la base de datos de Auth0, Auth0 no volverá a consultar su base de datos para obtener el perfil del usuario. Por lo tanto, los cambios realizados en la base de datos personalizada para ese usuario no se reflejarán en Auth0.

Una vez que se haya migrado un usuario, también podrá actualizar campos como `name`, `nickname`, `given_name`, `family_name`, `picture`, `email` y `email_verified` a través de la <Tooltip tip="Management API: Un producto que permite a los clientes realizar tareas administrativas." cta="Ver glosario" href="/docs/es/glossary?term=Management+API">Management API</Tooltip>.

Sin embargo, las reglas para actualizar otros campos del usuario seguirán aplicándose, tal como se describe en el [Perfil de usuario normalizado](/docs/es/manage-users/user-accounts/user-profiles/normalized-user-profiles).

<div id="user-profile-cache">
  ## Caché del perfil de usuario
</div>

Auth0 almacena en caché el perfil de usuario recibido de una [conexión de base de datos](/docs/es/authenticate/database-connections) antes de enviarlo a la aplicación cliente. Esta caché se guarda en la base de datos de Auth0 y se actualiza cada vez que el usuario se autentica.

Los valores en caché de los campos del [perfil de usuario normalizado](/docs/es/manage-users/user-accounts/user-profiles/normalized-user-profile-schema) se basan en los valores devueltos por el script de inicio de sesión de tu conexión de base de datos personalizada.

El perfil de usuario se almacena en caché por varias razones. En primer lugar, el almacenamiento en caché te permite implementar [inicio de sesión único (SSO)](/docs/es/authenticate/single-sign-on) en la capa de Auth0 para evitar tener que consultar la conexión en cada solicitud. Además, esto aporta resiliencia si una conexión no está disponible temporalmente.