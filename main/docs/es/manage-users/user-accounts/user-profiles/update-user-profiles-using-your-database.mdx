---
description: Aprende a actualizar perfiles de usuario cuando uses tu propia base de datos como proveedor de identidad.
title: Actualizar perfiles de usuario con tu base de datos
---

Actualiza perfiles de usuario cuando [uses tu propia base de datos como proveedor de identidad](/docs/es/authenticate/database-connections/custom-db) realizando las siguientes tareas:

* Usa la [Management API](https://auth0.com/docs/api/management/v2#!/Users/patch_users_by_id).
* Actualiza el usuario en tu base de datos.
* [Configura la migración de usuario](/docs/es/manage-users/user-migration/configure-automatic-migration-from-your-database) desde tu base de datos a Auth0.

<div id="update-users-with-the-management-api">
  ## Actualiza usuarios con la Management API
</div>

Si usas tu propia base de datos para la autenticación, puedes usar la [Management API](https://auth0.com/docs/api/management/v2) para actualizar los siguientes campos:

* `app_metadata`
* `user_metadata`
* `blocked`

Si necesitas actualizar otros campos del usuario, tendrás que hacerlo directamente en tu base de datos.

<div id="update-users-in-your-database">
  ## Actualiza usuarios en tu base de datos
</div>

Puedes actualizar los perfiles de usuario en tu base de datos como lo haces normalmente, y Auth0 actualizará el perfil de usuario que tiene en caché la próxima vez que ese usuario inicie sesión.

El perfil de usuario de la base de datos personalizada puede implementarse con cualquier estructura de perfil de usuario, pero debes mapearlo en la llamada a Login a los atributos del perfil de usuario normalizado de Auth0, como se muestra en la plantilla de base de datos personalizada &quot;Login&quot;.

Puedes acceder a las plantillas de la base de datos personalizada en
Connections -&gt; Database -&gt; Custom Database. Asegúrate de activar la opción &quot;Use my own database&quot; para poder editar los scripts.

Consulta la sección [Caché del perfil de usuario](#user-profile-cache) a continuación para obtener un breve resumen de cómo Auth0 almacena en caché los perfiles de usuario.

<div id="update-users-through-migration">
  ## Actualizar usuarios mediante la migración
</div>

Si has [habilitado la migración de usuario](/docs/es/manage-users/user-migration/configure-automatic-migration-from-your-database) y un usuario ya fue migrado a la base de datos de Auth0, Auth0 no volverá a consultar tu base de datos para obtener el perfil del usuario. Por lo tanto, los cambios realizados en la base de datos personalizada para ese usuario nunca se reflejarán en Auth0.

Una vez que un usuario haya sido migrado, también podrás actualizar campos como `name`, `nickname`, `given_name`, `family_name`, `picture`, `email` y `email_verified` a través de la <Tooltip tip="Management API: Un producto que permite a los clientes realizar tareas administrativas." cta="Ver glosario" href="/docs/es/glossary?term=Management+API">Management API</Tooltip>.

Sin embargo, las reglas para actualizar otros campos del usuario seguirán aplicándose, como se describe en el [Perfil de usuario normalizado](/docs/es/manage-users/user-accounts/user-profiles/normalized-user-profiles).

<div id="user-profile-cache">
  ## Caché del perfil de usuario
</div>

Auth0 almacena en caché el perfil de usuario recibido de una [conexión de base de datos](/docs/es/authenticate/database-connections) antes de enviarlo a la aplicación cliente. Esta caché se guarda en la base de datos de Auth0 y se actualiza cada vez que el usuario se autentica.

Los valores almacenados en caché de los campos del [Perfil de usuario normalizado](/docs/es/manage-users/user-accounts/user-profiles/normalized-user-profile-schema) se basan en los valores devueltos por el script de inicio de sesión de tu conexión de base de datos personalizada.

El perfil de usuario se almacena en caché por varias razones. En primer lugar, el almacenamiento en caché te permite implementar [inicio de sesión único (SSO)](/docs/es/authenticate/single-sign-on) en la capa de Auth0 para evitar tener que recurrir a la conexión en cada solicitud. Además, esto ofrece resiliencia si una conexión no está disponible temporalmente.