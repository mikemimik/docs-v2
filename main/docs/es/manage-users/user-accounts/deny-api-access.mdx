---
description: Aprende a usar Authorization Extension de Auth0 para denegar a los usuarios el acceso a una API mediante Rules.
title: Denegar el acceso de los usuarios a una API con Rules
---

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Solo para usar con Authorization Extension de Auth0. Si estás usando el conjunto de funcionalidades de Authorization Core, debes usar en su lugar los [dialectos del token integrados](/docs/es/get-started/apis/enable-role-based-access-control-for-apis#token-dialect-options). Para obtener más información, consulta [Authorization Core vs. Authorization Extension](/docs/es/manage-users/access-control/authorization-core-vs-authorization-extension).
</Callout>

Ve a [Dashboard &gt; Auth0 Pipeline &gt; Rules](https://manage.auth0.com/#/rules). Puedes configurar [Rules](/docs/es/customize/rules) para distintos fines, desde la gestión de usuarios hasta el enriquecimiento de perfiles de usuario. Si necesitas denegar a un usuario el acceso a tu API, puedes crear Roles con scopes asignados y, después, crear una rule para quitar scopes del <Tooltip tip="Access Token: Credencial de autorización, en forma de una cadena opaca o JWT, utilizada para acceder a una API." cta="Ver glosario" href="/docs/es/glossary?term=Access+Token">Access Token</Tooltip>:

```javascript lines
{
function (user, context, callback) {
  var permissions = user.permissions || [];
  var requestedScopes = context.request.body.scope || context.request.query.scope;
  var filteredScopes = requestedScopes.split(' ').filter( function(x) {
      return x.indexOf(':') < 0;
  });

  var allScopes = filteredScopes.concat(permissions);
  context.accessToken.scope = allScopes.join(' ');

  callback(null, user, context);
}
```