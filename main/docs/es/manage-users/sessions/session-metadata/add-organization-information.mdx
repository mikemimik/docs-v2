---
description: Aprende cómo agregar los metadatos de la organización a los metadatos de la sesión
title: 'Casos de uso: información de la organización en los metadatos de la sesión'
validatedOn: 2026-02-10
---

Los metadatos de la sesión hacen que los datos de contexto del usuario sean portátiles y visibles durante todo el ciclo de vida de la sesión y en los eventos de cierre de sesión.

Los sistemas de destino pueden usar la información de los metadatos de la sesión para realizar auditorías, análisis y aplicar flujos de revocación, entre otros usos.

<Warning>
  Auth0 Session Metadata no es un almacén de datos seguro y no debe usarse para almacenar información sensible. Esto incluye secretos y PII de alto riesgo, como números de seguridad social o de tarjetas de crédito. Se recomienda encarecidamente a los clientes de Auth0 que evalúen los datos almacenados en los metadatos y que solo almacenen los necesarios para fines de gestión de identidades y accesos. Para obtener más información, consulta [Cumplimiento del Reglamento General de Protección de Datos de Auth0](/docs/es/secure/data-privacy-and-compliance/gdpr).
</Warning>

<div id="add-organization-information-to-session-metadata">
  ## Añade información de la organización a los metadatos de la sesión
</div>

Puedes usar [Actions](/docs/es/customize/actions) para almacenar los identificadores de la organización en una sesión con el método `api.session.setMetadata()` de [Post Login](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger) y consultarlos mediante el objeto `event.session.metadata`.

Código de la Action de Post Login:

```javascript lines
/**
 * Post-Login Action (simple)
 * Agrega el contexto de organización a los metadatos de la sesión para que aparezca en Actions posteriores,
 * la Management API, y (si está habilitado) el token de cierre de sesión por back-channel.
 */
exports.onExecutePostLogin = async (event, api) => {
  // Continuar solo si la transacción apunta a una organización
  if (!event.organization) return;

  // Mantener los valores cortos y solo de tipo string (los metadatos de la sesión requieren strings)
  const orgId = String(event.organization.id || "");
  const orgSlug = String(event.organization.name || "");
  const orgDisplay = String(event.organization.display_name || orgSlug);

  // Escrituras mínimas e idempotentes (pocas claves para mantenerse bien por debajo de los límites)
  api.session.setMetadata("org_id", orgId);
  api.session.setMetadata("org_slug", orgSlug);
  api.session.setMetadata("org_name", orgDisplay);
};
```

Los metadatos de la sesión están disponibles para las Actions posteriores, pueden recuperarse mediante la [Management API](https://auth0.com/docs/api/management/v2) y pueden incluirse en el token de [OpenID Connect Back-Channel Logout](/docs/es/authenticate/login/logout/back-channel-logout).

* En las Actions posteriores, puedes consultar los datos mediante el objeto `event.session.metadata`:

```javascript lines
const orgId = event.session.metadata?.org_id;
```

* Si usa la Management API, puede consultar los datos mediante el endpoint [/api/v2/sessions/{id}](https://auth0.com/docs/api/management/v2/sessions/get-session):

```bash lines
GET /api/v2/sessions/{id}
```

Respuesta de ejemplo:

```json lines
{
  "session_metadata": {
    "org_id": "org_abc123",
    "org_slug": "acme",
    "org_name": "Acme Corp"
  }
}
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Las llamadas al endpoint `/api/v2/sessions/{id}` requieren un [token de acceso de la Management API](/docs/es/secure/tokens/access-tokens) con el scope `update:session`.
</Callout>

* Si tiene una aplicación configurada con [metadatos de la sesión para cierre de sesión OIDC por canal secundario](/docs/es/manage-users/sessions/session-metadata/configure-session-metadata), el `logout_token` incluye los metadatos de la sesión:

```json lines
{
  "events": { "http://schemas.openid.net/event/backchannel-logout": {} },
  "session_metadata": {
    "org_id": "org_abc123",
    "org_slug": "acme",
    "org_name": "Acme Corp"
  }
}
```