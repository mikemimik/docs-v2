---
description: Aprenda a configurar los metadatos de la sesión de Auth0
title: Configurar los metadatos de la sesión
validatedOn: 2026-02-10
---

Para configurar los metadatos de la sesión, puede usar una [Action de Post Login](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger) de Auth0 y la [Management API](https://auth0.com/docs/api/management/v2). También puede incluirlos en el token de [OpenID Connect cierre de sesión por back-channel](/docs/es/authenticate/login/logout/back-channel-logout).

<Warning>
  Los metadatos de la sesión de Auth0 no son un almacén de datos seguro y no deben usarse para almacenar información confidencial. Esto incluye secretos y PII de alto riesgo, como números de la seguridad social o de tarjetas de crédito. Se recomienda encarecidamente a los clientes de Auth0 que evalúen los datos almacenados en los metadatos y que solo guarden los necesarios para fines de gestión de identidades y accesos. Para obtener más información, lea [Cumplimiento de Auth0 con el Reglamento General de Protección de Datos](/docs/es/secure/data-privacy-and-compliance/gdpr).
</Warning>

## Auth0 Management API

Puedes gestionar las solicitudes CRUD (crear, reemplazar, actualizar y eliminar) de los metadatos de la sesión mediante la Management API:

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Las llamadas al endpoint `/api/v2/sessions/{id}` requieren un [token de acceso de la Management API](/docs/es/secure/tokens/access-tokens) con el `scope` `update:session`.
</Callout>

<div id="retrieve-existing-session-metadata">
  ### Recuperar los metadatos existentes de la sesión
</div>

Realiza una solicitud `GET` al endpoint [`/api/v2/sessions/{id}`](https://auth0.com/docs/api/management/v2/sessions/get-session):

```bash lines
GET /api/v2/sessions/{id}
```

<div id="add-or-update-existing-session-metadata">
  ### Agregar o actualizar los metadatos de una sesión existente
</div>

Haz una solicitud `PATCH` al endpoint [`/api/v2/sessions/{id}`](https://auth0.com/docs/api/management/v2/sessions/patch-sessions-by-id):

```bash lines
PATCH /api/v2/sessions/{id}
Content-Type: application/json

{
  "session_metadata": {
    "my_metadata": "my new metadata"
  }
}
```

<div id="delete-session-metadata">
  ### Eliminar metadatos de la sesión
</div>

Realiza una solicitud `PATCH` al endpoint [`/api/v2/sessions/{id}`](https://auth0.com/docs/api/management/v2/sessions/patch-sessions-by-id):

```bash lines
PATCH /api/v2/sessions/{id}
Content-Type: application/json

{
  "session_metadata": {}
}
```

<div id="auth0-post-login-actions">
  ## Actions de Post Login de Auth0
</div>

Puedes realizar operaciones CRUD sobre los metadatos de la sesión mediante los objetos `api.session` con una [Action de Post Login](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger). Esto te permite gestionar los metadatos de la sesión según la lógica específica del usuario o del contexto.

<div id="retrieve-existing-session-metadata">
  ### Recuperar los metadatos existentes de la sesión
</div>

Usa el objeto `event.session.metadata?.deviceName` para leer el metadato `deviceName`:

```javascript lines
const device = event.session.metadata?.deviceName;
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  El objeto `event.session.metadata` incluye metadatos definidos en:
  *Actions anteriores dentro del mismo flujo
  *Transacciones previas si se reutilizó la sesión
</Callout>

<div id="add-or-update-existing-metadata">
  ### Agregar o actualizar metadatos existentes
</div>

Usa el método `api.session.setMetadata()` para actualizar los metadatos de la sesión:

```javascript lines
api.session.setMetadata("deviceName", "Auth0's iPhone");
```

Los cambios están disponibles de inmediato en el objeto `event.session` en las Actions siguientes.

<div id="delete-session-metadata">
  ### Eliminar metadatos de la sesión
</div>

Utilice los siguientes objetos `api.session` para eliminar metadatos de la sesión:

* `api.session.deleteMetadata("key")` elimina el metadato de la sesión especificado

* `api.session.evictMetadata()` elimina todos los metadatos de la sesión

Para obtener más información sobre estos objetos, consulte:

* [Event object](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger/post-login-event-object): Obtenga información sobre el objeto Event del token de actualización y sus propiedades.
* [API object](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger/post-login-api-object): Obtenga información sobre el objeto de API del token de actualización y sus métodos.

<div id="oidc-back-channel-logout">
  ## OIDC cierre de sesión por back-channel
</div>

Puede configurar el `logout_token` para que incluya metadatos de la sesión a través del [Dashboard](/docs/es/get-started/auth0-overview/dashboard) o de la [Management API](https://auth0.com/docs/api/management/v2).

<div id="auth0-dashboard">
  ### Auth0 Dashboard
</div>

Para configurar el token de cierre de sesión OIDC por canal secundario con metadatos de la sesión:

1. Ve a [Dashboard &gt; Aplicaciones](https://manage.auth0.com/#/applications) y selecciona tu aplicación.

2. Selecciona la pestaña **Configuración**.

3. En **OpenID Connect cierre de sesión por back-channel &gt; URL de cierre de sesión por back-channel**, agrega la URI de cierre de sesión de la aplicación que recibirá los logout&#95;tokens.

4. Configura **Iniciadores del cierre de sesión por back-channel** como una de estas opciones:

   * **Solo los iniciadores seleccionados** o

   * **Todos los iniciadores compatibles**

5. Activa **Incluir metadatos de la sesión**.

6. Selecciona **Guardar cambios**.

Una vez configurado, el `logout_token` incluirá todos los metadatos de la sesión almacenados.

<div id="auth0-management-api">
  ### Auth0 Management API
</div>

Puedes usar el endpoint [`/api/v2/clients/{id}`](https://auth0.com/docs/api/management/v2/sessions/patch-sessions-by-id) para actualizar tu aplicación de modo que incluya metadatos de la sesión en el `logout_token`.

Haz una solicitud `PATCH` al endpoint [`/api/v2/sessions/{id}`](https://auth0.com/docs/api/management/v2/sessions/patch-sessions-by-id):

```json lines
"oidc_backchannel_logout": {
  "backchannel_logout_initiators": {
    "mode": "all"
  },
  "backchannel_logout_urls": [
    "https://httpdump.app/inspect/9bccf574-e55f-4b2e-9822-f37372588fc1"
  ],
  "backchannel_logout_session_metadata": {
    "include": true
  }
}
```

<div id="error-handling">
  ## Manejo de errores
</div>

Puede consultar los eventos de log de los metadatos de la sesión en [Dashboard &gt; monitoreo &gt; Logs](https://manage.auth0.com/#/logs) o recuperar logs mediante el endpoint de [logs de la Management API](/docs/es/api/management/v2/logs/get-logs).

* Si se produce un error al agregar o actualizar los metadatos de la sesión con Actions, la transacción de autenticación falla y se devuelve un error a la URL de devolución de llamada.

Se registra un código de evento de error `f` con el error correspondiente:

```json lines
{
  "error": "access_denied",
  "error_description": "Failed to set session metadata: Invalid metadata: Metadata keys may only include letters, numbers, underscores, or hyphens",
  "state": "my-custom-state"
}
```

* Si se produce un error al gestionar los metadatos de la sesión mediante Auth0 Management API, la API responde con un error `estado HTTP: 400` y el mensaje correspondiente:

```json lines
{
  "statusCode": 400,
  "message": "Metadata must not exceed 25 entries. Each key and value must be ≤ 255 characters."
}
```