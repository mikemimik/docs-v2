---
#
# ESTE ARCHIVO SE GENERA AUTOMÁTICAMENTE.
# NO LO EDITE MANUALMENTE.
#
description: Obtén información sobre el objeto `event` del trigger de Action custom-email-provider, que contiene información contextual sobre la ejecución del trigger.
title: 'Actions Triggers: custom-email-provider - Objeto event'
validatedOn: 2026-04-28
---

El objeto `event` del trigger de Actions custom-email-provider contiene información contextual sobre la ejecución del trigger.

<div id="eventclient">
  ## `event.client`
</div>

<ResponseField name="event.client" type="dictionary">
  Información sobre el cliente con el que se inició esta transacción de inicio de sesión.

  <Expandable title="propiedades del cliente" defaultOpen>
    <ResponseField name="client_id" type="string">
      El ID de cliente de la aplicación en la que el usuario inicia sesión.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Un objeto que contiene otras propiedades de la aplicación.
    </ResponseField>

    <ResponseField name="name" type="string">
      El nombre de la aplicación (tal como se define en el Dashboard).
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventconnection">
  ## `event.connection`
</div>

<ResponseField name="event.connection" type="dictionary" post={["optional"]}>
  Detalles sobre la conexión utilizada para autenticar al usuario.

  <Expandable title="propiedades de la conexión" defaultOpen>
    <ResponseField name="id" type="string">
      El identificador único de la conexión.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      Metadatos asociados a la conexión.
    </ResponseField>

    <ResponseField name="name" type="string">
      El nombre de la conexión utilizada para autenticar al usuario (como `twitter` o `some-g-suite-domain`).
    </ResponseField>

    <ResponseField name="strategy" type="string">
      El tipo de conexión. En las conexiones sociales, `event.connection.strategy === event.connection.name`. En las conexiones empresariales, la estrategia es `waad` (Windows Azure AD), `ad` (Active Directory/LDAP), `auth0` (conexiones de base de datos), etc.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventcustom_domain">
  ## `event.custom_domain`
</div>

<ResponseField name="event.custom_domain" type="dictionary" post={["optional"]}>
  Detalles sobre el dominio personalizado asociado a la transacción actual.

  <Expandable title="propiedades de custom_domain" defaultOpen>
    <ResponseField name="domain" type="string">
      El nombre de dominio personalizado.
    </ResponseField>

    <ResponseField name="domain_metadata" type="dictionary">
      Metadatos del dominio personalizado en pares clave-valor.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventnotification">
  ## `event.notification`
</div>

<ResponseField name="event.notification" type="object">
  <Expandable title="Propiedades de la notificación" defaultOpen>
    <ResponseField name="from" type="string">
      Dirección de correo electrónico del remitente.
    </ResponseField>

    <ResponseField name="html" type="string">
      Plantilla HTML renderizada.
    </ResponseField>

    <ResponseField name="locale" type="string">
      El locale en el que renderizamos el mensaje, por ejemplo `en_US`, según se define en la especificación BCP-47.
    </ResponseField>

    <ResponseField name="message_type" type="string">
      El tipo de mensaje que se envía, como `verify_email` o `welcome_email`.

      Valores posibles:

      * `verify_email`
      * `verify_email_by_code`
      * `reset_email`
      * `reset_email_by_code`
      * `welcome_email`
      * `verification_code`
      * `mfa_oob_code`
      * `enrollment_email`
      * `blocked_account`
      * `stolen_credentials`
      * `try_provider_configuration_email`
      * `organization_invitation`
    </ResponseField>

    <ResponseField name="subject" type="string">
      Asunto del correo electrónico.
    </ResponseField>

    <ResponseField name="text" type="string">
      Plantilla de texto renderizada.
    </ResponseField>

    <ResponseField name="to" type="string">
      Dirección de correo electrónico del destinatario.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventorganization">
  ## `event.organization`
</div>

<ResponseField name="event.organization" type="dictionary" post={["optional"]}>
  Detalles de la organización asociada a la transacción actual.

  <Expandable title="propiedades de la organización" defaultOpen>
    <ResponseField name="id" type="string">
      El identificador de la organización.
    </ResponseField>

    <ResponseField name="display_name" type="string">
      El nombre descriptivo de la organización.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Metadatos asociados a la organización.
    </ResponseField>

    <ResponseField name="name" type="string">
      El nombre de la organización.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrequest">
  ## `event.request`
</div>

<ResponseField name="event.request" type="dictionary">
  <Expandable title="propiedades de la solicitud" defaultOpen>
    <ResponseField name="geoip" type="dictionary" post={["optional"]}>
      <Expandable title="propiedades de geoip" defaultOpen>
        <ResponseField name="cityName" type="string" post={["optional"]} />

        <ResponseField name="continentCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode3" type="string" post={["optional"]} />

        <ResponseField name="countryName" type="string" post={["optional"]} />

        <ResponseField name="latitude" type="number" post={["optional"]} />

        <ResponseField name="longitude" type="number" post={["optional"]} />

        <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

        <ResponseField name="subdivisionName" type="string" post={["optional"]} />

        <ResponseField name="timeZone" type="string" post={["optional"]} />
      </Expandable>
    </ResponseField>

    <ResponseField name="hostname" type="string" post={["optional"]}>
      El hostname que se usa en el flujo de autenticación.
    </ResponseField>

    <ResponseField name="ip" type="string" post={["optional"]}>
      La dirección IP desde la que se originó la solicitud.
    </ResponseField>

    <ResponseField name="query" type="dictionary" post={["optional"]}>
      Los parámetros de la cadena de consulta enviados en la solicitud de autorización.
    </ResponseField>

    <ResponseField name="user_agent" type="string" post={["optional"]}>
      El valor del encabezado `User-Agent` recibido al iniciar la transacción.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsecrets">
  ## `event.secrets`
</div>

<ResponseField name="event.secrets" type="secrets">
  Valores secretos asociados de forma segura a esta Action.
</ResponseField>

<div id="eventtenant">
  ## `event.tenant`
</div>

<ResponseField name="event.tenant" type="object">
  <Expandable title="propiedades del tenant" defaultOpen>
    <ResponseField name="id" type="string">
      El nombre del tenant.
    </ResponseField>

    <ResponseField name="friendly_name" type="string" post={["optional"]}>
      El nombre descriptivo del tenant, normalmente una versión más fácil de leer del ID.
    </ResponseField>

    <ResponseField name="home_url" type="string" post={["optional"]}>
      La URL de inicio del tenant, si está definida y tal como aparece en su configuración.
    </ResponseField>

    <ResponseField name="logo_url" type="string" post={["optional"]}>
      La URL del logotipo del tenant, si está definida y tal como aparece en su configuración.
    </ResponseField>

    <ResponseField name="support_email" type="string" post={["optional"]}>
      El correo electrónico del servicio de soporte del tenant, si está definido y tal como aparece en su configuración.
    </ResponseField>

    <ResponseField name="support_url" type="string" post={["optional"]}>
      La URL del servicio de soporte del tenant, si está definida y tal como aparece en su configuración.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtransaction">
  ## `event.transaction`
</div>

<ResponseField name="event.transaction" type="object" post={["optional"]}>
  Detalles de la transacción actual para fines de seguimiento.

  <Expandable title="propiedades de la transacción" defaultOpen>
    <ResponseField name="correlation_id" type="string" post={["optional"]}>
      Un identificador único para correlacionar esta solicitud entre varios servicios para el seguimiento distribuido.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventuser">
  ## `event.user`
</div>

<ResponseField name="event.user" type="dictionary">
  Un objeto que describe al usuario en cuyo nombre se inició la transacción actual.

  <Expandable title="propiedades del usuario" defaultOpen>
    <ResponseField name="app_metadata" type="dictionary">
      Campos personalizados que almacenan información sobre un usuario que influye en su acceso, como el plan de soporte, los roles de seguridad o los grupos de control de acceso.
    </ResponseField>

    <ResponseField name="email" type="string" post={["optional"]}>
      (único) Dirección de correo electrónico del usuario.
    </ResponseField>

    <ResponseField name="email_verified" type="boolean">
      Indica si el usuario ha verificado su dirección de correo electrónico.
    </ResponseField>

    <ResponseField name="family_name" type="string" post={["optional"]}>
      Apellido del usuario.
    </ResponseField>

    <ResponseField name="given_name" type="string" post={["optional"]}>
      Nombre de pila del usuario.
    </ResponseField>

    <ResponseField name="name" type="string" post={["optional"]}>
      Nombre completo del usuario.
    </ResponseField>

    <ResponseField name="nickname" type="string" post={["optional"]}>
      Alias del usuario.
    </ResponseField>

    <ResponseField name="picture" type="string" post={["optional"]}>
      URL que apunta a la [foto de perfil del usuario](https://auth0.com/docs/users/change-user-picture).
    </ResponseField>

    <ResponseField name="user_id" type="string">
      (único) Identificador único del usuario.
    </ResponseField>

    <ResponseField name="user_metadata" type="dictionary">
      Campos personalizados que almacenan información sobre un usuario que no influye en aquello a lo que puede o no acceder, como la dirección laboral, la dirección particular o las preferencias del usuario.
    </ResponseField>

    <ResponseField name="username" type="string" post={["optional"]}>
      (único) Nombre de usuario.
    </ResponseField>
  </Expandable>
</ResponseField>