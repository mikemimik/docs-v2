---
description: Más información sobre el flujo Post User Registration y el trigger de Action post-user-registration, que se ejecuta después de que se crea un usuario para una conexión Database o Passwordless.
sidebarTitle: Información general
title: Trigger de Post-user Registration
---

El trigger Post-user Registration se ejecuta después de que se agrega un usuario a una conexión Database o <Tooltip tip="Passwordless: Forma de autenticación que no depende de una contraseña como primer factor." cta="Ver glosario" href="/docs/es/glossary?term=Passwordless">Passwordless</Tooltip>.

<Frame>![Diagrama del flujo Post User Registration de Actions.](/docs/images/cdy7uua7fh8z/4bqF9YEPYQshnJGCx39pws/079798c354f4a83355a756a6a02650d5/post-user-registration-flow.png)</Frame>

Las Actions de este flujo no bloquean la ejecución (son asincrónicas), lo que significa que el pipeline de Auth0 seguirá ejecutándose sin esperar a que la Action termine. Por lo tanto, el resultado de la Action no afecta la transacción de Auth0.

<div id="triggers">
  ## Triggers
</div>

<div id="post-user-registration">
  ### Post User Registration
</div>

El trigger `post-user-registration` se ejecuta después de que se haya creado un usuario en una conexión Database o Passwordless. Este trigger puede usarse para notificar a otro sistema que un usuario se ha registrado en tu aplicación. Se pueden vincular varias Actions a este trigger, y se ejecutarán en orden. Sin embargo, estas Actions se ejecutarán de forma asíncrona y no bloquearán el proceso de registro del usuario.

<div id="references">
  ### Referencias
</div>

* [objeto de evento](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/post-user-registration-trigger/post-user-registration-event-object): Proporciona información contextual sobre el usuario recién creado.
* [objeto de API](/docs/es/customize/actions/explore-triggers/signup-and-login-triggers/post-user-registration-trigger/api-object): Proporciona métodos para cambiar el comportamiento del flujo.

<div id="common-use-cases">
  ## Casos de uso comunes
</div>

<div id="notify-slack-when-a-new-user-registers">
  ### Notificar a Slack cuando se registra un nuevo usuario
</div>

```javascript lines
/**
* Manejador que será invocado durante la ejecución de un flujo PostUserRegistration.
* 
 * @param {Event} event - Detalles sobre el contexto y el usuario que se ha registrado.
 * @param {PostUserRegistrationAPI} api - Interfaz cuyos métodos pueden usarse para modificar el comportamiento del registro posterior de usuarios.
 */

exports.onExecutePostUserRegistration = async (event, api) => {
  const { IncomingWebhook } = require("@slack/webhook");
  const webhook = new IncomingWebhook(event.secrets.SLACK_WEBHOOK_URL);

  const text = `New User: ${event.user.email}`;
  const channel = '#some_channel';

  webhook.send({ text, channel });
};
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Para que esta Action se ejecute correctamente, debe contener un Secret llamado `SLACK_WEBHOOK_URL` y tener como dependencia el paquete `@slack/webhook` de `npm`.
</Callout>

<div id="store-the-auth0-user-id-in-a-remote-system">
  ### Guardar el ID de usuario de Auth0 en un sistema remoto
</div>

Se puede usar una Action post-user-registration para guardar el ID de usuario de Auth0 en un sistema remoto.

```javascript lines
/**
* Manejador que será invocado durante la ejecución de un flujo PostUserRegistration.
* 
* @param {Event} event - Detalles sobre el contexto y el usuario que se ha registrado.
* @param {PostUserRegistrationAPI} api - Interfaz cuyos métodos pueden utilizarse para modificar el comportamiento del registro posterior de usuarios.
*/

const axios = require("axios");

exports.onExecutePostUserRegistration = async (event, api) => {
  await axios.post("https://my-api.exampleco.com/users", { params: { email: event.user.email }});
};
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Para usar una biblioteca de `npm` como `axios`, debes agregar la biblioteca a la Action como dependencia. Para obtener más información, consulta la sección &quot;Agregar una dependencia&quot; de [Write Your First Action](/docs/es/customize/actions/write-your-first-action).
</Callout>

<div id="deny-access-to-specific-ja3ja4-fingerprints">
  ### Denegar el acceso a huellas JA3/JA4 específicas
</div>

El objeto `event.security_context` contiene los valores de las huellas JA3/JA4 de la transacción actual.

```js lines
exports.onExecutePreUserRegistration = async (event, api) => {
  const clientJa4 = event?.security_context?.ja4;
  console.log('[ACTION]', {clientJa4});
  const badFingerprints = ['t13d1517h2_8daaf6152771_b6f405a00624','t13d1516h2_8daaf6152771_d8a2da3f94cd'];
  if (clientJa4 && badFingerprints.includes(clientJa4)){
    api.access.deny('suspicious_tls_fingerprint', 'Your TLS fingerprint has been flagged as suspicious');
  }
};
```