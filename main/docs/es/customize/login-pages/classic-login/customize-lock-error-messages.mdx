---
description: Personalizar los mensajes de error con Lock
title: Personalizar los mensajes de error en Lock
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

<Warning>
  En este momento, los esfuerzos de desarrollo activo de Auth0 se centran en Universal Login, y Classic Login ya no recibe actualizaciones. Para personalizar tus páginas de inicio de sesión, Auth0 recomienda [Advanced Customizations for Universal Login](/docs/es/customize/login-pages/advanced-customizations).
</Warning>

Puedes personalizar los mensajes de error que se mostrarán en determinadas situaciones mediante la opción `languageDictionary`. Puedes encontrar una lista completa de los campos `languageDictionary` disponibles para personalizar en el [archivo English Dictionary de Lock](https://github.com/auth0/lock/blob/master/src/i18n/en.js) del repositorio de GitHub. A continuación, se muestra un ejemplo de algunos mensajes de error personalizados:

{/* codeblockOld.header.login.logInButton codeblockOld.header.login.configureSnippet */}

export const codeExample = `// Ejemplos de mensajes de error personalizados en la opción languageDictionary
var options = {
  languageDictionary: {
    error: {
      login: {
        "lock.invalid_email_password": "Mensaje personalizado sobre credenciales no válidas",
        "lock.network": "Mensaje personalizado que indica un error de red y sugiere al usuario que compruebe la conexión",
        "lock.unauthorized": "Mensaje personalizado sobre un problema de permisos",
        "too_many_attempts": "Mensaje personalizado que indica que el usuario ha intentado iniciar sesión demasiadas veces sin éxito."
      },
      signUp: {
        "invalid_password": "Mensaje personalizado que indica que la contraseña no era válida",
        "user_exists": "Mensaje personalizado que indica que el usuario ya existe"
      }
    }
  }
};

// Inicialización de Auth0Lock
var lock = new Auth0Lock(
  '{yourClientId}',
  '{yourDomain}',
  options
);`;

<AuthCodeBlock children={codeExample} language="javascript" />

<div id="custom-errors-in-rules">
  ## Errores personalizados en Rules
</div>

Si devuelves códigos de error personalizados desde una [rule](/docs/es/customize/rules) o un script de base de datos personalizado, puedes gestionar errores personalizados:

* En la URL de redirección de tu aplicación, leyendo los parámetros de query string `error` y `error_mesage`.
* Redirigiendo al usuario de vuelta a tus páginas alojadas con un mensaje de error personalizado y mostrando el mensaje con un [mensaje flash](/docs/es/libraries/lock/lock-api-reference).

<div id="learn-more">
  ## Más información
</div>

* [Lock for Web](/docs/es/libraries/lock)