---
description: Aprende a configurar un proveedor telefónico personalizado con Twilio Verify
title: Configurar un proveedor telefónico personalizado con Twilio Verify
---

Puedes configurar un proveedor telefónico personalizado con [Twilio Verify](https://www.twilio.com/docs/verify), que ayuda a combatir el fraude y a proteger las cuentas de usuario. De este modo, Twilio Verify quedará configurado como tu proveedor telefónico personalizado.

<div id="prerequisites">
  ## Requisitos previos
</div>

Debe tener una cuenta de [Twilio](https://twilio.com/) con una opción válida de entrega por SMS y/o por llamada telefónica.

<div id="set-up-twilio-verify-service">
  ## Configurar el servicio Twilio Verify
</div>

Puedes crear un servicio Twilio Verify a través de la [Consola de Twilio](https://www.twilio.com/console/verify/services) o de la [API de REST de Verify](https://www.twilio.com/docs/verify/api/service#create-a-verification-service). Necesitarás lo siguiente para conectar tu proveedor telefónico personalizado de Auth0 con Twilio Verify:

* TWILIO&#95;ACCOUNT&#95;SID
* TWILIO&#95;AUTH&#95;TOKEN
* TWILIO&#95;VERIFY&#95;SID

<div id="connect-custom-phone-provider-with-twilio-verify">
  ## Conectar un proveedor telefónico personalizado con Twilio Verify
</div>

1. En el Auth0 Dashboard, vaya a [Branding &gt; Proveedor telefónico](https://manage.auth0.com/dashboard/#/phone/templates/phone/provider). Esto lo llevará a la página **Proveedor de mensajes telefónicos**.
2. Seleccione **Custom** como **Proveedor telefónico**.
3. En **Configuración del proveedor**, agregue el siguiente ejemplo de código para permitir que la API de Twilio envíe mensajes telefónicos al número de teléfono del usuario:

```javascript lines expandable
/**
* Manejador que se ejecuta al enviar una notificación telefónica
* @param {Event} event - Detalles sobre el usuario y el contexto en el que está iniciando sesión.
* @param {CustomPhoneProviderAPI} api - Métodos y utilidades para modificar el comportamiento del envío de una notificación telefónica.
*/
exports.onExecuteCustomPhoneProvider = async (event, api) => {
 const { TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_VERIFY_SID } = event.secrets;

  const client = require('twilio')(TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN);

  // asignar el valor de voz de auth0 a llamada
  const messageType = event.notification.delivery_method === 'voice' ? 'call' : 'sms';

  const { recipient, code } = event.notification;

  // agregar esto para corregir " 1333444999  "
  // "333 444 5555"
  const sanitizedNumber = recipient.replace(/\s/g, '').trim();

  await client.verify.v2.services(TWILIO_VERIFY_SID)
    .verifications.create({
      to: sanitizedNumber,
      channel: messageType,
      customCode: code
    })

};
```

3. Haz clic en el ícono de **Key** para abrir el menú **Secrets**. Agrega los siguientes valores de la configuración de tu servicio Twilio Verify:

* TWILIO&#95;ACCOUNT&#95;SID
* TWILIO&#95;AUTH&#95;TOKEN
* TWILIO&#95;VERIFY&#95;SID

4. Para cargar la biblioteca auxiliar de Twilio, haz clic en el ícono de **Dependency**. Luego, haz clic en el botón **Add Dependency**. Cuando aparezca la ventana emergente **Add Dependency**, ingresa lo siguiente:

* **Name:** Twilio
* **Version:** latest (debería completarse automáticamente)

Al hacer clic en **Create**, Auth0 buscará la biblioteca auxiliar de Twilio y cargará la versión más reciente.

5. Haz clic en **Save**. Al hacer clic en **Save,** la Action se guarda y se implementa automáticamente. Para probar la configuración del proveedor telefónico personalizado antes de usarla en un entorno de producción, haz clic en **Enviar mensaje de prueba**.