---
title: "Revocar un token de actualización"
description: "Invalida un token de actualización para evitar que vuelva a utilizarse."
---

import { ResponseSchema } from '/snippets/es/ResponseSchema.jsx';

<div id="endpoint">
  ## Endpoint
</div>

`POST /oauth/revoke`

Usa este endpoint para invalidar un token de actualización si se ha visto comprometido.

El comportamiento de este endpoint depende del estado de la opción [Refresh Token Revocation Deletes Grant](https://auth0.com/docs/secure/tokens/refresh-tokens/revoke-refresh-tokens#choose-whether-token-revocation-deletes-grants).
Si esta opción está habilitada, cada solicitud de revocación invalida no solo el token específico, sino también todos los demás tokens basados en la misma concesión de autorización. Esto significa que **todos los tokens de actualización que se hayan emitido para el mismo usuario, aplicación y audiencia se revocarán**.
Si esta opción está deshabilitada, solo se revoca el token de actualización, mientras que la concesión permanece intacta.

<div id="remarks">
  ## Observaciones
</div>

* Para las aplicaciones no confidenciales que no pueden mantener seguro el secreto de cliente (por ejemplo, las aplicaciones nativas), el endpoint permite no enviar ningún secreto de cliente, pero la propia aplicación debe tener la propiedad `tokenEndpointAuthMethod` establecida en `none`. Puede hacerlo desde la UI ([Dashboard &gt; Applications &gt; Configuración de la aplicación](https://manage.auth0.com/dashboard)) o mediante la [Management API](https://auth0.com/docs/api/management/v2/clients/patch-clients-by-id).

<div id="learn-more">
  ## Más información
</div>

* [Tokens de actualización](https://auth0.com/docs/ja-jp/secure/tokens/refresh-tokens)

<div id="body-parameters">
  ## Parámetros del cuerpo
</div>

<ParamField body="client_id" type="string" required>
  El `client_id` de tu aplicación.
</ParamField>

<ParamField body="client_assertion" type="string">
  Un JWT que contiene una aserción firmada con las credenciales de tu aplicación. Obligatorio cuando la clave privada JWT es el método de autenticación de la aplicación.
</ParamField>

<ParamField body="client_assertion_type" type="string">
  El valor es `urn:ietf:params:oauth:client-assertion-type:jwt-bearer`. Obligatorio cuando la clave privada JWT es el método de autenticación de la aplicación.
</ParamField>

<ParamField body="client_secret" type="string">
  El `client_secret` de tu aplicación. Obligatorio cuando Client Secret Basic o Client Secret Post son el método de autenticación de la aplicación. Es obligatorio **solo** para las aplicaciones web clásicas.
</ParamField>

<ParamField body="token" type="string" required>
  El token de actualización que quieres revocar.
</ParamField>

<div id="response-schema">
  ## Esquema de respuesta
</div>

<ResponseSchema>
  Una respuesta 200 exitosa no tiene cuerpo. Las respuestas de error usan esta estructura:

  <ResponseField name="error" type="string">
    Código de error.
  </ResponseField>

  <ResponseField name="error_description" type="string">
    Descripción del error.
  </ResponseField>
</ResponseSchema>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado      | Descripción           |
| ----------- | --------------------- |
| 200         | (empty-response-body) |
| 400         | Solicitud incorrecta  |
| 401         | No autorizado         |
| por defecto | Error inesperado      |