---
title: "Revocar un token de actualización"
description: "Invalida un token de actualización para impedir que se vuelva a usar."
---

import { ResponseSchema } from '/snippets/ResponseSchema.jsx';

<div id="endpoint">
  ## Endpoint
</div>

`POST /oauth/revoke`

Usa este endpoint para invalidar un token de actualización si se ha visto comprometido.

El comportamiento de este endpoint depende del estado del toggle [Refresh Token Revocation Deletes Grant](https://auth0.com/docs/secure/tokens/refresh-tokens/revoke-refresh-tokens#choose-whether-token-revocation-deletes-grants).
Si este toggle está habilitado, cada solicitud de revocación invalida no solo el token específico, sino también todos los demás tokens basados en la misma concesión de autorización. Esto significa que **todos los tokens de actualización emitidos para el mismo usuario, aplicación y audience serán revocados**.
Si este toggle está deshabilitado, solo se revoca el token de actualización, mientras que la concesión permanece intacta.

<div id="remarks">
  ## Observaciones
</div>

* En el caso de las aplicaciones no confidenciales que no pueden proteger el secreto de cliente (por ejemplo, las aplicaciones nativas), el endpoint permite omitir el secreto de cliente, pero la propia aplicación debe tener la propiedad `tokenEndpointAuthMethod` establecida en `none`. Puedes hacerlo desde la UI ([Dashboard &gt; Applications &gt; Application Settings](https://manage.auth0.com/dashboard)) o mediante la [Management API](https://auth0.com/docs/api/management/v2/clients/patch-clients-by-id).

<div id="learn-more">
  ## Más información
</div>

* [Tokens de actualización](https://auth0.com/docs/ja-jp/secure/tokens/refresh-tokens)

<div id="body-parameters">
  ## Parámetros del cuerpo
</div>

<ParamField body="client_id" type="string" required>
  El `client_id` de tu aplicación.
</ParamField>

<ParamField body="client_assertion" type="string">
  Un JWT que contiene una aserción firmada con las credenciales de tu aplicación. Obligatorio cuando la clave privada JWT es el método de autenticación de la aplicación.
</ParamField>

<ParamField body="client_assertion_type" type="string">
  El valor es `urn:ietf:params:oauth:client-assertion-type:jwt-bearer`. Obligatorio cuando la clave privada JWT es el método de autenticación de la aplicación.
</ParamField>

<ParamField body="client_secret" type="string">
  El `client_secret` de tu aplicación. Obligatorio cuando el método de autenticación de la aplicación es secreto de cliente Basic o secreto de cliente Post. Solo es obligatorio para las aplicaciones web clásicas.
</ParamField>

<ParamField body="token" type="string" required>
  El token de actualización que quieres revocar.
</ParamField>

<div id="response-schema">
  ## Esquema de la respuesta
</div>

<ResponseSchema>
  Una respuesta 200 satisfactoria no tiene cuerpo. Las respuestas de error tienen esta estructura:

  <ResponseField name="error" type="string">
    Código de error.
  </ResponseField>

  <ResponseField name="error_description" type="string">
    Descripción del error.
  </ResponseField>
</ResponseSchema>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado  | Descripción           |
| ------- | --------------------- |
| 200     | (empty-response-body) |
| 400     | Solicitud incorrecta  |
| 401     | No autorizado         |
| default | Error inesperado      |