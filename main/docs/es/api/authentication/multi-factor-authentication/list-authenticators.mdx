---
title: "Listar autenticadores"
description: "Devuelve una lista de autenticadores de MFA asociados al usuario."
---

import { ResponseSchema } from '/snippets/es/ResponseSchema.jsx';

<div id="endpoint">
  ## Endpoint
</div>

`GET /mfa/authenticators`

Devuelve una lista de autenticadores asociados a tu aplicación.

Para acceder a este endpoint, debes establecer un token de acceso en el encabezado Authorization, con los siguientes claims:

* `scope`: `read:authenticators`
* `audience`: `https://{yourDomain}/mfa/`

<div id="headers">
  ## Cabeceras
</div>

<ParamField header="Authorization" type="string" required>
  Bearer ACCESS&#95;TOKEN
</ParamField>

<div id="response-schema">
  ## Esquema de respuesta
</div>

<ResponseSchema>
  La respuesta es un arreglo de objetos de tipo autenticador.

  <ResponseField name="id" type="string" required>
    El ID del autenticador.
  </ResponseField>

  <ResponseField name="authenticator_type" type="string" required>
    El tipo de autenticador.
  </ResponseField>

  <ResponseField name="active" type="boolean" required>
    Indica si el autenticador está activo.
  </ResponseField>

  <ResponseField name="oob_channels" type="string">
    Los canales OOB compatibles con este autenticador.
  </ResponseField>

  <ResponseField name="name" type="string">
    El nombre del autenticador.
  </ResponseField>
</ResponseSchema>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                  |
| ------ | ---------------------------- |
| 200    | Una lista de autenticadores. |