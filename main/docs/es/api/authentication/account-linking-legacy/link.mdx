---
title: "Vincular"
description: "Endpoint obsoleto para vincular cuentas de usuario; usa en su lugar la Management API."
---

<div id="endpoint">
  ## Endpoint
</div>

`GET /authorize`

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Este endpoint está **obsoleto** para la vinculación de cuentas. En su lugar, debe usarse [POST /api/v2/users/{id}/identities](/api/management/v2/users/post-identities). Para más información, consulta el [Aviso de migración](/migrations/past-migrations#account-linking-removal).
</Callout>

Usa este endpoint cuando un usuario quiera vincular un segundo método de autenticación (por ejemplo, una conexión de base de datos de usuario/contraseña con Facebook).

Este endpoint activará el flujo de inicio de sesión para vincular una cuenta existente con una nueva. Devolverá una redirección 302 a la `conexión` que el usuario actual quiere agregar. El usuario se identifica mediante el token de acceso devuelto cuando el inicio de sesión se completa correctamente.

<div id="remarks">
  ### Notas
</div>

* El valor de `redirect_uri` debe especificarse como una URL de devolución de llamada válida en la [Configuración de la aplicación](https://manage.auth0.com/dashboard).

<div id="learn-more">
  ### Más información
</div>

* [Vincular cuentas de usuario](https://auth0.com/docs/manage-users/user-accounts/user-account-linking/link-user-accounts)
* [Escenario de vinculación de cuentas de usuario iniciada por los usuarios](http://auth0.com/docs/users/references/link-accounts-user-initiated-scenario)
* [Escenario de vinculación de cuentas de usuario del lado del servidor](https://auth0.com/docs/ja-jp/manage-users/user-accounts/user-account-linking/suggested-account-linking-server-side-implementation)

<div id="query-parameters">
  ## Parámetros de consulta
</div>

<ParamField query="response_type" type="string" required>
  Usa `code` para los flujos del lado del servidor y `token` para los flujos del lado del cliente.
</ParamField>

<ParamField query="client_id" type="string" required>
  El `client_id` de tu aplicación.
</ParamField>

<ParamField query="connection" type="string">
  El nombre de la conexión configurada para tu aplicación. Si es null, redirigirá a la [Auth0 Login Page](https://auth0.com/#/login_page) y mostrará el Login Widget usando la primera conexión de base de datos.
</ParamField>

<ParamField query="redirect_uri" type="string" required>
  La URL a la que Auth0 redirigirá el navegador después de que el usuario haya concedido la autorización.
</ParamField>

<ParamField query="access_token" type="string" required>
  El token de acceso del usuario que ha iniciado sesión.
</ParamField>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                                  |
| ------ | -------------------------------------------- |
| 302    | Redirige a la conexión para vincular cuentas |