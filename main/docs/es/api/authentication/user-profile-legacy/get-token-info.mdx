---
title: "Información del token"
description: "Endpoint obsoleto que valida un JWT y devuelve el perfil de usuario asociado; usa /userinfo en su lugar."
---

import { ResponseSchema } from '/snippets/ResponseSchema.jsx';

<div id="endpoint">
  ## Endpoint
</div>

`POST /tokeninfo`

<Note>
  Este endpoint forma parte del pipeline de autenticación heredada y se deshabilitará para quienes usen nuestro pipeline más reciente, compatible con OIDC. Recomendamos usar el [endpoint /userinfo](#get-user-info) en su lugar. Para obtener más información sobre el pipeline de autenticación más reciente, consulta [Introducing OIDC Conformant Authentication](/api-auth/intro).
</Note>

Este endpoint valida un JSON Web Token (JWT) (firma y expiración) y devuelve la información del usuario asociada al ID de usuario indicado en la propiedad `sub` del token.

<div id="remarks">
  ### Observaciones
</div>

* Este endpoint devuelve tres encabezados de respuesta HTTP que proporcionan datos relevantes sobre sus límites de frecuencia:
  * `X-RateLimit-Limit`: Número de solicitudes permitidas por minuto.
  * `X-RateLimit-Remaining`: Número de solicitudes disponibles. Cada nueva solicitud reduce este número en 1. Por cada minuto que transcurre, se vuelve a añadir una solicitud, por lo que este número aumenta en 1 cada vez.
  * `X-RateLimit-Reset`: Tiempo restante hasta que se restablezca el límite de frecuencia (`X-RateLimit-Limit`). El valor está en [segundos de la época UTC](https://en.wikipedia.org/wiki/Unix_time).

<div id="learn-more">
  ### Más información
</div>

* [Estructura del perfil de usuario](https://auth0.com/docs/manage-users/user-accounts/user-profiles/user-profile-structure)
* [Política de límite de frecuencia de la API de Auth0](https://auth0.com/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy)

<div id="response-schema">
  ## Esquema de respuesta
</div>

<ResponseSchema>
  <ResponseField name="email_verified" type="boolean">
    Indica si se ha verificado la dirección de correo electrónico del usuario.
  </ResponseField>

  <ResponseField name="email" type="string">
    La dirección de correo electrónico del usuario.
  </ResponseField>

  <ResponseField name="clientID" type="string">
    El ID de cliente asociado al token.
  </ResponseField>

  <ResponseField name="updated_at" type="string">
    Momento en que se actualizó por última vez la información del usuario.
  </ResponseField>

  <ResponseField name="name" type="string">
    El nombre completo del usuario.
  </ResponseField>

  <ResponseField name="picture" type="string">
    URL de la foto de perfil del usuario.
  </ResponseField>

  <ResponseField name="user_id" type="string">
    El identificador único del usuario.
  </ResponseField>

  <ResponseField name="nickname" type="string">
    El apodo del usuario.
  </ResponseField>

  <ResponseField name="provider" type="string">
    El proveedor de identidad del usuario.
  </ResponseField>

  <ResponseField name="connection" type="string">
    La conexión utilizada para autenticar al usuario.
  </ResponseField>

  <ResponseField name="isSocial" type="boolean">
    Indica si el usuario se autenticó mediante un proveedor social.
  </ResponseField>

  <ResponseField name="created_at" type="string">
    Momento en que se creó el usuario.
  </ResponseField>

  <ResponseField name="global_client_id" type="string">
    El ID de cliente global.
  </ResponseField>
</ResponseSchema>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                                   |
| ------ | --------------------------------------------- |
| 200    | Respuesta exitosa con información del usuario |