---
title: "Obtener código o enlace"
description: "Envía un código de un solo uso o un Magic Link al correo electrónico o al teléfono del usuario para iniciar la autenticación passwordless."
---

<div id="endpoint">
  ## Endpoint
</div>

`POST /passwordless/start`

Las conexiones passwordless no requieren que el usuario recuerde una contraseña. En su lugar, se utiliza otro mecanismo para verificar la identidad, como un código de un solo uso enviado por correo electrónico o SMS cada vez que el usuario inicia sesión.

Tienes tres opciones de [autenticación passwordless](https://auth0.com/docs/authenticate/passwordless):

* Enviar un código de verificación por correo electrónico.
* Enviar un enlace por correo electrónico.
* Enviar un código de verificación por SMS.

<div id="remarks">
  ### Observaciones
</div>

* Si enviaste un código de verificación, ya sea por correo electrónico o por SMS, una vez que lo recibas, debes autenticar al usuario mediante el [endpoint /passwordless/verify](/passwordless/authenticate-user), usando `email` o `phone_number` como `username` y el código de verificación como `password`.
* Este endpoint está diseñado para llamarse desde el client-side y está sujeto a [límites de frecuencia]https://auth0.com/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy).
* El script de ejemplo de auth0.js usa la versión 8 de la biblioteca. Si usas la versión 7 de auth0.js, consulta esta [guía de referencia](/libraries/auth0js/v7).

<div id="error-codes">
  ### Códigos de error
</div>

Para ver la referencia completa de los códigos de error de este endpoint, consulta [Errors &gt; POST /passwordless/start](#post-passwordless-start).

<div id="learn-more">
  ### Más información
</div>

* [Autenticación passwordless](https://auth0.com/docs/authenticate/passwordless)
* [Prácticas recomendadas para la autenticación passwordless](https://auth0.com/docs/authenticate/passwordless/best-practices)

<div id="body-parameters">
  ## Parámetros del cuerpo
</div>

<ParamField body="client_id" type="string" required>
  El `client_id` de tu aplicación.
</ParamField>

<ParamField body="client_assertion" type="string">
  Un JWT que contiene una aserción firmada con las credenciales de tu aplicación. Obligatorio cuando la clave privada JWT es el método de autenticación de la aplicación.
</ParamField>

<ParamField body="client_assertion_type" type="string">
  Usa el valor `urn:ietf:params:oauth:client-assertion-type:jwt-bearer`. Obligatorio cuando la clave privada JWT es el método de autenticación de la aplicación.
</ParamField>

<ParamField body="client_secret" type="string">
  El `client_secret` de tu aplicación. Obligatorio **solo** para aplicaciones web clásicas.
</ParamField>

<ParamField body="connection" type="string" required>
  Cómo enviar el código o enlace al usuario. Usa `email` para enviarlo por correo electrónico o `sms` para enviarlo por SMS.

  Valores permitidos: `email`, `sms`
</ParamField>

<ParamField body="email" type="string">
  Establécelo como la dirección de correo electrónico del usuario cuando `connection=email`.
</ParamField>

<ParamField body="phone_number" type="string">
  Establécelo como el número de teléfono del usuario cuando `connection=sms`.
</ParamField>

<ParamField body="send" type="string">
  Usa `link` para enviar un enlace o `code` para enviar un código de verificación. Si es `null`, se enviará un enlace.

  Valores permitidos: `link`, `code`
</ParamField>

<ParamField body="authParams" type="object">
  Cualquier parámetro de autenticación que quieras añadir, como `scope`, `state`, `redirect_uri`, etc.

  <Expandable title="propiedades">
    <ParamField body="scope" type="string">
      Scopes de OAuth.
    </ParamField>

    <ParamField body="state" type="string">
      Parámetro `state` para mantener el estado entre la solicitud y la devolución de llamada.
    </ParamField>
  </Expandable>
</ParamField>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                                        |
| ------ | -------------------------------------------------- |
| 200    | Código o enlace enviados correctamente.            |
| 400    | Solicitud incorrecta. Parámetros no válidos.       |
| 401    | No autorizado. Credenciales de cliente no válidas. |