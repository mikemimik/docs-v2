---
title: "Change Password"
description: "Envía un correo electrónico para cambiar la contraseña a un usuario para una conexión especificada."
---

import { ResponseSchema } from '/snippets/es/ResponseSchema.jsx';

<div id="endpoint">
  ## Endpoint
</div>

`POST /dbconnections/change_password`

Envía un correo electrónico para cambiar la contraseña a la dirección de correo electrónico y la `conexión` proporcionadas por el usuario.

Opcionalmente, puedes proporcionar un ID de organización para admitir variables específicas de la organización en las [plantillas de correo electrónico personalizadas](https://auth0.com/docs/customize/email/email-templates) e incluir los parámetros `organization_id` y `organization_name` en la URL de **Redirect To**.

**Nota:** Este endpoint solo funciona con conexiones de base de datos.

<div id="remarks">
  ### Observaciones
</div>

* Cuando el usuario haga clic en el enlace de cambio de contraseña, será redirigido a una página en la que se le pedirá que introduzca una nueva contraseña.
* Este endpoint devolverá tres encabezados de respuesta HTTP que proporcionan datos relevantes sobre sus límites de frecuencia:
  * `X-RateLimit-Limit`: Número de solicitudes permitidas por minuto.
  * `X-RateLimit-Remaining`: Número de solicitudes disponibles. Cada nueva solicitud reduce este número en 1. Con cada minuto que pasa, se vuelven a añadir solicitudes, por lo que este número aumenta en 1 cada vez.
  * `X-RateLimit-Reset`: Tiempo restante hasta que se restablezca el límite de frecuencia (`X-RateLimit-Limit`). El valor se expresa en [segundos de época UTC](https://en.wikipedia.org/wiki/Unix_time).

<div id="learn-more">
  ### Más información
</div>

* [Cambiar la contraseña de un usuario](https://auth0.com/docs/authenticate/database-connections/password-change)
* [Fortaleza de la contraseña en Auth0 Database Connections](https://auth0.com/docs/authenticate/database-connections/password-strength)
* [Opciones de contraseña en Auth0 Database Connections](https://auth0.com/docs/authenticate/database-connections/password-options)
* [Política de límite de tasa de la API de Auth0](https://auth0.com/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy/rate-limit-configurations)

<div id="body-parameters">
  ## Parámetros del cuerpo
</div>

<ParamField body="client_id" type="string" required>
  El `client_id` de tu cliente.
</ParamField>

<ParamField body="email" type="string" required>
  La dirección de correo electrónico del usuario.
</ParamField>

<ParamField body="connection" type="string" required>
  El nombre de la conexión de base de datos configurada para tu cliente.
</ParamField>

<ParamField body="organization" type="string">
  El `organization_id` de la Organización asociada al usuario.
</ParamField>

<div id="response-schema">
  ## Esquema de respuesta
</div>

<ResponseSchema type="string">
  El cuerpo de la respuesta 200 es una cadena de texto sin formato.

  <br />

  Ejemplo: `"Acabamos de enviarte un correo electrónico para restablecer tu contraseña."`
</ResponseSchema>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                                                     |
| ------ | --------------------------------------------------------------- |
| 200    | Se ha enviado un correo electrónico para cambiar la contraseña. |
| 400    | Solicitud no válida; faltan parámetros o son no válidos.        |
| 404    | Usuario no encontrado.                                          |