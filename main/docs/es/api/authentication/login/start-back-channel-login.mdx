---
title: "Back-Channel Login"
description: "Envíe una solicitud de autenticación directamente a un dispositivo registrado del usuario mediante el flujo Back-Channel Login de CIBA."
---

<div id="endpoint">
  ## Endpoint
</div>

`POST /bc-authorize`

El endpoint de Back-Channel Login permite a las aplicaciones enviar una solicitud de autenticación al teléfono de un usuario o al dispositivo de autenticación, siempre que tengan una aplicación instalada y estén registrados para [notificaciones push con el Guardian SDK](https://auth0.com/docs/secure/multi-factor-authentication/auth0-guardian#enroll-in-push-notifications).

Usa el endpoint de Back-Channel Login para autenticar usuarios en los siguientes casos de uso:

* Los usuarios no están frente a la aplicación que requiere autenticación, como cuando llaman a un centro de atención telefónica.
* El dispositivo de consumo, o el dispositivo desde el que el usuario utiliza un servicio, no es seguro para operaciones sensibles (p. ej., un navegador web para transacciones financieras).
* El dispositivo de consumo tiene una capacidad de interacción limitada (p. ej., bicicletas o patinetes eléctricos).

<div id="response-body">
  ### Cuerpo de la respuesta
</div>

Si la solicitud se procesa correctamente, deberías recibir una respuesta como la siguiente:

```http
{
  "auth_req_id": "eyJh...",
  "expires_in": 300,
  "interval": 5
}
```

El valor de `auth_req_id` debe conservarse, ya que se usa más adelante en el flujo para identificar la solicitud de autenticación.

El valor de `expires_in` indica cuántos segundos faltan para que caduque la solicitud de autenticación.

El valor de `interval` indica cuántos segundos debes esperar entre una solicitud de consulta y la siguiente.

La solicitud debe aprobarse o rechazarse en el dispositivo de autenticación del usuario mediante el Guardian SDK.

<div id="remarks">
  ### Observaciones
</div>

* El valor de `login_hint` tiene el formato `iss_sub`. Por ejemplo:

```http
{ "format": "iss_sub", "iss": "https://[TENANT_DOMAIN]/", "sub": "auth0|[USER ID]" }
```

* La autenticación puede realizarse con secreto de cliente (mediante HTTP Basic Auth o Post), clave privada JWT o mTLS. Consulta los ejemplos detallados anteriores.

<div id="body-parameters">
  ## Parámetros del cuerpo
</div>

<ParamField body="client_id" type="string" required>
  El `client_id` de tu aplicación.
</ParamField>

<ParamField body="binding_message" type="string" required>
  Una cadena legible para el usuario que le permita asegurarse de que aprueba la solicitud correcta.
</ParamField>

<ParamField body="login_hint" type="string" required>
  Un objeto JSON que contiene los detalles del usuario para la autenticación en formato `iss_sub`.
</ParamField>

<ParamField body="scope" type="string" required>
  Lista de scopes de OIDC y de API personalizadas, separados por espacios, que debe incluir `openid` como mínimo.
</ParamField>

<ParamField body="audience" type="string">
  Identificador único opcional de la API de destino para los tokens de acceso.
</ParamField>

<ParamField body="resource" type="string">
  El identificador de la API de destino (servidor de recursos) a la que quieres acceder. Debe coincidir con un identificador de API registrado en tu tenant de Auth0. Se usa como alternativa a `audience` cuando el [Resource Parameter Compatibility Profile](https://auth0.com/docs/get-started/tenant-settings#settings-advanced) del tenant está configurado como `compatibility`.
</ParamField>

<ParamField body="requested_expiry" type="string">
  Tiempo de expiración personalizado de la solicitud en segundos (1-300). El valor predeterminado es 300 segundos.
</ParamField>

<ParamField body="authorization_details" type="array">
  Un arreglo JSON opcional de objetos que describen los permisos que se autorizarán. El valor `type` de cada objeto debe haberse registrado previamente en el servidor de recursos mediante el parámetro `authorization_details` del servidor de recursos. Para obtener más información, consulta la documentación de la Management API sobre [Update a resource server](https://auth0.com/docs/api/management/v2/resource-servers/patch-resource-servers-by-id#body-parameters).
</ParamField>

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                                        |
| ------ | -------------------------------------------------- |
| 200    | Solicitud de autenticación iniciada correctamente. |
| 400    | Solicitud incorrecta: parámetros no válidos        |
| 500    | Error interno del servidor                         |