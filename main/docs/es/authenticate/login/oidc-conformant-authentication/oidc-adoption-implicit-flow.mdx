---
description: Aprende cómo el flujo compatible con OIDC afecta al flujo implícito.
title: Flujo implícito con OIDC
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

Tradicionalmente, el [flujo implícito](/docs/es/get-started/authentication-and-authorization-flow/implicit-flow-with-form-post) lo usaban aplicaciones que no podían almacenar secretos de forma segura. El uso de este flujo ya no se considera una práctica recomendada para solicitar <Tooltip tip="Token de acceso: credencial de autorización, en forma de cadena opaca o JWT, que se utiliza para acceder a una API." cta="Ver glosario" href="/docs/es/glossary?term=access+tokens">tokens de acceso</Tooltip>; las implementaciones nuevas deben usar [flujo de código de autorización con PKCE](/docs/es/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce). Sin embargo, cuando se usa con el modo de respuesta Form Post, el flujo implícito sí ofrece un flujo de trabajo más ágil si la aplicación solo necesita un <Tooltip tip="Token de ID: token de OpenID Connect que contiene información sobre el usuario autenticado y el evento de autenticación." cta="Ver glosario" href="/docs/es/glossary?term=ID+token">token de identidad</Tooltip> para realizar la autenticación de usuario; en estos casos, se usaría como parte del [flujo híbrido](/docs/es/get-started/authentication-and-authorization-flow/hybrid-flow).

<Tooltip tip="Token de actualización: token que se usa para obtener un nuevo token de acceso sin obligar a los usuarios a volver a iniciar sesión." cta="Ver glosario" href="/docs/es/glossary?term=Refresh+tokens">Los tokens de actualización</Tooltip> ya no se devolverán cuando se use el flujo implícito para la autenticación.

Además, el flujo compatible con OIDC afecta al flujo implícito en las siguientes áreas: solicitud de autenticación, respuesta de autenticación, estructura del token de identidad y estructura del token de acceso.

<div id="authentication-request">
  ## Solicitud de autenticación
</div>

<div id="legacy">
  ### Anterior
</div>

```http lines
GET /authorize?
    response_type=token
    &scope=openid email favorite_color offline_access
    &client_id=123
    &state=af0ifjsldkj
    &redirect_uri=https://app.example.com
    &device=my-device-name
```

El parámetro `device` solo es necesario si se solicita un token de actualización al pasar el scope `offline_access`. Para obtener más información, consulta [Tokens de actualización](/docs/es/secure/tokens/refresh-tokens).

<div id="oidc-conformant">
  ### Compatible con OIDC
</div>

```http lines
GET /authorize?
    response_type=token id_token
    &scope=openid email
    &client_id=123
    &state=af0ifjsldkj
    &nonce=jxdlsjfi0fa
    &redirect_uri=https://app.example.com
    &audience=https://api.example.com
```

* `response_type` indica que queremos recibir tanto un access token como un token de identidad.
* Los tokens de actualización no están permitidos en la concesión implícita. En su lugar, usa `prompt=none`. Para obtener más información, consulta [Configurar la autenticación silenciosa](/docs/es/authenticate/login/configure-silent-authentication).
* `favorite_color` ya no es un scope válido.
* `audience` es opcional.
* `nonce` debe ser una cadena aleatoria criptográficamente segura. Para obtener más información, consulta [Mitigar ataques de repetición al usar el flujo implícito](/docs/es/get-started/authentication-and-authorization-flow/implicit-flow-with-form-post/mitigate-replay-attacks-when-using-the-implicit-flow).

<div id="authentication-response">
  ## Respuesta de autenticación
</div>

<div id="legacy">
  ### Anterior
</div>

```json lines
HTTP/1.1 302 Found
Location: https://app.example.com/#
    access_token=SlAV32hkKG
    &expires_in=86400
    &state=af0ifjsldk
    &id_token=eyJ...
    &refresh_token=8xLOxBtZp8
    &token_type=Bearer
```

* El token de acceso devuelto es válido para llamar al endpoint [`/userinfo`](https://auth0.com/docs/api/authentication#get-user-info).
* Solo se devolverá un token de actualización si se envió un parámetro `device` y se solicitó el scope `offline_access`.

<div id="oidc-conformant">
  ### Compatible con OIDC
</div>

```json lines
HTTP/1.1 302 Found
Location: https://app.example.com/#
    access_token=eyJ...
    &expires_in=86400
    &state=af0ifjsldk
    &id_token=eyJ...
    &token_type=Bearer
```

* El token de acceso devuelto es válido para llamar al endpoint [`/userinfo`](https://auth0.com/docs/api/authentication#get-user-info) (siempre que la API especificada por el parámetro `audience` use `RS256` como [algoritmo de firma](/docs/es/get-started/applications/signing-algorithms)) y, de forma opcional, para el <Tooltip tip="Servidor de recursos: servidor que aloja recursos protegidos. Los servidores de recursos aceptan y responden a solicitudes de recursos protegidos." cta="Ver glosario" href="/docs/es/glossary?term=resource+server">servidor de recursos</Tooltip> especificado por el parámetro `audience`.
* Si usa `response_type=id_token`, Auth0 solo devolverá un token de identidad.
  Los tokens de actualización no están permitidos en la concesión implícita. Use `prompt=none` en su lugar.

<div id="id-token-structure">
  ## Estructura del token de identidad
</div>

### Heredado

export const codeExample1 = `{
    "sub": "auth0|alice",
    "iss": "https://{yourDomain}/",
    "aud": "123",
    "exp": 1482809609,
    "iat": 1482773609,
    "email": "alice@example.com",
    "email_verified": true,
    "favorite_color": "blue"
}`;

<AuthCodeBlock children={codeExample1} language="json" filename="JSON" />

<div id="oidc-conformant">
  ### Compatible con OIDC
</div>

export const codeExample2 = `{
    "sub": "auth0|alice",
    "iss": "https://{yourDomain}/",
    "aud": "123",
    "exp": 1482809609,
    "iat": 1482773609,
    "email": "alice@example.com",
    "email_verified": true,
    "https://app.example.com/favorite_color": "blue",
    "nonce": "jxdlsjfi0fa"
}`;

<AuthCodeBlock children={codeExample2} language="json" filename="JSON" />

* La claim `favorite_color` debe usar un espacio de nombres y añadirse mediante una rule. Para obtener más información, lee [Create Namespaced Custom Claims](/docs/es/secure/tokens/json-web-tokens/create-custom-claims).
* Después de validar el token de identidad, la aplicación debe validar el <Tooltip tip="Nonce: Número arbitrario generado una sola vez en un protocolo de autenticación para detectar y prevenir ataques de repetición." cta="Ver glosario" href="/docs/es/glossary?term=nonce">nonce</Tooltip> para mitigar ataques de repetición.

<div id="access-token-structure-optional">
  ## Estructura del token de acceso (opcional)
</div>

<div id="legacy">
  ### Anterior
</div>

```bash HTTP lines
SlAV32hkKG
```

El token de acceso devuelto es opaco y solo es válido para invocar el endpoint `/userinfo`.

<div id="oidc-conformant">
  ### Compatible con OIDC
</div>

export const codeExample3 = `{
    "sub": "auth0|alice",
    "iss": "https://{yourDomain}/",
    "aud": [
        "https://api.example.com",
        "https://{yourDomain}/userinfo"
    ],
    "azp": "123",
    "exp": 1482816809,
    "iat": 1482809609,
    "scope": "openid email"
}`;

<AuthCodeBlock children={codeExample3} language="json" filename="JSON" />

* El token de acceso devuelto es un <Tooltip tip="JSON Web Token (JWT): formato estándar de token de ID (y, a menudo, de token de acceso) que se usa para representar claims de forma segura entre dos partes." cta="Ver glosario" href="/docs/es/glossary?term=JWT">JWT</Tooltip> válido para llamar al endpoint `/userinfo` (siempre que la API especificada por el parámetro `audience` use `RS256` como [algoritmo de firma](/docs/es/get-started/applications/change-application-signing-algorithms)), así como al servidor de recursos especificado por el parámetro `audience`.
* Aun así, podría devolverse un token de acceso opaco si `/userinfo` es la única <Tooltip tip="Audience: identificador único de la audiencia de un token emitido. Se denomina aud en un token y su valor contiene el ID de una aplicación (ID de cliente) para un token de ID o de una API (identificador de API) para un token de acceso." cta="Ver glosario" href="/docs/es/glossary?term=audience">audience</Tooltip> especificada.

<div id="learn-more">
  ## Más información
</div>

* [Tokens de acceso con OIDC](/docs/es/authenticate/login/oidc-conformant-authentication/oidc-adoption-access-tokens)
* [API externas con OIDC](/docs/es/authenticate/login/oidc-conformant-authentication/oidc-adoption-apis)
* [Flujo de código de autorización con OIDC](/docs/es/authenticate/login/oidc-conformant-authentication/oidc-adoption-auth-code-flow)
* [Flujo de credenciales de cliente con OIDC](/docs/es/authenticate/login/oidc-conformant-authentication/oidc-adoption-client-credentials-flow)
* [Delegación con OIDC](/docs/es/authenticate/login/oidc-conformant-authentication/oidc-adoption-delegation)
* [Tokens de actualización con OIDC](/docs/es/authenticate/login/oidc-conformant-authentication/oidc-adoption-refresh-tokens)