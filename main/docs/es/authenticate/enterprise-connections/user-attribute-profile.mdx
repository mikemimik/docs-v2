---
description: Aprende cómo User Attribute Profile te permite configurar usuarios para el aprovisionamiento de SCIM mediante la experiencia de autoservicio.
sidebarTitle: User Attribute Profile
title: User Attribute Profile
---

import { ReleaseStageNotice } from "/snippets/es/ReleaseStageNotice.jsx"

<ReleaseStageNotice feature="User Attribute Profile with Self-Service Enterprise Configuration" stage="ea" plans="B2B Professional and B2B Enterprise" terms="true" />

User Attribute Profile (UAP) proporciona una forma coherente de definir, gestionar y asignar atributos de usuario entre protocolos como [SCIM](/docs/es/authenticate/protocols/scim), [SAML](/docs/es/authenticate/protocols/saml) y [OIDC](/docs/es/authenticate/login/oidc-conformant-authentication). UAP con [Self-Service Enterprise Configuration](/docs/es/authenticate/enterprise-connections/self-service-enterprise-configuration) ofrece a los administradores un mayor control sobre los datos de identidad de los usuarios al definir atributos de usuario y aplicar el perfil en los distintos protocolos de autenticación.

<div id="how-it-works">
  ## Cómo funciona
</div>

* **Definición del perfil**
  Un administrador crea un User Attribute Profile para definir atributos, entre ellos:
  * Cómo mostrar los atributos
  * Cómo hacer que los atributos sean obligatorios
  * Cómo se mapean los atributos en Auth0 y en sistemas de identidad externos

* **Alcance flexible**
  Los perfiles están vinculados a los flujos de Self-Service Enterprise Configuration, pero están pensados para el aprovisionamiento, la incorporación y la gestión de derechos.

* **Capa de mapeo unificada**
  Cada atributo admite mapeos entre protocolos de autenticación, con la opción de sobrescribir valores para proveedores o estrategias de conexión específicos, como Okta y Entra ID.

<div id="attribute-mapping-and-override">
  ## Mapeo de atributos y sobrescritura
</div>

UAP admite definiciones de atributos para varios protocolos y sobrescrituras de estrategia para necesidades específicas de cada proveedor.

**Mapeo de atributos**

| **Protocol**  | **Description**                                                                                                                                                                                                                      |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Auth0 Mapping | Atributo canónico almacenado en Auth0 (`email`, `name`, `app_metadata.department`).                                                                                                                                                  |
| OIDC Mapping  | Claims estándar de OIDC (`sub`, `preferred_username`, `zoneinfo`). Para obtener más información sobre los claims estándar de OIDC, consulta [Standard Claims](https://openid.net/specs/openid-connect-core-1_0.html#StandardClaims). |
| SAML Mapping  | Admite uno o varios URI de aserción `(http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress).`                                                                                                                          |
| SCIM Mapping  | Atributos de aprovisionamiento (`name.familyName`, `addresses[type eq "work"].country`).                                                                                                                                             |

**Sobrescrituras de estrategia**

Algunos proveedores usan mapeos no estándar. UAP permite sobrescrituras:

| **Protocol**    | **Description**                                                                             |
| --------------- | ------------------------------------------------------------------------------------------- |
| SAML            | Mapea `userName` en lugar de `externalId`.                                                  |
| WAAD (Entra ID) | Usa `oid` como identificador de OIDC.                                                       |
| Okta            | Mapea atributos como `middleNam`e o `federated_groups` mediante claims específicos de Okta. |

<div id="user-id">
  ### ID de usuario
</div>

La propiedad `user_id` define cómo se mapean los claims de OIDC, los atributos de SAML o los atributos de SCIM al ID de usuario de Auth0. Todos los usuarios de Auth0 deben tener un ID, por lo que este mapeo es obligatorio.

* En OIDC, las opciones son fijas (normalmente `sub`, `oid` para Azure AD o `email` para Google).
  * En SAML y SCIM, el mapeo es más flexible y puede apuntar a varios atributos posibles.

<div id="user-attributes">
  ### Atributos de usuario
</div>

La propiedad `user_attributes` contiene información de mapeo que permite al sistema interpretar los claims entrantes del IdP y almacenarlos como atributos del perfil de usuario de Auth0.

Cada atributo debe proporcionarse como un par clave/valor:

* La clave corresponde al nombre del atributo.
  * El valor es un objeto con:
    * `label`
    * `description`
    * `profile_required`
    * `auth0_mapping`
    * `saml_mapping`
    * `scim_mapping`
    * `oidc_mapping`, un objeto con propiedades
      * `mapping` representa el claim entrante del IdP (valor literal, objeto `context` dinámico, o ambos mediante la sintaxis `${variable}`, compatible con el [objeto `context`](/docs/es/authenticate/identity-providers/enterprise-identity-providers/configure-pkce-claim-mapping-for-oidc#context-object))
      * `display_name`, la label que se muestra a los usuarios finales en flujos de autoservicio

<div id="strategy-overrides">
  ### Sobrescrituras de estrategia
</div>

La propiedad `strategy_overrides` le permite especificar excepciones para proveedores de identidad (IdPs) individuales, ya que no todos los IdPs exponen los mismos identificadores o claims.

Cada sobrescritura define mapeos específicos del protocolo que sustituyen los valores predeterminados definidos en `user_id` o `user_attributes`.

<div id="examples">
  ## Ejemplos
</div>

<div id="user-identifier">
  ### Identificador de usuario
</div>

```json
"user_id": {
  "oidc_mapping": "sub",
  "saml_mapping": [
    "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/nameidentifier",
    "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/upn",
    "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/name"
  ],
  "scim_mapping": "externalId",
  "strategy_overrides": {
    "waad": {
      "oidc_mapping": "oid"
    },
    "samlp": {
      "scim_mapping": "userName"
    },
    "google-apps": {
      "oidc_mapping": "email"
    }
  }
},
```

* **Identificador predeterminado**: `externalId` mediante SCIM.
* **SAML**: Admite varios URI de identificador.
* **OIDC**: Usa `sub`.
* **Sobrescrituras**: SAML y WAAD personalizan el mapeo.

<div id="email-attribute">
  ### Atributo de correo electrónico
</div>

```json
"email": {
  "description": "Email",
  "label": "Email",
  "profile_required": true,
  "auth0_mapping": "email",
  "scim_mapping": "emails[primary eq true].value",
  "oidc_mapping": {
    "mapping": "${context.tokenset.email}",
    "display_name": "email"
  },
  "saml_mapping": [
    "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress"
  ],
  "strategy_overrides": {
    "waad": {
      "scim_mapping": "emails[type eq \"work\"].value"
    }
  }
}
```

* Recomendado para la mayoría de los perfiles.
* Unificado en Auth0, OIDC, SAML y SCIM.
* La sobrescritura de WAAD garantiza el mapeo correcto de los correos electrónicos laborales.

<div id="create-a-user-attribute-profile">
  ## Crear un User Attribute Profile
</div>

Puedes definir un UAP mediante Self-Service Enterprise Configuration desde el Auth0 Dashboard o la Management API. Actualmente, puede configurarse a través de la experiencia de Self-Service Enterprise Configuration.

<div id="configure-with-auth0-dashboard">
  ### Configurar con Auth0 Dashboard
</div>

1. Ve a [**Autenticación &gt; empresarial &gt; Self-Service Enterprise Configuration**](http://manage.auth0.com/*/connections/enterprise/self-service-profiles).
2. Selecciona **+Crear perfil**.
3. Proporciona un **Nombre** y una **Descripción** opcional para el nuevo perfil.
4. Agrega una entrada de **User Attribute Profile** seleccionando un perfil existente o eligiendo **+Crear nuevo**.
   * Para un perfil nuevo, proporciona un **Nombre del atributo del perfil de usuario**.
   * Revisa los mapeos para asegurarte de que los atributos del perfil se mapeen a los atributos de Auth0 que prefieras.
5. Elige **Crear**.

Tu nuevo UAP ya está disponible para configurarse para SSO.

<div id="configure-with-management-api">
  ### Configurar con Management API
</div>

Para gestionar los perfiles de atributos de usuario, están disponibles los siguientes endpoints de [Management API](https://auth0.com/docs/api/management/v2/introduction):

* `POST` `/api/v2/user-attribute-profiles`
* `GET` `/api/v2/user-attribute-profiles`
* `PATCH` `/api/v2/user-attribute-profiles/{id}`
* `GET` `/api/v2/user-attribute-profiles/{id}`
* `GET` `/api/v2/user-attribute-profiles/templates`
* `GET` `/api/v2/user-attribute-profiles/templates/{id}`

<div id="learn-more">
  ## Más información
</div>

* [Self-Service Enterprise Configuration](/docs/es/authenticate/enterprise-connections/self-service-enterprise-configuration)
* [Configurar PKCE y el mapeo de claims para conexiones OIDC](/docs/es/authenticate/identity-providers/enterprise-identity-providers/configure-pkce-claim-mapping-for-oidc)
* [Mapear atributos SAML con Auth0 como IdP/add-on de SAML](/docs/es/authenticate/protocols/saml/saml-configuration/saml-attribute-mapping-examples)
* [Mapear los atributos de perfil de AD/LDAP al perfil de usuario de Auth0](/docs/es/authenticate/protocols/saml/saml-configuration/saml-attribute-mapping-examples)