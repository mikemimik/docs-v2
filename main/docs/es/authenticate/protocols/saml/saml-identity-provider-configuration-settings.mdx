---
description: Describe las opciones de configuración del proveedor de identidad SAML.
title: Opciones de configuración del proveedor de identidad SAML
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/AuthCodeGroup.jsx";

<div id="common-settings">
  ## Configuración común
</div>

Estos son los ajustes que se utilizan para configurar un <Tooltip tip="Security Assertion Markup Language (SAML): Protocolo estandarizado que permite a dos partes intercambiar información de autenticación sin contraseña." cta="Ver glosario" href="/docs/es/glossary?term=SAML">SAML</Tooltip> <Tooltip tip="Security Assertion Markup Language (SAML): Protocolo estandarizado que permite a dos partes intercambiar información de autenticación sin contraseña." cta="Ver glosario" href="/docs/es/glossary?term=identity+provider">proveedor de identidad</Tooltip> (IdP).

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Si ha configurado un [Dominio personalizado](/docs/es/customize/custom-domains), debe usar el CNAME de su dominio personalizado en lugar del dominio de Auth0. Para obtener más información, lea [configurar funcionalidades para usar dominios personalizados](/docs/es/customize/custom-domains/configure-features-to-use-custom-domains#configure-saml-identity-providers).
</Callout>

<div id="post-back-url">
  ### URL de retorno
</div>

Cuando uses <Tooltip tip="inicio de sesión único (SSO): Servicio que, una vez que un usuario inicia sesión en una aplicación, lo autentica automáticamente en otras aplicaciones." cta="Ver glosario" href="/docs/es/glossary?term=SSO">SSO</Tooltip> iniciado por el IdP, asegúrate de incluir el parámetro de conexión en la URL de retorno:

export const codeExample1 = `https://{yourDomain}/login/callback?connection={yourConnectionName}`;

<AuthCodeBlock children={codeExample1} language="http" />

Si utiliza la funcionalidad [Organizations](/docs/es/manage-users/organizations), puede incluir opcionalmente un parámetro `organization` que contenga el ID de organización de la organización deseada:

export const codeExample2 = `https://{yourDomain}/login/callback?connection={yourConnectionName}&organization={yourCustomersOrganizationId}`;

<AuthCodeBlock children={codeExample2} language="http" />

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Para que los usuarios puedan iniciar sesión correctamente con este método, la conexión debe estar habilitada para la organización. Además, debes configurar la [membresía automática](/docs/es/manage-users/organizations/configure-organizations/grant-just-in-time-membership) para la conexión habilitada o asegurarte de que los usuarios pertenezcan a la organización.
</Callout>

<div id="entity-id">
  ### ID de entidad
</div>

El ID del proveedor de servicios es:

```http lines
urn:auth0:{yourTenant}:{yourConnectionName}
```

Puede crear un ID de entidad personalizado con la propiedad `connection.options.entityId`. Para obtener más información, consulte [Especificar un ID de entidad personalizado](/docs/es/authenticate/identity-providers/enterprise-identity-providers/saml#specify-a-custom-entity-id).

Puede obtener el valor del ID de entidad personalizado mediante el endpoint obtener una conexión:

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D' \
    --header 'authorization: Bearer {yourAccessToken}'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer {yourAccessToken}");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer {yourAccessToken}")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D")
    .header("authorization", "Bearer {yourAccessToken}")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D',
    headers: {authorization: 'Bearer {yourAccessToken}'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer {yourAccessToken}"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer {yourAccessToken}" }

  conn.request("GET", "/{yourDomain}/api/v2/connections/%7ByourConnectionID%7D", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/connections/%7ByourConnectionID%7D")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer {yourAccessToken}'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

Reemplace el valor del header `ACCESS_TOKEN` por un <Tooltip tip="Token de acceso: credencial de autorización, en forma de una cadena opaca o JWT, utilizada para acceder a una API." cta="Ver glosario" href="/docs/es/glossary?term=access+token">token de acceso</Tooltip> de la Management API v2.

<div id="saml-request-binding">
  ### Enlace de la solicitud SAML
</div>

También llamado **enlace de protocolo**, Auth0 lo envía al IdP. Si es posible, establece el valor dinámicamente según `connection.options.protocolBinding`:

| valor de `connection.options.protocolBinding`        | valor de Enlace de la solicitud SAML |
| ---------------------------------------------------- | ------------------------------------ |
| Valor vacío (&quot;&quot;) o no presente             | `HTTP-Redirect`                      |
| `urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect` | `HTTP-Redirect`                      |
| `urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST`     | `HTTP-POST`                          |

Si no es posible establecer el valor dinámicamente, configúralo como `HTTP-Redirect` (predeterminado) o `HTTP-Post` si seleccionaste esta opción en **enlace de protocolo**.

<div id="saml-response-binding">
  ### Binding de la respuesta SAML
</div>

Cómo Auth0 recibe el token SAML del IdP, establecido como `HTTP-Post`.

<div id="nameid-format">
  ### Formato de NameID
</div>

Sin especificar.

<div id="saml-assertion-and-response">
  ### Aserción SAML y respuesta SAML
</div>

La aserción SAML y la respuesta SAML pueden firmarse por separado o simultáneamente.

<div id="singlelogout-service-url">
  ### URL del servicio SingleLogout
</div>

Aquí es donde el proveedor de identidad SAML enviará las solicitudes y respuestas de cierre de sesión:

export const codeExample13 = `https://{yourDomain}/logout`;

<AuthCodeBlock children={codeExample13} language="http" />

Las solicitudes de cierre de sesión de SAML deben estar firmadas por el proveedor de identidad.

<div id="signed-assertions">
  ## Aserciones firmadas
</div>

Use los siguientes enlaces para obtener la clave pública en distintos formatos:

* <AuthLink href="https://{yourDomain}/cer?cert=connection">CER</AuthLink>
* <AuthLink href="https://{yourDomain}/pem?cert=connection">PEM</AuthLink>
* <AuthLink href="https://{yourDomain}/rawpem?cert=connection">PEM sin procesar</AuthLink>
* <AuthLink href="https://{yourDomain}/pb7?cert=connection">PKCS#7</AuthLink>
* <AuthLink href="https://{yourDomain}/fingerprint?cert=connection">Huella digital</AuthLink>

Descargue el certificado en el formato solicitado por el IdP.

<div id="idp-initiated-single-sign-on">
  ### Inicio de sesión único iniciado por el IdP
</div>

Para obtener más información sobre el inicio de sesión único iniciado por el IdP, lea [Configurar el inicio de sesión único con SAML iniciado por el IdP](/docs/es/authenticate/protocols/saml/saml-sso-integrations/identity-provider-initiated-single-sign-on).

<div id="metadata">
  ## Metadatos
</div>

Algunos proveedores de identidad SAML admiten importar los metadatos directamente con toda la información necesaria. Puedes acceder a los metadatos de tu conexión en Auth0 aquí:

export const codeExample14 = `https://{yourDomain}/samlp/metadata?connection={yourConnectionName}`;

<AuthCodeBlock children={codeExample14} language="http" />

<div id="organizations">
  ## Organizations
</div>

Usa la URL ACS de tu organización en el IdP federado para iniciar el flujo de inicio de sesión de Organizations.

export const codeExample15 = `https://{yourDomain}/samlp?connection={yourConnectionName}&organization={yourOrgID}`;

<AuthCodeBlock children={codeExample15} language="http" />

<div id="learn-more">
  ## Más información
</div>

* [Encuentra el ID o el nombre de la conexión](/docs/es/authenticate/identity-providers/locate-the-connection-id)
* [Personaliza las aserciones SAML](/docs/es/authenticate/protocols/saml/saml-configuration/customize-saml-assertions)