---
description: Detalles sobre los modos de autenticación con Lock.
title: Modos de autenticación con Lock
---

import {AuthCodeBlock} from "/snippets/es/AuthCodeBlock.jsx";

Lock puede funcionar de dos maneras distintas. El modo predeterminado es el **modo de redirección**. En este modo, el usuario es redirigido para autenticarse y luego vuelve a la aplicación. En el segundo modo, **modo popup**, una ventana emergente permite al usuario autenticarse con el <Tooltip tip="Proveedor de identidad (IdP): servicio que almacena y gestiona identidades digitales." cta="Ver glosario" href="/docs/es/glossary?term=identity+provider">proveedor de identidad</Tooltip> sin salir de la aplicación.

<div id="redirect-mode">
  ## Modo de redirección
</div>

Cuando haces clic en el botón de IdP (por ejemplo, Facebook) en el modo de redirección, se te redirige momentáneamente a Facebook. El modo de redirección es el modo predeterminado de Lock y se recomienda para casi todos los casos de uso. Una vez que inicias sesión correctamente (en Facebook, en este ejemplo), Facebook te redirigirá de vuelta a tu aplicación (a través de Auth0). La mayoría de los ejemplos de la documentación de referencia emplean el modo de redirección.

<div id="popup-mode">
  ## Modo popup
</div>

Si después de hacer clic en el botón del IdP (Facebook, por ejemplo) se abre un popup (una pestaña o ventana nueva), significa que estás usando el modo popup. En ese popup, verás la página de Facebook. Una vez que inicies sesión correctamente en Facebook, el popup se cerrará y tu aplicación web reconocerá que el usuario se ha autenticado. La aplicación web **nunca fue redirigida a ninguna otra página**.

Implementar Lock con el modo popup vuelve a ser un cambio sencillo en la opción `redirect` con respecto a su valor predeterminado.

export const codeExample = `var lock = new Auth0Lock(
  '{yourClientId}',
  '{yourDomain}',
  {
    auth: {
      redirect: false
    }
  }
);`;

<AuthCodeBlock children={codeExample} language="js" />

<Tooltip tip="Autenticación multifactor (MFA): proceso de autenticación de usuario que usa un factor además de nombre de usuario y contraseña, como un código por SMS." cta="Ver glosario" href="/docs/es/glossary?term=Multi-factor+authentication">Autenticación multifactor</Tooltip> (MFA) no se admite cuando Lock está en modo popup e integrado en tu aplicación. Además, el modo popup no funciona con <Tooltip tip="Universal Login: tu aplicación redirige a Universal Login, alojado en el servidor de autorización de Auth0, para verificar la identidad de un usuario." cta="Ver glosario" href="/docs/es/glossary?term=Universal+Login">Universal Login</Tooltip>.

Algunas funcionalidades de Auth0, como el [inicio de sesión único (SSO)](/docs/es/authenticate/single-sign-on) entre varias aplicaciones, dependen de que se redirija a los usuarios a Auth0 para establecer una cookie en `'{yourDomain}'`.

Al usar el modo popup, se mostrará una ventana emergente para establecer esta cookie. Si los prompts no son necesarios, esta ventana emergente estará en blanco y dentro de un iframe oculto para minimizar las interrupciones. Esto se debe a que las solicitudes de origen cruzado enviadas desde tu aplicación a Auth0 no pueden establecer cookies.

Si no quieres mostrar una ventana emergente y no necesitas <Tooltip tip="Inicio de sesión único (SSO): servicio que, después de que un usuario inicia sesión en una aplicación, inicia automáticamente la sesión de ese usuario en otras aplicaciones." cta="Ver glosario" href="/docs/es/glossary?term=SSO">SSO</Tooltip> entre varias aplicaciones, puedes establecer `sso: false` al usar Lock o auth0.js.

```js lines
var options = {
  auth: {
    sso: false
  }
}
```