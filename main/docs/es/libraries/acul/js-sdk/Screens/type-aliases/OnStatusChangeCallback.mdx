---
title: "Alias de tipo: OnStatusChangeCallback"
---

<ParamField body="OnStatusChangeCallback" type="(remainingSeconds, isDisabled) => void">
  Función de devolución de llamada para los cambios de estado durante la cuenta regresiva del reenvío.

  ## Devuelve

  `void`

  ```ts Example
  export type OnStatusChangeCallback = (remainingSeconds: number, isDisabled: boolean) => void;
  ```

  <Expandable title="Parámetros">
    <ParamField body="remainingSeconds" type="number">
      `number`

      Número de segundos restantes en la cuenta regresiva
    </ParamField>

    <ParamField body="isDisabled" type="boolean">
      `boolean`

      Si la funcionalidad de reenvío está deshabilitada en ese momento
    </ParamField>
  </Expandable>
</ParamField>