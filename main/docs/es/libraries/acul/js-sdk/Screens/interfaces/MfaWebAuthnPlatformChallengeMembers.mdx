---
title: "MfaWebAuthnPlatformChallengeMembers"
---

MfaWebAuthnPlatformChallengeMembers

````ts Example
export interface MfaWebAuthnPlatformChallengeMembers extends BaseMembers {
  /**
   * Propiedades y datos específicos de la pantalla, incluidas las opciones del WebAuthn challenge y la preferencia de recordar dispositivo.
   * @type {ScreenMembersOnMfaWebAuthnPlatformChallenge}
   */
  screen: ScreenMembersOnMfaWebAuthnPlatformChallenge;

  /**
   * Inicia el WebAuthn platform authenticator challenge.
   * Este método llama internamente a `navigator.credentials.get()` usando las opciones del challenge
   * proporcionadas en `screen.publicKey`.
   * Si tiene éxito, envía la credencial resultante a Auth0 con `action: "default"`.
   *
   * Si `navigator.credentials.get()` falla (por ejemplo, cancelación del usuario, timeout u otros errores de la API de WebAuthn),
   * se recomienda llamar a `reportBrowserError` con los detalles del error.
   *
   * @param {VerifyPlatformAuthenticatorOptions} [options] - Parámetros opcionales para el proceso de verificación,
   * como `rememberDevice` (si `screen.showRememberDevice` es true) y otras opciones personalizadas.
   * @returns {Promise<void>} Una Promise que se resuelve cuando se envía el intento de verificación.
   *                          Una operación exitosa generalmente resulta en una redirección.
   * @throws {Error} Lanza un error si falta `screen.publicKey`,
   *                 si `navigator.credentials.get()` falla con un error inesperado no gestionado por `getPasskeyCredentials`,
   *                 o si el envío del formulario a Auth0 falla.
   *
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnPlatformChallenge
   * try {
   *   const remember = sdk.screen.showRememberDevice && userCheckedRememberDeviceBox;
   *   await sdk.verify({ rememberDevice: remember });
   *   // En caso de éxito, Auth0 gestiona la redirección.
   * } catch (error) {
   *   console.error("Platform authenticator verification failed:", error);
   *   // Si es un error de la API de WebAuthn, repórtalo
   *   if (error.name && error.message) { // Verificación básica de error similar a DOMException
   *     await sdk.reportBrowserError({ error: { name: error.name, message: error.message } });
   *   }
   *   // Comprueba sdk.transaction.errors para mensajes de validación del lado del servidor si la página se recarga.
   * }
   * ```
   */
  verify(options?: VerifyPlatformAuthenticatorOptions): Promise<void>;

  /**
   * Reporta un error específico de la API de WebAuthn (de `navigator.credentials.get()`) a Auth0.
   * Este método debe usarse cuando `verify()` (o una llamada manual a `navigator.credentials.get()`) falla debido a
   * un problema de WebAuthn en el navegador (por ejemplo, cancelación del usuario `NotAllowedError`, timeout).
   * Envía los detalles del error con `action: "showError::{errorDetailsJsonString}"` y una `response` vacía.
   *
   * @param {ReportBrowserErrorOptions} options - Contiene el objeto `error` (con `name` y `message`
   * del DOMException de la API de WebAuthn) y cualquier otra opción personalizada.
   * @returns {Promise<void>} Una Promise que se resuelve cuando se envía el reporte del error.
   * @throws {Error} Lanza un error si el envío del formulario falla (por ejemplo, error de red, estado inválido).
   *
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnPlatformChallenge
   * // Y webAuthnDomException es un objeto de error de una llamada fallida a navigator.credentials.get().
   * try {
   *   await sdk.reportBrowserError({
   *     error: { name: webAuthnDomException.name, message: webAuthnDomException.message }
   *   });
   *   // Auth0 procesará este error y puede que vuelva a renderizar la página o redirija.
   * } catch (submitError) {
   *   console.error("Failed to report WebAuthn browser error:", submitError);
   * }
   * ```
   */
  reportBrowserError(options: ReportBrowserErrorOptions): Promise<void>;

  /**
   * Permite al usuario omitir el WebAuthn platform challenge y seleccionar un método MFA diferente.
   * Esta acción envía `action: "pick-authenticator"` a Auth0, lo que debería llevar
   * al usuario a una pantalla de selección de factor MFA.
   *
   * @param {TryAnotherMethodOptions} [options] - Parámetros personalizados opcionales que se enviarán con la solicitud.
   * @returns {Promise<void>} Una Promise que se resuelve cuando se envía la acción 'pick-authenticator'.
   * @throws {Error} Lanza un error si el envío del formulario falla.
   *
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnPlatformChallenge
   * try {
   *   await sdk.tryAnotherMethod();
   *   // En caso de éxito, Auth0 gestiona la redirección a la selección de MFA.
   * } catch (error) {
   *   console.error("Failed to switch MFA method:", error);
   * }
   * ```
   */
  tryAnotherMethod(options?: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnPlatformChallenge">ScreenMembersOnMfaWebAuthnPlatformChallenge</a></span>}>
  Propiedades y datos específicos de la pantalla, incluidas las opciones del desafío de WebAuthn y la preferencia de recordar dispositivo.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="reportBrowserError" type="Promise<void>">
  Informa a Auth0 sobre un error específico de la API de WebAuthn (de `navigator.credentials.get()`).
  Este método debe usarse cuando `verify()` (o una llamada manual a `navigator.credentials.get()`) falla debido a
  un problema de WebAuthn en el navegador (p. ej., cancelación del usuario `NotAllowedError`, `timeout`).
  Envía los detalles del error con `action: "showError::{errorDetailsJsonString}"` y un `response` vacío.

  Una promesa que se resuelve cuando se envía el reporte del error.

  #### Lanza

  Lanza un error si falla el envío del formulario (p. ej., error de red, `state` no válido).

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnPlatformChallenge
  // Y que webAuthnDomException es un objeto de error de una llamada fallida a navigator.credentials.get().
  try {
    await sdk.reportBrowserError({
      error: { name: webAuthnDomException.name, message: webAuthnDomException.message }
    });
    // Auth0 procesará este error y puede volver a renderizar la página o redirigir.
  } catch (submitError) {
    console.error("Failed to report WebAuthn browser error:", submitError);
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="options" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnPlatformChallengeReportErrorOptions">MfaWebAuthnPlatformChallengeReportErrorOptions</a></span>}>
      Contiene el objeto `error` (con `name` y `message`
      de la DOMException de la API de WebAuthn) y cualquier otra opción personalizada.

      **Propiedades**

      <ParamField body="error" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        El objeto de error de la API de WebAuthn (`navigator.credentials.get()`) que se va a informar.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permite al usuario salir del desafío de plataforma de WebAuthn y seleccionar un método MFA diferente.
  Esta acción envía `action: "pick-authenticator"` a Auth0, lo que debería llevar
  al usuario a una pantalla de selección de factor MFA.

  Una promesa que se resuelve cuando se envía la acción &#39;pick-authenticator&#39;.

  #### Lanza

  Lanza un error si falla el envío del formulario.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnPlatformChallenge
  try {
    await sdk.tryAnotherMethod();
    // Si se realiza correctamente, Auth0 gestiona la redirección a la selección de MFA.
  } catch (error) {
    console.error("Failed to switch MFA method:", error);
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnPlatformChallengeTryAnotherMethodOptions">MfaWebAuthnPlatformChallengeTryAnotherMethodOptions</a></span>}>
      Parámetros personalizados opcionales que se enviarán con la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="verify" type="Promise<void>">
  Inicia el desafío del autenticador de plataforma de WebAuthn.
  Este método llama internamente a `navigator.credentials.get()` con las opciones de desafío
  proporcionadas en `screen.publicKey`.
  Si se completa correctamente, envía la credencial resultante a Auth0 con `action: "default"`.

  Si `navigator.credentials.get()` falla (por ejemplo, por cancelación del usuario, `timeout` u otros errores de la API de WebAuthn),
  se recomienda llamar a `reportBrowserError` con los detalles del error.

  Una promesa que se resuelve cuando se envía el intento de verificación.
  Una operación correcta suele dar lugar a una redirección.

  #### Lanza

  Lanza un error si falta `screen.publicKey`,
  si `navigator.credentials.get()` falla con un error inesperado que `getPasskeyCredentials` no controla,
  o si falla el envío del formulario a Auth0.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnPlatformChallenge
  try {
    const remember = sdk.screen.showRememberDevice && userCheckedRememberDeviceBox;
    await sdk.verify({ rememberDevice: remember });
    // Si se realiza correctamente, Auth0 se encarga de la redirección.
  } catch (error) {
    console.error("Platform authenticator verification failed:", error);
    // Si es un error de la API de WebAuthn, infórmalo
    if (error.name && error.message) { // Comprobación básica de un error similar a DOMException
      await sdk.reportBrowserError({ error: { name: error.name, message: error.message } });
    }
    // Revisa sdk.transaction.errors para ver mensajes de validación del servidor si la página se recarga.
  }
  ```

  <Expandable title="Parameters">
    <ParamField body="options?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/VerifyPlatformAuthenticatorOptions">VerifyPlatformAuthenticatorOptions</a></span>}>
      Parámetros opcionales para el proceso de verificación,
      como `rememberDevice` (si `screen.showRememberDevice` es `true`) y otras opciones personalizadas.

      **Propiedades**

      <ParamField body="rememberDevice?" type="boolean">
        Opcional. Si es `true`, intenta recordar el navegador para futuros desafíos MFA.
        Corresponde al campo de formulario `rememberBrowser`. Esto solo se aplica si `screen.showRememberDevice` es `true`.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>