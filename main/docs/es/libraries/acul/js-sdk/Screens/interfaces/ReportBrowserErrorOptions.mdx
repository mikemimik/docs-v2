---
title: "ReportBrowserErrorOptions"
---

ReportBrowserErrorOptions

```ts Example
export interface ReportBrowserErrorOptions {
  /**
   * @property {object} error - El objeto de error de la API de WebAuthn.
   * @property {string} error.name - El nombre del error (p. ej., "NotAllowedError").
   * @property {string} error.message - El mensaje de error detallado.
   */
  error: {
    name: string;
    message: string;
  };
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="error" type="object">
  <Expandable title="Propiedades">
    <ParamField body="message" type="string" />

    <ParamField body="name" type="string" />
  </Expandable>
</ParamField>