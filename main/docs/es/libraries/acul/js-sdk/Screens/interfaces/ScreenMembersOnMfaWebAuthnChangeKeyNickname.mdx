---
title: "ScreenMembersOnMfaWebAuthnChangeKeyNickname"
---

ScreenMembersOnMfaWebAuthnChangeKeyNickname

```ts Example
export interface ScreenMembersOnMfaWebAuthnChangeKeyNickname extends ScreenMembers {
  /**
   * Datos específicos de la pantalla que contienen el apodo actual de la clave de WebAuthn.
   * @type {{ nickname: string; } | null}
   */
  data: {
    /**
     * El apodo actual de la clave de seguridad de WebAuthn que el usuario está a punto de cambiar.
     * Este valor debe mostrarse al usuario para proporcionarle contexto.
     * @type {string}
     * @example "My YubiKey"
     * @example "Work Security Key"
     */
    nickname: string;
  } | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="{ nickname: string; }">
  Datos específicos de la pantalla.

  * Declaración de tipo: `{ nickname: string; }`

  <Expandable title="propiedades">
    <ParamField body="nickname" type="string">
      El apodo actual de la clave de seguridad de WebAuthn que el usuario está a punto de cambiar.
      Este valor debe mostrarse al usuario para proporcionar contexto.

      <CodeGroup>
        ```ts Ejemplo 1
        "My YubiKey"
        ```

        ```ts Ejemplo 2
        "Work Security Key"
        ```

        ```ts Ejemplo 3
        null
        ```
      </CodeGroup>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />