---
title: "MfaVoiceChallengeContinueOptions"
---

Opciones para enviar el código del desafío por voz.

```ts Example
export interface MfaVoiceChallengeContinueOptions {
  /**
   * El código de verificación recibido mediante llamada de voz.
   */
  code: string;

  /**
   * Indicador opcional para recordar el device durante 30 días, omitiendo futuros desafíos MFA.
   */
  rememberDevice?: boolean;

  /**
   * Opciones personalizadas adicionales para incluir en la solicitud.
   */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string">
  El código de verificación recibido mediante una llamada de voz.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indicador opcional para recordar el dispositivo durante 30 días y así omitir futuros desafíos MFA.
</ParamField>