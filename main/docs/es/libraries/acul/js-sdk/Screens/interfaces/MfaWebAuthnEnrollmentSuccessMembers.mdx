---
title: "MfaWebAuthnEnrollmentSuccessMembers"
---

MfaWebAuthnEnrollmentSuccessMembers

````ts Example
export interface MfaWebAuthnEnrollmentSuccessMembers extends BaseMembers {
  /**
   * Acceso a las propiedades y datos específicos de la pantalla 'mfa-webauthn-enrollment-success'.
   * Incluye el `nickname` del autenticador registrado y su `webauthnType`.
   * @type {ScreenMembersOnMfaWebAuthnEnrollmentSuccess}
   */
  screen: ScreenMembersOnMfaWebAuthnEnrollmentSuccess;

  /**
   * Permite al usuario Continuar el flujo de autenticación tras un registro WebAuthn exitoso.
   * Este método envía una solicitud POST al endpoint `/u/mfa-webauthn-enrollment-success` con `action: "default"`.
   *
   * @param {ContinueOptions} [payload] - Opcional. Un objeto para `CustomOptions` si se necesitan enviar
   *                                        parámetros adicionales con la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la acción de Continuar se envía correctamente.
   *                          En caso de éxito, el navegador normalmente será redirigido al siguiente paso del flujo.
   * @throws {Error} Lanza un error si el envío del formulario falla (p. ej., problema de red, estado inválido).
   *
   * @example
   * ```typescript
   * // Se asume que 'sdk' es una instancia de MfaWebAuthnEnrollmentSuccess
   * try {
   *   await sdk.continue();
   *   // El usuario es redirigido al siguiente paso.
   * } catch (error) {
   *   console.error("Failed to continue after WebAuthn enrollment:", error);
   *   // Manejar el error, posiblemente inspeccionando sdk.transaction.errors si la página se vuelve a renderizar con un error.
   * }
   * ```
   */
  continue(payload?: ContinueOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnEnrollmentSuccess">ScreenMembersOnMfaWebAuthnEnrollmentSuccess</a></span>}>
  Acceso a las propiedades y los datos específicos de la pantalla &#39;mfa-webauthn-enrollment-success&#39;.
  Incluye el `nickname` del autenticador registrado y su `webauthnType`.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="Continuar" type="Promise<void>">
  Permite al usuario Continuar el flujo de autenticación después de completar correctamente el registro WebAuthn.
  Este método envía una solicitud POST al endpoint `/u/mfa-webauthn-enrollment-success` con `action: "default"`.

  Una promesa que se resuelve cuando la acción Continuar se envía correctamente.
  Si la operación se completa correctamente, el navegador normalmente será redirigido al siguiente paso del flujo.

  #### Excepciones

  Lanza un error si falla el envío del formulario (p. ej., un problema de red o invalid state).

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnEnrollmentSuccess
  try {
    await sdk.continue();
    // El usuario es redirigido al siguiente paso.
  } catch (error) {
    console.error("Failed to continue after WebAuthn enrollment:", error);
    // Gestiona el error, por ejemplo, inspeccionando sdk.transaction.errors si la página se vuelve a renderizar con un error.
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/WebAuthnEnrollSuccessContinueOptions">WebAuthnEnrollSuccessContinueOptions</a></span>}>
      Opcional. Un objeto para `CustomOptions` si es necesario
      enviar parámetros adicionales con la solicitud.
    </ParamField>
  </Expandable>
</ParamField>