---
title: "ScreenMembersOnMfaRecoveryCodeChallengeNewCode"
---

ScreenMembersOnMfaRecoveryCodeChallengeNewCode

```ts Example
export interface ScreenMembersOnMfaRecoveryCodeChallengeNewCode extends ScreenMembers {
  /**
   * @property {object | null} data - Datos específicos de la pantalla.
   * @property {string} data.textCode - El código de recuperación recién generado que el usuario debe guardar.
   */
  data: {
    /**
     * El código de recuperación recién generado para mostrar al usuario.
     * Es fundamental que el usuario guarde este código de forma segura, ya que será necesario para futuros inicios de sesión si otros factores de MFA no están disponibles.
     */
    textCode: string;
  } | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="textCode">
  #### Declaración de tipo

  &#123; `textCode`: `string`; &#125;

  #### textCode

  El código de recuperación recién generado que se mostrará al usuario.
  Es fundamental que el usuario guarde este código de forma segura, ya que será necesario para futuros inicios de sesión si no se dispone de otros factores de MFA.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />