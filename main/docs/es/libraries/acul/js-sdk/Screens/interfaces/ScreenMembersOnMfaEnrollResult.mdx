---
title: "ScreenMembersOnMfaEnrollResult"
---

ScreenMembersOnMfaEnrollResult

```ts Example
export interface ScreenMembersOnMfaEnrollResult extends ScreenMembers {
  /**
   * Datos específicos de la pantalla que contienen el estado de la inscripción en MFA.
   * @type {{ status: string; } | null}
   */
  data: {
    /**
     * El estado del proceso de inscripción en MFA.
     * Los valores posibles pueden incluir "success", "failure" u otros códigos de estado específicos.
     * Este estado puede usarse para mostrar un mensaje apropiado al usuario.
     * @type {string}
     */
    status: string;
  } | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="status">
  Datos específicos de la pantalla.

  #### Declaración de tipo

  &#123; `status`: `string`; &#125;

  #### status

  El estado del proceso de inscripción en MFA.
  Entre los valores posibles se incluyen &quot;success&quot;, &quot;failure&quot; u otros códigos de estado específicos.
  Este estado puede usarse para mostrar un mensaje adecuado al usuario.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />