---
title: "ResetPasswordMfaVoiceChallengeContinueOptions"
---

Payload del método `continue()` de ResetPasswordMfaVoiceChallengeMembers.

```ts Example
export interface ContinueOptions extends CustomOptions {
  /**
   * El código OTP introducido por el usuario.
   */
  code: string;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string">
  El código OTP ingresado por el usuario.
</ParamField>