---
title: "MfaPhoneChallengeMembers"
---

MfaPhoneChallengeMembers

````ts Example
export interface MfaPhoneChallengeMembers extends BaseMembers {
  /**
   * @property {ScreenMembersOnMfaPhoneChallenge} screen - Información específica de la pantalla para el MFA Phone Challenge.
   */
  screen: ScreenMembersOnMfaPhoneChallenge;

  /**
   * continue
   * Envía la elección del método de entrega de MFA (SMS o voz) para continuar con el challenge.
   * Esto activará el envío del código de verificación al teléfono del usuario.
   *
   * @param {ContinueOptions} payload - Las opciones que especifican el tipo de entrega ('sms' o 'voice').
   * @returns {Promise<void>} Una promesa que se resuelve cuando la Action es enviada.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';
   *
   * const mfaPhoneChallenge = new MfaPhoneChallenge();
   *
   * // Para enviar el código por SMS
   * await mfaPhoneChallenge.continue({ type: 'sms' });
   *
   * // Para enviar el código por llamada de voz
   * await mfaPhoneChallenge.continue({ type: 'voice' });
   * ```
   */
  continue(payload: ContinueOptions): Promise<void>;

  /**
   * pickPhone
   * Inicia la Action para seleccionar un número de teléfono diferente.
   * Por lo general, redirige al usuario a una pantalla donde puede elegir entre varios números de teléfono registrados, si corresponde.
   *
   * @param {PickPhoneOptions} [payload] - Opciones personalizadas opcionales para incluir en la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la Action es enviada.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';
   *
   * const mfaPhoneChallenge = new MfaPhoneChallenge();
   * await mfaPhoneChallenge.pickPhone();
   * ```
   */
  pickPhone(payload?: PickPhoneOptions): Promise<void>;

  /**
   * tryAnotherMethod
   * Inicia la Action para seleccionar un factor/autenticador de MFA diferente.
   * Redirige al usuario a una pantalla donde puede elegir un método de MFA alternativo (por ejemplo, una OTP app o un código de recuperación).
   *
   * @param {PickAuthenticatorOptions} [payload] - Opciones personalizadas opcionales para incluir en la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la Action es enviada.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';
   *
   * const mfaPhoneChallenge = new MfaPhoneChallenge();
   * await mfaPhoneChallenge.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: PickAuthenticatorOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaPhoneChallenge">ScreenMembersOnMfaPhoneChallenge</a></span>} />

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="continue" type="Promise<void>">
  continue
  Envía la selección del método de entrega de MFA (SMS o voz) para continuar con el challenge.
  Esto activará el envío del código de verificación al teléfono del usuario.

  Una promesa que se resuelve cuando se envía la Action.

  #### Throws

  Lanza un error si el envío falla.

  ```typescript Example
  import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';

  const mfaPhoneChallenge = new MfaPhoneChallenge();

  // Para enviar el código por SMS
  await mfaPhoneChallenge.continue({ type: 'sms' });

  // Para enviar el código mediante una llamada de voz
  await mfaPhoneChallenge.continue({ type: 'voice' });
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneChallengeContinueOptions">MfaPhoneChallengeContinueOptions</a></span>}>
      Las opciones que especifican el tipo de entrega (`sms` o `voice`).

      **Propiedades**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        El tipo de challenge que se debe realizar.

        * `sms`: Envía el código por mensaje de texto.
        * `voice`: Envía el código mediante una llamada de voz.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="pickPhone" type="Promise<void>">
  pickPhone
  Inicia la Action para elegir un número de teléfono distinto.
  Esto normalmente redirige al usuario a una pantalla donde puede seleccionar entre varios números de teléfono registrados, si corresponde.

  Una promesa que se resuelve cuando se envía la Action.

  #### Throws

  Lanza un error si el envío falla.

  ```typescript Example
  import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';

  const mfaPhoneChallenge = new MfaPhoneChallenge();
  await mfaPhoneChallenge.pickPhone();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneChallengePickPhoneOptions">MfaPhoneChallengePickPhoneOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Inicia la Action para seleccionar un autenticador o factor de MFA diferente.
  Esto redirige al usuario a una pantalla donde puede elegir un método de MFA alternativo (por ejemplo, una aplicación OTP o un código de recuperación).

  Una promesa que se resuelve cuando se envía la Action.

  #### Throws

  Lanza un error si el envío falla.

  ```typescript Example
  import MfaPhoneChallenge from '@auth0/auth0-acul-js/mfa-phone-challenge';

  const mfaPhoneChallenge = new MfaPhoneChallenge();
  await mfaPhoneChallenge.tryAnotherMethod();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaPhoneChallengePickAuthenticatorOptions">MfaPhoneChallengePickAuthenticatorOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>