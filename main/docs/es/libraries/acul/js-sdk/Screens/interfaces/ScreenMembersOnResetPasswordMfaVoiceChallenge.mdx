---
title: "ScreenMembersOnResetPasswordMfaVoiceChallenge"
---

Interfaz para los datos específicos de la pantalla reset-password-mfa-voice-challenge.

```ts Example
export interface ScreenMembersOnResetPasswordMfaVoiceChallenge extends ScreenMembers {
  data: {
    /**
     * El número de teléfono al que enviar la llamada de voz.
     */
    phoneNumber: string;

    /**
     * Si se debe mostrar el enlace para cambiar a la verificación por SMS.
     */
    showLinkSms?: boolean;
  } | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="phoneNumber">
  #### Declaración de tipo

  &#123; `phoneNumber`: `string`; `showLinkSms?`: `boolean`; &#125;

  #### phoneNumber

  El número de teléfono al que se enviará la llamada de voz.

  #### showLinkSms?

  Indica si se debe mostrar el enlace para cambiar a la verificación por SMS.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />