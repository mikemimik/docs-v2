---
title: "VerifySecurityKeyOptions"
---

VerifySecurityKeyOptions

```ts Example
export interface VerifySecurityKeyOptions extends CustomOptions {
  rememberDevice?: boolean;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="rememberDevice?" type="boolean">
  Opcional. Si es true, intenta recordar el navegador para futuros desafíos MFA.
  Se corresponde con el campo `rememberBrowser` del formulario.
</ParamField>