---
title: "MfaVoiceChallengeMembers"
---

Interfaz que define todos los miembros y las operaciones disponibles en la pantalla de verificación por voz de MFA.

````ts Example
export interface MfaVoiceChallengeMembers extends BaseMembers {
  /**
   * Propiedades y datos específicos de la pantalla.
   */
  screen: ScreenMembersOnMfaVoiceChallenge;

  untrustedData: UntrustedDataMembersOnMfaVoiceChallenge;

  /**
   * Envía el código de verificación por voz para validar el desafío de MFA.
   *
   * @param payload - Objeto que contiene el código de verificación y parámetros opcionales
   * @returns Promise que se resuelve cuando el código se valida correctamente
   *
   * @example
   * ```typescript
   * const mfaVoiceChallenge = new MfaVoiceChallenge();
   * mfaVoiceChallenge.continue({
   *   code: '123456',
   *   rememberDevice: true
   * });
   * ```
   */
  continue(payload: MfaVoiceChallengeContinueOptions): Promise<void>;

  /**
   * Navega a la pantalla para seleccionar un número de teléfono diferente.
   *
   * @param payload - Parámetros personalizados opcionales
   * @returns Promise que se resuelve cuando la navegación finaliza
   *
   * @example
   * ```typescript
   * const mfaVoiceChallenge = new MfaVoiceChallenge();
   * mfaVoiceChallenge.pickPhone();
   * ```
   */
  pickPhone(payload?: CustomOptions): Promise<void>;

  /**
   * Cambia al método de verificación por SMS en lugar de llamada de voz.
   *
   * @param payload - Parámetros personalizados opcionales
   * @returns Promise que se resuelve cuando el cambio finaliza
   *
   * @example
   * ```typescript
   * const mfaVoiceChallenge = new MfaVoiceChallenge();
   * mfaVoiceChallenge.switchToSms();
   * ```
   */
  switchToSms(payload?: CustomOptions): Promise<void>;

  /**
   * Solicita una nueva llamada de voz con un código de verificación.
   *
   * @param payload - Parámetros personalizados opcionales
   * @returns Promise que se resuelve cuando el nuevo código es enviado
   *
   * @example
   * ```typescript
   * const mfaVoiceChallenge = new MfaVoiceChallenge();
   * mfaVoiceChallenge.resendCode();
   * ```
   */
  resendCode(payload?: CustomOptions): Promise<void>;

  /**
   * Navega a la pantalla para seleccionar un método de MFA alternativo.
   *
   * @param payload - Parámetros personalizados opcionales
   * @returns Promise que se resuelve cuando la navegación finaliza
   *
   * @example
   * ```typescript
   * const mfaVoiceChallenge = new MfaVoiceChallenge();
   * mfaVoiceChallenge.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: CustomOptions): Promise<void>;

  /**
   * Obtiene la funcionalidad de reenvío con gestión de tiempo de espera para esta pantalla
   * @param options Opciones de configuración para la funcionalidad de reenvío
   */
  resendManager(options?: StartResendOptions): ResendControl;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaVoiceChallenge">ScreenMembersOnMfaVoiceChallenge</a></span>}>
  Propiedades y datos específicos de la pantalla.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembersOnMfaVoiceChallenge">UntrustedDataMembersOnMfaVoiceChallenge</a></span>} />

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="continue" type="Promise<void>">
  Envía el código de verificación por voz para validar el desafío de MFA.

  Promesa que se resuelve cuando el código se valida correctamente

  ```typescript Example
  const mfaVoiceChallenge = new MfaVoiceChallenge();
  mfaVoiceChallenge.continue({
    code: '123456',
    rememberDevice: true
  });
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaVoiceChallengeContinueOptions">MfaVoiceChallengeContinueOptions</a></span>}>
      Objeto que contiene el código de verificación y parámetros opcionales

      **Propiedades**

      <ParamField body="code" type="string">
        El código de verificación recibido por llamada de voz.
      </ParamField>

      <ParamField body="rememberDevice?" type="boolean">
        Indicador opcional para recordar el dispositivo durante 30 días y omitir futuros desafíos de MFA.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="pickPhone" type="Promise<void>">
  Navega a la pantalla para seleccionar otro número de teléfono.

  Promesa que se resuelve cuando se completa la navegación

  ```typescript Example
  const mfaVoiceChallenge = new MfaVoiceChallenge();
  mfaVoiceChallenge.pickPhone();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Parámetros personalizados opcionales
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendCode" type="Promise<void>">
  Solicita una nueva llamada de voz con un código de verificación.

  Promesa que se resuelve cuando se envía el nuevo código

  ```typescript Example
  const mfaVoiceChallenge = new MfaVoiceChallenge();
  mfaVoiceChallenge.resendCode();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Parámetros personalizados opcionales
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendManager" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ResendControl">ResendControl</a></span>}>
  Obtiene la funcionalidad de reenvío con gestión del tiempo de espera para esta pantalla

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/StartResendOptions">StartResendOptions</a></span>}>
      Opciones de configuración para la funcionalidad de reenvío

      **Propiedades**

      <ParamField body="onStatusChange?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/type-aliases/OnStatusChangeCallback">OnStatusChangeCallback</a></span>} />

      <ParamField body="onTimeout" type="void" />

      <ParamField body="timeoutSeconds?" type="number" />
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="switchToSms" type="Promise<void>">
  Cambia al método de verificación por SMS en lugar de la llamada de voz.

  Promesa que se resuelve cuando se completa el cambio

  ```typescript Example
  const mfaVoiceChallenge = new MfaVoiceChallenge();
  mfaVoiceChallenge.switchToSms();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Parámetros personalizados opcionales
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Navega a la pantalla para seleccionar otro método de MFA.

  Promesa que se resuelve cuando se completa la navegación

  ```typescript Example
  const mfaVoiceChallenge = new MfaVoiceChallenge();
  mfaVoiceChallenge.tryAnotherMethod();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Parámetros personalizados opcionales
    </ParamField>
  </Expandable>
</ParamField>