---
title: "ResetPasswordMfaPhoneChallengeContinueOptions"
---

ContinueOptions

```ts Example
export interface ContinueOptions extends CustomOptions {
  /**
   * El método de entrega seleccionado por el usuario para recibir el código de verificación.
   * - `sms`: Envía el código mediante mensaje de texto.
   * - `voice`: Envía el código mediante una llamada de voz.
   * @type {'sms' | 'voice'}
   */
  type: 'sms' | 'voice';
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
  El método de entrega que seleccionó el usuario para recibir el código de verificación.

  * `sms`: Envía el código por mensaje de texto.
  * `voice`: Envía el código mediante una llamada de voz.
</ParamField>