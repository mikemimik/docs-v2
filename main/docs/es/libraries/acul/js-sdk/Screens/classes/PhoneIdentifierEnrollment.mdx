---
title: "PhoneIdentifierEnrollment"
description: "Describe todas las propiedades y métodos disponibles para personalizar la pantalla `phone-identifier-enrollment` de Universal Login."
---

La clase PhoneIdentifierEnrollment implementa la funcionalidad de la pantalla `phone-identifier-enrollment`. Esta pantalla permite a los usuarios registrar un número de teléfono como identificador seleccionando su método de entrega preferido para la verificación.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/7kuQwDVmt0NkHCbQObLj8d/b690fd899f7c8525532d6d640298f2cd/PhoneIdentifierEnrollment.png" alt="PhoneIdentifierEnrollment" />
</Frame>

<div id="constructors">
  ## Constructores
</div>

Crea una instancia del administrador de la pantalla PhoneIdentifierEnrollment:

```typescript Example
import PhoneIdentifierEnrollment from '@auth0/auth0-acul-js/phone-identifier-enrollment';
const phoneIdentifierEnrollmentManager = new PhoneIdentifierEnrollment();
await phoneIdentifierEnrollmentManager.continuePhoneEnrollment({
  type: 'voice',
});
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Proporciona configuraciones relacionadas con la marca, como el tema de marca y otros ajustes.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `phone-identifier-enrollment`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Proporciona información sobre la organización del usuario, como `id` y `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contiene datos sobre el prompt actual en el flujo de autenticación.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnPhoneIdentifierEnrollment">ScreenMembersOnPhoneIdentifierEnrollment</a></span>}>
  Contiene detalles específicos de la pantalla `phone-identifier-enrollment`, incluida su configuración y contexto.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contiene datos relacionados con el tenant, como `id` y los metadatos asociados.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Proporciona datos específicos de la transacción para la pantalla `phone-identifier-enrollment`, como identificadores activos y estados del flujo.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gestiona datos no confiables enviados al SDK, como la entrada del usuario durante el flujo de inscripción del teléfono.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Detalles del usuario activo, incluidos `username`, `email` y `roles`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Este método cambia el idioma que se muestra en la página de Universal Login.

  ```typescript Example
  import PhoneIdentifierEnrollment from '@auth0/auth0-acul-js/phone-identifier-enrollment';
  const phoneIdentifierEnrollmentManager = new PhoneIdentifierEnrollment();
  phoneIdentifierEnrollmentManager.changeLanguage({
    language: 'fr',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      El código de configuración regional del idioma deseado (por ejemplo, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Cuando se establece en `'session'`, el idioma seleccionado se conserva durante la sesión.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="continuePhoneEnrollment" type="Promise<void>">
  Este método continúa el proceso de inscripción del teléfono con el método de entrega seleccionado.

  ```typescript Example
  import PhoneIdentifierEnrollment from '@auth0/auth0-acul-js/phone-identifier-enrollment';
  const phoneIdentifierEnrollmentManager = new PhoneIdentifierEnrollment();
  await phoneIdentifierEnrollmentManager.continuePhoneEnrollment({
    type: 'voice',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [PhoneEnrollmentOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/PhoneEnrollmentOptions).
    </ParamField>

    <ParamField body="type" type="&#x22;text&#x22; | &#x22;voice&#x22;" required>
      El método de entrega del código de verificación.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Este método obtiene el arreglo de errores de la transacción desde el contexto, o un arreglo vacío si no existe ninguno.
</ParamField>

<ParamField body="returnToPrevious" type="Promise<void>">
  Este método devuelve al usuario a la pantalla anterior.

  ```typescript Example
  import PhoneIdentifierEnrollment from '@auth0/auth0-acul-js/phone-identifier-enrollment';
  const phoneIdentifierEnrollmentManager = new PhoneIdentifierEnrollment();
  await phoneIdentifierEnrollmentManager.returnToPrevious();
  ```
</ParamField>