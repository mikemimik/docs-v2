---
title: "ResetPasswordMfaPushChallengePush"
description: "Describe todas las propiedades y los métodos disponibles para personalizar la pantalla `reset-password-mfa-push-challenge-push` de Universal Login."
---

La clase ResetPasswordMfaPushChallengePush implementa la funcionalidad de la pantalla `reset-password-mfa-push-challenge-push`. Esta pantalla se muestra cuando se ha enviado una notificación push al dispositivo del usuario como parte del paso de MFA durante el flujo de restablecimiento de contraseña.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/5ANIhgOJURo2z0YXmWKp7M/5f266f0a935c047d6159874cbfc2001a/Screenshot_2025-03-26_at_09.15.46.png" alt="" />
</Frame>

<div id="constructors">
  ## Constructores
</div>

Crea una instancia del gestor de pantalla ResetPasswordMfaPushChallengePush:

```typescript Example
import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
await resetPasswordMfaPushChallengePushManager.continue();
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Proporciona configuraciones relacionadas con la marca, como el tema y sus ajustes.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `reset-password-mfa-push-challenge-push`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Proporciona información sobre la organización del usuario, como `id` y `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contiene datos sobre la solicitud actual dentro del flujo de autenticación.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnResetPasswordMfaPushChallengePush">ScreenMembersOnResetPasswordMfaPushChallengePush</a></span>}>
  Contiene detalles específicos de la pantalla `reset-password-mfa-push-challenge-push`, incluida su configuración y contexto.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contiene datos relacionados con el tenant, como `id` y los metadatos asociados.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Proporciona datos específicos de la transacción para la pantalla `reset-password-mfa-push-challenge-push`, como identificadores activos y estados del flujo.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gestiona datos no confiables que se pasan al SDK, como la entrada del usuario durante el flujo del desafío MFA.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Detalles del usuario activo, incluidos `username`, `email` y `roles`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Este método cambia el idioma que se muestra en la página de Universal Login.

  ```typescript Example
  import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
  const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
  resetPasswordMfaPushChallengePushManager.changeLanguage({
    language: 'fr',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      El código de configuración regional del idioma deseado (por ejemplo, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Cuando se establece en `'session'`, el idioma seleccionado se conserva durante toda la sesión.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="continue" type="Promise<void>">
  Este método confirma el desafío de notificación push para continuar con el paso de MFA durante el flujo de restablecimiento de contraseña.

  ```typescript Example
  import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
  const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
  await resetPasswordMfaPushChallengePushManager.continue();
  ```
</ParamField>

<ParamField body="enterCodeManually" type="Promise<void>">
  Este método cambia al usuario a la introducción manual del código en lugar de esperar la aprobación de la notificación push.

  ```typescript Example
  import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
  const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
  await resetPasswordMfaPushChallengePushManager.enterCodeManually();
  ```
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Este método recupera la lista de errores de transacción del contexto, o una lista vacía si no existe ninguno.
</ParamField>

<ParamField body="pollingManager" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaPushPollingControl">MfaPushPollingControl</a></span>}>
  Este método inicia y gestiona el sondeo del desafío push de MFA. Comprueba repetidamente si la notificación push ha sido aprobada en el intervalo especificado y ejecuta la devolución de llamada `onCompleted` cuando se detecta la aprobación. Las respuestas HTTP 429 por límite de velocidad se gestionan internamente: el sondeo espera a que el límite se restablezca antes de volver a intentarlo.

  El [`MfaPushPollingControl`](/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaPushPollingControl) devuelto expone `startPolling()`, `stopPolling()` e `isRunning()` para gestionar el ciclo de vida del sondeo.

  ```typescript Example
  import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
  const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
  const control = resetPasswordMfaPushChallengePushManager.pollingManager({
    intervalMs: 5000,
    onCompleted: () => resetPasswordMfaPushChallengePushManager.continue(),
    onError: (error) => console.error('Polling error:', error),
  });

  control.startPolling();

  // Cancelar el sondeo si es necesario
  control.stopPolling();
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [MfaPollingOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/MfaPollingOptions).
    </ParamField>

    <ParamField body="intervalMs?" type="number">
      El intervalo en milisegundos entre intentos de sondeo.
    </ParamField>

    <ParamField body="onCompleted?" type="() => void">
      Devolución de llamada que se ejecuta cuando se aprueba el desafío push y el sondeo se detiene automáticamente.
    </ParamField>

    <ParamField body="onError?" type="(error: Error) => void">
      Devolución de llamada que se ejecuta si se produce un error durante el sondeo.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendPushNotification" type="Promise<void>">
  Este método vuelve a enviar la notificación push al dispositivo del usuario.

  ```typescript Example
  import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
  const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
  await resetPasswordMfaPushChallengePushManager.resendPushNotification();
  ```
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Este método permite al usuario seleccionar otro método de MFA para completar el desafío.

  ```typescript Example
  import ResetPasswordMfaPushChallengePush from '@auth0/auth0-acul-js/reset-password-mfa-push-challenge-push';
  const resetPasswordMfaPushChallengePushManager = new ResetPasswordMfaPushChallengePush();
  await resetPasswordMfaPushChallengePushManager.tryAnotherMethod();
  ```
</ParamField>