---
title: "LoginPasswordlessEmailCode"
description: "Describe todas las propiedades y los métodos disponibles para personalizar la pantalla `login-passwordless-email-code` de Universal Login."
---

La clase LoginPasswordlessEmailCode implementa la funcionalidad de la pantalla `login-passwordless-email-code`. Esta pantalla solicita el correo electrónico y el código del usuario.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/4DKgjU2dGw0mpLJKGfGhp0/470b29266da87f9bfc3e7eef55069620/Login_Passwordless_Email_Code.png" alt="Código por correo electrónico de login passwordless" />
</Frame>

<div id="constructors">
  ## Constructores
</div>

Crea una instancia del administrador de la pantalla LoginPasswordlessEmailCode:

```typescript Example
import LoginPasswordlessEmailCode from '@auth0/auth0-acul-js/login-passwordless-email-code';
const loginPasswordlessEmailCodeManager = new LoginPasswordlessEmailCode();
loginPasswordlessEmailCodeManager.submitCode({
  code: '123456',
});
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Proporciona configuraciones relacionadas con la marca, como el tema de marca y otros ajustes.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `login-passwordless-email-code`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Proporciona información sobre la organización del usuario, como `id` y `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contiene datos sobre el prompt actual dentro del flujo de autenticación.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnLoginPasswordlessEmailCode">ScreenMembersOnLoginPasswordlessEmailCode</a></span>}>
  Contiene detalles específicos de la pantalla `login-passwordless-email-code`, incluida su configuración y contexto.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contiene datos relacionados con el tenant, como `id` y los metadatos asociados.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembersOnLoginPasswordlessEmailCode">TransactionMembersOnLoginPasswordlessEmailCode</a></span>}>
  Proporciona datos específicos de la transacción para la pantalla `login-passwordless-email-code`, como identificadores activos y estados del flujo.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gestiona datos no confiables que se pasan al SDK, como la entrada del usuario durante el inicio de sesión.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Detalles del usuario activo, incluidos `username`, `email` y `roles`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Este método cambia el idioma de visualización de la página de Universal Login.

  ```typescript Example
  import LoginPasswordlessEmailCode from '@auth0/auth0-acul-js/login-passwordless-email-code';
  const loginPasswordlessEmailCodeManager = new LoginPasswordlessEmailCode();
  loginPasswordlessEmailCodeManager.changeLanguage({
    language: 'fr',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      El código de configuración regional del idioma deseado (por ejemplo, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Cuando se establece en `'session'`, el idioma seleccionado se mantiene durante toda la sesión.
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22; | &#x22;undefined&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Este método recupera el arreglo de errores de la transacción desde el contexto, o un arreglo vacío si no existe ninguno. Un arreglo de objetos de error del contexto de la transacción.
</ParamField>

<ParamField body="resendCode" type="Promise<void>">
  Este método vuelve a enviar el código a la dirección de correo electrónico del usuario.

  ```typescript Example
  import LoginPasswordlessEmailCode from '@auth0/auth0-acul-js/login-passwordless-email-code';
  const loginPasswordlessEmailCodeManager = new LoginPasswordlessEmailCode();
  loginPasswordlessEmailCodeManager.resendCode();
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [CustomOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/CustomOptions).
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22; | &#x22;undefined&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendManager" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ResendControl">ResendControl</a></span>}>
  Este método proporciona la funcionalidad de reenvío con gestión del tiempo de espera. Devuelve un objeto [`ResendControl`](/docs/es/libraries/acul/js-sdk/Screens/interfaces/ResendControl) con un método `startResend()` que activa el reenvío e impone un período de espera antes de que el usuario pueda volver a reenviar.

  ```typescript Example
  import LoginPasswordlessEmailCode from '@auth0/auth0-acul-js/login-passwordless-email-code';

  const loginPasswordlessEmailCodeManager = new LoginPasswordlessEmailCode();
  const { startResend } = loginPasswordlessEmailCodeManager.resendManager({
    timeoutSeconds: 15,
    onStatusChange: (remainingSeconds, isDisabled) => {
      console.log(`Reenvío disponible en ${remainingSeconds}s, deshabilitado: ${isDisabled}`);
    },
    onTimeout: () => {
      console.log('El reenvío ya está disponible');
    }
  });

  // Llama a startResend cuando el usuario haga clic en el botón de reenvío
  startResend();
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [StartResendOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/StartResendOptions).
    </ParamField>

    <ParamField body="timeoutSeconds?" type="number">
      La duración del período de espera en segundos antes de que el usuario pueda volver a reenviar el código.
    </ParamField>

    <ParamField body="onStatusChange?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/type-aliases/OnStatusChangeCallback">OnStatusChangeCallback</a></span>}>
      Devolución de llamada que se invoca cada vez que cambia el estado del reenvío; devuelve los segundos restantes y si la funcionalidad de reenvío está deshabilitada en ese momento.
    </ParamField>

    <ParamField body="onTimeout?" type="() => void">
      Devolución de llamada que se invoca cuando expira el período de espera y el reenvío vuelve a estar disponible.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="submitCode" type="Promise<void>">
  Este método continúa el proceso de inicio de sesión con el código enviado por correo electrónico.

  ```typescript Example
  import LoginPasswordlessEmailCode from '@auth0/auth0-acul-js/login-passwordless-email-code';
  const loginPasswordlessEmailCodeManager = new LoginPasswordlessEmailCode();
  loginPasswordlessEmailCodeManager.submitCode({
    code: '123456',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [SubmitCodeOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/SubmitCodeOptions).
    </ParamField>

    <ParamField body="code" type="string | number" required>
      El código introducido por el usuario.
    </ParamField>

    <ParamField body="captcha?" type="string">
      El código o la respuesta del captcha proporcionados por el proveedor de CAPTCHA. Esta propiedad es obligatoria si tu tenant de Auth0 tiene [Bot Detection](/docs/es/secure/attack-protection/bot-detection) habilitado.
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22; | &#x22;undefined&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="switchConnection" type="Promise<void>">
  Este método alterna entre una conexión de base de datos (contraseña) y una conexión passwordless (OTP por correo electrónico/SMS).

  ```typescript Example
  import LoginPasswordlessEmailCode from '@auth0/auth0-acul-js/login-passwordless-email-code';
  const loginPasswordlessEmailCodeManager = new LoginPasswordlessEmailCode();
  loginPasswordlessEmailCodeManager.switchConnection({
    connection: 'email',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [SwitchConnectionOptionsEmailCode](/docs/es/libraries/acul/js-sdk/Screens/interfaces/SwitchConnectionOptionsEmailCode).
    </ParamField>

    <ParamField body="connection" type="string" required>
      El nombre de la conexión a la que quieres cambiar. Usa `'email'` o `'sms'` para passwordless, o el nombre de una conexión de base de datos para la autenticación basada en contraseña.
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>