---
title: "changeLanguage"
---

> **changeLanguage**(`options`: [`LanguageChangeOptions`](/docs/es/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions)): `Promise`&lt;`void`&gt;

Envía la nueva preferencia de configuración regional al servidor mediante la acción `change-language` y vuelve a renderizar la interfaz en el idioma seleccionado.

<div id="parameters">
  ## Parámetros
</div>

<ParamField body="options" type="LanguageChangeOptions" required>
  La configuración para cambiar el idioma. Consulta [`LanguageChangeOptions`](/docs/es/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).

  <Expandable title="opciones">
    <ParamField body="language" type="string" required>
      Un código de configuración regional para el idioma de destino (p. ej., `'en'`, `'fr'`, `'es'`). Debe ser una de las configuraciones regionales habilitadas en tu tenant de Auth0.
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Ámbito de persistencia de la preferencia de idioma. Actualmente, solo se admite `"session"`.
    </ParamField>
  </Expandable>
</ParamField>

<div id="returns">
  ## Devuelve
</div>

`Promise`&lt;`void`&gt;

Se resuelve cuando se ha enviado la preferencia de configuración regional y la pantalla se vuelve a renderizar en el idioma seleccionado.