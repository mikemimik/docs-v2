---
title: "MfaSmsListOptions"
---

MfaSmsListOptions
Opciones para ejecutar acciones en la pantalla mfa-sms-list.

```ts Example
export interface MfaSmsListOptions {
  /**
   * @param {number} index - El índice del número de teléfono a seleccionar.
   */
  index: number;
  /** Any additional opción personalizada */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Cualquier opción personalizada adicional

<div id="properties">
  ## Propiedades
</div>

<ParamField body="index" type="number">
  Índice del número de teléfono que se va a seleccionar.
</ParamField>