---
title: "LoginPayloadOptions"
---

Opciones para realizar operaciones de inicio de sesión

```ts Example
export interface LoginOptions {
  /** El nombre de usuario/email con el que iniciar sesión */
  username: string;
  /** La contraseña para la autenticación */
  password: string;
  /** Valor de captcha opcional si es necesario */
  captcha?: string;
  /** Cualquier opción personalizada adicional */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Cualquier opción personalizada adicional

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha?" type="string">
  Valor opcional del captcha, si es necesario
</ParamField>

<ParamField body="password" type="string">
  La contraseña para autenticarse
</ParamField>

<ParamField body="username" type="string">
  El nombre de usuario o correo electrónico que se usa para iniciar sesión
</ParamField>