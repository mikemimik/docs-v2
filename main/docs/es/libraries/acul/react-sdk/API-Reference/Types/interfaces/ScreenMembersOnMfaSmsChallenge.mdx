---
title: "ScreenMembersOnMfaSmsChallenge"
---

Interfaz para los datos de pantalla correspondientes a la pantalla mfa-sms-challenge

```ts Example
export interface ScreenMembersOnMfaSmsChallenge extends ScreenMembers {
  data: {
    /**
     * El número de teléfono al que se envió el SMS
     */
    phoneNumber?: string;
    /**
     * Si se debe mostrar la opción de recordar dispositivo
     */
    showRememberDevice?: boolean;
    /**
     * Si se debe mostrar el enlace para cambiar a verificación por llamada de voz
     */
    showLinkVoice?: boolean;
  } | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="phoneNumber?">
  #### Declaración de tipo

  &#123; `phoneNumber?`: `string`; `showLinkVoice?`: `boolean`; `showRememberDevice?`: `boolean`; &#125;

  #### phoneNumber?

  El número de teléfono al que se envió el SMS

  #### showLinkVoice?

  Si se muestra el enlace para cambiar a la verificación por llamada de voz

  #### showRememberDevice?

  Si se muestra la opción para recordar el dispositivo

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />