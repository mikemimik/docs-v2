---
title: "MfaPushPollingControl"
---

Interfaz de control para gestionar una sesión de sondeo periódico de notificaciones push de MFA.

Esta interfaz proporciona métodos imperativos para iniciar, detener e inspeccionar el estado de una
sondeo de larga duración que comprueba si se ha aprobado una
solicitud push de MFA.

````ts Example
export interface MfaPushPollingControl {
  /**
   * Detiene el proceso de sondeo de inmediato.
   *
   * - Cancela cualquier temporizador programado o solicitud pendiente.
   * - Una vez detenido, `isRunning()` devuelve `false`.
   * - Es seguro llamarlo varias veces; las llamadas posteriores no tienen efecto.
   *
   * @example
   * ```ts
   * const control = mfaPushChallengePush.pollingManager({ intervalMs: 5000 });
   * control.startPolling();
   *
   * // Más tarde, si el usuario cancela:
   * control.stopPolling();
   * ```
   */
  stopPolling: () => void;

  /**
   * Inicia o reanuda el proceso de sondeo.
   *
   * - Si el sondeo ya está activa, volver a llamar a este método no tiene efecto.
   * - Si se detuvo previamente, llamarlo reinicia el bucle de sondeo.
   *
   * @example
   * ```ts
   * control.startPolling(); // Comienza a verificar el desafío push de MFA
   * ```
   */
  startPolling: () => void;

  /**
   * Indica si el proceso de sondeo está en ejecución actualmente.
   *
   * - Devuelve `true` si el sondeo está activa y no ha sido cancelada.
   * - Devuelve `false` si el sondeo se ha detenido o ha finalizado.
   *
   * @example
   * ```ts
   * if (control.isRunning()) {
   *   console.log('Sondeo en progreso...');
   * } else {
   *   console.log('La consulta está detenida o completada.');
   * }
   * ```
   */
  isRunning: () => boolean;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="isRunning" type="boolean">
  Indica si el proceso de sondeo se está ejecutando actualmente.

  ```ts Example
  if (control.isRunning()) {
    console.log('Polling in progress...');
  } else {
    console.log('Polling is stopped or completed.');
  }
  ```
</ParamField>

<ParamField body="startPolling" type="void">
  Inicia o reanuda el proceso de sondeo.

  * Si el sondeo ya está activo, volver a llamarlo no tiene efecto.
  * Si se detuvo anteriormente, volver a llamarlo reinicia el bucle de sondeo.

  ```ts Example
  control.startPolling(); // Empieza a consultar el desafío push de MFA
  ```
</ParamField>

<ParamField body="stopPolling" type="void">
  Detiene el proceso de sondeo de inmediato.

  * Cancela cualquier temporizador programado o solicitud pendiente.
  * Una vez detenido, `isRunning()` devuelve `false`.
  * Se puede llamar varias veces de forma segura; las llamadas posteriores no tienen efecto.

  ```ts Example
  const control = mfaPushChallengePush.pollingManager({ intervalMs: 5000 });
  control.startPolling();

  // Más tarde, si el usuario cancela:
  control.stopPolling();
  ```
</ParamField>