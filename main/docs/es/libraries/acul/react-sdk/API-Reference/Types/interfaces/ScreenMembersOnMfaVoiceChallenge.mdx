---
title: "ScreenMembersOnMfaVoiceChallenge"
---

Interfaz extendida de los miembros de la pantalla para la pantalla de desafío de voz de MFA.

```ts Example
export interface ScreenMembersOnMfaVoiceChallenge extends ScreenMembers {
  /**
   * Datos adicionales de pantalla específicos del desafío de voz de MFA.
   */
  data: {
    /**
     * El número de teléfono al que se envió el código de voz.
     */
    phoneNumber?: string;

    /**
     * Si se debe mostrar la opción de recordar dispositivo.
     */
    showRememberDevice?: boolean;

    /**
     * Si se debe mostrar el enlace para cambiar a la verificación por SMS.
     */
    showLinkSms?: boolean;
  } | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="phoneNumber?">
  Datos adicionales de la pantalla específicos del desafío de voz de MFA.

  #### Declaración de tipo

  &#123; `phoneNumber?`: `string`; `showLinkSms?`: `boolean`; `showRememberDevice?`: `boolean`; &#125;

  #### phoneNumber?

  El número de teléfono al que se envió el código por voz.

  #### showLinkSms?

  Si se debe mostrar el enlace para cambiar a la verificación por SMS.

  #### showRememberDevice?

  Si se debe mostrar la opción de recordar el dispositivo.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />