---
title: "MfaVoiceChallengeContinueOptions"
---

Opciones para enviar el código del desafío de voz.

```ts Example
export interface MfaVoiceChallengeContinueOptions {
  /**
   * El código de verificación recibido mediante llamada de voz.
   */
  code: string;

  /**
   * Indicador opcional para recordar el dispositivo durante 30 días, omitiendo futuros desafíos MFA.
   */
  rememberDevice?: boolean;

  /**
   * Opciones personalizadas adicionales para enviar con la solicitud.
   */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Opciones personalizadas adicionales para incluir en la solicitud.

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string">
  El código de verificación recibido por llamada de voz.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indicador opcional para recordar el dispositivo durante 30 días y omitir futuros desafíos MFA.
</ParamField>