---
title: "MfaSmsEnrollmentContinueOptions"
---

Opciones para continuar con el registro de MFA por SMS.

Extiende `CustomOptions` para permitir proporcionar opciones personalizadas junto con phone y captcha.

```ts Example
export interface MfaSmsEnrollmentContinueOptions extends CustomOptions {
  phone: string;
  captcha?: string;
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propiedades
</div>

<ParamField body="phone" type="string" required>
  El número de teléfono del usuario para el registro por SMS.
</ParamField>

<ParamField body="captcha?" type="string">
  Token de CAPTCHA opcional para la verificación.
</ParamField>