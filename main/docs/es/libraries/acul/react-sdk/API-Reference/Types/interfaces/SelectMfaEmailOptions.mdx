---
title: "SelectMfaEmailOptions"
---

Opciones para seleccionar una dirección de correo electrónico

```ts Example
export interface SelectMfaEmailOptions {
  /**
   * @param {number} index - El índice del número de teléfono a seleccionar.
   *
   */
  index: number;
  /** Cualquier opciones personalizadas adicionales */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Cualquier opciones personalizadas adicionales

<div id="properties">
  ## Propiedades
</div>

<ParamField body="index" type="number">
  El índice del número de teléfono que se desea seleccionar.
</ParamField>