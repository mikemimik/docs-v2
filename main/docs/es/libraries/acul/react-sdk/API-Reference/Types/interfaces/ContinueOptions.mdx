---
title: "ContinueOptions"
---

Opciones para continuar con la verificación por correo electrónico.

```ts Example
export interface ContinueOptions {
  /** El código introducido por el usuario. */
  code: string;
  /** Indica si se debe recordar el device. */
  rememberDevice?: boolean;
  /** Cualesquiera opciones personalizadas adicionales */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Cualesquiera otras opciones personalizadas

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string">
  El código introducido por el usuario.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indica si se debe recordar el dispositivo.
</ParamField>