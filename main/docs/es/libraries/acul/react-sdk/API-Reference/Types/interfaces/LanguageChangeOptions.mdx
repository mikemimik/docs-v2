---
title: "LanguageChangeOptions"
---

Opciones que acepta la función devuelta por [`useChangeLanguage`](/docs/es/libraries/acul/react-sdk/API-Reference/Hooks/useChangeLanguage).

```typescript
interface LanguageChangeOptions {
  language: string;
  persist?: "session";
  [key: string]: string | number | boolean | undefined;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="language" type="string" required>
  El código de idioma al que se cambiará. Debe coincidir con uno de los locales habilitados en tu tenant de Auth0. Ejemplo: `"en"`, `"fr"`, `"es"`.
</ParamField>

<ParamField body="persist" type="&#x22;session&#x22;">
  El alcance de persistencia de la preferencia de idioma. Actualmente, solo se admite `"session"`. El valor predeterminado es `"session"`.
</ParamField>

<div id="remarks">
  ## Observaciones
</div>

* Se pueden pasar campos personalizados adicionales junto con `language` y `persist`. Los campos personalizados deben llevar el prefijo `"ulp-"` y aceptar valores de tipo `string`, `number`, `boolean` o `undefined`.