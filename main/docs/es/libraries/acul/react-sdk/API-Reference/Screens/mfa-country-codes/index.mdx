---
title: "MfaCountryCodes"
description: "Describe todos los hooks y métodos disponibles para personalizar la pantalla `mfa-country-codes` de Universal Login."
---

La pantalla `mfa-country-codes` permite a los usuarios ver y seleccionar un código de país para los flujos de inscripción en MFA o de desafío por teléfono.

<Frame>
  <img style={{maxHeight:"400px"}} src="https://mintlify.s3.us-west-1.amazonaws.com/auth0/docs/images/ja-jp/cdy7uua7fh8z/7hp8LeL9cbS3nbxw8wTuqK/3c862b1c47ae18fb43f22f55ea4d695a/Screenshot_2025-02-19_at_13.23.44.png" alt="MFACountryCodes" />
</Frame>

<div id="import">
  ## Importación
</div>

Cada pantalla tiene su propio conjunto de hooks y métodos. El SDK admite **importación parcial** e **importación desde la raíz** para cada pantalla.

* Usar la importación parcial te permite incluir solo el código que necesitas para tu caso de uso específico.
* Usar la importación desde la raíz te permite cargar todos las pantallas desde un único paquete, lo que resulta útil cuando quieres una compilación unificada que pueda manejar todas las pantallas posibles.

```jsx Import Example
// importación desde la raíz
import { useMfaCountryCodes } from '@auth0/auth0-acul-react';

// importación parcial
import {
  useMfaCountryCodes,
  // Hooks de contexto
  useUser,
  useTenant,
  useBranding,
  useClient,
  useOrganization,
  usePrompt,
  useScreen,
  useTransaction,
  useUntrustedData,
  // Hooks comunes
  useCurrentScreen,
  useAuth0Themes,
  useErrors,
  // Hooks de utilidad
  useChangeLanguage,
  // Métodos
  goBack,
  selectCountryCode,
} from "@auth0/auth0-acul-react/mfa-country-codes";

function MfaCountryCodesScreen() {
  const { selectCountryCode } = useMfaCountryCodes();
  return (
    <button onClick={() => selectCountryCode({ country_code: 'US', phone_prefix: '+1' })}>
      Select +1 (US)
    </button>
  );
}
```

<div id="context-hooks">
  ## Context Hooks
</div>

Hooks con alcance de pantalla que proporcionan acceso de solo lectura a los datos de contexto de Auth0 en la pantalla `mfa-country-codes`. Impórtalos desde `@auth0/auth0-acul-react/mfa-country-codes`.

<ParamField body="useBranding" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Este hook proporciona configuraciones de branding, como el logotipo, los colores y la configuración del tema que se muestran en la pantalla `mfa-country-codes`.

  ```jsx Example
  import { useBranding } from '@auth0/auth0-acul-react/mfa-country-codes';
  function CustomTheme() {
    const branding = useBranding();
  }
  ```
</ParamField>

<ParamField body="useClient" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>}>
  Este hook proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `mfa-country-codes`.

  ```jsx Example
  import { useClient } from '@auth0/auth0-acul-react/mfa-country-codes';
  function AppInfo() {
    const client = useClient();
  }
  ```
</ParamField>

<ParamField body="useOrganization" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Este hook proporciona información sobre la organización del usuario si el flujo de MFA tiene alcance de organización. Devuelve `null` cuando no hay contexto de organización.

  ```jsx Example
  import { useOrganization } from '@auth0/auth0-acul-react/mfa-country-codes';
  function OrgSelector() {
    const organization = useOrganization();
    if (!organization) {
      return <p>No organization context</p>;
    }
  }
  ```
</ParamField>

<ParamField body="usePrompt" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>}>
  Este hook contiene datos sobre el prompt actual en el flujo de autenticación.

  ```jsx Example
  import { usePrompt } from '@auth0/auth0-acul-react/mfa-country-codes';
  function FlowInfo() {
    const prompt = usePrompt();
  }
  ```
</ParamField>

<ParamField body="useScreen" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaCountryCodes">ScreenMembersOnMfaCountryCodes</a></span>}>
  Este hook contiene detalles específicos de la pantalla `mfa-country-codes`, incluida la lista de códigos de país disponibles y el contexto actual de la pantalla.

  ```jsx Example
  import { useScreen } from '@auth0/auth0-acul-react/mfa-country-codes';
  function ScreenDebug() {
    const screen = useScreen();
  }
  ```
</ParamField>

<ParamField body="useTenant" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>}>
  Este hook contiene datos relacionados con el tenant, como `id` y los metadatos asociados.

  ```jsx Example
  import { useTenant } from '@auth0/auth0-acul-react/mfa-country-codes';
  function TenantInfo() {
    const tenant = useTenant();
  }
  ```
</ParamField>

<ParamField body="useTransaction" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Este hook proporciona datos específicos de la transacción para la pantalla `mfa-country-codes`, como el estado actual del flujo de MFA.

  ```jsx Example
  import { useTransaction } from '@auth0/auth0-acul-react/mfa-country-codes';
  function TransactionInfo() {
    const transaction = useTransaction();
  }
  ```
</ParamField>

<ParamField body="useUntrustedData" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Este hook maneja datos no confiables que se pasan a la pantalla, como valores rellenados previamente a partir de parámetros de URL.

  ```jsx Example
  import { useUntrustedData } from '@auth0/auth0-acul-react/mfa-country-codes';
  function PrefilledForm() {
    const untrustedData = useUntrustedData();
  }
  ```
</ParamField>

<ParamField body="useUser" type={<span>() =&gt; <a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>}>
  Este hook proporciona información del usuario activo, incluidos `username`, `email` y los métodos de autenticación disponibles.

  ```jsx Example
  import { useUser } from '@auth0/auth0-acul-react/mfa-country-codes';
  function UserProfile() {
    const user = useUser();
  }
  ```
</ParamField>

<ParamField body="useMfaCountryCodes" type={<a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/MfaCountryCodesMembers">MfaCountryCodesMembers</a>}>
  Este hook devuelve todos los métodos y el contexto disponibles en la pantalla `mfa-country-codes`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="goBack" type="Promise<void>">
  Este método vuelve a la pantalla anterior en el flujo de MFA.

  ```jsx Example
  import { useMfaCountryCodes } from '@auth0/auth0-acul-react/mfa-country-codes';

  function BackButton() {
    const { goBack } = useMfaCountryCodes();
    return (
      <button onClick={() => goBack()}>
        Back
      </button>
    );
  }
  ```
</ParamField>

<ParamField body="selectCountryCode" type="Promise<void>">
  Este método confirma el código de país seleccionado y devuelve al usuario a la pantalla de inscripción o desafío.

  ```jsx Example
  import { useMfaCountryCodes } from '@auth0/auth0-acul-react/mfa-country-codes';

  function CountryCodePicker() {
    const { selectCountryCode } = useMfaCountryCodes();
    return (
      <button onClick={() => selectCountryCode({ country_code: 'US', phone_prefix: '+1' })}>
        Select +1 (US)
      </button>
    );
  }
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [SelectCountryCodeOptions](/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/SelectCountryCodeOptions).
    </ParamField>

    <ParamField body="country_code" type="string" required>
      El código de país (p. ej., `"US"`, `"GB"`).
    </ParamField>

    <ParamField body="phone_prefix" type="string" required>
      El prefijo telefónico (p. ej., `"+1"`, `"+44"`).
    </ParamField>
  </Expandable>
</ParamField>

<div id="commonutility-hooks">
  ## Hooks comunes/de utilidad
</div>

<ParamField body={<a href="/docs/es/libraries/acul/react-sdk/API-Reference/Hooks/useAuth0Themes">useAuth0Themes</a>} type="Hooks">
  Este hook obtiene las opciones del tema actual con la configuración simplificada del contexto de branding.
</ParamField>

<ParamField body={<a href="/docs/es/libraries/acul/react-sdk/API-Reference/Hooks/useChangeLanguage">useChangeLanguage</a>} type="Hooks">
  Este hook devuelve una función para cambiar el idioma que se muestra en la pantalla actual de ACUL.
</ParamField>

<ParamField body={<a href="/docs/es/libraries/acul/react-sdk/API-Reference/Hooks/useCurrentScreen">useCurrentScreen</a>} type="Hooks">
  Este hook obtiene el context y el state de la pantalla actual.
</ParamField>

<ParamField body={<a href="/docs/es/libraries/acul/react-sdk/API-Reference/Hooks/useErrors">useErrors</a>} type="Hooks">
  Este hook lee y gestiona los errores del servidor, del cliente y del desarrollador en la pantalla.
</ParamField>