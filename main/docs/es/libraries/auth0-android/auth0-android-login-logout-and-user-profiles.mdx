---
description: Aprende a añadir el inicio de sesión, el cierre de sesión y a leer el perfil de usuario con el Android SDK.
title: Inicio de sesión, cierre de sesión y perfiles de usuario en Auth0.Android
---

<div id="add-login-to-your-android-application">
  ## Añade el inicio de sesión a tu aplicación de Android
</div>

Puedes hacer que el usuario inicie sesión con el método `WebAuthProvider.login`:

```kotlin lines
WebAuthProvider.login(account)
  .withScheme(getString(R.string.com_auth0_scheme))
  .withScope("openid profile email")
  .start(this, object : Callback<Credentials, AuthenticationException> {
    override fun onFailure(exception: AuthenticationException) {
       // Autenticación fallida
     }

      override fun onSuccess(credentials: Credentials) {
         // Autenticación exitosa
       }
  })
```

El resultado de la autenticación se enviará a la devolución de llamada `onSuccess`.

Consulta la [configuración de Auth0.Android](/docs/es/libraries/auth0-android/auth0-android-configuration) para ver más opciones de la clase `WebAuthProvider`.

<div id="add-logout-to-your-android-application">
  ## Añade el cierre de sesión a tu aplicación Android
</div>

Para cerrar la sesión del usuario, llama al método `WebAuthProvider.logout`. El resultado del cierre de sesión se devolverá en la devolución de llamada `onSuccess`.

Este método elimina la cookie que el navegador estableció durante la autenticación, por lo que obliga a los usuarios a volver a introducir sus credenciales la próxima vez que intenten autenticarse.

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Si se cancela el cierre de sesión, quizá quieras devolver al usuario a donde estaba antes de intentar cerrar la sesión.
</Callout>

```kotlin lines
WebAuthProvider.logout(account)
  .withScheme("demo")
  .start(this, object: Callback<Void?, AuthenticationException> {
    override fun onSuccess(payload: Void?) {
      // ¡El usuario ha cerrado sesión!
    }

    override fun onFailure(error: AuthenticationException) {
      // ¡Algo salió mal!
    }
  })
```

<div id="show-the-users-profile">
  ## Mostrar el perfil del usuario
</div>

Utiliza la clase `AuthenticationAPIClient` para recuperar el perfil del usuario de Auth0. Para ello, necesitas lo siguiente:

* El <Tooltip tip="Access Token: Credencial de autorización, en forma de una cadena opaca o JWT, utilizada para acceder a una API." cta="Ver glosario" href="/docs/es/glossary?term=access+token">access token</Tooltip> recibido durante el proceso de inicio de sesión
* El scope `profile`, que debes incluir al llamar a `WebAuthProvider.login`
* El scope `email` (si quieres recuperar la dirección de correo electrónico del usuario)

Este ejemplo muestra una función que recupera el perfil del usuario y lo muestra en pantalla:

```kotlin lines
var client = AuthenticationAPIClient(account)

// Usa el token de acceso recibido para llamar a `userInfo` y obtener el perfil de Auth0.
client.userInfo(accessToken)
  .start(object : Callback<UserProfile, AuthenticationException> {
      override fun onFailure(exception: AuthenticationException) {
          // ¡Algo salió mal!
      }

      override fun onSuccess(profile: UserProfile) {
        // ¡Tenemos el perfil del usuario!
        val email = profile.email
        val name = profile.name
      }
})
```