---
title: Editar proveedor de SSO
sidebarTitle: Editar proveedores de SSO
description: "Interfaz con pestañas para editar proveedores de SSO: configuración de SSO, aprovisionamiento (SCIM) y gestión de dominios"
---

import Spinner from "/snippets/Spinner.jsx";
import { ComponentLoader } from "/snippets/ComponentLoader.jsx";

El componente `SsoProviderEdit` proporciona una interfaz unificada para editar proveedores de [inicio de sesión único](/docs/es/authenticate/enterprise-connections/self-service-enterprise-configuration).

<ComponentLoader componentSelector="sso-provider-edit" componentPreviewText="Vista previa del componente SsoProviderEdit" />

<Tabs>
  <Tab title="React">
    ## Requisitos de Setup

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      **Se requiere Auth0 Configuration** - Asegúrate de que tu tenant tenga configurada la
      My Organization API. [Ver guía de configuración
      →](/docs/es/get-started/universal-components/my-organization/build-delegated-admin#enable-the-my-organization-api)
    </Callout>

    ## Instalación

    <CodeGroup>
      ```bash pnpm
      pnpm add @auth0/universal-components-react
      ```

      ```bash npm
      npm install @auth0/universal-components-react
      ```
    </CodeGroup>

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      Ejecutar cualquiera de los dos comandos también instala la dependencia @auth0/universal-components-core
      para utilidades compartidas y la integración con Auth0.
    </Callout>

    ## Primeros pasos

    ```tsx
    import { SsoProviderEdit } from "@auth0/universal-components-react";
    import { useNavigate, useParams } from "react-router-dom";

    export function EditProviderPage() {
      const { providerId } = useParams();
      const navigate = useNavigate();

      return (
        <SsoProviderEdit
          providerId={providerId}
          backButton={{
            onClick: () => navigate("/providers"),
          }}
          sso={{
            updateAction: {
              onAfter: () => console.log("Provider updated"),
            },
          }}
        />
      );
    }
    ```

    <Accordion title="Ejemplo completo de integración">
      ```tsx
      import React from "react";
      import { SsoProviderEdit } from "@auth0/universal-components-react";
      import { Auth0Provider } from "@auth0/auth0-react";
      import { Auth0ComponentProvider } from "@auth0/universal-components-react/spa";
      import { useNavigate, useParams } from "react-router-dom";
      import { analytics } from "./lib/analytics";

      function EditProviderPage() {
        const { providerId } = useParams();
        const navigate = useNavigate();

        return (
          <div className="max-w-4xl mx-auto p-6">
            <SsoProviderEdit
              providerId={providerId}
              backButton={{
                onClick: () => navigate("/providers"),
              }}
              sso={{
                updateAction: {
                  onAfter: (provider) => {
                    analytics.track("Provider Updated", { name: provider.name });
                    toast.success("Provider configuration saved");
                  },
                },
                deleteAction: {
                  onBefore: (provider) => {
                    return confirm(
                      `Delete "${provider.display_name}"? This cannot be undone.`,
                    );
                  },
                  onAfter: () => {
                    toast.success("Provider deleted");
                    navigate("/providers");
                  },
                },
              }}
              provisioning={{
                createAction: {
                  onAfter: () => toast.success("SCIM provisioning enabled"),
                },
                createScimTokenAction: {
                  onAfter: () =>
                    toast.info("Copy your SCIM token - it won't be shown again"),
                },
              }}
              domains={{
                verifyAction: {
                  onAfter: (domain) => {
                    toast.success(`${domain.domain} verified`);
                  },
                },
                deleteAction: {
                  onBefore: (domain) => confirm(`Delete ${domain.domain}?`),
                },
              }}
              customMessages={{
                tabs: {
                  sso: "Configuration",
                  provisioning: "User Sync (SCIM)",
                  domains: "Linked Domains",
                },
              }}
              styling={{
                variables: {
                  common: { "--color-primary": "#059669" },
                },
              }}
            />
          </div>
        );
      }

      export default function App() {
        const domain = "your-domain.auth0.com";

        return (
          <Auth0Provider
            domain={domain}
            clientId="your-client-id"
            authorizationParams={{
              redirect_uri: window.location.origin,
            }}
            interactiveErrorHandler="popup" // Necesario para gestionar los desafíos de autenticación step-up mediante el popup de Universal Login
          >
            <Auth0ComponentProvider domain={domain}>
              <EditProviderPage />
            </Auth0ComponentProvider>
          </Auth0Provider>
        );
      }

      ```
    </Accordion>

    ## Props

    ### Props obligatorias

    Las props requeridas son fundamentales para el funcionamiento del componente. `SsoProviderEdit` requiere el ID del proveedor para cargar y editar el proveedor correcto.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>providerId</code>
          </td>

          <td>
            <code>string</code>
          </td>

          <td>
            <strong>Obligatorio.</strong> El ID del Provider de SSO que se editará.
          </td>
        </tr>
      </tbody>
    </table>

    ***

    ### Props de visualización

    Las props de visualización controlan cómo se renderiza el componente sin afectar su comportamiento.

    <table class="table">
      <thead>
        <tr>
          <th>Prop</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>hideHeader</code>
          </td>

          <td>
            <code>boolean</code>
          </td>

          <td>
            Oculta la sección del encabezado. Valor predeterminado: <code>false</code>
          </td>
        </tr>
      </tbody>
    </table>

    ***

    ### Props de Action

    Las interacciones del usuario en las tres pestañas se gestionan mediante varias props de acción organizadas bajo el nombre de la pestaña correspondiente (`sso`, `provisioning` y `domains`).

    <table class="table">
      <thead>
        <tr>
          <th>Prop</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>backButton</code>
          </td>

          <td>
            <code>Object</code>
          </td>

          <td>
            Configuración del botón de retroceso.
          </td>
        </tr>

        <tr>
          <td>
            <code>sso</code>
          </td>

          <td>
            <code>SsoProviderTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña SSO. <a href="#sso-tab-actions">Detalles</a>
          </td>
        </tr>

        <tr>
          <td>
            <code>provisioning</code>
          </td>

          <td>
            <code>SsoProvisioningTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña de aprovisionamiento.
            <a href="#provisioning-tab-actions">Detalles</a>
          </td>
        </tr>

        <tr>
          <td>
            <code>domains</code>
          </td>

          <td>
            <code>SsoDomainsTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña Dominios. <a href="#domains-tab-actions">Detalles</a>
          </td>
        </tr>
      </tbody>
    </table>

    **backButton**

    **Type:** `{ icon?: LucideIcon; onClick: (e: MouseEvent) => void }`

    Configura el botón de retroceso en el encabezado del componente. Úsalo para volver a tu lista de proveedores.

    **Propiedades:**

    * `icon` - Componente de icono de Lucide personalizado (opcional; por defecto, ArrowLeft)
    * `onClick` - Controlador de clic para la navegación

    **Ejemplo:**

    ```tsx
    import { ChevronLeft } from "lucide-react";

    <SsoProviderEdit
      providerId={providerId}
      backButton={{
        icon: ChevronLeft,
        onClick: () => navigate("/providers"),
      }}
    />;
    ```

    ***

    ### Actions de la pestaña SSO

    La propiedad `sso` configura Actions para la pestaña de configuración de SSO. Esta pestaña administra la configuración de autenticación del proveedor.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>updateAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider, IdentityProvider&gt;</code>
          </td>

          <td>
            Actualiza la configuración del proveedor.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Elimina el proveedor de forma permanente.
          </td>
        </tr>

        <tr>
          <td>
            <code>removeFromOrganizationAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Quita de la organización.
          </td>
        </tr>
      </tbody>
    </table>

    **sso.updateAction**

    **Type:** `ComponentAction<IdentityProvider, IdentityProvider>`

    Controla el guardado de cambios en la configuración del proveedor (ID de cliente, secrets, certificados, etc.).

    **Propiedades:**

    * `disabled` - Desactiva el botón Guardar
    * `onBefore(provider)` - Se ejecuta antes de la actualización. Devuelve `false` para impedir que se guarde (por ejemplo, para mostrar un cuadro de diálogo de confirmación).
    * `onAfter(provider, result)` - Se ejecuta después de que el proveedor se actualice correctamente. Úsalo para mostrar una notificación o hacer seguimiento del evento.

    **Ejemplo:**

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      sso={{
        updateAction: {
          onBefore: (provider) => {
            return confirm("Save changes to provider configuration?");
          },
          onAfter: (provider) => {
            toast.success("Provider configuration saved");
            analytics.track("Provider Updated", { name: provider.name });
          },
        },
      }}
    />
    ```

    ***

    **sso.deleteAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Controla la eliminación permanente del proveedor de tu tenant de Auth0.

    **Ejemplo:**

    ```tsx
    sso={{
      deleteAction: {
        onBefore: (provider) => {
          return confirm(`Permanently delete "${provider.display_name}"? This cannot be undone.`);
        },
        onAfter: () => navigate("/providers"),
      },
    }}
    ```

    ***

    **sso.removeFromOrganizationAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Controla la eliminación del proveedor de la organización sin eliminarlo. El proveedor seguirá disponible para volver a añadirse más adelante.

    **Ejemplo:**

    ```tsx
    sso={{
      removeFromOrganizationAction: {
        onBefore: (provider) => {
          return confirm(`Remove "${provider.display_name}" from this organization?`);
        },
        onAfter: () => navigate("/providers"),
      },
    }}
    ```

    ***

    ### Actions de la pestaña de aprovisionamiento

    La prop `provisioning` configura Actions para la pestaña de aprovisionamiento SCIM. Esta pestaña gestiona el aprovisionamiento automatizado de usuarios a través del protocolo SCIM.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>createAction</code>
          </td>

          <td>
            <code>
              ComponentAction&lt;IdentityProvider,
              CreateIdPProvisioningConfigResponseContent&gt;
            </code>
          </td>

          <td>
            Habilita el aprovisionamiento SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Deshabilita el aprovisionamiento SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>createScimTokenAction</code>
          </td>

          <td>
            <code>
              ComponentAction&lt;IdentityProvider,
              CreateIdpProvisioningScimTokenResponseContent&gt;
            </code>
          </td>

          <td>
            Genera el token SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteScimTokenAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Revoca el token SCIM.
          </td>
        </tr>
      </tbody>
    </table>

    **provisioning.createAction**

    **Type:** `ComponentAction<IdentityProvider, CreateIdPProvisioningConfigResponseContent>`

    Habilita el aprovisionamiento SCIM para el proveedor. Una vez habilitado, puedes generar un token SCIM para que lo utilice tu proveedor de identidad.

    **Ejemplo:**

    ```tsx
    provisioning={{
      createAction: {
        onBefore: (provider) => {
          return confirm("Enable SCIM provisioning for this provider?");
        },
        onAfter: (provider, config) => {
          toast.success("SCIM provisioning enabled");
          console.log("SCIM endpoint:", config.scim_endpoint);
        },
      },
    }}
    ```

    ***

    **provisioning.deleteAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Deshabilita el aprovisionamiento SCIM y elimina toda la configuración de aprovisionamiento para ese proveedor de identidad.

    **Ejemplo:**

    ```tsx
    provisioning={{
      deleteAction: {
        onBefore: (provider) => {
          return confirm("Disable SCIM provisioning? This will stop automatic user sync.");
        },
        onAfter: () => toast.success("SCIM provisioning disabled"),
      },
    }}
    ```

    ***

    **provisioning.createScimTokenAction**

    **Type:** `ComponentAction<IdentityProvider, CreateIdpProvisioningScimTokenResponseContent>`

    Genera un nuevo bearer token de SCIM para que tu proveedor de identidad se autentique con Auth0.

    **Ejemplo:**

    ```tsx
    provisioning={{
      createScimTokenAction: {
        onAfter: (provider, tokenResponse) => {
          // El token se muestra una sola vez: el usuario debe copiarlo de inmediato
          console.log("New SCIM token generated");
        },
      },
    }}
    ```

    ***

    **provisioning.deleteScimTokenAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Revoca el token SCIM. El proveedor de identidad no podrá sincronizar usuarios hasta que se genere un nuevo token.

    **Ejemplo:**

    ```tsx
    provisioning={{
      deleteScimTokenAction: {
        onBefore: () => {
          return confirm("Revoke SCIM token? Your identity provider will lose access immediately.");
        },
      },
    }}
    ```

    ***

    ### Actions de la pestaña Dominios

    La propiedad `domains` configura Actions para la pestaña de dominios. Esta pestaña gestiona los dominios asociados al proveedor para el enrutamiento automático de usuarios.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>createAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Añadir un dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>verifyAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Verificar la titularidad del dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Eliminar un dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>associateToProviderAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain, IdentityProvider | null&gt;</code>
          </td>

          <td>
            Asociar el dominio al proveedor.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteFromProviderAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain, IdentityProvider | null&gt;</code>
          </td>

          <td>
            Desasociar el dominio del proveedor.
          </td>
        </tr>
      </tbody>
    </table>

    **domains.createAction**

    **Type:** `ComponentAction<Domain>`

    Controla la adición de nuevos dominios a la organización desde la interfaz de edición del proveedor.

    **Ejemplo:**

    ```tsx
    domains={{
      createAction: {
        onAfter: (domain) => {
          toast.success(`Domain ${domain.domain} added`);
        },
      },
    }}
    ```

    ***

    **domains.verifyAction**

    **Type:** `ComponentAction<Domain>`

    Controla la verificación de dominio mediante un registro TXT de DNS.

    **Ejemplo:**

    ```tsx
    domains={{
      verifyAction: {
        onBefore: (domain) => {
          return confirm(`Verify ${domain.domain}? Ensure your DNS record is configured.`);
        },
        onAfter: (domain) => {
          toast.success(`${domain.domain} verified successfully`);
        },
      },
    }}
    ```

    ***

    **domains.deleteAction**

    **Type:** `ComponentAction<Domain>`

    Controla la eliminación del dominio.

    **Ejemplo:**

    ```tsx
    domains={{
      deleteAction: {
        onBefore: (domain) => {
          return confirm(`Delete ${domain.domain}?`);
        },
      },
    }}
    ```

    ***

    **domains.associateToProviderAction**

    **Type:** `ComponentAction<Domain, IdentityProvider | null>`

    Asocia un dominio verificado con este proveedor de SSO para el enrutamiento automático de usuarios.

    **Ejemplo:**

    ```tsx
    domains={{
      associateToProviderAction: {
        onAfter: (domain, provider) => {
          console.log(`${domain.domain} now routes to ${provider?.name}`);
        },
      },
    }}
    ```

    ***

    **domains.deleteFromProviderAction**

    **Type:** `ComponentAction<Domain, IdentityProvider | null>`

    Elimina la asociación de un dominio con este provider.

    ***

    ### Props de personalización

    Las props de personalización te permiten adaptar el componente a tu marca, locale y requisitos de validación sin modificar el código fuente.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>schema</code>
          </td>

          <td>
            <code>SsoProviderEditSchema</code>
          </td>

          <td>
            Reglas de validación de campos.
          </td>
        </tr>

        <tr>
          <td>
            <code>customMessages</code>
          </td>

          <td>
            <code>Partial&lt;SsoProviderEditMessages&gt;</code>
          </td>

          <td>
            Sobrescrituras de texto para i18n.
          </td>
        </tr>

        <tr>
          <td>
            <code>styling</code>
          </td>

          <td>
            <code>ComponentStyling&lt;SsoProviderEditClasses&gt;</code>
          </td>

          <td>
            Variables CSS y sobrescrituras de clases.
          </td>
        </tr>
      </tbody>
    </table>

    **schema**

    Establece reglas de validación personalizadas para los campos de proveedor y dominio.

    <Accordion title="Campos disponibles del esquema">
      Todos los campos del esquema admiten: `regex`, `errorMessage`, `minLength`, `maxLength`, `required`

      **provider.*** - Campos de configuración de Provider según la strategy

      * Comunes: `name`, `displayName`
      * Campos específicos de la strategy (igual que `SsoProviderCreate`)

      **domains.create.domainUrl** - Validación de la URL del dominio
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      schema={{
        provider: {
          displayName: {
            required: true,
            maxLength: 100,
          },
        },
        domains: {
          create: {
            domainUrl: {
              regex: /^[a-z0-9.-]+\.[a-z]{2,}$/,
              errorMessage: "Enter a valid domain (example.com)",
            },
          },
        },
      }}
    />
    ```

    ***

    **customMessages**

    Personaliza todos los textos y traducciones. Todos los campos son opcionales y, si no se completan, se usarán los valores predeterminados.

    <Accordion title="Mensajes disponibles">
      **header** - Encabezado del componente

      * `title`, `back_button_text`

      **tabs** - Etiquetas de las pestañas

      * `sso`, `provisioning`, `domains`

      **sso&#95;tab** - Pestaña de configuración de SSO

      * `title`, `description`
      * `fields.*` - Etiquetas de los campos del formulario según la estrategia
      * `actions.save_button_text`, `actions.delete_button_text`
      * `delete_modal.*`, `remove_modal.*`

      **provisioning&#95;tab** - Pestaña de aprovisionamiento

      * `title`, `description`
      * `scim_endpoint.label`, `scim_token.label`
      * `actions.enable_button_text`, `actions.disable_button_text`
      * `actions.generate_token_button_text`, `actions.revoke_token_button_text`

      **domains&#95;tab** - Pestaña de dominios

      * `title`, `description`
      * Misma estructura que los mensajes de DomainTable

      **notifications** - Respuestas de la API

      * `provider_update_success`, `provider_delete_success`
      * `provisioning_enable_success`, `provisioning_disable_success`
      * `scim_token_create_success`, `scim_token_delete_success`
      * Notificaciones relacionadas con los dominios
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      customMessages={{
        header: {
          title: "Edit Provider",
        },
        tabs: {
          sso: "Settings",
          provisioning: "User Sync",
          domains: "Domains",
        },
        sso_tab: {
          actions: {
            save_button_text: "Save Changes",
          },
        },
        notifications: {
          provider_update_success: "Provider saved successfully!",
        },
      }}
    />
    ```

    ***

    **styling**

    Personaliza la apariencia con variables CSS y sobrescritura de clases. Compatible con estilos adaptados al tema.

    <Accordion title="Opciones de estilo disponibles">
      **Variables** - propiedades personalizadas de CSS

      * `common` - Se aplica a todos los temas
      * `light` - Solo para el tema claro
      * `dark` - Solo para el tema oscuro

      **Clases** - sobrescrituras de clases de componente

      * `SsoProviderEdit-header`
      * `SsoProviderEdit-tabs`
      * `SsoProviderEdit-ssoTab`
      * `SsoProviderEdit-provisioningTab`
      * `SsoProviderEdit-domainsTab`
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      styling={{
        variables: {
          common: {
            "--font-size-title": "1.5rem",
          },
          light: {
            "--color-primary": "#059669",
          },
        },
        classes: {
          "SsoProviderEdit-tabs": "border-b border-gray-200",
        },
      }}
    />
    ```

    ***

    ## Personalización avanzada

    El componente `SsoProviderEdit` está compuesto por subcomponentes y hooks más pequeños. Puedes importarlos individualmente para crear flujos de trabajo de edición de proveedores personalizados si usas shadcn.

    ### Subcomponentes disponibles

    Para casos de uso avanzados, puedes importar subcomponentes individuales para incrustar pestañas o secciones específicas en distintos contextos.

    <table class="table">
      <thead>
        <tr>
          <th>Componente</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>SsoProviderSsoTab</code>
          </td>

          <td>Formulario de configuración de SSO</td>
        </tr>

        <tr>
          <td>
            <code>SsoProviderProvisioningTab</code>
          </td>

          <td>Gestión del aprovisionamiento SCIM</td>
        </tr>

        <tr>
          <td>
            <code>SsoProviderDomainsTab</code>
          </td>

          <td>Gestión de asociaciones de dominios</td>
        </tr>

        <tr>
          <td>
            <code>ProviderConfigureFields</code>
          </td>

          <td>Campos dinámicos del formulario según la estrategia</td>
        </tr>

        <tr>
          <td>
            <code>ScimTokenDisplay</code>
          </td>

          <td>Visualización del token SCIM con función de copia</td>
        </tr>
      </tbody>
    </table>

    ### Hooks disponibles

    Estos hooks proporcionan la lógica subyacente sin ninguna interfaz de usuario. Úsalos para crear interfaces completamente personalizadas aprovechando la integración con la Auth0 API.

    <table class="table">
      <thead>
        <tr>
          <th>Hook</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>useSsoProviderEdit</code>
          </td>

          <td>Lógica para cargar y actualizar el Provider</td>
        </tr>

        <tr>
          <td>
            <code>useSsoProviderProvisioning</code>
          </td>

          <td>Gestión del aprovisionamiento SCIM</td>
        </tr>

        <tr>
          <td>
            <code>useSsoProviderDomains</code>
          </td>

          <td>Gestión de la asociación de dominios</td>
        </tr>
      </tbody>
    </table>
  </Tab>

  <Tab title="Next.js">
    ## Requisitos de Setup

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      **Se requiere configurar Auth0** - Asegúrate de que tu tenant tenga configurada
      My Organization API. [Consulta la guía de configuración
      →](/docs/es/get-started/universal-components/my-organization/build-delegated-admin#configure-auth0-dashboard)
    </Callout>

    ## Instalación

    <CodeGroup>
      ```bash npm (Recommended)
      npm install @auth0/universal-components-react
      ```

      ```bash pnpm
      pnpm add @auth0/universal-components-react
      ```
    </CodeGroup>

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      Ejecutar los comandos npm o pnpm instala la dependencia @auth0/universal-components-core para utilidades compartidas y la integración con Auth0.
    </Callout>

    ## Primeros pasos

    ```tsx page.tsx
    import { SsoProviderEdit } from "@auth0/universal-components-react";
    import { useRouter } from "next/navigation";

    export function EditProviderPage({ providerId }) {
      const router = useRouter();

      return (
        <SsoProviderEdit
          providerId={providerId}
          backButton={{
            onClick: () => router.push("/providers"),
          }}
          sso={{
            updateAction: {
              onAfter: () => console.log("Provider updated"),
            },
          }}
        />
      );
    }
    ```

    <Accordion title="Ejemplo completo de integración">
      ```tsx
      import React from "react";
      import { SsoProviderEdit } from "@auth0/universal-components-react";
      import { useRouter, useSearchParams } from "next/navigation";
      import { analytics } from "./lib/analytics";

      function EditProviderPage() {
        const router = useRouter();
        const searchParams = useSearchParams();
        const providerId = searchParams.get("id");

        return (
          <div className="max-w-4xl mx-auto p-6">
            <SsoProviderEdit
              providerId={providerId}
              backButton={{
                onClick: () => router.push("/providers"),
              }}
              sso={{
                updateAction: {
                  onAfter: (provider) => {
                    analytics.track("Provider Updated", { name: provider.name });
                    toast.success("Provider configuration saved");
                  },
                },
                deleteAction: {
                  onBefore: (provider) => {
                    return confirm(
                      `Delete "${provider.display_name}"? This cannot be undone.`,
                    );
                  },
                  onAfter: () => {
                    toast.success("Provider deleted");
                    router.push("/providers");
                  },
                },
              }}
              provisioning={{
                createAction: {
                  onAfter: () => toast.success("SCIM provisioning enabled"),
                },
                createScimTokenAction: {
                  onAfter: () =>
                    toast.info("Copy your SCIM token - it won't be shown again"),
                },
              }}
              domains={{
                verifyAction: {
                  onAfter: (domain) => {
                    toast.success(`${domain.domain} verified`);
                  },
                },
                deleteAction: {
                  onBefore: (domain) => confirm(`Delete ${domain.domain}?`),
                },
              }}
              customMessages={{
                tabs: {
                  sso: "Configuration",
                  provisioning: "User Sync (SCIM)",
                  domains: "Linked Domains",
                },
              }}
              styling={{
                variables: {
                  common: { "--color-primary": "#059669" },
                },
              }}
            />
          </div>
        );
      }

      export default EditProviderPage;
      ```
    </Accordion>

    ## Props

    ### Props principales

    Las props principales son fundamentales para el funcionamiento del componente. `SsoProviderEdit` requiere el ID del proveedor para cargar y editar el proveedor correcto.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>providerId</code>
          </td>

          <td>
            <code>string</code>
          </td>

          <td>
            <strong>Obligatorio.</strong> El ID del proveedor de SSO que se desea editar.
          </td>
        </tr>
      </tbody>
    </table>

    ***

    ### Props de visualización

    Las props de visualización controlan cómo se renderiza el componente sin afectar su comportamiento.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>hideHeader</code>
          </td>

          <td>
            <code>boolean</code>
          </td>

          <td>
            Oculta la sección del encabezado. Valor predeterminado: <code>false</code>
          </td>
        </tr>
      </tbody>
    </table>

    ***

    ### Props de Action

    Las interacciones del usuario en las tres pestañas se gestionan mediante varias props de acción organizadas bajo el nombre de la pestaña correspondiente (`sso`, `provisioning` y `domains`).

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>backButton</code>
          </td>

          <td>
            <code>Object</code>
          </td>

          <td>
            Configuración del botón de retroceso.
          </td>
        </tr>

        <tr>
          <td>
            <code>sso</code>
          </td>

          <td>
            <code>SsoProviderTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña SSO. <a href="#sso-tab-actions">Detalles</a>
          </td>
        </tr>

        <tr>
          <td>
            <code>provisioning</code>
          </td>

          <td>
            <code>SsoProvisioningTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña de aprovisionamiento.
            <a href="#provisioning-tab-actions">Detalles</a>
          </td>
        </tr>

        <tr>
          <td>
            <code>domains</code>
          </td>

          <td>
            <code>SsoDomainsTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña de dominios. <a href="#domains-tab-actions">Detalles</a>
          </td>
        </tr>
      </tbody>
    </table>

    **backButton**

    **Type:** `{ icon?: LucideIcon; onClick: (e: MouseEvent) => void }`

    Configura el botón de retroceso en el encabezado del componente. Úsalo para volver a tu lista de proveedores.

    **Propiedades:**

    * `icon` - Componente de icono personalizado de Lucide (opcional; por defecto, ArrowLeft)
    * `onClick` - controlador del clic para la navegación

    **Ejemplo:**

    ```tsx
    import { ChevronLeft } from "lucide-react";

    <SsoProviderEdit
      providerId={providerId}
      backButton={{
        icon: ChevronLeft,
        onClick: () => router.push("/providers"),
      }}
    />;
    ```

    ***

    ### Actions de la pestaña SSO

    La propiedad `sso` configura Actions para la pestaña de configuración de SSO. Esta pestaña administra la configuración de autenticación del proveedor.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>updateAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider, IdentityProvider&gt;</code>
          </td>

          <td>
            Actualizar la configuración del proveedor.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Eliminar el proveedor de forma permanente.
          </td>
        </tr>

        <tr>
          <td>
            <code>removeFromOrganizationAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Eliminar de la organización.
          </td>
        </tr>
      </tbody>
    </table>

    **sso.updateAction**

    **Type:** `ComponentAction<IdentityProvider, IdentityProvider>`

    Controla el guardado de cambios en la configuración del proveedor (ID de cliente, secrets, certificados, etc.).

    **Propiedades:**

    * `disabled` - Desactiva el botón Guardar
    * `onBefore(provider)` - Se ejecuta antes de la actualización. Devuelve `false` para impedir que se guarde (por ejemplo, para mostrar un cuadro de diálogo de confirmación).
    * `onAfter(provider, result)` - Se ejecuta después de que el proveedor se haya actualizado correctamente. Úsalo para mostrar una notificación o registrar el evento.

    **Ejemplo:**

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      sso={{
        updateAction: {
          onBefore: (provider) => {
            return confirm("Save changes to provider configuration?");
          },
          onAfter: (provider) => {
            toast.success("Provider configuration saved");
            analytics.track("Provider Updated", { name: provider.name });
          },
        },
      }}
    />
    ```

    ***

    **sso.deleteAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Controla la eliminación permanente del proveedor de tu tenant de Auth0.

    **Ejemplo:**

    ```tsx
    sso={{
      deleteAction: {
        onBefore: (provider) => {
          return confirm(`Permanently delete "${provider.display_name}"? This cannot be undone.`);
        },
        onAfter: () => router.push("/providers"),
      },
    }}
    ```

    ***

    **sso.removeFromOrganizationAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Controla la eliminación del proveedor de la organización sin eliminarlo. El proveedor seguirá disponible para volver a añadirse más adelante.

    **Ejemplo:**

    ```tsx
    sso={{
      removeFromOrganizationAction: {
        onBefore: (provider) => {
          return confirm(`Remove "${provider.display_name}" from this organization?`);
        },
        onAfter: () => router.push("/providers"),
      },
    }}
    ```

    ***

    ### Actions de la pestaña de aprovisionamiento

    La prop `provisioning` configura Actions para la pestaña de aprovisionamiento SCIM. Esta pestaña gestiona el aprovisionamiento automatizado de usuarios a través del protocolo SCIM.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>createAction</code>
          </td>

          <td>
            <code>
              ComponentAction&lt;IdentityProvider,
              CreateIdPProvisioningConfigResponseContent&gt;
            </code>
          </td>

          <td>
            Habilitar el aprovisionamiento SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Desactivar el aprovisionamiento SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>createScimTokenAction</code>
          </td>

          <td>
            <code>
              ComponentAction&lt;IdentityProvider,
              CreateIdpProvisioningScimTokenResponseContent&gt;
            </code>
          </td>

          <td>
            Generar el token SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteScimTokenAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Revocar el token SCIM.
          </td>
        </tr>
      </tbody>
    </table>

    **provisioning.createAction**

    **Type:** `ComponentAction<IdentityProvider, CreateIdPProvisioningConfigResponseContent>`

    Habilita el aprovisionamiento SCIM para el proveedor. Una vez habilitado, puedes generar un token SCIM para que lo utilice tu proveedor de identidad.

    **Ejemplo:**

    ```tsx
    provisioning={{
      createAction: {
        onBefore: (provider) => {
          return confirm("Enable SCIM provisioning for this provider?");
        },
        onAfter: (provider, config) => {
          toast.success("SCIM provisioning enabled");
          console.log("SCIM endpoint:", config.scim_endpoint);
        },
      },
    }}
    ```

    ***

    **provisioning.deleteAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Deshabilita el aprovisionamiento SCIM y elimina toda la configuración de aprovisionamiento para ese proveedor de identidad.

    **Ejemplo:**

    ```tsx
    provisioning={{
      deleteAction: {
        onBefore: (provider) => {
          return confirm("Disable SCIM provisioning? This will stop automatic user sync.");
        },
        onAfter: () => toast.success("SCIM provisioning disabled"),
      },
    }}
    ```

    ***

    **provisioning.createScimTokenAction**

    **Type:** `ComponentAction<IdentityProvider, CreateIdpProvisioningScimTokenResponseContent>`

    Genera un nuevo bearer token de SCIM para que tu proveedor de identidad se autentique con Auth0.

    **Ejemplo:**

    ```tsx
    provisioning={{
      createScimTokenAction: {
        onAfter: (provider, tokenResponse) => {
          // El token se muestra una sola vez: el usuario debe copiarlo de inmediato
          console.log("New SCIM token generated");
        },
      },
    }}
    ```

    ***

    **provisioning.deleteScimTokenAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Revoca el token SCIM. El proveedor de identidad no podrá sincronizar usuarios hasta que se genere un nuevo token.

    **Ejemplo:**

    ```tsx
    provisioning={{
      deleteScimTokenAction: {
        onBefore: () => {
          return confirm("Revoke SCIM token? Your identity provider will lose access immediately.");
        },
      },
    }}
    ```

    ***

    ### Actions de la pestaña Dominios

    La propiedad `domains` configura las Actions para la pestaña de dominios. Esta pestaña gestiona los dominios asociados al proveedor para el enrutamiento automático de usuarios.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>createAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Agregar un dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>verifyAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Verificar la titularidad del dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Eliminar un dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>associateToProviderAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain, IdentityProvider | null&gt;</code>
          </td>

          <td>
            Asociar el dominio al proveedor.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteFromProviderAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain, IdentityProvider | null&gt;</code>
          </td>

          <td>
            Eliminar el dominio del proveedor.
          </td>
        </tr>
      </tbody>
    </table>

    **domains.createAction**

    **Type:** `ComponentAction<Domain>`

    Controla la adición de nuevos dominios a la organización desde la interfaz de edición del proveedor.

    **Ejemplo:**

    ```tsx
    domains={{
      createAction: {
        onAfter: (domain) => {
          toast.success(`Domain ${domain.domain} added`);
        },
      },
    }}
    ```

    ***

    **domains.verifyAction**

    **Type:** `ComponentAction<Domain>`

    Controla la verificación de dominio mediante un registro TXT de DNS.

    **Ejemplo:**

    ```tsx
    domains={{
      verifyAction: {
        onBefore: (domain) => {
          return confirm(`Verify ${domain.domain}? Ensure your DNS record is configured.`);
        },
        onAfter: (domain) => {
          toast.success(`${domain.domain} verified successfully`);
        },
      },
    }}
    ```

    ***

    **domains.deleteAction**

    **Type:** `ComponentAction<Domain>`

    Controla la eliminación de dominios.

    **Ejemplo:**

    ```tsx
    domains={{
      deleteAction: {
        onBefore: (domain) => {
          return confirm(`Delete ${domain.domain}?`);
        },
      },
    }}
    ```

    ***

    **domains.associateToProviderAction**

    **Type:** `ComponentAction<Domain, IdentityProvider | null>`

    Asocia un dominio verificado con este proveedor de SSO para el enrutamiento automático de usuarios.

    **Ejemplo:**

    ```tsx
    domains{{
      associateToProviderAction: {
        onAfter: (domain, provider) => {
          console.log(`${domain.domain} now routes to ${provider?.name}`);
        },
      },
    }}
    ```

    ***

    **domains.deleteFromProviderAction**

    **Type:** `ComponentAction<Domain, IdentityProvider | null>`

    Elimina la asociación de un dominio con este proveedor.

    ***

    ### Props de personalización

    Las props de personalización te permiten adaptar el componente a tu marca, locale y requisitos de validación sin modificar el código fuente.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>schema</code>
          </td>

          <td>
            <code>SsoProviderEditSchema</code>
          </td>

          <td>
            Reglas de validación de campos.
          </td>
        </tr>

        <tr>
          <td>
            <code>customMessages</code>
          </td>

          <td>
            <code>Partial&lt;SsoProviderEditMessages&gt;</code>
          </td>

          <td>
            Sobrescrituras de texto para i18n.
          </td>
        </tr>

        <tr>
          <td>
            <code>styling</code>
          </td>

          <td>
            <code>ComponentStyling&lt;SsoProviderEditClasses&gt;</code>
          </td>

          <td>
            Variables CSS y sobrescrituras de clases.
          </td>
        </tr>
      </tbody>
    </table>

    **schema**

    Establece reglas de validación personalizadas para los campos de proveedor y dominio.

    <Accordion title="Campos disponibles del esquema">
      Todos los campos del schema admiten: `regex`, `errorMessage`, `minLength`, `maxLength`, `required`

      **provider.*** - Campos de configuración de Provider según la estrategia

      * Comunes: `name`, `displayName`
      * Campos específicos de la estrategia (igual que SsoProviderCreate)

      **domains.create.domainUrl** - Validación de la URL del dominio
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      schema={{
        provider: {
          displayName: {
            required: true,
            maxLength: 100,
          },
        },
        domains: {
          create: {
            domainUrl: {
              regex: /^[a-z0-9.-]+\.[a-z]{2,}$/,
              errorMessage: "Enter a valid domain (example.com)",
            },
          },
        },
      }}
    />
    ```

    ***

    **customMessages**

    Personaliza todos los textos y traducciones. Todos los campos son opcionales y, si no se completan, se usarán los valores predeterminados.

    <Accordion title="Mensajes disponibles">
      **header** - Encabezado del componente

      * `title`, `back_button_text`

      **tabs** - Etiquetas de las pestañas

      * `sso`, `provisioning`, `domains`

      **sso&#95;tab** - Pestaña de configuración de SSO

      * `title`, `description`
      * `fields.*` - Etiquetas de los campos del formulario por estrategia
      * `actions.save_button_text`, `actions.delete_button_text`
      * `delete_modal.*`, `remove_modal.*`

      **provisioning&#95;tab** - Pestaña de aprovisionamiento

      * `title`, `description`
      * `scim_endpoint.label`, `scim_token.label`
      * `actions.enable_button_text`, `actions.disable_button_text`
      * `actions.generate_token_button_text`, `actions.revoke_token_button_text`

      **domains&#95;tab** - Pestaña de dominios

      * `title`, `description`
      * La misma estructura que los mensajes de DomainTable

      **notifications** - Respuestas de la API

      * `provider_update_success`, `provider_delete_success`
      * `provisioning_enable_success`, `provisioning_disable_success`
      * `scim_token_create_success`, `scim_token_delete_success`
      * Notificaciones relacionadas con los dominios
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      customMessages={{
        header: {
          title: "Edit Provider",
        },
        tabs: {
          sso: "Settings",
          provisioning: "User Sync",
          domains: "Domains",
        },
        sso_tab: {
          actions: {
            save_button_text: "Save Changes",
          },
        },
        notifications: {
          provider_update_success: "Provider saved successfully!",
        },
      }}
    />
    ```

    ***

    **styling**

    Personaliza la apariencia con variables CSS y sobrescritura de clases. Compatible con estilos adaptados al tema.

    <Accordion title="Opciones de estilo disponibles">
      **Variables** - propiedades personalizadas de CSS

      * `common` - Se aplica a todos los temas
      * `light` - Solo para el tema claro
      * `dark` - Solo para el tema oscuro

      **Clases** - Sobrescrituras de las clases del componente

      * `SsoProviderEdit-header`
      * `SsoProviderEdit-tabs`
      * `SsoProviderEdit-ssoTab`
      * `SsoProviderEdit-provisioningTab`
      * `SsoProviderEdit-domainsTab`
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      styling={{
        variables: {
          common: {
            "--font-size-title": "1.5rem",
          },
          light: {
            "--color-primary": "#059669",
          },
        },
        classes: {
          "SsoProviderEdit-tabs": "border-b border-gray-200",
        },
      }}
    />
    ```

    ***

    ## Personalización avanzada

    El componente `SsoProviderEdit` está compuesto por subcomponentes y hooks más pequeños. Puedes importarlos individualmente para crear flujos de trabajo personalizados de edición de proveedores.

    ### Subcomponentes disponibles

    Para casos de uso avanzados, puedes importar subcomponentes individuales para incrustar pestañas o secciones específicas en distintos contextos.

    <table class="table">
      <thead>
        <tr>
          <th>Componente</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>SsoProviderSsoTab</code>
          </td>

          <td>Formulario de configuración de SSO</td>
        </tr>

        <tr>
          <td>
            <code>SsoProviderProvisioningTab</code>
          </td>

          <td>Gestión del aprovisionamiento SCIM</td>
        </tr>

        <tr>
          <td>
            <code>SsoProviderDomainsTab</code>
          </td>

          <td>Gestión de la asociación de dominios</td>
        </tr>

        <tr>
          <td>
            <code>ProviderConfigureFields</code>
          </td>

          <td>Campos dinámicos del formulario según la estrategia</td>
        </tr>

        <tr>
          <td>
            <code>ScimTokenDisplay</code>
          </td>

          <td>Visualización del token SCIM con función de copia</td>
        </tr>
      </tbody>
    </table>

    ### Hooks disponibles

    Estos hooks proporcionan la lógica subyacente sin ninguna UI. Úsalos para crear interfaces completamente personalizadas aprovechando la integración con la Auth0 API.

    <table class="table">
      <thead>
        <tr>
          <th>Hook</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>useSsoProviderEdit</code>
          </td>

          <td>Lógica de carga y actualización del Provider</td>
        </tr>

        <tr>
          <td>
            <code>useSsoProviderProvisioning</code>
          </td>

          <td>Gestión del aprovisionamiento SCIM</td>
        </tr>

        <tr>
          <td>
            <code>useSsoProviderDomains</code>
          </td>

          <td>Gestión de asociaciones de dominios</td>
        </tr>
      </tbody>
    </table>
  </Tab>

  <Tab title="shadcn">
    ## Requisitos de Setup

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      **Se requiere Auth0 Configuration** - Asegúrate de que tu tenant esté configurado con la
      My Organization API. [Ver la guía de configuración
      →](/docs/es/get-started/universal-components/my-organization/build-delegated-admin#configure-auth0-dashboard)
    </Callout>

    ## Instalación

    ```bash
    npx shadcn@latest add https://auth0-universal-components.vercel.app/r/my-organization/sso-provider-edit.json
    ```

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      Ejecutar el comando shadcn también instala la dependencia @auth0/universal-components-core para utilidades compartidas y la integración con Auth0.
    </Callout>

    ## Primeros pasos

    ```tsx
    import { SsoProviderEdit } from "@/components/auth0/my-organization/sso-provider-edit";

    export function EditProviderPage({ providerId }) {
      const navigate = useNavigate();

      return (
        <SsoProviderEdit
          providerId={providerId}
          backButton={{
            onClick: () => navigate("/providers"),
          }}
          sso={{
            updateAction: {
              onAfter: () => console.log("Provider updated"),
            },
          }}
        />
      );
    }
    ```

    <Accordion title="Ejemplo completo de integración">
      ```tsx
      import React from "react";
      import { SsoProviderEdit } from "@/components/auth0/my-organization/sso-provider-edit";
      import { Auth0Provider } from "@auth0/auth0-react";
      import { Auth0ComponentProvider } from "@auth0/universal-components-react/spa";
      import { useNavigate, useParams } from "react-router-dom";
      import { analytics } from "./lib/analytics";

      function EditProviderPage() {
        const { providerId } = useParams();
        const navigate = useNavigate();

        return (
          <div className="max-w-4xl mx-auto p-6">
            <SsoProviderEdit
              providerId={providerId}
              backButton={{
                onClick: () => navigate("/providers"),
              }}
              sso={{
                updateAction: {
                  onAfter: (provider) => {
                    analytics.track("Provider Updated", { name: provider.name });
                    toast.success("Provider configuration saved");
                  },
                },
                deleteAction: {
                  onBefore: (provider) => {
                    return confirm(
                      `Delete "${provider.display_name}"? This cannot be undone.`,
                    );
                  },
                  onAfter: () => {
                    toast.success("Provider deleted");
                    navigate("/providers");
                  },
                },
              }}
              provisioning={{
                createAction: {
                  onAfter: () => toast.success("SCIM provisioning enabled"),
                },
                createScimTokenAction: {
                  onAfter: () =>
                    toast.info("Copy your SCIM token - it won't be shown again"),
                },
              }}
              domains={{
                verifyAction: {
                  onAfter: (domain) => {
                    toast.success(`${domain.domain} verified`);
                  },
                },
                deleteAction: {
                  onBefore: (domain) => confirm(`Delete ${domain.domain}?`),
                },
              }}
              customMessages={{
                tabs: {
                  sso: "Configuration",
                  provisioning: "User Sync (SCIM)",
                  domains: "Linked Domains",
                },
              }}
              styling={{
                variables: {
                  common: { "--color-primary": "#059669" },
                },
              }}
            />
          </div>
        );
      }

      export default function App() {
        const domain = "your-domain.auth0.com";

        return (
          <Auth0Provider
            domain={domain}
            clientId="your-client-id"
            authorizationParams={{
              redirect_uri: window.location.origin,
            }}
            interactiveErrorHandler="popup" // Necesario para gestionar los desafíos de autenticación step-up a través del popup de Universal Login
          >
            <Auth0ComponentProvider domain={domain}>
              <EditProviderPage />
            </Auth0ComponentProvider>
          </Auth0Provider>
        );
      }

      ```
    </Accordion>

    ## Props

    ### Props principales

    Las props principales son fundamentales para el funcionamiento del componente. `SsoProviderEdit` requiere el ID del proveedor para cargar y editar el proveedor correcto.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>providerId</code>
          </td>

          <td>
            <code>string</code>
          </td>

          <td>
            <strong>Obligatorio.</strong> El ID del proveedor de SSO que se va a editar.
          </td>
        </tr>
      </tbody>
    </table>

    ***

    ### Props de visualización

    Las props de visualización controlan cómo se renderiza el componente sin afectar su comportamiento.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>hideHeader</code>
          </td>

          <td>
            <code>boolean</code>
          </td>

          <td>
            Oculta la sección del encabezado. Valor predeterminado: <code>false</code>
          </td>
        </tr>
      </tbody>
    </table>

    ***

    ### Props de Action

    Las interacciones del usuario en las tres pestañas se gestionan mediante varias props de acción organizadas bajo el nombre de la pestaña correspondiente (`sso`, `provisioning` y `domains`).

    <table class="table">
      <thead>
        <tr>
          <th>Prop</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>backButton</code>
          </td>

          <td>
            <code>Object</code>
          </td>

          <td>
            Configuración del botón Atrás.
          </td>
        </tr>

        <tr>
          <td>
            <code>sso</code>
          </td>

          <td>
            <code>SsoProviderTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña SSO. <a href="#sso-tab-actions">Detalles</a>
          </td>
        </tr>

        <tr>
          <td>
            <code>provisioning</code>
          </td>

          <td>
            <code>SsoProvisioningTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña de aprovisionamiento.
            <a href="#provisioning-tab-actions">Detalles</a>
          </td>
        </tr>

        <tr>
          <td>
            <code>domains</code>
          </td>

          <td>
            <code>SsoDomainsTabEditProps</code>
          </td>

          <td>
            Acciones de la pestaña de dominios. <a href="#domains-tab-actions">Detalles</a>
          </td>
        </tr>
      </tbody>
    </table>

    **backButton**

    **Type:** `{ icon?: LucideIcon; onClick: (e: MouseEvent) => void }`

    Configura el botón de retroceso en el encabezado del componente. Úsalo para volver a tu lista de proveedores.

    **Propiedades:**

    * `icon` - Componente de icono de Lucide personalizado (opcional; por defecto, ArrowLeft)
    * `onClick` - Controlador del clic para la navegación

    **Ejemplo:**

    ```tsx
    import { ChevronLeft } from "lucide-react";

    <SsoProviderEdit
      providerId={providerId}
      backButton={{
        icon: ChevronLeft,
        onClick: () => navigate("/providers"),
      }}
    />;
    ```

    ***

    ### Actions de la pestaña SSO

    La propiedad `sso` configura Actions para la pestaña de configuración de SSO. Esta pestaña administra la configuración de autenticación del proveedor.

    <table class="table">
      <thead>
        <tr>
          <th>Acción</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>updateAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider, IdentityProvider&gt;</code>
          </td>

          <td>
            Actualizar la configuración del proveedor.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Eliminar el proveedor de forma permanente.
          </td>
        </tr>

        <tr>
          <td>
            <code>removeFromOrganizationAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Quitar de la organización.
          </td>
        </tr>
      </tbody>
    </table>

    **sso.updateAction**

    **Type:** `ComponentAction<IdentityProvider, IdentityProvider>`

    Controla el guardado de cambios en la configuración del proveedor (ID de cliente, secrets, certificados, etc.).

    **Propiedades:**

    * `disabled` - Desactiva el botón Guardar
    * `onBefore(provider)` - Se ejecuta antes de la actualización. Devuelve `false` para impedir que se guarde (por ejemplo, para mostrar un cuadro de diálogo de confirmación).
    * `onAfter(provider, result)` - Se ejecuta después de que el Provider se haya actualizado correctamente. Úsalo para mostrar una notificación o registrar el evento.

    **Ejemplo:**

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      sso={{
        updateAction: {
          onBefore: (provider) => {
            return confirm("Save changes to provider configuration?");
          },
          onAfter: (provider) => {
            toast.success("Provider configuration saved");
            analytics.track("Provider Updated", { name: provider.name });
          },
        },
      }}
    />
    ```

    ***

    **sso.deleteAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Controla la eliminación permanente del proveedor de tu tenant de Auth0.

    **Ejemplo:**

    ```tsx
    sso={{
      deleteAction: {
        onBefore: (provider) => {
          return confirm(`Permanently delete "${provider.display_name}"? This cannot be undone.`);
        },
        onAfter: () => navigate("/providers"),
      },
    }}
    ```

    ***

    **sso.removeFromOrganizationAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Controla la eliminación del proveedor de la organización sin eliminarlo. El proveedor seguirá disponible para volver a añadirse más adelante.

    **Ejemplo:**

    ```tsx
    sso={{
      removeFromOrganizationAction: {
        onBefore: (provider) => {
          return confirm(`Remove "${provider.display_name}" from this organization?`);
        },
        onAfter: () => navigate("/providers"),
      },
    }}
    ```

    ***

    ### Actions de la pestaña de aprovisionamiento

    La prop `provisioning` configura Actions para la pestaña de aprovisionamiento SCIM. Esta pestaña gestiona el aprovisionamiento automatizado de usuarios a través del protocolo SCIM.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>createAction</code>
          </td>

          <td>
            <code>
              ComponentAction&lt;IdentityProvider,
              CreateIdPProvisioningConfigResponseContent&gt;
            </code>
          </td>

          <td>
            Habilitar el aprovisionamiento SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Deshabilitar el aprovisionamiento SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>createScimTokenAction</code>
          </td>

          <td>
            <code>
              ComponentAction&lt;IdentityProvider,
              CreateIdpProvisioningScimTokenResponseContent&gt;
            </code>
          </td>

          <td>
            Generar un token SCIM.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteScimTokenAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;IdentityProvider&gt;</code>
          </td>

          <td>
            Revocar el token SCIM.
          </td>
        </tr>
      </tbody>
    </table>

    **provisioning.createAction**

    **Type:** `ComponentAction<IdentityProvider, CreateIdPProvisioningConfigResponseContent>`

    Habilita el aprovisionamiento SCIM para el proveedor. Una vez habilitado, puedes generar un token SCIM para que lo utilice tu proveedor de identidad.

    **Ejemplo:**

    ```tsx
    provisioning={{
      createAction: {
        onBefore: (provider) => {
          return confirm("Enable SCIM provisioning for this provider?");
        },
        onAfter: (provider, config) => {
          toast.success("SCIM provisioning enabled");
          console.log("SCIM endpoint:", config.scim_endpoint);
        },
      },
    }}
    ```

    ***

    **provisioning.deleteAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Deshabilita el aprovisionamiento SCIM y elimina toda la configuración de aprovisionamiento para ese proveedor de identidad.

    **Ejemplo:**

    ```tsx
    provisioning={{
      deleteAction: {
        onBefore: (provider) => {
          return confirm("Disable SCIM provisioning? This will stop automatic user sync.");
        },
        onAfter: () => toast.success("SCIM provisioning disabled"),
      },
    }}
    ```

    ***

    **provisioning.createScimTokenAction**

    **Type:** `ComponentAction<IdentityProvider, CreateIdpProvisioningScimTokenResponseContent>`

    Genera un nuevo bearer token de SCIM para que tu proveedor de identidad se autentique con Auth0.

    **Ejemplo:**

    ```tsx
    provisioning={{
      createScimTokenAction: {
        onAfter: (provider, tokenResponse) => {
          // El token se muestra una sola vez: el usuario debe copiarlo de inmediato
          console.log("New SCIM token generated");
        },
      },
    }}
    ```

    ***

    **provisioning.deleteScimTokenAction**

    **Type:** `ComponentAction<IdentityProvider>`

    Revoca el token SCIM. El proveedor de identidad no podrá sincronizar usuarios hasta que se genere un nuevo token.

    **Ejemplo:**

    ```tsx
    provisioning={{
      deleteScimTokenAction: {
        onBefore: () => {
          return confirm("Revoke SCIM token? Your identity provider will lose access immediately.");
        },
      },
    }}
    ```

    ***

    ### Actions de la pestaña Dominios

    La propiedad `domains` configura las Actions para la pestaña de dominios. Esta pestaña gestiona los dominios asociados al proveedor para el enrutamiento automático de usuarios.

    <table class="table">
      <thead>
        <tr>
          <th>Action</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>createAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Añadir un dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>verifyAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Verificar la titularidad del dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain&gt;</code>
          </td>

          <td>
            Eliminar un dominio.
          </td>
        </tr>

        <tr>
          <td>
            <code>associateToProviderAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain, IdentityProvider | null&gt;</code>
          </td>

          <td>
            Asociar el dominio con el proveedor.
          </td>
        </tr>

        <tr>
          <td>
            <code>deleteFromProviderAction</code>
          </td>

          <td>
            <code>ComponentAction&lt;Domain, IdentityProvider | null&gt;</code>
          </td>

          <td>
            Quitar el dominio del proveedor.
          </td>
        </tr>
      </tbody>
    </table>

    **domains.createAction**

    **Type:** `ComponentAction<Domain>`

    Controla la adición de nuevos dominios a la organización desde la interfaz de edición del proveedor.

    **Ejemplo:**

    ```tsx
    domains={{
      createAction: {
        onAfter: (domain) => {
          toast.success(`Domain ${domain.domain} added`);
        },
      },
    }}
    ```

    ***

    **domains.verifyAction**

    **Type:** `ComponentAction<Domain>`

    Controla la verificación de dominio mediante un registro TXT de DNS.

    **Ejemplo:**

    ```tsx
    domains={{
      verifyAction: {
        onBefore: (domain) => {
          return confirm(`Verify ${domain.domain}? Ensure your DNS record is configured.`);
        },
        onAfter: (domain) => {
          toast.success(`${domain.domain} verified successfully`);
        },
      },
    }}
    ```

    ***

    **domains.deleteAction**

    **Type:** `ComponentAction<Domain>`

    Controla la eliminación del dominio.

    **Ejemplo:**

    ```tsx
    domains={{
      deleteAction: {
        onBefore: (domain) => {
          return confirm(`Delete ${domain.domain}?`);
        },
      },
    }}
    ```

    ***

    **domains.associateToProviderAction**

    **Type:** `ComponentAction<Domain, IdentityProvider | null>`

    Asocia un dominio verificado con este proveedor de SSO para el enrutamiento automático de usuarios.

    **Ejemplo:**

    ```tsx
    domains={{
      associateToProviderAction: {
        onAfter: (domain, provider) => {
          console.log(`${domain.domain} now routes to ${provider?.name}`);
        },
      },
    }}
    ```

    ***

    **domains.deleteFromProviderAction**

    **Type:** `ComponentAction<Domain, IdentityProvider | null>`

    Elimina la asociación de un dominio con este proveedor.

    ***

    ### Props de personalización

    Las props de personalización te permiten adaptar el componente a tu marca, locale y requisitos de validación sin modificar el código fuente.

    <table class="table">
      <thead>
        <tr>
          <th>Propiedad</th>
          <th>Tipo</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>schema</code>
          </td>

          <td>
            <code>SsoProviderEditSchema</code>
          </td>

          <td>
            Reglas de validación de campos.
          </td>
        </tr>

        <tr>
          <td>
            <code>customMessages</code>
          </td>

          <td>
            <code>Partial&lt;SsoProviderEditMessages&gt;</code>
          </td>

          <td>
            Sobrescrituras de texto para i18n.
          </td>
        </tr>

        <tr>
          <td>
            <code>styling</code>
          </td>

          <td>
            <code>ComponentStyling&lt;SsoProviderEditClasses&gt;</code>
          </td>

          <td>
            Variables CSS y sobrescrituras de clases.
          </td>
        </tr>
      </tbody>
    </table>

    **schema**

    Establece reglas de validación personalizadas para los campos de proveedor y dominio.

    <Accordion title="Campos de esquema disponibles">
      Todos los campos del `schema` admiten: `regex`, `errorMessage`, `minLength`, `maxLength`, `required`

      **provider.*** - Campos de configuración de Provider según la estrategia

      * Comunes: `name`, `displayName`
      * Campos específicos de cada estrategia (igual que `SsoProviderCreate`)

      **domains.create.domainUrl** - Validación de la URL del dominio
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      schema={{
        provider: {
          displayName: {
            required: true,
            maxLength: 100,
          },
        },
        domains: {
          create: {
            domainUrl: {
              regex: /^[a-z0-9.-]+\.[a-z]{2,}$/,
              errorMessage: "Enter a valid domain (example.com)",
            },
          },
        },
      }}
    />
    ```

    ***

    **customMessages**

    Personaliza todo el texto y las traducciones. Todos los campos son opcionales y, si no se completan, se usarán los valores predeterminados.

    <Accordion title="Mensajes disponibles">
      **header** - Encabezado del componente

      * `title`, `back_button_text`

      **tabs** - Etiquetas de las pestañas

      * `sso`, `provisioning`, `domains`

      **sso&#95;tab** - Pestaña de configuración de SSO

      * `title`, `description`
      * `fields.*` - Etiquetas de los campos del formulario por estrategia
      * `actions.save_button_text`, `actions.delete_button_text`
      * `delete_modal.*`, `remove_modal.*`

      **provisioning&#95;tab** - Pestaña de aprovisionamiento

      * `title`, `description`
      * `scim_endpoint.label`, `scim_token.label`
      * `actions.enable_button_text`, `actions.disable_button_text`
      * `actions.generate_token_button_text`, `actions.revoke_token_button_text`

      **domains&#95;tab** - Pestaña de dominios

      * `title`, `description`
      * La misma estructura que los mensajes de DomainTable

      **notifications** - Respuestas de la API

      * `provider_update_success`, `provider_delete_success`
      * `provisioning_enable_success`, `provisioning_disable_success`
      * `scim_token_create_success`, `scim_token_delete_success`
      * Notificaciones relacionadas con dominios
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      customMessages={{
        header: {
          title: "Edit Provider",
        },
        tabs: {
          sso: "Settings",
          provisioning: "User Sync",
          domains: "Domains",
        },
        sso_tab: {
          actions: {
            save_button_text: "Save Changes",
          },
        },
        notifications: {
          provider_update_success: "Provider saved successfully!",
        },
      }}
    />
    ```

    ***

    **styling**

    Personaliza la apariencia con variables CSS y sobrescritura de clases. Compatible con estilos adaptados al tema.

    <Accordion title="Opciones de estilo disponibles">
      **Variables** - propiedades personalizadas de CSS

      * `common` - Se aplica a todos los temas
      * `light` - Solo para el tema claro
      * `dark` - Solo para el tema oscuro

      **Clases** - sobrescrituras de las clases del componente

      * `SsoProviderEdit-header`
      * `SsoProviderEdit-tabs`
      * `SsoProviderEdit-ssoTab`
      * `SsoProviderEdit-provisioningTab`
      * `SsoProviderEdit-domainsTab`
    </Accordion>

    ```tsx
    <SsoProviderEdit
      providerId={providerId}
      styling={{
        variables: {
          common: {
            "--font-size-title": "1.5rem",
          },
          light: {
            "--color-primary": "#059669",
          },
        },
        classes: {
          "SsoProviderEdit-tabs": "border-b border-gray-200",
        },
      }}
    />
    ```

    ***

    ## Personalización avanzada

    El componente `SsoProviderEdit` está compuesto por subcomponentes y hooks más pequeños. Puedes importarlos individualmente para crear flujos de trabajo personalizados de gestión de proveedores SSO si usas shadcn.

    ### Subcomponentes disponibles

    Para casos de uso avanzados, puedes importar componentes de pestaña individuales para crear interfaces personalizadas de edición de proveedores.

    <table class="table">
      <thead>
        <tr>
          <th>Componente</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>SsoProviderEditTabs</code>
          </td>

          <td>Contenedor de pestañas y navegación</td>
        </tr>

        <tr>
          <td>
            <code>SsoTab</code>
          </td>

          <td>Pestaña de configuración de SSO</td>
        </tr>

        <tr>
          <td>
            <code>ProvisioningTab</code>
          </td>

          <td>Pestaña de aprovisionamiento de SCIM</td>
        </tr>

        <tr>
          <td>
            <code>DomainsTab</code>
          </td>

          <td>Pestaña de asociaciones de dominios</td>
        </tr>
      </tbody>
    </table>

    ### Hooks disponibles

    Estos hooks proporcionan la lógica subyacente sin ninguna interfaz de usuario. Úsalos para crear interfaces completamente personalizadas aprovechando la integración con la Auth0 API.

    <table class="table">
      <thead>
        <tr>
          <th>Hook</th>
          <th>Descripción</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            <code>useSsoProviderEdit</code>
          </td>

          <td>Lógica de obtención y edición del Provider</td>
        </tr>

        <tr>
          <td>
            <code>useSsoProviderEditLogic</code>
          </td>

          <td>Estado de las pestañas y controladores de acciones</td>
        </tr>
      </tbody>
    </table>
  </Tab>
</Tabs>