---
description: Aprende cómo funciona el flujo de autorización de dispositivos y por qué debes usarlo en dispositivos con limitación de entrada, como televisores inteligentes y consolas multimedia. Para su uso con aplicaciones nativas.
sidebarTitle: Información general
title: Flujo de autorización de dispositivos
---

En los dispositivos con limitación de entrada que se conectan a internet, en lugar de autenticar al usuario directamente, el dispositivo le pide que acceda a un enlace desde su computadora o smartphone y autorice el dispositivo. Así se evita una mala experiencia de usuario en dispositivos que no tienen una forma sencilla de introducir texto. Para ello, las aplicaciones del dispositivo usan el Device <Tooltip tip="Flujo de autorización: concesión de autorización (o workflow) especificada en el framework OAuth 2.0." cta="Ver glosario" href="/docs/es/glossary?term=Authorization+Flow">Flujo de autorización</Tooltip> (ratificado en [OAuth 2.0](https://tools.ietf.org/html/rfc8628)), en el que envían su <Tooltip tip="ID de cliente: valor de identificación asignado a tu recurso registrado por Auth0." cta="Ver glosario" href="/docs/es/glossary?term=Client+ID">ID de cliente</Tooltip> para iniciar el proceso de autorización y obtener un token.

<div id="how-it-works">
  ## Cómo funciona
</div>

El flujo de autorización de dispositivos consta de dos rutas distintas: una tiene lugar en el dispositivo que solicita la autorización y la otra, en un navegador. La ruta del flujo en el navegador, en la que un device code se vincula a la sesión del navegador, se produce en paralelo a una parte de la ruta del flujo del dispositivo.

<Frame>![Flows - Device Authorization - Diagrama de secuencia de autorización](/docs/images/cdy7uua7fh8z/1A6jpG3W1H6SC9ZK92NyKd/40af53209f90a7c392f621f329fb4424/auth-sequence-device-auth.png)</Frame>

<div id="device-flow">
  ### Flujo de dispositivo
</div>

1. El usuario inicia la aplicación en el dispositivo.
2. La aplicación del dispositivo solicita autorización al servidor de autorización de Auth0 usando su ID de cliente (endpoint `/oauth/device/code`).
3. El servidor de autorización de Auth0 responde con un `device_code`, `user_code`, `verification_uri`, `verification_uri_complete`, `expires_in` (tiempo de vida en segundos para `device_code` y `user_code`) e intervalo de consulta (`interval`).
4. La aplicación del dispositivo le pide al usuario que active el proceso con su computadora o smartphone. La aplicación puede hacerlo de las siguientes maneras:

   * pidiéndole al usuario que visite la `verification_uri` e introduzca el `user_code` después de mostrar estos valores en pantalla
   * pidiéndole al usuario que interactúe con un código QR o una URL abreviada con el código de usuario integrado, generada a partir de `verification_uri_complete`
   * navegando directamente a la página de verificación con el código de usuario integrado mediante `verification_uri_complete`, si se ejecuta de forma nativa en un dispositivo basado en navegador
5. La aplicación del dispositivo comienza a consultar periódicamente tu servidor de autorización de Auth0 para obtener un token de acceso (endpoint **/oauth/token**) con la frecuencia especificada por `interval`, contada desde que se recibe la respuesta a la última solicitud de consulta. La aplicación del dispositivo sigue consultando hasta que el usuario complete la ruta del flujo del navegador o el código de usuario caduque.
6. Cuando el usuario completa correctamente la ruta del flujo del navegador, tu servidor de autorización de Auth0 responde con un token de acceso (y, opcionalmente, un token de actualización). La aplicación del dispositivo debe entonces descartar su `device_code`, ya que caducará.
7. Tu aplicación del dispositivo puede usar el token de acceso para llamar a una API y acceder a información sobre el usuario.
8. La API responde con los datos solicitados.

<div id="browser-flow">
  ### Flujo en el navegador
</div>

1. El usuario visita la `verification_uri` en su equipo, introduce el `user_code` y confirma que el dispositivo que se está activando muestra el `user_code`. Si el usuario visita la `verification_uri_complete` por cualquier otro medio (por ejemplo, escaneando un código QR), solo será necesaria la confirmación del dispositivo.
2. Tu servidor de autorización de Auth0 redirige al usuario a los prompts de inicio de sesión y consentimiento, si es necesario.
3. El usuario se autentica mediante una de las opciones de inicio de sesión configuradas y puede ver una página de consentimiento en la que se le pide que autorice la app del dispositivo.
4. Tu app del dispositivo queda autorizada para acceder a la API.

<div id="how-to-implement-it">
  ## Cómo implementarlo
</div>

La forma más sencilla de implementar el flujo de autorización de dispositivos es seguir nuestro tutorial: [Llamar a la API usando flujo de autorización de dispositivos](/docs/es/get-started/authentication-and-authorization-flow/device-authorization-flow/call-your-api-using-the-device-authorization-flow).

<div id="force-device-reauthorization">
  ## Forzar la reautorización del dispositivo
</div>

Para obligar al usuario a reautorizar un dispositivo, debes revocar el <Tooltip tip="Token de actualización: token usado para obtener un token de acceso renovado sin obligar a los usuarios a iniciar sesión de nuevo." cta="Ver glosario" href="/docs/es/glossary?term=Refresh+Token">token de actualización</Tooltip> asignado al dispositivo. Para saber cómo hacerlo, lee [Desvincular dispositivos de los usuarios](/docs/es/manage-users/user-accounts/unlink-devices-from-users). Ten en cuenta que no se obligará al dispositivo a reautorizarse hasta que expire el <Tooltip tip="Token de acceso: credencial de autorización, en forma de una cadena opaca o JWT, usada para acceder a una API." cta="Ver glosario" href="/docs/es/glossary?term=Access+Token">token de acceso</Tooltip> actual y la aplicación intente usar el token de actualización revocado. Para obtener más información sobre los tokens de actualización, lee [Tokens de actualización](/docs/es/secure/tokens/refresh-tokens).

<div id="learn-more">
  ## Más información
</div>

* [Tokens](/docs/es/secure/tokens)
* [Prácticas recomendadas sobre tokens](/docs/es/secure/tokens/token-best-practices)
* [¿Qué flujo de OAuth 2.0 debería usar?](/docs/es/get-started/authentication-and-authorization-flow/which-oauth-2-0-flow-should-i-use)
* [Auth0 Actions](/docs/es/customize/actions)