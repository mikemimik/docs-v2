---
title: Configurar aplicaciones de terceros
description: "Crea y configura aplicaciones de terceros con el Auth0 Dashboard o la Management API."
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";
import {AuthCodeGroup} from "/snippets/AuthCodeGroup.jsx";

Cree aplicaciones de terceros que permitan a desarrolladores externos, partners o agentes de IA acceder a sus APIs con controles de seguridad reforzados.

<div id="prerequisites">
  ## Requisitos previos
</div>

Antes de crear una aplicación de terceros, asegúrate de tener:

* Un tenant de Auth0 con al menos una [API (servidor de recursos)](/docs/es/get-started/apis) configurada
* Al menos una [conexión](/docs/es/authenticate/identity-providers) promovida a [nivel de dominio](/docs/es/authenticate/identity-providers/promote-connections-to-domain-level) (para los flujos de cara al usuario)

<div id="create-a-third-party-application">
  ## Crear una aplicación de terceros
</div>

Crea una aplicación de terceros con Auth0 Dashboard o Management API.

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. Ve a **Applications &gt; Applications**.
    2. Selecciona **Create Application**.
    3. Ingresa un nombre para la aplicación y selecciona el tipo de aplicación:
       * Regular Web App para clientes confidenciales del lado del servidor
       * Single Page App para clientes públicos basados en navegador
       * Native para clientes públicos móviles o de escritorio
    4. Activa el interruptor **This application is owned by a third party**.
    5. Selecciona **Create**.

    <Frame>![Cuadro de diálogo de Dashboard Create Application con el interruptor de terceros habilitado](/docs/images/third-party-applications/create-application-dialog.png)</Frame>
  </Tab>

  <Tab title="Management API">
    Haz una solicitud `POST` al endpoint `/api/v2/clients` con el siguiente cuerpo de la solicitud:

    <AuthCodeGroup>
      ```bash cURL wrap lines
      curl --request POST \
        --url 'https://YOUR_DOMAIN/api/v2/clients' \
        --header 'Authorization: Bearer YOUR_MANAGEMENT_API_TOKEN' \
        --header 'Content-Type: application/json' \
        --data '{
          "name": "Partner Application",
          "is_first_party": false,
          "app_type": "regular_web",
          "callbacks": ["https://partner.example.com/callback"],
          "grant_types": ["authorization_code", "refresh_token"],
          "token_endpoint_auth_method": "client_secret_post"
        }'
      ```
    </AuthCodeGroup>

    | **Parameter**                | **Type** | **Description**                                                                                                                                                                                                                                                                                                                               |
    | ---------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | `name`                       | String   | Obligatorio. El nombre de la aplicación.                                                                                                                                                                                                                                                                                                      |
    | `is_first_party`             | Boolean  | Indica si la aplicación es de primera parte (`true`) o de terceros (`false`).                                                                                                                                                                                                                                                                 |
    | `app_type`                   | String   | El tipo de aplicación (por ejemplo, `regular_web`, `native`, `spa`, `non_interactive`).                                                                                                                                                                                                                                                       |
    | `callbacks`                  | Array    | Una lista de URL de devolución de llamada permitidas para la redirección después de la autenticación.                                                                                                                                                                                                                                         |
    | `grant_types`                | Array    | Una lista de tipos de concesión de OAuth2 que este cliente puede usar. Las aplicaciones de terceros admiten `authorization_code`, `refresh_token` y `client_credentials`. Para obtener más información, consulta [Security Controls for Third-Party Applications](/docs/es/get-started/applications/third-party-applications/security-controls). |
    | `token_endpoint_auth_method` | String   | El método de autenticación para el token endpoint (por ejemplo, `client_secret_post`).                                                                                                                                                                                                                                                        |

    Si la solicitud se realiza correctamente, Auth0 crea la aplicación con un prefijo `tpc_` en el ID de cliente, los tipos de concesión `authorization_code` y `refresh_token`, y establece `third_party_security_mode` en `strict`, lo que indica que los controles de seguridad reforzados están habilitados.

    ```json wrap lines
    {
      "client_id": "tpc_aBcDeFgHiJkLmNoPqRsTuV",
      "third_party_security_mode": "strict",
      "is_first_party": false,
      "name": "Partner Application",
      "grant_types": ["authorization_code", "refresh_token"],
      ...
    }
    ```

    | **Parameter**               | **Type** | **Description**                                                                                                                                      |
    | --------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
    | `client_id`                 | String   | El identificador único de la aplicación con un prefijo `tpc_` para indicar que es una aplicación de terceros creada con el modo estricto habilitado. |
    | `third_party_security_mode` | String   | Define el perfil de seguridad. Se establece en `strict` para aplicar controles de seguridad reforzados a las aplicaciones de terceros.               |
    | `is_first_party`            | Boolean  | Debe establecerse en `false` cuando se usa el modo de seguridad `strict` para identificar la aplicación como de terceros.                            |
    | `name`                      | String   | El nombre para mostrar de la aplicación.                                                                                                             |
    | `grant_types`               | Array    | Los tipos de concesión de OAuth2 que esta aplicación está autorizada a usar (por ejemplo, `authorization_code`).                                     |
  </Tab>
</Tabs>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  La propiedad `third_party_security_mode` se establece al crear la aplicación y no se puede cambiar. Para usar un modo de seguridad diferente, crea una aplicación nueva.
</Callout>

<div id="configure-api-access-policies">
  ## Configurar políticas de acceso a la API
</div>

Las aplicaciones de terceros requieren autorizaciones de cliente explícitas para acceder a las API. Puedes configurar las políticas de acceso a la API de las siguientes maneras:

* [Permisos por aplicación](#per-application-permissions): Aplica permisos granulares a cada aplicación de tu tenant.
* [Permisos predeterminados para todas las aplicaciones de terceros](#default-permissions-for-all-third-party-applications): Aplica permisos predeterminados a todas las aplicaciones de terceros de tu tenant.

Cuando existen tanto permisos por aplicación como permisos predeterminados para aplicaciones de terceros en la misma API, los permisos por aplicación tienen prioridad. Para obtener más información, consulta [Acceso de aplicaciones a las API: autorizaciones de cliente](/docs/es/get-started/applications/application-access-to-apis-client-grants).

<div id="per-application-permissions">
  ### Permisos por aplicación
</div>

Para otorgar a una aplicación de terceros específica un acceso más amplio o más limitado que el predeterminado, cree una autorización de cliente para el `client_id` de esa aplicación:

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. Vaya a **Applications &gt; APIs** y seleccione la API.
    2. Vaya a la pestaña **Settings**.
    3. Desplácese hasta **Application Access Policy** y establezca **User-Delegated Access** y **Client Access** en **autorización por aplicación**.
    4. Seleccione **Save**.

    <Frame>![Configuración de la API en el Dashboard para la política de acceso de la aplicación](/docs/images/third-party-applications/application_access_policy.png)</Frame>

    Al configurar permisos por aplicación, debe autorizar individualmente el acceso a la API para cada aplicación:

    1. Vaya a **Applications &gt; APIs** y seleccione la API.
    2. Vaya a la pestaña **Application Access**.
    3. Desplácese hasta la aplicación, seleccione **Edit** y luego **Grant Access** para **User-Delegated Access** y/o **Client Access**. Después, seleccione los permisos que quiera.
    4. Seleccione **Save**.

    <Frame>![Configuración de la API en el Dashboard para otorgar acceso a la API a una aplicación](/docs/images/third-party-applications/grant-api-access.png)</Frame>
  </Tab>

  <Tab title="Management API">
    <AuthCodeGroup>
      ```bash cURL wrap lines
      curl --request POST \
        --url 'https://YOUR_DOMAIN/api/v2/client-grants' \
        --header 'Authorization: Bearer YOUR_MANAGEMENT_API_TOKEN' \
        --header 'Content-Type: application/json' \
        --data '{
          "client_id": "tpc_aBcDeFgHiJkLmNoPqRsTuV",
          "audience": "https://api.example.com",
          "scope": ["read:items", "write:items", "delete:items"],
          "subject_type": "user"
        }'
      ```

      | **Parámetro**  | **Tipo** | **Descripción**                                                                                                                                                                                                                                                                                                      |
      | -------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
      | `client_id`    | String   | Especifica la aplicación para la que quiere crear la autorización de cliente.                                                                                                                                                                                                                                        |
      | `audience`     | String   | El identificador único (URI) de la API para la que se está creando la concesión.                                                                                                                                                                                                                                     |
      | `scope`        | Arreglo  | Una lista de permisos (scopes) permitidos como parte de esta concesión.                                                                                                                                                                                                                                              |
      | `subject_type` | String   | Define el tipo de acceso de la aplicación permitido para la API:<br /><ul><li>`user`: Se usa para el acceso delegado por el usuario, que corresponde a flows que generan un token asociado a un usuario final.</li><li>`client`: Se usa para el acceso máquina a máquina, como el Client Credentials Flow.</li></ul> |
    </AuthCodeGroup>
  </Tab>
</Tabs>

<div id="default-permissions-for-all-third-party-applications">
  ### Permisos predeterminados para todas las aplicaciones de terceros
</div>

Los permisos predeterminados definen un conjunto básico de API y scopes disponibles para todas las aplicaciones de terceros. Esto es necesario para el [registro dinámico de clientes](/docs/es/get-started/applications/dynamic-client-registration), ya que no puedes configurar el acceso de cada aplicación individualmente.

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. Ve a **Applications &gt; APIs** y selecciona la API.
    2. Ve a la pestaña **Settings**.
    3. Desplázate hasta **Default Permissions for Third Party Apps**.
    4. Selecciona **Authorized** para **User-Delegated Access** o **Client Access**.
    5. Selecciona los scopes que quieres conceder y, a continuación, selecciona **Save**.

    <Frame>![Configuración de la API en el Dashboard con permisos predeterminados para aplicaciones de terceros](/docs/images/third-party-applications/default-permissions-settings.png)</Frame>
  </Tab>

  <Tab title="Management API">
    Haz una solicitud `POST` al endpoint `/api/v2/client-grants` con el siguiente cuerpo de la solicitud:

    <AuthCodeGroup>
      ```bash cURL wrap lines
      curl --request POST \
        --url 'https://YOUR_DOMAIN/api/v2/client-grants' \
        --header 'Authorization: Bearer YOUR_MANAGEMENT_API_TOKEN' \
        --header 'Content-Type: application/json' \
        --data '{
          "default_for": "third_party_clients",
          "audience": "https://api.example.com",
          "scope": ["read:items", "write:items"],
          "subject_type": "user"
        }'
      ```
    </AuthCodeGroup>

    | **Parámetro**  | **Tipo** | **Descripción**                                                                                                                                                                                                                                                                                                      |
    | -------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | `default_for`  | String   | Especifica si esta concesión se aplica automáticamente a determinados tipos de aplicaciones. Configúralo como `third_party_clients` para garantizar que todas las aplicaciones de terceros tengan acceso a esta API de forma predeterminada.                                                                         |
    | `audience`     | String   | El identificador único (URI) de la API para la que se crea la concesión.                                                                                                                                                                                                                                             |
    | `scope`        | Arreglo  | Una lista de permisos (scopes) permitidos como parte de esta concesión.                                                                                                                                                                                                                                              |
    | `subject_type` | String   | Define el tipo de acceso de aplicación permitido para la API:<br /><ul><li>`user`: Se usa para el acceso delegado por el usuario, que corresponde a flujos que generan un token asociado con un usuario final.</li><li>`client`: Se usa para el acceso de máquina a máquina, como Client Credentials Flow.</li></ul> |
  </Tab>
</Tabs>

<div id="configure-connections">
  ## Configurar conexiones
</div>

Las aplicaciones de terceros solo pueden autenticar usuarios mediante conexiones a nivel de dominio. Una vez que una conexión se promueve al nivel de dominio, queda disponible para todas las aplicaciones de terceros del tenant.

Para promover una conexión:

1. Ve a **Auth0 Dashboard &gt; Autenticación** y selecciona el tipo de conexión (Database, Social, empresarial).
2. Selecciona la conexión que quieras usar con aplicaciones de terceros.
3. Activa el interruptor **Promover conexión al nivel de dominio**.

Para obtener más información, consulta [Promover conexiones al nivel de dominio](/docs/es/authenticate/identity-providers/promote-connections-to-domain-level).

<div id="configure-open-redirect-protection">
  ## Configurar la protección contra redirección abierta
</div>

Las aplicaciones de terceros con controles de seguridad reforzados tienen **Open Redirect Protection** activada de forma predeterminada. Cuando está activada, Auth0 no hace lo siguiente:

* Redirigir a la URL de devolución de llamada de la aplicación cuando se producen errores de autenticación
* Exponer `application.callback_domain` en las plantillas de correo electrónico

Desactiva **Open Redirect Protection** solo para aplicaciones de terceros cuyas URI de devolución de llamada configuradas sean de confianza. Para obtener más información, consulta [Redirect Protection](/docs/es/get-started/applications/third-party-applications/security-controls#redirect-protection).

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. Ve a **Applications &gt; Applications** y selecciona la aplicación de terceros.
    2. Ve a la pestaña **Settings** y desplázate hasta **Open Redirect Protection**.
    3. Activa o desactiva la opción.
    4. Selecciona **Save**.

    <Frame>![Interruptor de Open Redirect Protection en el Dashboard](/docs/images/third-party-applications/open-redirect-protection.png)</Frame>
  </Tab>

  <Tab title="Management API">
    Establece la propiedad `redirection_policy` al crear o actualizar la aplicación:

    ```bash
    curl --request PATCH \
      --url 'https://{yourDomain}/api/v2/clients/{CLIENT_ID}' \
      --header 'Authorization: Bearer {YOUR_MANAGEMENT_API_TOKEN}' \
      --header 'Content-Type: application/json' \
      --data '{
        "redirection_policy": "allow_always"
      }'
    ```

    Establécela en `open_redirect_protection` para activarla (valor predeterminado) o en `allow_always` para desactivarla.
  </Tab>
</Tabs>

<div id="dynamic-client-registration">
  ## Registro dinámico de clientes
</div>

Todos los [clientes registrados dinámicamente](/docs/es/get-started/applications/dynamic-client-registration) son aplicaciones de terceros. Antes de habilitar DCR, configura los [permisos predeterminados de la API](#default-permissions-for-all-third-party-applications) para que los clientes registrados dinámicamente puedan acceder a tus APIs.

<div id="learn-more">
  ## Más información
</div>

* [Aplicaciones de terceros](/docs/es/get-started/applications/third-party-applications)
* [Controles de seguridad para aplicaciones de terceros](/docs/es/get-started/applications/third-party-applications/security-controls)
* [Consentimiento del usuario y aplicaciones de terceros](/docs/es/get-started/applications/third-party-applications/user-consent-and-third-party-applications)
* [Registro dinámico de clientes](/docs/es/get-started/applications/dynamic-client-registration)
* [Promover conexiones al nivel de dominio](/docs/es/authenticate/identity-providers/promote-connections-to-domain-level)