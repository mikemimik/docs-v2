---
description: Describe cómo desactivar la rotación del token de actualización.
title: Desactivar la rotación del token de actualización
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

export const codeExample = `const auth0 = await createAuth0Client({
  domain: '{yourDomain}',
  client_id: '{yourClientId}',
  audience: '{yourApiIdentifier}',
  useRefreshTokens: false
});`;

Puedes deshabilitar la rotación del <Tooltip tip="token de actualización: token que se usa para obtener un nuevo Access Token sin que los usuarios tengan que volver a iniciar sesión." cta="Ver glosario" href="/docs/es/glossary?term=refresh+token">token de actualización</Tooltip> en cada aplicación desde el Dashboard o la <Tooltip tip="token de actualización: token que se usa para obtener un nuevo Access Token sin que los usuarios tengan que volver a iniciar sesión." cta="Ver glosario" href="/docs/es/glossary?term=Management+API">Management API</Tooltip>.

<div id="disable-with-the-dashboard">
  ## Deshabilitar con el Dashboard
</div>

1. En el [Auth0 Dashboard](https://manage.auth0.com/#), ve a **Applications &gt; Applications** y selecciona la aplicación que quieres configurar.
2. En la pestaña Settings, busca la sección de rotación del token de actualización y desactiva el toggle Allow Refresh Token Rotation.
3. Selecciona **Save Changes** al final de la pantalla.

<div id="disable-with-the-management-api">
  ## Deshabilitar con la Management API
</div>

1. Deshabilita la rotación del token de actualización para cada aplicación con la Management API:

   {/* codeblockOld.header.login.logInButton codeblockOld.header.login.configureSnippet */}

   <AuthCodeBlock children={codeExample} language="javascript" />

2. Configura los ajustes del token de actualización sin rotación de la siguiente manera:

   ```http lines
   PATCH /api/v2/clients/{client_id}
   {
      "refresh_token": {
        "rotation_type": "non-rotating",
        "expiration_type": "non-expiring"
      }
   }
   ```

<div id="learn-more">
  ## Más información
</div>

* [Rotación del token de actualización](/docs/es/secure/tokens/refresh-tokens/refresh-token-rotation)
* [Configurar la rotación del token de actualización](/docs/es/secure/tokens/refresh-tokens/configure-refresh-token-rotation)
* [Uso de la rotación del token de actualización](/docs/es/secure/tokens/refresh-tokens/use-refresh-token-rotation)