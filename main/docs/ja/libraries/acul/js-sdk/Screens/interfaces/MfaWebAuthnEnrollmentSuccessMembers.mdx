---
title: "MfaWebAuthnEnrollmentSuccessMembers"
---

MfaWebAuthnEnrollmentSuccessMembers

````ts Example
export interface MfaWebAuthnEnrollmentSuccessMembers extends BaseMembers {
  /**
   * 'mfa-webauthn-enrollment-success' 画面の特定のプロパティとデータへのアクセス。
   * 登録された認証器の `nickname` とその `webauthnType` を含みます。
   * @type {ScreenMembersOnMfaWebAuthnEnrollmentSuccess}
   */
  screen: ScreenMembersOnMfaWebAuthnEnrollmentSuccess;

  /**
   * WebAuthn 登録が成功した後、ユーザーが認証フローを続行できるようにします。
   * このメソッドは `/u/mfa-webauthn-enrollment-success` エンドポイントに `action: "default"` を指定して POST リクエストを送信します。
   *
   * @param {ContinueOptions} [payload] - 省略可能。リクエストに追加のパラメーターを含める必要がある場合の `CustomOptions` オブジェクト。
   * @returns {Promise<void>} 続行アクションが正常に送信されたときに解決される Promise。
   *                          成功した場合、ブラウザは通常フローの次のステップにリダイレクトされます。
   * @throws {Error} フォームの送信に失敗した場合（例：ネットワークエラー、無効な state）にエラーをスローします。
   *
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnEnrollmentSuccess のインスタンスと仮定します
   * try {
   *   await sdk.continue();
   *   // ユーザーは次のステップにリダイレクトされます。
   * } catch (error) {
   *   console.error("WebAuthn 登録後の続行に失敗しました:", error);
   *   // エラーを処理します。ページがエラーで再レンダリングされる場合は sdk.transaction.errors を確認してください。
   * }
   * ```
   */
  continue(payload?: ContinueOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnEnrollmentSuccess">ScreenMembersOnMfaWebAuthnEnrollmentSuccess</a></span>}>
  &#39;mfa-webauthn-enrollment-success&#39; 画面固有のプロパティとデータにアクセスできます。
  登録されている認証器の `nickname` と `webauthnType` が含まれます。
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="continue" type="Promise<void>">
  WebAuthn の登録が正常に完了した後、ユーザーが認証フローを続行できるようにします。
  このメソッドは、`action: "default"` を指定して `/u/mfa-webauthn-enrollment-success` エンドポイントに POST リクエストを送信します。

  continue アクションが正常に送信されると解決される Promise です。
  成功すると、通常、ブラウザーはフローの次のステップにリダイレクトされます。

  #### Throws

  フォームの送信に失敗した場合 (例: ネットワークの問題、無効な state) にエラーをスローします。

  ```typescript Example
  // 'sdk' は MfaWebAuthnEnrollmentSuccess のインスタンスであると仮定します
  try {
    await sdk.continue();
    // ユーザーは次のステップにリダイレクトされます。
  } catch (error) {
    console.error("Failed to continue after WebAuthn enrollment:", error);
    // エラーを処理します。ページがエラーとともに再度レンダリングされる場合は、sdk.transaction.errors を確認してください。
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/WebAuthnEnrollSuccessContinueOptions">WebAuthnEnrollSuccessContinueOptions</a></span>}>
      任意。リクエストとともに追加のパラメーターを送信する必要がある場合に指定する、`CustomOptions` 用のオブジェクトです。
    </ParamField>
  </Expandable>
</ParamField>