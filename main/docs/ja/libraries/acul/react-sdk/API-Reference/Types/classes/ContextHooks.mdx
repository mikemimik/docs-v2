---
title: "ContextHooks<T>"
---

Auth0 Universal Login のデータにアクセスするためのコンテキストフックを作成するファクトリークラスです。
これらのフックを使うと、認証フローやテナント設定のさまざまな情報にアクセスできます。

<div id="type-parameters">
  ## 型パラメータ
</div>

<div id="t">
  ### T
</div>

`T` *は* [`BaseMembers`](/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/BaseMembers) *を拡張します*

<div id="constructor">
  ### コンストラクター
</div>

<ParamField body="new ContextHooks<T>(instances)" type="ContextHooks<T>">
  <Expandable title="パラメータ">
    <ParamField body="instance" type="T" />
  </Expandable>
</ParamField>

<div id="methods">
  ## メソッド
</div>

<ParamField body="useUser()" type="T[&#x22;user&#x22;]">
  ユーザー情報とプロファイルデータにアクセスするためのフックです。

  プロファイル情報、属性、ユーザー固有のデータを含むユーザーオブジェクト

  ```jsx Example
  import { useUser } from '@auth0/auth0-acul-react/login-id';
  function UserProfile() {
    const user = useUser();
  }
  ```
</ParamField>

<ParamField body="useTenant()" type="T[&#x22;tenant&#x22;]">
  テナントの構成と設定にアクセスするためのフックです。

  ドメイン、リージョン、テナント固有の構成を含むテナントオブジェクト

  ```jsx Example
  import { useTenant } from '@auth0/auth0-acul-react/login-id';
  function TenantInfo() {
    const tenant = useTenant();
  }
  ```
</ParamField>

<ParamField body="useBranding()" type="T[&#x22;branding&#x22;]">
  ブランディングとテーマの構成にアクセスするためのフックです。

  色、ロゴ、フォント、外観のカスタマイズ設定を含むブランディングオブジェクト

  ```jsx Example
  import { useBranding } from '@auth0/auth0-acul-react/login-id';
  function CustomTheme() {
    const branding = useBranding();
  }
  ```
</ParamField>

<ParamField body="useClient()" type="T[&#x22;client&#x22;]">
  Auth0 application (クライアント) の構成にアクセスするためのフックです。

  アプリケーション設定、コールバック、クライアント固有のデータを含むクライアントオブジェクト

  ```jsx Example
  import { useClient } from '@auth0/auth0-acul-react/login-id';
  function AppInfo() {
    const client = useClient();
  }
  ```
</ParamField>

<ParamField body="useOrganization()" type="T[&#x22;organization&#x22;]">
  組織コンテキストと設定にアクセスするためのフックです。

  組織固有のデータ、メタデータ、構成を含む組織オブジェクト

  ```jsx Example
  import { useOrganization } from '@auth0/auth0-acul-react/login-id';
  function OrgSelector() {
    const organization = useOrganization();
    if (!organization) {
      return <p>組織コンテキストがありません</p>;
    }
  }
  ```
</ParamField>

<ParamField body="usePrompt()" type="T[&#x22;prompt&#x22;]">
  promptの構成とフロー設定にアクセスするためのフックです。

  フロー構成、画面設定、prompt固有のデータを含むPromptオブジェクト

  ```jsx Example
  import { usePrompt } from '@auth0/auth0-acul-react/login-id';
  function FlowInfo() {
    const prompt = usePrompt();
  }
  ```
</ParamField>

<ParamField body="useUntrustedData()" type="T[&#x22;untrustedData&#x22;]">
  URLパラメーターとフォーム送信からの信頼できないデータにアクセスするためのフックです。

  使用前に検証が必要な、信頼できないユーザー入力を含むオブジェクト

  ```jsx Example
  import { useUntrustedData } from '@auth0/auth0-acul-react/login-id';
  function PrefilledForm() {
    const untrustedData = useUntrustedData();
  }
  ```
</ParamField>

<ParamField body="useScreen()" type="T[&#x22;screen&#x22;]">
  現在の画面情報とメタデータにアクセスするためのフックです。

  現在の画面名、構成、画面固有のデータを含む画面オブジェクト

  ```jsx Example
  import { useScreen } from '@auth0/auth0-acul-react/login-id';
  function ScreenDebug() {
    const screen = useScreen();
  }
  ```
</ParamField>

<ParamField body="useTransaction()" type="T[&#x22;transaction&#x22;]">
  transaction stateと認証フローのデータにアクセスするためのフックです。

  フローの状態、sessionデータ、transaction固有の情報を含むTransactionオブジェクト

  ```jsx Example
  import { useTransaction } from '@auth0/auth0-acul-react/login-id';
  function TransactionInfo() {
    const transaction = useTransaction();
  }
  ```
</ParamField>