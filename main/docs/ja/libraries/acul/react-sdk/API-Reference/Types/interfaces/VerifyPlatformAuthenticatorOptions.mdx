---
title: "VerifyPlatformAuthenticatorOptions"
---

VerifyPlatformAuthenticatorOptions

```ts Example
export interface VerifyPlatformAuthenticatorOptions extends CustomOptions {
  /**
   * 省略可能。trueかつ`screen.showRememberDevice`がtrueの場合、ユーザーがこのデバイスを記憶することを選択したことを示します。通常30日間、以降のログイン時のMFAを回避します。
   * @type {boolean}
   */
  rememberDevice?: boolean;
}
```

<div id="indexable">
  ## 添字アクセス可能
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## プロパティ
</div>

<ParamField body="rememberDevice?" type="boolean">
  省略可能。true の場合、今後の MFA チャレンジでこのブラウザを記憶するよう試みます。
  `rememberBrowser` フォームフィールドに対応します。これは `screen.showRememberDevice` が true の場合にのみ適用されます。
</ParamField>