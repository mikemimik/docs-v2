---
title: "FlattenedTheme"
---

[`useAuth0Themes`](/docs/ja/libraries/acul/react-sdk/API-Reference/Hooks/useAuth0Themes) が返す構造化されたテーマオブジェクト。Auth0 Universal Login コンポーネントに適用されるすべての視覚スタイルに、フラット化されたトークンレベルでアクセスできます。

```ts Example
export interface FlattenedTheme {
  colors: Record<string, string>;
  fonts: Record<string, string | number | boolean | object>;
  borders: Record<string, string | number | boolean>;
  pageBackground: Record<string, string>;
  widget: Record<string, string | number>;
}
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="colors" type="Record<string, string>" required>
  ログインUI全体に適用されるカラートークンです。テキスト、背景、境界線、インタラクティブな状態の色が含まれます。
</ParamField>

<ParamField body="fonts" type="Record<string, string | number | boolean | object>" required>
  フォントファミリー、サイズ、太さ、行の高さなど、関連するテキストスタイルを制御するタイポグラフィトークンです。
</ParamField>

<ParamField body="borders" type="Record<string, string | number | boolean>" required>
  UI要素の角丸、幅、スタイルを定義するボーダートークンです。
</ParamField>

<ParamField body="pageBackground" type="Record<string, string>" required>
  ログインウィジェットを囲むページコンテナに適用される背景スタイルトークンです。
</ParamField>

<ParamField body="widget" type="Record<string, string | number>" required>
  余白、影、寸法など、ログインウィジェットのカード自体に適用されるスタイルトークンです。
</ParamField>