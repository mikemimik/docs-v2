---
title: "ResetPasswordMfaPhoneChallengeMembers"
---

ResetPasswordMfaPhoneChallengeMembers

````ts Example
export interface ResetPasswordMfaPhoneChallengeMembers extends BaseMembers {
  /**
   * クライアント固有の情報（例：クライアントID、名前）へのアクセス。
   * @type {ClientMembers}
   */
  client: ClientMembers;

  /**
   * organization固有の情報へのアクセス（該当する場合）。
   * @type {OrganizationMembers}
   */
  organization: OrganizationMembers;

  /**
   * プロンプトの詳細（例：プロンプト名）へのアクセス。
   * @type {PromptMembers}
   */
  prompt: PromptMembers;

  /**
   * 'reset-password-mfa-phone-challenge' 画面固有のプロパティおよびデータへのアクセス。
   * @type {ScreenMembersOnResetPasswordMfaPhoneChallenge}
   */
  screen: ScreenMembersOnResetPasswordMfaPhoneChallenge;

  /**
   * 現在のトランザクションの詳細（例：state、エラー）へのアクセス。
   * @type {TransactionMembers}
   */
  transaction: TransactionMembers;

  /**
   * 指定された方法（SMSまたは音声）でユーザーの電話に確認コードを送信します。
   * このアクションは、ユーザーが「SMSでコードを送信」または「音声通話でコードを送信」ボタンをクリックしたときに対応します。
   * Auth0バックエンドをトリガーしてコード配信プロセスを開始します。
   *
   * @param {ContinueOptions} payload - 配信タイプ（'sms' または 'voice'）およびCustom Dataを指定するオプション。
   * @returns {Promise<void>} アクションがサーバーに正常に送信されると解決されるPromise。
   * @throws {Error} 送信に失敗した場合（例：ネットワークエラー、invalid state）にエラーをスローします。
   * @example
   * ```typescript
   * import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';
   *
   * const screen = new ResetPasswordMfaPhoneChallenge();
   *
   * // SMSでコードを送信
   * await screen.continue({ type: 'sms' });
   *
   * // 音声通話でコードを送信
   * await screen.continue({ type: 'voice' });
   * ```
   */
  continue(payload: ContinueOptions): Promise<void>;

  /**
   * 別のMFA方式を選択できる画面にユーザーを誘導します。
   * このアクションは、ユーザーが「Try Another Method」ボタンをクリックしたときに対応します。
   *
   * @param {TryAnotherMethodOptions} payload - 現在のチャレンジの種類（`sms` または `voice`）およびCustom Dataを指定するオプション。`type` フィールドは、ユーザーがどのフローから離脱しようとしているかをバックエンドに通知するために必要です。
   * @returns {Promise<void>} アクションがサーバーに正常に送信されると解決されるPromise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';
   *
   * const screen = new ResetPasswordMfaPhoneChallenge();
   *
   * // ユーザーに最初にSMSオプションが表示された場合：
   * await screen.tryAnotherMethod({ type: 'sms' });
   *
   * // ユーザーに最初に音声オプションが表示された場合：
   * await screen.tryAnotherMethod({ type: 'voice' });
   * ```
   */
  tryAnotherMethod(payload: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>}>
  クライアント固有の情報 (例: client ID、名前) にアクセスできます。
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  該当する場合は、organization 固有の情報にアクセスできます。
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>}>
  プロンプト の詳細 (例: プロンプト 名) にアクセスできます。
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnResetPasswordMfaPhoneChallenge">ScreenMembersOnResetPasswordMfaPhoneChallenge</a></span>}>
  &#39;reset-password-mfa-phone-challenge&#39; 画面固有のプロパティとデータにアクセスできます。
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  現在のトランザクションの詳細 (例: state、エラー) にアクセスできます。
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="continue" type="Promise<void>">
  指定された方法 (SMS または音声通話) で、ユーザーの電話番号に確認コードを送信します。
  この操作は、ユーザーが「Send Code via SMS」または「Send Code via Voice Call」ボタンをクリックしたときに対応します。
  これにより、Auth0バックエンド でコード配信プロセスが開始されます。

  この操作がサーバーに正常に送信されると resolve される Promise。

  #### 例外

  送信に失敗した場合 (例: ネットワークの問題、invalid state) 、エラーをスローします。

  ```typescript Example
  import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';

  const screen = new ResetPasswordMfaPhoneChallenge();

  // SMS でコードを送信
  await screen.continue({ type: 'sms' });

  // 音声通話でコードを送信
  await screen.continue({ type: 'voice' });
  ```

  <Expandable title="パラメータ">
    <ParamField body="payload" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaPhoneChallengeContinueOptions">ResetPasswordMfaPhoneChallengeContinueOptions</a></span>}>
      配信タイプ (`sms` または `voice`) と任意の Custom Data を指定するオプション。

      **プロパティ**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        ユーザーが確認コードの受信方法として選択した配信方法。

        * `sms`: テキストメッセージでコードを送信します。
        * `voice`: 音声通話でコードを送信します。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  ユーザーを、別の MFA方式 を選択できる画面に移動させます。
  この操作は、ユーザーが「Try Another Method」ボタンをクリックしたときに対応します。

  この操作がサーバーに正常に送信されると resolve される Promise。

  #### 例外

  送信に失敗した場合、エラーをスローします。

  ```typescript Example
  import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';

  const screen = new ResetPasswordMfaPhoneChallenge();

  // ユーザーに最初に SMS オプションが表示されていた場合:
  await screen.tryAnotherMethod({ type: 'sms' });

  // ユーザーに最初に Voice オプションが表示されていた場合:
  await screen.tryAnotherMethod({ type: 'voice' });
  ```

  <Expandable title="パラメータ">
    <ParamField body="payload" type={<span><a href="/docs/ja/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions">ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions</a></span>}>
      現在のチャレンジの種類 (`sms` または `voice`) と任意の Custom Data を指定するオプションです。`type` フィールドは、ユーザーがどのフローから移動しようとしているかを backend に伝えるために必要です。

      **プロパティ**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        現在の画面で最初に表示された、または選択された配信方法。
        これは、API endpoint が `'pick-authenticator'` アクションを正しく処理するために必要です。

        * `sms`: 現在の画面が SMS challenge 用であることを示します。
        * `voice`: 現在の画面が Voice challenge 用であることを示します。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>