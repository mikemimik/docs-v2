---
description: データベース接続で Auth0.Android を使用する方法
title: Auth0.Android データベース認証
---

<Warning>
  2017 年 6 月 8 日以降、新規テナントではネイティブアプリケーションからユーザー名/メールアドレスとパスワードを使用する認証はデフォルトで無効になっています。代わりに Universal Login を使用した Web 認証を行うことを推奨します。それでも続行する場合は、まず Auth0 Dashboard でパスワードグラントタイプを有効にする必要があります。詳しくは [アプリケーションのグラントタイプ](/docs/ja/get-started/applications/application-grant-types) を参照してください。
</Warning>

<div id="log-in-with-a-database-connection">
  ## データベース接続でログインする
</div>

データベース接続でログインするには、ユーザーの**メールアドレス**、**パスワード**、および認証に使用する**接続**を指定して `login` を呼び出します。戻り値は `Credentials` オブジェクトです。

```kotlin lines
authentication
    .login("username@domain.com", "a secret password", "my-database-connection")
    .start(object: Callback<Credentials, AuthenticationException> {
        override fun onSuccess(payload: Credentials) {
            // ログインしました！
        }

        override fun onFailure(error: AuthenticationException) {
            // エラー！
        }
    })
```

デフォルトのスコープは `openid profile email` です。

<div id="sign-up-with-a-database-connection">
  ## データベース接続でサインアップする
</div>

データベース接続でサインアップするには、ユーザーのメールアドレス、パスワード、接続名を指定して `signUp` メソッドを呼び出します。

```kotlin lines
authentication
    .signUp("username@domain.com", "a secret password", "my-database-connection")
    .start(object: Callback<Credentials, AuthenticationException> {
        override fun onSuccess(result: Credentials) {
            // サインアップ & ログイン完了！
        }

        override fun onFailure(error: AuthenticationException) {
            // エラー！
        }
    });
```