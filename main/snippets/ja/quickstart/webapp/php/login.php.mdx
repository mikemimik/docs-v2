```php login.php lines
<?php

  declare(strict_types=1);

  /**
   * アプリケーションセッションを準備し、Auth0 Universal Login ページにリダイレクトします。
   *
   * 認証フローを完了するため、ユーザーはコールバックルートにリダイレクトされます。
   */

  header(sprintf('Location: %s', $sdk->login()));
```