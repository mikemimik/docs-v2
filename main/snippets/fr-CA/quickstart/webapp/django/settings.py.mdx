```py webappexample/settings.py lines 
import os
from pathlib import Path
from dotenv import load_dotenv, find_dotenv

# Construire les chemins dans le projet comme ceci : BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent
TEMPLATE_DIR = os.path.join(BASE_DIR, "webappexample", "templates")

# ... autres paramètres ...

TEMPLATES = [
    {
# Laisser les autres lignes telles quelles ; on met seulement à jour `DIRS`.
"DIRS": [TEMPLATE_DIR],
    },
]

# ... autres paramètres ...

# Charger le fichier de définition d'environnement
ENV_FILE = find_dotenv()
if ENV_FILE:
    load_dotenv(ENV_FILE)

# Charger les paramètres de l'application Auth0 en mémoire
AUTH0_DOMAIN = os.environ.get("AUTH0_DOMAIN")
AUTH0_CLIENT_ID = os.environ.get("AUTH0_CLIENT_ID")
AUTH0_CLIENT_SECRET = os.environ.get("AUTH0_CLIENT_SECRET")
```