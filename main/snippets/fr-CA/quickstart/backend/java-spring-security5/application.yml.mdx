```yml application.yml lines highlight={}
okta:
  oauth2:
    # Remplacez par le domaine de votre tenant Auth0.
    issuer: https://{yourDomain}/
    # Remplacez par l'identifiant d'API de votre Auth0 API.
    audience: {yourApiIdentifier}
```