```php logout.php lines
<?php

  declare(strict_types=1);

  /**
   * Efface la session de l'application et redirige vers le point de terminaison logout d'Auth0.
   *
   * L'utilisateur est ensuite redirigé vers la route d'index.
   */

  header(sprintf('Location: %s', $sdk->logout()));
```