```php login.php lines
<?php

  declare(strict_types=1);

  /**
   * Prépare la session de l'application et redirige vers la page Auth0 Universal Login.
   *
   * L'utilisateur est redirigé vers la route de rappel pour terminer le flux d'authentification.
   */

  header(sprintf('Location: %s', $sdk->login()));
```