```js server.js highlight={2-16} lines 
const express = require('express');
const { auth, requiresAuth } = require('express-openid-connect');
const app = express();

const config = {
  authRequired: false,
  auth0Logout: true,
  baseURL: 'http://localhost:3000',
  clientID: '{yourClientId}',
  issuerBaseURL: 'https://{yourDomain}',
  secret: 'LONG_RANDOM_STRING'
};

// Le Router `auth` ajoute les routes /login, /logout
// et /callback à baseURL
app.use(auth(config));

// req.oidc.isAuthenticated est fourni par le router auth
app.get('/', (req, res) => {
  res.send(
    req.oidc.isAuthenticated() ? 'Logged in' : 'Logged out'
  )
});

// La route /profile affiche le profil utilisateur au format JSON
app.get('/profile', requiresAuth(), (req, res) => {
  res.send(JSON.stringify(req.oidc.user, null, 2));
});

app.listen(3000, function() {
  console.log('Listening on http://localhost:3000');
});
```