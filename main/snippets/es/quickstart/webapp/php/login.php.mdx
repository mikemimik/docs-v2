```php login.php lines
<?php

  declare(strict_types=1);

  /**
   * Prepara la sesión de la aplicación y redirige a la página de Auth0 Universal Login.
   *
   * El usuario será redirigido a tu ruta de devolución de llamada para completar el flujo de autenticación.
   */

  header(sprintf('Location: %s', $sdk->login()));
```