```py webappexample/settings.py lines 
import os
from pathlib import Path
from dotenv import load_dotenv, find_dotenv

# Construya rutas dentro del proyecto de esta forma: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent
TEMPLATE_DIR = os.path.join(BASE_DIR, "webappexample", "templates")

# ... other settings ...

TEMPLATES = [
    {
# Deje las demás líneas como están y actualice solo `DIRS`.
"DIRS": [TEMPLATE_DIR],
    },
]

# ... other settings ...

# Cargar el archivo de variables de entorno
ENV_FILE = find_dotenv()
if ENV_FILE:
    load_dotenv(ENV_FILE)

# Cargar la configuración de la aplicación Auth0 en memoria
AUTH0_DOMAIN = os.environ.get("AUTH0_DOMAIN")
AUTH0_CLIENT_ID = os.environ.get("AUTH0_CLIENT_ID")
AUTH0_CLIENT_SECRET = os.environ.get("AUTH0_CLIENT_SECRET")
```