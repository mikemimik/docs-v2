```php login.php lines
<?php

  declare(strict_types=1);

  /**
   * Prepara la sesión de la aplicación y redirige al usuario a la página de Auth0 Universal Login.
   *
   * El usuario será redirigido a la ruta de callback para completar el flujo de autenticación.
   */

  header(sprintf('Location: %s', $sdk->login()));
```