---
description: Décrit la mise en œuvre de l’authentification sans mot de passe avec connexion intégrée dans les applications monopages (SPA).
title: Authentification sans mot de passe avec connexion intégrée pour les applications monopages (SPA)
---

import { ReleaseStageNotice } from "/snippets/ReleaseStageNotice.jsx"

<Warning>
  La connexion intégrée pour les applications Web utilise l’[authentification inter-origines](/fr-CA/docs/authenticate/login/cross-origin-authentication), sauf si vous [configurez un domaine personnalisé](/fr-CA/docs/customize/custom-domains) pour votre tenant. L’authentification inter-origines utilise des cookies tiers pour permettre des transactions d’authentification sécurisées entre différentes origines.
</Warning>

<div id="using-auth0s-sdks-to-implement-embedded-login">
  ## Utiliser les SDK d&#39;Auth0 pour implémenter la connexion intégrée
</div>

Vous pouvez implémenter le login <Tooltip tip="Passwordless : forme d’authentification qui n’utilise pas de mot de passe comme premier facteur." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=Passwordless">Passwordless</Tooltip> à l’aide du widget Lock d’Auth0 ou, si vous avez besoin d’un contrôle total sur l’expérience utilisateur, l’implémenter avec Auth0.js :

* [Lock for Web](/fr-CA/docs/libraries/lock)
* [Référence d’Auth0.js](/fr-CA/docs/libraries/auth0js)

<div id="configure-cross-origin-resource-sharing-cors">
  ### Configurer le partage de ressources entre origines (CORS)
</div>

Pour des raisons de sécurité, l’URL d’origine de votre application doit figurer parmi les URL autorisées. Si vous ne l’avez pas déjà ajoutée aux **Allowed Callback URLS** de votre application, vous devrez l’ajouter à la liste des **Allowed Origins (CORS)**.

1. Accédez à [Auth0 Dashboard &gt; Applications &gt; Applications](https://manage.auth0.com/#/applications), puis sélectionnez le nom de votre application pour en afficher les paramètres.
2. Repérez **Allowed Origins (CORS)**, entrez l’[URL d’origine](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Origin) de votre application, puis sélectionnez **Save Changes**.

<div id="customize-mfa">
  ## Personnaliser l’authentification multifacteur
</div>

<ReleaseStageNotice feature="Authentification multifacteur personnalisable avec les flux Resource Owner Password Grant, Embedded ou Refresh Token" stage="ea" terms="true" contact="Auth0 Support" />

Personnalisez la <Tooltip tip="MFA (authentification multifacteur) : processus d’authentification de l’utilisateur qui utilise un facteur supplémentaire, en plus du nom d’utilisateur et du mot de passe, comme un code envoyé par SMS." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=MFA">MFA</Tooltip> avec des flux intégrés. Utilisez la MFA API pour permettre aux utilisateurs d’inscrire les facteurs pris en charge par votre application de leur choix et de répondre aux demandes de vérification avec ceux-ci.

Lorsque vous utilisez [Lock for Web](/fr-CA/docs/libraries/lock#2-authenticating-and-getting-user-info), l’endpoint `oauth/token` renvoie l’erreur `mfa_required` et inclut le `mfa_token` dont vous avez besoin pour utiliser la MFA API, ainsi que le paramètre `mfa_requirements`, avec une liste des authentificateurs actuellement pris en charge par votre application :

```json lines
{
  "error": "mfa_required",
  "error_description": "Multifactor authentication required",
  "mfa_token": "Fe26...Ha",
  "mfa_requirements": {
    "challenge": [
      { "type": "otp" },
      { "type": "push-notification" },
      { "type": "phone" },
      { "type": "recovery-code" }
      { "type": "email"} //fonctionne uniquement avec challenge
    ]
  }
}
```

Utilisez le `mfa_token` pour appeler l’endpoint [`mfa/authenticator`](https://auth0.com/docs/api/authentication/muti-factor-authentication/list-authenticators) afin de lister tous les facteurs que l’utilisateur a configurés et de repérer le même `type` que celui pris en charge par votre application. Vous devez aussi obtenir l’`authenticator_type` correspondant pour envoyer des demandes de vérification :

```json lines
[
  {
    "type": "recovery-code",
    "id": "recovery-code|dev_qpOkGUOxBpw6R16t",
    "authenticator_type": "recovery-code",
    "active": true
  },
  {
    "type": "otp",
    "id": "totp|dev_6NWz8awwC8brh2dN",
    "authenticator_type": "otp",
    "active": true
  }
]
```

Déclenchez la vérification MFA en appelant le point de terminaison [`request/mfa/challenge`](https://auth0.com/docs/api/authentication/muti-factor-authentication/request-mfa-challenge).

Personnalisez davantage votre flux MFA avec Auth0 Actions. Pour en savoir plus, consultez [Actions Triggers : post-challenge - objet API](/fr-CA/docs/customize/actions/explore-triggers/password-reset-triggers/post-challenge-trigger/post-challenge-api-object).