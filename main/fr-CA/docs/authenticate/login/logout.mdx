---
title: Comment fonctionne déconnexion dans Auth0
sidebarTitle: déconnexion
description: Vous pouvez déconnecter un utilisateur de la session Auth0 et (au besoin) de la session IdP, puis rediriger les utilisateurs après la déconnexion.
---

<div id="logout-session-layers">
  ## Couches de session de déconnexion
</div>

Lorsque vous mettez en œuvre la fonctionnalité de déconnexion, il y a généralement trois couches de session dont vous devez tenir compte :

1. **Application Session Layer**: La première couche est la session dans votre application. Même si votre application utilise Auth0 pour authentifier les utilisateurs, vous devez quand même garder une trace du fait que l’utilisateur s’est connecté à votre application. Dans une application Web traditionnelle, cela se fait en stockant des données dans un cookie. [Déconnectez les utilisateurs de vos applications](/fr-CA/docs/authenticate/login/logout/log-users-out-of-applications) en mettant fin à leurs sessions. La gestion de la session de l’application doit se faire dans votre application.

2. **Auth0 Session Layer**: Auth0 maintient également une session pour l’utilisateur et stocke ses renseignements dans un cookie. La prochaine fois qu’un utilisateur sera redirigé vers l’écran Auth0 Lock, Auth0 se souviendra de lui. [Déconnectez les utilisateurs d’Auth0](/fr-CA/docs/authenticate/login/logout/log-users-out-of-auth0) en effaçant le cookie de Single Sign-On (SSO).

3. **Identity Provider Session Layer**: La dernière couche de session est celle du fournisseur d’identité (par exemple, Facebook ou Google). Lorsque des utilisateurs tentent de se connecter avec l’un de ces fournisseurs alors qu’ils y sont déjà connectés, ils n’auront pas à se reconnecter. Il se peut qu’on leur demande d’autoriser le partage de leurs renseignements avec Auth0 et, par conséquent, avec votre application. Il n’est pas nécessaire de déconnecter les utilisateurs de cette couche de session, mais vous pouvez forcer la déconnexion. (Pour en savoir plus, consultez [Déconnecter les utilisateurs des fournisseurs d’identité](/fr-CA/docs/authenticate/login/logout/log-users-out-of-idps) et [Déconnecter les utilisateurs des fournisseurs d’identité SAML](/fr-CA/docs/authenticate/login/logout/log-users-out-of-saml-idps).)

<div id="redirect-users-after-logout">
  ## Rediriger les utilisateurs après la déconnexion
</div>

Une fois les utilisateurs déconnectés, vous pouvez [rediriger les utilisateurs](/fr-CA/docs/authenticate/login/logout/redirect-users-after-logout) vers une URL précise. Vous devez enregistrer l’URL de redirection dans les paramètres de votre tenant ou de votre application. Après la déconnexion, Auth0 redirige uniquement vers les URL figurant dans la liste d’autorisation. Si vous avez besoin de redirections différentes pour chaque application, vous pouvez ajouter les URL à la liste d’autorisation dans les paramètres de votre application.