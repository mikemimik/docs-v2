---
description: Découvrez le fonctionnement des initiateurs de déconnexion back-channel OIDC et comment les configurer pour votre ou vos applications.
title: Initiateurs de déconnexion back-channel OIDC
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

Les OIDC Back-Channel Logout Initiators vous permettent de déconnecter à distance les utilisateurs de leurs applications en fonction d’événements de fin de session. Les OIDC Back-Channel Logout Initiators fonctionnent d’un protocole à l’autre — par exemple, une demande de logout lancée par un <Tooltip tip="Fournisseur d’identité (IdP) : Service qui stocke et gère les identités numériques." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=identity+provider">fournisseur d’identité</Tooltip> (à l’initiative de l’IdP) <Tooltip tip="Fournisseur d’identité (IdP) : Service qui stocke et gère les identités numériques." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=SAML">SAML</Tooltip> — et ne sont pas touchés par les restrictions liées aux cookies tiers.

Cette fonctionnalité est une extension de la spécification OIDC back-channel standard. Vous pouvez la configurer pour lancer une demande OIDC Back-Channel Logout pour des événements de fin de session précis, comme un changement de mot de passe ou l’expiration d’une session, ou pour tous les événements de fin de session.

Les administrateurs peuvent activer cette fonctionnalité pour certaines applications à l’aide de la <Tooltip tip="Management API : Un produit permettant aux clients d’effectuer des tâches administratives." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=Management+API">Management API</Tooltip> d’Auth0.

<div id="how-oidc-back-channel-logout-initiators-work">
  ## Fonctionnement des OIDC Back-Channel Logout Initiators
</div>

Les initiateurs associent une réponse OIDC Back-Channel Logout à un événement de fin de session. Ils captent l’événement et s’en servent pour déclencher un jeton de déconnexion OIDC dans toutes les applications associées à la session en question.

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Les initiateurs ne contrôlent pas la gestion des sessions dans votre tenant, y compris les événements de fin de session.
</Callout>

Le diagramme suivant illustre le fonctionnement d’un OIDC Back-Channel Logout Initiator dans le cas d’un événement de changement de mot de passe :

<Frame>![](/docs/images/cdy7uua7fh8z/2GwtCTwecxYONxL1DbewxO/b504426017276605735ab59dc3242794/OIDC_Back-Channel_Logout_Initiators_Diagram.png)</Frame>

<div id="configure-oidc-back-channel-logout-initiators">
  ## Configurer OIDC Back-Channel Logout Initiators
</div>

Vous pouvez configurer les OIDC Back-Channel Logout Initiators à l’aide de l’Auth0 Management API.

<div id="management-api">
  ### Management API
</div>

Vous pouvez configurer les OIDC Back-Channel Logout Initiators d’une application à l’aide de la Management API en utilisant le endpoint [Update a Client](https://auth0.com/docs/api/management/v2/clients/patch-clients-by-id).

1. Obtenez un [jeton d’accès à la Management API](/fr-CA/docs/secure/tokens/access-tokens/management-api-access-tokens) avec la portée `update:clients`.
2. Appelez le endpoint [Update a Client](https://auth0.com/docs/api/management/v2/clients/patch-clients-by-id) avec les données de configuration appropriées dans le payload. Par exemple, pour mettre fin à la session d’une application après un événement de changement de mot de passe, fournissez ce qui suit :

   {/* codeblockOld.header.login.logInButton codeblockOld.header.login.configureSnippet */}

export const codeExample1 = `   PATCH /api/v2/clients/{yourClientId}
{
  ...
  "oidc_logout": {
    "backchannel_logout_urls": ["https://example.com/cb"]
    "backchannel_logout_initiators": {
      "mode":"custom",
      "selected_initiators": ["rp-logout", "idp-logout", "password-changed"]
    }
  }
  ...
}
`;

<AuthCodeBlock children={codeExample1} language="json" filename="JSON" />

<div id="properties">
  #### Propriétés
</div>

L’objet `backchannel_logout_initiators` comprend les propriétés suivantes :

| Propriété             | Type   | Obligatoire ?                                 | Description                                             | Valeurs prises en charge                                                                                                           |
| --------------------- | ------ | --------------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `mode`                | string | Obligatoire                                   | Méthode de configuration pour activer les initiateurs. | `custom`, `all`                                                                                                                    |
| `selected_initiators` | array  | Obligatoire si `mode` est défini sur `custom` | Liste des initiateurs à activer.                       | `rp-logout`, `idp-logout`, `password-changed`, `session-expired`, `session-revoked`, `account-deleted`, `email-identifier-changed` |

<div id="mode-property">
  ##### propriété `mode`
</div>

La propriété `mode` détermine comment configurer l’activation des initiateurs.

Par défaut, sa valeur est `custom`, ce qui vous permet de préciser quels initiateurs vous souhaitez activer. Si vous voulez que votre application se déconnecte dès que la session de l’IdP prend fin, définissez-la sur `all`.

La propriété `mode` prend en charge les valeurs suivantes :

| Valeur   | Description                                                                          |
| -------- | ------------------------------------------------------------------------------------ |
| `custom` | Active uniquement les initiateurs répertoriés dans le tableau `selected_initiators`. |
| `all`    | Active automatiquement tous les initiateurs actuels et futurs.                       |

<div id="selected_initiators-property">
  ##### Propriété `selected_initiators`
</div>

La propriété `selected_initiators` contient la liste des initiateurs à activer pour l’application donnée.

La propriété `selected_initiators` prend en charge les valeurs suivantes :

| Valeur                     | Description                                                              |
| -------------------------- | ------------------------------------------------------------------------ |
| `rp-logout`                | La requête a été déclenchée par une partie de confiance (RP).            |
| `idp-logout`               | La requête a été déclenchée par un fournisseur d’identité externe (IdP). |
| `password-changed`         | La requête a été déclenchée par un changement de mot de passe.           |
| `session-expired`          | La requête a été déclenchée par l’expiration de la session.              |
| `session-revoked`          | La requête a été déclenchée par la suppression de la session.            |
| `account-deleted`          | La requête a été déclenchée par la suppression du compte.                |
| `email-identifier-changed` | La requête a été déclenchée par un changement d’identifiant de courriel. |

<div id="examples">
  #### Exemples
</div>

<div id="subscribe-an-application-to-all-current-and-future-initiators">
  ##### Abonner une application à tous les initiateurs actuels et futurs
</div>

{/* codeblockOld.header.login.logInButton codeblockOld.header.login.configureSnippet */}

export const codeExample2 = `PATCH /api/v2/clients/{yourClientId}

{
  ...
  "oidc_logout": {
    "backchannel_logout_urls": ["https://example.com/cb"]
    "backchannel_logout_initiators": {
      "mode":"all"
    }
  }
  ...
}`;

<AuthCodeBlock children={codeExample2} language="json" filename="JSON" />

<div id="subscribe-an-application-to-password-changed-initiator-only-rp-logout-and-idp-logout-are-required">
  ##### Abonner une application uniquement à l’initiateur password-changed (rp-logout et idp-logout sont requis)
</div>

{/* codeblockOld.header.login.logInButton codeblockOld.header.login.configureSnippet */}

export const codeExample3 = `PATCH /api/v2/clients/{yourClientId}

{
  ...
  "oidc_logout": {
    "backchannel_logout_urls": ["https://example.com/cb"]
    "backchannel_logout_initiators": {
      "mode":"custom",
      "selected_initiators": ["rp-logout", "idp-logout", "password-changed"]
    }
  }
  ...
}`;

<AuthCodeBlock children={codeExample3} language="json" filename="JSON" />

<div id="unsubscribe-all-initiators-rp-logout-remains-the-default">
  ##### Désabonner tous les initiateurs (rp-logout reste l’option par défaut)
</div>

{/* codeblockOld.header.login.logInButton codeblockOld.header.login.configureSnippet */}

export const codeExample4 = `PATCH /api/v2/clients/{yourClientId}

{
  ...
  "oidc_logout": {
	  "backchannel_logout_urls": ["https://example.com/cb"]
  }
  ...
}`;

<AuthCodeBlock children={codeExample4} language="json" filename="JSON" />

<div id="dashboard">
  ### Dashboard
</div>

<Tooltip tip="OpenID : norme ouverte d’authentification qui permet aux applications de vérifier l’identité des utilisateurs sans recueillir ni stocker d’informations de connexion." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=OpenID">OpenID</Tooltip> Connect Back-Channel Logout peut être configuré avec les autres paramètres de votre application. Cette fonctionnalité s’active automatiquement dès qu’un URI de Back-Channel Logout est fourni.

<Frame>![Dashboard > Applications > Paramètres de l’application](/docs/images/cdy7uua7fh8z/vgN0wudP38zgRZNL59Mhy/83199c9ea81dd94f5dd589c4c46bea80/Screenshot_2024-11-05_at_13.22.54.png)</Frame>

<div id="selected-initiators-only">
  #### Initiateurs sélectionnés uniquement
</div>

Seuls les initiateurs requis (`rp-logout` et `idp-logout`) seront activés par défaut. Tout initiateur supplémentaire, y compris ceux qui seront ajoutés plus tard, doit d’abord être sélectionné avant de pouvoir déclencher la déconnexion de votre application.

Sélectionnez cette option si vous voulez que la déconnexion de votre application soit déclenchée uniquement par les initiateurs que vous sélectionnez.

<div id="all-supported-initiators">
  #### Tous les initiateurs pris en charge
</div>

Tous les initiateurs pris en charge, y compris ceux qui seront ajoutés ultérieurement, seront abonnés par défaut.

Sélectionnez cette option si vous voulez que votre application se déconnecte chaque fois que la session de l’IdP prend fin.