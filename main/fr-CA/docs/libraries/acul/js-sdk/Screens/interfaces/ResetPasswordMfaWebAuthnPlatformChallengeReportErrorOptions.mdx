---
title: "ResetPasswordMfaWebAuthnPlatformChallengeReportErrorOptions"
---

ReportBrowserErrorOptions

```ts Example
export interface ReportBrowserErrorOptions {
  /**
   * L'objet d'erreur provenant de la WebAuthn API. Il s'agit généralement d'une `DOMException`.
   * Le SDK s'attend à un objet ayant au moins les propriétés `name` et `message`.
   * @type {WebAuthnErrorDetails}
   * @example { name: "NotAllowedError", message: "The operation was cancelled by the user." }
   */
  error: WebAuthnErrorDetails;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="error" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
  L’objet d’erreur de la WebAuthn API (`navigator.credentials.get()`)
  à signaler. Il doit inclure `name` et `message`.
</ParamField>