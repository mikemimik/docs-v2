---
title: "ScreenMembersOnEmailVerificationResult"
---

ScreenMembersOnEmailVerificationResult

```ts Example
export interface ScreenMembersOnEmailVerificationResult extends ScreenMembers {
  /**
   * Données propres à l'écran contenant le statut de la vérification du courriel.
   * @type {{ status: string; } | null}
   */
  data: {
    /**
     * Le statut du processus de vérification du courriel.
     * Les valeurs possibles peuvent inclure "success", "failure", "already_verified", etc.
     * Ce statut doit être affiché à l'utilisateur pour l'informer du résultat.
     */
    status: string;
  } | null;

  /**
   * Liens de navigation disponibles sur cet écran.
   * @type {string | null}
   */
  loginLink: string | null;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="captcha" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="status">
  Données propres à cet écran.

  #### Déclaration de type

  &#123; `status`: `string`; &#125;

  #### status

  Le statut du processus de vérification du courriel.
  Les valeurs possibles incluent notamment &quot;success&quot;, &quot;failure&quot;, &quot;already&#95;verified&quot;, etc.
  Ce statut doit être affiché à l’utilisateur pour l’informer du résultat.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record">
  Liens de navigation disponibles sur cet écran.
</ParamField>

<ParamField body="loginLink" type="string">
  Liens de navigation disponibles sur cet écran.
</ParamField>

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />