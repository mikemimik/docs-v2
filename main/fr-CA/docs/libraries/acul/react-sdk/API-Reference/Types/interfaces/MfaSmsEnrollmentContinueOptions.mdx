---
title: "MfaSmsEnrollmentContinueOptions"
---

Options permettant de poursuivre l’inscription à l’authentification MFA par SMS.

Étend `CustomOptions` afin de permettre la transmission d’options personnalisées avec `phone` et `captcha`.

```ts Example
export interface MfaSmsEnrollmentContinueOptions extends CustomOptions {
  phone: string;
  captcha?: string;
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body="phone" type="string" required>
  Le numéro de téléphone de l’utilisateur pour l’inscription à l’authentification par SMS.
</ParamField>

<ParamField body="captcha?" type="string">
  Jeton CAPTCHA facultatif pour la vérification.
</ParamField>