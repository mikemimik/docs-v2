---
title: "ScreenMembersOnMfaEnrollResult"
---

ScreenMembersOnMfaEnrollResult

```ts Example
export interface ScreenMembersOnMfaEnrollResult extends ScreenMembers {
  /**
   * Données propres à l'écran contenant le statut de l'inscription à l'AMF.
   * @type {{ status: string; } | null}
   */
  data: {
    /**
     * Le statut du processus d'inscription à l'AMF.
     * Les valeurs possibles peuvent inclure "success", "failure" ou d'autres codes de statut spécifiques.
     * Ce statut peut être utilisé pour afficher un message approprié à l'utilisateur.
     * @type {string}
     */
    status: string;
  } | null;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="captcha" type={<span><a href="/fr-CA/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="status">
  Données propres à l’écran.

  #### Déclaration du type

  &#123; `status`: `string`; &#125;

  #### status

  Le statut du processus d’inscription à l’AMF.
  Les valeurs possibles incluent notamment &quot;success&quot;, &quot;failure&quot; ou d’autres codes de statut précis.
  Ce statut peut servir à afficher un message approprié à l’utilisateur.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />