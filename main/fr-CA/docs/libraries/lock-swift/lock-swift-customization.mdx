---
description: Options de style et de personnalisation de l’apparence de Lock.Swift
title: 'Lock.swift : options de personnalisation du style'
---

Vous trouverez ci-dessous de nombreuses options pour configurer le style et l’apparence de Lock. De plus, la page [Options de configuration du comportement](/fr-CA/docs/libraries/lock-swift/lock-swift-configuration-options) présente aussi plusieurs options permettant de modifier le comportement et les fonctionnalités de Lock.

<div id="customizing-locks-appearance">
  ## Personnalisation de l’apparence de Lock
</div>

Vous pouvez ajouter des options de personnalisation du style à l’initialisation de votre Lock à l’aide de `withStyle`.

```swift lines
Lock
  .classic()
  .withStyle {
	  $0.title = "Company LLC"
	  $0.logo = LazyImage(name: "company_logo")
	  $0.primaryColor = UIColor(red: 0.6784, green: 0.5412, blue: 0.7333, alpha: 1.0)
	}
  .present(from: self)
```

<div id="header-style-options">
  ## Options de style de l’en-tête
</div>

<div id="headerblur">
  ### headerBlur
</div>

Style d’effet de flou utilisé. Peut prendre n’importe quelle valeur définie dans `UIBlurEffectStyle`.

```swift lines
.withStyle {
  $0.headerBlur = .extraLight
}
```

<div id="headercolor">
  ### headerColor
</div>

Couleur d’arrière-plan utilisée pour l’en-tête. Par défaut, aucune couleur n’est appliquée, seulement un effet de flou.

```swift lines
.withStyle {
  $0.headerColor = UIColor? = nil
}
```

<div id="logo">
  ### logo
</div>

Image du logo de l’en-tête. La résolution minimale recommandée est de 200 pixels de largeur sur 200 pixels de hauteur.

```swift lines
.withStyle {
  $0.logo = LazyImage(name: "company_logo")
}
```

<div id="headercloseicon">
  ### headerCloseIcon
</div>

L’icône &quot;fermer&quot; de l’en-tête peut être modifiée.

```swift lines
.withStyle {
  $0.headerCloseIcon = LazyImage(name: "ic_close")
}
```

<div id="headerbackicon">
  ### headerBackIcon
</div>

L’icône « retour » de l’en-tête peut être modifiée.

```swift lines
.withStyle {
  $0.headerBackIcon = LazyImage(name: "ic_close")
}
```

<div id="title-style-options">
  ## Options de style pour le titre
</div>

<div id="hidetitle">
  ### hideTitle
</div>

Masque le titre de l’en-tête et affiche uniquement le logo. Par défaut, cette option est désactivée.

```swift lines
.withStyle {
  $0.hideTitle = false
}
```

<div id="title">
  ### title
</div>

Texte du titre utilisé dans l’en-tête

```swift lines
.withStyle {
  $0.title = "Company LLC"
}
```

<div id="titlecolor">
  ### titleColor
</div>

Couleur utilisée pour le titre de l’en-tête.

```swift lines
.withStyle {
  $0.titleColor = UIColor.black
}
```

<div id="button-and-component-style-options">
  ## Options de style pour les boutons et les composants
</div>

<div id="buttontintcolor">
  ### buttonTintColor
</div>

Couleur utilisée comme teinte principale du bouton.

```swift lines
.withStyle {
  $0.buttonTintColor = UIColor.white
}
```

<div id="disabledcolor">
  ### disabledColor
</div>

Couleur utilisée pour les composants désactivés de Lock.

```swift lines
.withStyle {
  $0.disabledColor = UIColor(red: 0.8902, green: 0.898, blue: 0.9059, alpha: 1.0)
}
```

<div id="disabledtextcolor">
  ### disabledTextColor
</div>

Couleur utilisée pour le texte des composants désactivés de Lock.

```swift lines
.withStyle {
  $0.disabledTextColor = UIColor(red: 0.5725, green: 0.5804, blue: 0.5843, alpha: 1.0)
}
```

<div id="hidebuttontitle">
  ### hideButtonTitle
</div>

Masquez le libellé du bouton principal et affichez uniquement l’icône. Par défaut, cette option est désactivée.

```swift lines
.withStyle {
  $0.hideButtonTitle = false
}
```

<div id="primarycolor">
  ### primaryColor
</div>

Couleur principale utilisée par Lock.

```swift lines
.withStyle {
  $0.primaryColor = UIColor.orange
}
```

<div id="input-field-styles">
  ## Styles du champ de saisie
</div>

<div id="inputtextcolor">
  ### inputTextColor
</div>

La couleur du texte dans le champ de saisie.

```swift lines
.withStyle {
  $0.inputTextColor = UIColor.black
}
```

<div id="inputplaceholdertextcolor">
  ### inputPlaceholderTextColor
</div>

La couleur du texte indicatif dans les champs de saisie.

```swift lines
.withStyle {
  $0.inputPlaceholderTextColor = UIColor(red: 0.780, green: 0.780, blue: 0.804, alpha: 1.00)
}
```

<div id="inputbordercolor">
  ### inputBorderColor
</div>

La couleur de la bordure des champs de saisie.

```swift lines
.withStyle {
  $0.inputBorderColor = UIColor(red: 0.780, green: 0.780, blue: 0.804, alpha: 1.00)
}
```

<div id="inputbordercolorerror">
  ### inputBorderColorError
</div>

La couleur de la bordure des champs de saisie qui contiennent des valeurs invalides.

```swift lines
.withStyle {
  $0.inputBorderColorError = UIColor.red
}
```

<div id="inputbackgroundcolor">
  ### inputBackgroundColor
</div>

La couleur de l’arrière-plan des champs de saisie.

```swift lines
.withStyle {
  $0.inputBackgroundColor = UIColor.white
}
```

<div id="inputiconbackgroundcolor">
  ### inputIconBackgroundColor
</div>

La couleur d’arrière-plan des icônes du champ de saisie.

```swift lines
.withStyle {
  $0.inputIconBackgroundColor = UIColor(red: 0.9333, green: 0.9333, blue: 0.9333, alpha: 1.0)
}
```

<div id="inputiconcolor">
  ### inputIconColor
</div>

La couleur des icônes du champ de saisie.

```swift lines
.withStyle {
  $0.inputIconColor = UIColor(red: 0.5725, green: 0.5804, blue: 0.5843, alpha: 1.0)
}
```

<div id="status-bar-styles">
  ## Styles de la barre d’état
</div>

<div id="uistatusbaranimation">
  ### UIStatusBarAnimation
</div>

L’animation utilisée pour mettre à jour la barre d’état du Lock Controller.

```swift lines
.withStyle {
  $0.UIStatusBarAnimation = .none
}
```

<div id="statusbarhidden">
  ### statusBarHidden
</div>

La visibilité de la barre d’état du Lock Controller.

```swift lines
.withStyle {
  $0.statusBarHidden = false
}
```

<div id="uistatusbarstyle">
  ### UIStatusBarStyle
</div>

Le style de la barre d’état de Lock Controller.

```swift lines
.withStyle {
  $0.UIStatusBarStyle = .default
}
```

<div id="uisearchbarstyle">
  ### UISearchBarStyle
</div>

Le style de la barre de recherche de Lock <Tooltip tip="Passwordless : forme d’authentification qui ne repose pas sur l’utilisation d’un mot de passe comme premier facteur." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=Passwordless">Passwordless</Tooltip>.

```swift lines
.withStyle {
  $0.UISearchBarStyle = .default
}
```

<div id="other-style-options">
  ## Autres options de style
</div>

<div id="textcolor">
  ### textColor
</div>

La couleur du texte dans le corps du texte.

```swift lines
.withStyle {
  $0.textColor = UIColor.black
}
```

<div id="backgroundcolor">
  ### backgroundColor
</div>

Couleur utilisée pour l’arrière-plan de Lock.

```swift lines
.withStyle {
  $0.backgroundColor = UIColor.white
}
```

<div id="backgroundimage">
  ### backgroundImage
</div>

Image utilisée comme image d’arrière-plan de Lock

```swift lines
.withStyle {
  $0.backgroundImage = LazyImage(name: "company_logo")
}
```

<div id="oauth2">
  ### OAuth2
</div>

Toute connexion OAuth2 autre que `db` peut avoir des styles personnalisés en associant un nom de connexion à un `AuthStyle`

```swift lines
.withStyle {
  $0.oauth2["slack"] = AuthStyle(
    name: "Slack",
    color: UIColor(red: 0.4118, green: 0.8078, blue: 0.6588, alpha: 1.0),
    withImage: LazyImage(name: "ic_slack")
  )
}
```

<div id="seperatortextcolor">
  ### seperatorTextColor
</div>

Couleur du libellé du séparateur Social.

```swift lines
.withStyle {
  $0.seperatorTextColor = UIColor(red: 0.0, green: 0.0, blue: 0.0, alpha: 0.54)
}
```

<div id="secondarybuttoncolor">
  ### secondaryButtonColor
</div>

La couleur des boutons secondaires.

```swift lines
.withStyle {
  $0.secondaryButtonColor = UIColor.black
}
```

<div id="tabtextcolor">
  ### tabTextColor
</div>

La couleur du texte sur l’onglet de connexion à la base de données.

```swift lines
.withStyle {
  $0.tabTextColor = UIColor(red: 0.3608, green: 0.4, blue: 0.4353, alpha: 0.6)
}
```

<div id="tabtintcolor">
  ### tabTintColor
</div>

La couleur de la teinte appliquée à l’onglet de connexion à la base de données.

```swift lines
.withStyle {
  $0.tabTintColor = UIColor(red: 0.3608, green: 0.4, blue: 0.4353, alpha: 0.6)
}
```