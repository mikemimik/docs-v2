---
description: Utiliser les connexions de base de données avec Auth0.Swift
title: 'Auth0.swift : Utiliser les connexions de base de données'
---

<Card title="Authentification de base de données sur les plateformes natives">
  L’authentification par nom d’utilisateur/adresse courriel et mot de passe à partir d’applications natives est désactivée par défaut pour les nouveaux tenants depuis le 8 juin 2017. Les utilisateurs sont encouragés à utiliser Universal Login et à effectuer plutôt une Web Authentication. Si vous souhaitez tout de même continuer, vous devrez d’abord activer le type d’attribution Password dans votre Dashboard. Consultez [Application Grant Types](/docs/fr-CA/get-started/applications/application-grant-types) pour en savoir plus.
</Card>

L’Authentication API fournit des méthodes pour authentifier les utilisateurs d’une base de données et les inscrire.

<div id="signing-up-with-a-database-connection">
  ## Inscription avec une connexion de base de données
</div>

Pour s’inscrire, il faut appeler la méthode `createUser` en fournissant l’`email`, le `password` et le nom de la `connection` de l’utilisateur afin de lancer le processus d’inscription. Vous pouvez aussi préciser des métadonnées utilisateur supplémentaires à enregistrer.

```swift lines
Auth0
    .authentication()
    .createUser(
        email: "support@auth0.com",
        password: "secret-password",
        connection: "Username-Password-Authentication",
        userMetadata: ["first_name": "First",
                       "last_name": "Last"])
    .start { result in
        switch result {
        case .success(let user):
            print("User: \(user)")
        case .failure(let error):
            print("Failed with \(error)")
        }
    }
```

<div id="login-with-a-database-connection">
  ## Se connecter avec une connexion de base de données
</div>

Pour se connecter avec une connexion de base de données, il faut appeler `login` avec le nom d’utilisateur ou l’adresse courriel de l’utilisateur, le mot de passe et le nom de la connexion (comme `Username-Password-Authentication`) avec laquelle vous souhaitez vous authentifier. La réponse sera un objet Credentials.

```swift lines
Auth0
    .authentication()
    .login(
        usernameOrEmail: "support@auth0.com",
        password: "secret-password",
        realm: "Username-Password-Authentication",
        scope: "openid")
     .start { result in
         switch result {
         case .success(let credentials):
            print("Credentials: \(credentials)")
         case .failure(let error):
            print("Failed with \(error)")
         }
     }
```