---
title: "ScreenMembersOnMfaWebAuthnChangeKeyNickname"
---

ScreenMembersOnMfaWebAuthnChangeKeyNickname

```ts Example
export interface ScreenMembersOnMfaWebAuthnChangeKeyNickname extends ScreenMembers {
  /**
   * WebAuthnキーの現在のニックネームを含む画面固有のデータ。
   * @type {{ nickname: string; } | null}
   */
  data: {
    /**
     * ユーザーが変更しようとしているWebAuthnセキュリティキーの現在のニックネーム。
     * この値は参考情報としてユーザーに表示する必要があります。
     * @type {string}
     * @example "My YubiKey"
     * @example "Work Security Key"
     */
    nickname: string;
  } | null;
}
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="captcha" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="{ nickname: string; }">
  画面固有のデータです。

  * 型宣言: `{ nickname: string; }`

  <Expandable title="プロパティ">
    <ParamField body="nickname" type="string">
      ユーザーがこれから変更しようとしている WebAuthn セキュリティキーの現在のニックネームです。
      この値は、ユーザーが状況を把握できるよう表示する必要があります。

      <CodeGroup>
        ```ts Example 1
        "My YubiKey"
        ```

        ```ts Example 2
        "Work Security Key"
        ```

        ```ts Example 3
        null
        ```
      </CodeGroup>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />