---
title: "メンバーのパスワード再設定用MFAのWebAuthnローミングチャレンジ画面"
---

メンバーのパスワード再設定用MFAのWebAuthnローミングチャレンジ画面

```ts Example
export interface ScreenMembersOnResetPasswordMfaWebAuthnRoamingChallenge extends ScreenMembers {
  /**
   * `screen.data` から取得した `passkey.public_key` データへの直接アクセサーです。
   * このオブジェクトには、`navigator.credentials.get()` WebAuthn API 呼び出しに必要な
   * チャレンジおよびその他のオプションが含まれています。
   * `screen.data.passkey.public_key` が利用できない場合は `null` になります。
   * @type {PasskeyRead['public_key'] | null}
   * @public
   */
  publicKey: PasskeyRead['public_key'] | null;

  /**
   * `screen.data` から取得した `show_remember_device` フラグへの直接アクセサーです。
   * 今後のログイン時に MFA を回避できるよう、このデバイス／ブラウザーを記憶するオプションを
   * UI がユーザーに提示するかどうかを示します。`screen.data` に設定されていない場合のデフォルトは `false` です。
   * @type {boolean}
   * @public
   */
  showRememberDevice: boolean;
}
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="captcha" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/PasskeyCreate">PasskeyCreate</a></span>}>
  画面固有のデータです。
  WebAuthn のチャレンジオプションと、「このデバイスを記憶する」フラグが含まれます。
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="publicKey" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/AllowCredential">AllowCredential</a></span>}>
  `screen.data.passkey.public_key` にアクセスするための便利なアクセサーです。
  `navigator.credentials.get()` のチャレンジと関連オプションを提供します。
</ParamField>

<ParamField body="showRememberDevice" type="boolean">
  `screen.data.show_remember_device` にアクセスするための便利なアクセサーです。
  「このデバイスを記憶する」オプションを表示するかどうかを示します。存在しない場合は、デフォルトで `false` になります。
</ParamField>

<ParamField body="texts" type="Record" />