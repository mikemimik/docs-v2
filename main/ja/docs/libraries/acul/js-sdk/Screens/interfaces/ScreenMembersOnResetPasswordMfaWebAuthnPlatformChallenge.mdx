---
title: "ScreenMembersOnResetPasswordMfaWebAuthnPlatformChallenge"
---

ScreenMembersOnResetPasswordMfaWebAuthnPlatformChallenge

```ts Example
export interface ScreenMembersOnResetPasswordMfaWebAuthnPlatformChallenge extends ScreenMembers {
  /**
   * WebAuthn に必要な公開鍵クレデンシャルリクエストオプション（チャレンジ）を取得するための便利なゲッターです。
   * `screen.data.passkey.public_key` から導出されます。
   * `screen.data.passkey` または `screen.data.passkey.public_key` が利用できない場合、この値は `null` になります。
   * @type {PasskeyRead['public_key'] | null}
   */
  publicKey: PasskeyRead['public_key'] | null;

  /**
   * 「このデバイスを記憶する」オプションを表示するかどうかを取得するための便利なゲッターです。
   * `screen.data.show_remember_device` から導出されます。指定されていない場合のデフォルト値は `false` です。
   * @type {boolean}
   */
  showRememberDevice: boolean;
}
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="captcha" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/PasskeyCreate">PasskeyCreate</a></span>}>
  画面固有のデータです。
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="publicKey" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/AllowCredential">AllowCredential</a></span>}>
  `data.passkey.public_key` にアクセスするための便利なアクセサーです。
  WebAuthn の
  `navigator.credentials.get()` API 呼び出しに必要なチャレンジやその他のパラメータを提供します。
</ParamField>

<ParamField body="showRememberDevice" type="boolean">
  `data.show_remember_device` にアクセスするための便利なアクセサーです。
  「このデバイスを記憶する」オプションを表示するかどうかを示します。
  コンテキストに存在しない場合、デフォルトは `false` です。
</ParamField>

<ParamField body="texts" type="Record" />