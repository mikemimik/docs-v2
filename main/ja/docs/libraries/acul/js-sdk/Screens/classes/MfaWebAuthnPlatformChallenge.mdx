---
title: "MfaWebAuthnPlatformChallenge"
description: "Universal Login の `mfa-webauthn-platform-challenge` 画面をカスタマイズするために使用できる、すべてのプロパティとメソッドについて説明します。"
---

MfaWebAuthnPlatformChallenge クラスは、`mfa-webauthn-platform-challenge` 画面の機能を提供します。この画面は、ユーザーが Touch ID や Face ID などのプラットフォーム認証器を使用して本人確認を行う必要がある場合に表示されます。

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/1DFJ4Od7ukDasV7uAPH53P/f4018367fbf636f488cf94b6c6c62fda/Screenshot_2025-05-27_at_20.34.24.png" alt="MfaWebAuthnPlatformChallenge" />
</Frame>

<div id="constructors">
  ## コンストラクター
</div>

MFA WebAuthn Platform Challenge 画面マネージャーのインスタンスを作成します:

```typescript Example
import MfaWebAuthnPlatformChallenge from '@auth0/auth0-acul-js/mfa-webauthn-platform-challenge';

const mfaWebAuthnPlatformChallenge = new MfaWebAuthnPlatformChallenge();
await mfaWebAuthnPlatformChallenge.verify();
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  ブランディングテーマや設定など、ブランディング関連の構成を提供します。
</ParamField>

<ParamField body="client" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  `mfa-webauthn-platform-challenge` 画面向けに、`id`、`name`、`logoUrl` などのクライアント関連の構成を提供します。
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  ユーザーの組織に関する情報 (`id` や `name` など) を提供します。
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  認証フローにおける現在の prompt に関するデータを含みます。
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnPlatformChallenge">ScreenMembersOnMfaWebAuthnPlatformChallenge</a></span>}>
  設定やコンテキストなど、`mfa-webauthn-platform-challenge` 画面固有の詳細を含みます。
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  `id` や関連するメタデータなど、tenant に関するデータを含みます。
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  `mfa-webauthn-platform-challenge` 画面向けに、有効な identifier やフローの状態など、トランザクション固有のデータを提供します。
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  MFA WebAuthn プラットフォーム チャレンジ中のユーザー入力など、SDK に渡される信頼できないデータを扱います。
</ParamField>

<ParamField body="user" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  現在アクティブなユーザーの詳細 (`username`、`email`、`roles` を含む) です。
</ParamField>

<div id="methods">
  ## メソッド
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  このメソッドは、Universal Login ページの表示言語を変更します。

  ```typescript Example
  import MfaWebAuthnPlatformChallenge from '@auth0/auth0-acul-js/mfa-webauthn-platform-challenge';

  const mfaWebAuthnPlatformChallenge = new MfaWebAuthnPlatformChallenge();
  mfaWebAuthnPlatformChallenge.changeLanguage({
    language: 'fr',
  });
  ```

  **メソッドパラメータ**

  <Expandable title="パラメータ">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/ja/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions)。
    </ParamField>

    <ParamField body="language" type="string" required>
      使用する言語のロケールコード (例: `'en'`、`'fr'`、`'es'`) 。
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      `'session'` に設定すると、選択した言語はセッション中保持されます。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  このメソッドは、コンテキストからトランザクションエラーの配列を取得します。エラーが存在しない場合は空の配列を返します。
</ParamField>

<ParamField body="reportBrowserError" type="Promise<void>">
  このメソッドは、WebAuthn の `navigator.credentials.get()` 操作中に発生したブラウザー側エラーを報告します。

  ```typescript Example
  import MfaWebAuthnPlatformChallenge from '@auth0/auth0-acul-js/mfa-webauthn-platform-challenge';

  const mfaWebAuthnPlatformChallenge = new MfaWebAuthnPlatformChallenge();
  await mfaWebAuthnPlatformChallenge.reportBrowserError({
    error: { name: 'NotAllowedError', message: 'The operation was not allowed.' },
  });
  ```

  **メソッドパラメータ**

  <Expandable title="パラメータ">
    <ParamField body="options">
      [MfaWebAuthnPlatformChallengeReportErrorOptions](/docs/ja/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnPlatformChallengeReportErrorOptions)。
    </ParamField>

    <ParamField body="error" type={<span><a href="/docs/ja/libraries/acul/js-sdk/Screens/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>} required>
      報告する WebAuthn API (`navigator.credentials.get()`) のエラーオブジェクト。

      <ParamField body="name" type="string" required>
        エラー名 (例: `'NotAllowedError'`) 。
      </ParamField>

      <ParamField body="message" type="string" required>
        問題の内容を示すエラーメッセージ。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  このメソッドを使用すると、ユーザーは別のMFA方式を試すことができます。

  ```typescript Example
  import MfaWebAuthnPlatformChallenge from '@auth0/auth0-acul-js/mfa-webauthn-platform-challenge';

  const mfaWebAuthnPlatformChallenge = new MfaWebAuthnPlatformChallenge();
  await mfaWebAuthnPlatformChallenge.tryAnotherMethod();
  ```
</ParamField>

<ParamField body="verify" type="Promise<void>">
  このメソッドは、WebAuthn プラットフォーム認証器チャレンジを開始します。

  ```typescript Example
  import MfaWebAuthnPlatformChallenge from '@auth0/auth0-acul-js/mfa-webauthn-platform-challenge';

  const mfaWebAuthnPlatformChallenge = new MfaWebAuthnPlatformChallenge();
  await mfaWebAuthnPlatformChallenge.verify();
  ```

  **メソッドパラメータ**

  <Expandable title="パラメータ">
    <ParamField body="options">
      [CustomOptions](/docs/ja/libraries/acul/js-sdk/Screens/interfaces/CustomOptions)。
    </ParamField>

    <ParamField body="rememberDevice?" type="boolean">
      `true` の場合、今後のMFAチャレンジに備えてブラウザーを記憶します。これは `screen.showRememberDevice` が `true` の場合にのみ適用されます。
    </ParamField>
  </Expandable>
</ParamField>