---
title: "ResetPasswordMfaEmailChallenge"
description: "Universal Login の `reset-password-mfa-email-challenge` 画面をカスタマイズするために使用できる、すべてのフックとメソッドについて説明します。"
---

`reset-password-mfa-email-challenge` 画面は、パスワードのリセットフロー中に、メールによる多要素認証が必要な場合に表示されます。リセットを続行するには、メールアドレスに送信された認証コードを入力する必要があります。

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/3syDeetKM505pFoJ1ND8M3/a8d25ad3d0ba2af508af9a9aa0d5d25e/Screenshot_2025-03-26_at_09.16.08.png" alt="ACUL パスワードリセット MFA メールチャレンジ" />
</Frame>

<div id="import">
  ## インポート
</div>

各画面には、それぞれ専用のフックとメソッドがあります。SDK は各画面ごとに、**部分インポート** と **ルートインポート** をサポートしています。

* 部分インポートを使用すると、特定のユースケースに必要なコードだけを取り込めます。
* ルートインポートを使用すると、単一のバンドルからすべての画面を読み込めるため、想定されるあらゆる画面に対応できる統一ビルドが必要な場合に便利です。

```jsx Import Example
// ルートインポート
import { useResetPasswordMfaEmailChallenge } from '@auth0/auth0-acul-react';

// 部分インポート
import {
  useResetPasswordMfaEmailChallenge,
  // コンテキストフック
  useUser,
  useTenant,
  useBranding,
  useClient,
  useOrganization,
  usePrompt,
  useScreen,
  useTransaction,
  useUntrustedData,
  // 共通フック
  useCurrentScreen,
  useAuth0Themes,
  useErrors,
  useResend,
  // ユーティリティフック
  useChangeLanguage,
  // メソッド
  continueMethod,
  resendCode,
  tryAnotherMethod,
} from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';

function ResetPasswordMfaEmailChallengeScreen() {
  const { continueMethod } = useResetPasswordMfaEmailChallenge();
  return (
    <button onClick={() => continueMethod({ code: '123456' })}>
      Verify Code
    </button>
  );
}
```

<div id="context-hooks">
  ## コンテキストフック
</div>

`reset-password-mfa-email-challenge` 画面で、Auth0 コンテキストデータへの読み取り専用アクセスを提供する画面スコープのフックです。`@auth0/auth0-acul-react/reset-password-mfa-email-challenge` からインポートします。

<ParamField body="useBranding" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  このフックは、`reset-password-mfa-email-challenge` 画面に表示されるロゴ、色、テーマ設定などのブランディング設定を提供します。

  ```jsx Example
  import { useBranding } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function CustomTheme() {
    const branding = useBranding();
  }
  ```
</ParamField>

<ParamField body="useClient" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>}>
  このフックは、`reset-password-mfa-email-challenge` 画面の `id`、`name`、`logoUrl` など、クライアント関連の設定を提供します。

  ```jsx Example
  import { useClient } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function AppInfo() {
    const client = useClient();
  }
  ```
</ParamField>

<ParamField body="useOrganization" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  このフックは、パスワードリセットが Organization スコープの場合に、ユーザーの Organization に関する情報を提供します。Organization コンテキストが存在しない場合は `null` を返します。

  ```jsx Example
  import { useOrganization } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function OrgSelector() {
    const organization = useOrganization();
    if (!organization) {
      return <p>No Organization context</p>;
    }
  }
  ```
</ParamField>

<ParamField body="usePrompt" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>}>
  このフックには、認証フロー内の現在のプロンプトに関するデータが含まれます。

  ```jsx Example
  import { usePrompt } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function FlowInfo() {
    const prompt = usePrompt();
  }
  ```
</ParamField>

<ParamField body="useScreen" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnResetPasswordMfaEmailChallenge">ScreenMembersOnResetPasswordMfaEmailChallenge</a></span>}>
  このフックには、設定やコンテキストなど、`reset-password-mfa-email-challenge` 画面に固有の詳細が含まれます。

  ```jsx Example
  import { useScreen } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function ScreenDebug() {
    const screen = useScreen();
  }
  ```
</ParamField>

<ParamField body="useTenant" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>}>
  このフックには、`id` や関連メタデータなど、テナントに関するデータが含まれます。

  ```jsx Example
  import { useTenant } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function TenantInfo() {
    const tenant = useTenant();
  }
  ```
</ParamField>

<ParamField body="useTransaction" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  このフックは、`reset-password-mfa-email-challenge` 画面のトランザクション固有データ (アクティブな接続や現在のフロー状態など) を提供します。

  ```jsx Example
  import { useTransaction } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function TransactionInfo() {
    const transaction = useTransaction();
  }
  ```
</ParamField>

<ParamField body="useUntrustedData" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  このフックは、URL パラメータから事前入力されたメールアドレスやユーザー名など、画面に渡される未検証データを扱います。

  ```jsx Example
  import { useUntrustedData } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function PrefilledForm() {
    const untrustedData = useUntrustedData();
  }
  ```
</ParamField>

<ParamField body="useUser" type={<span>() =&gt; <a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>}>
  このフックは、`username`、`email`、利用可能な認証方法など、現在のユーザーの情報を提供します。

  ```jsx 例
  import { useUser } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';
  function UserProfile() {
    const user = useUser();
  }
  ```
</ParamField>

<ParamField body="useResetPasswordMfaEmailChallenge" type={<a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaEmailChallengeMembers">ResetPasswordMfaEmailChallengeMembers</a>}>
  このフックは、`reset-password-mfa-email-challenge` 画面で使用できるすべてのメソッドとコンテキストを返します。
</ParamField>

<div id="methods">
  ## メソッド
</div>

<ParamField body="continueMethod" type="Promise<void>">
  このメソッドは、パスワードリセットフローの MFA ステップを続行するために、メールの認証コードを送信します。

  ```jsx Example
  import { useResetPasswordMfaEmailChallenge } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';

  function VerifyCodeForm() {
    const { continueMethod } = useResetPasswordMfaEmailChallenge();
    return (
      <button onClick={() => continueMethod({ code: '123456' })}>
        Verify Code
      </button>
    );
  }
  ```

  **メソッドのパラメーター**

  <Expandable title="パラメーター">
    <ParamField body="options">
      [ContinueOptions](/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ContinueOptions)。
    </ParamField>

    <ParamField body="code" type="string" required>
      ユーザーが入力した認証コード。
    </ParamField>

    <ParamField body="rememberDevice" type="boolean">
      今後の認証でこのデバイスを記憶するかどうかを指定します。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendCode" type="Promise<void>">
  このメソッドは、メールの認証コードをユーザーに再送します。

  ```jsx Example
  import { useResetPasswordMfaEmailChallenge } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';

  function ResendButton() {
    const { resendCode } = useResetPasswordMfaEmailChallenge();
    return (
      <button onClick={() => resendCode()}>
        Resend Code
      </button>
    );
  }
  ```
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  このメソッドは、別の MFA 方法を選択する画面にユーザーを移動します。

  ```jsx Example
  import { useResetPasswordMfaEmailChallenge } from '@auth0/auth0-acul-react/reset-password-mfa-email-challenge';

  function TryAnotherMethodButton() {
    const { tryAnotherMethod } = useResetPasswordMfaEmailChallenge();
    return (
      <button onClick={() => tryAnotherMethod()}>
        Try Another Method
      </button>
    );
  }
  ```
</ParamField>

<div id="commonutility-hooks">
  ## 共通/ユーティリティ フック
</div>

<ParamField body={<a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Hooks/useResend">useResend</a>} type="Hooks">
  このフックは、現在の画面における認証コードの再送信のクールダウン状態とタイマーを管理します。
</ParamField>

<ParamField body={<a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Hooks/useAuth0Themes">useAuth0Themes</a>} type="Hooks">
  このフックは、ブランディングコンテキストから、設定をフラット化した現在のテーマオプションを取得します。
</ParamField>

<ParamField body={<a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Hooks/useChangeLanguage">useChangeLanguage</a>} type="Hooks">
  このフックは、現在の ACUL 画面の表示言語を変更するための関数を返します。
</ParamField>

<ParamField body={<a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Hooks/useCurrentScreen">useCurrentScreen</a>} type="Hooks">
  このフックは、現在の画面のコンテキストと状態を取得します。
</ParamField>

<ParamField body={<a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Hooks/useErrors">useErrors</a>} type="Hooks">
  このフックは、画面上のサーバー、クライアント、開発者のエラーを読み取り、管理します。
</ParamField>