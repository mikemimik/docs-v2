---
title: "MfaWebAuthnErrorMembers"
---

MfaWebAuthnErrorMembers

````ts Example
export interface MfaWebAuthnErrorMembers extends BaseMembers {
  /**
   * MFA WebAuthn Error固有のデータ構造を持つ画面オブジェクト。
   */
  screen: ScreenMembersOnMfaWebAuthnError;
  
  /**
   * ユーザーが前回のWebAuthn操作を再試行できるようにします。
   * 通常、WebAuthn操作が開始された画面にユーザーをリダイレクトします。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるカスタムオプション（省略可能）。
   * @returns {Promise<void>} アクションが正常に送信されると解決されるPromise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.tryAgain();
   * ```
   */
  tryAgain(payload?: CustomOptions): Promise<void>;

  /**
   * 設定されている場合、ユーザーが代替手段としてパスワードベースの認証を選択できるようにします。
   * 通常、プライマリ認証または前のステップでパスワードが使用されていた場合に利用できます。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるカスタムオプション（省略可能）。
   * @returns {Promise<void>} アクションが正常に送信されると解決されるPromise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.usePassword();
   * ```
   */
  usePassword(payload?: CustomOptions): Promise<void>;

  /**
   * 他の認証要素が登録済みまたは利用可能な場合、ユーザーが別のMFA方式を選択できるようにします。
   * ユーザーを認証器選択画面に誘導します。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるカスタムオプション（省略可能）。
   * @returns {Promise<void>} アクションが正常に送信されると解決されるPromise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: CustomOptions): Promise<void>;

  /**
   * ユーザーがデバイスの追加またはWebAuthnステップの完了を拒否し、現在のWebAuthnフローをキャンセルできるようにします。
   * このアクションは通常、ユーザーが続行しないことを選択した登録シナリオで使用されます。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるカスタムオプション（省略可能）。
   * @returns {Promise<void>} アクションが正常に送信されると解決されるPromise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.noThanks();
   * ```
   */
  noThanks(payload?: CustomOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnError">ScreenMembersOnMfaWebAuthnError</a></span>}>
  MFA WebAuthn Error 固有のデータ構造を持つ画面オブジェクトです。
</ParamField>

<ParamField body="tenant" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="noThanks" type="Promise<void>">
  ユーザーがデバイスの追加や WebAuthn ステップの完了を行わず、現在の WebAuthn フローを辞退できるようにします。
  このアクションは通常、ユーザーが続行しないことを選択した登録シナリオで使用されます。

  アクションの送信が正常に完了すると解決される Promise。

  #### 例外

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.noThanks();
  ```

  <Expandable title="パラメータ">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAgain" type="Promise<void>">
  ユーザーが直前の WebAuthn 操作をもう一度試せるようにします。
  通常、ユーザーは WebAuthn 操作を開始した画面にリダイレクトされます。

  アクションの送信が正常に完了すると解決される Promise。

  #### 例外

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.tryAgain();
  ```

  <Expandable title="パラメータ">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  他の認証要素が登録済みまたは利用可能な場合に、ユーザーが別のMFA方式を選択できるようにします。
  これにより、ユーザーは認証器の選択画面に移動します。

  アクションの送信が正常に完了すると解決される Promise。

  #### 例外

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.tryAnotherMethod();
  ```

  <Expandable title="パラメータ">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="usePassword" type="Promise<void>">
  設定されている場合、ユーザーは代替手段としてパスワードベースの認証を選択できます。
  通常、これは最初の認証または前のステップでパスワードが使用されていた場合に利用できます。

  アクションの送信が正常に完了すると解決される Promise。

  #### 例外

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.usePassword();
  ```

  <Expandable title="パラメータ">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>