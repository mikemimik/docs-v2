---
title: "MfaWebAuthnEnrollmentSuccessMembers"
---

MfaWebAuthnEnrollmentSuccessMembers

````ts Example
export interface MfaWebAuthnEnrollmentSuccessMembers extends BaseMembers {
  /**
   * 'mfa-webauthn-enrollment-success' 画面の特定のプロパティとデータへのアクセス。
   * 登録された認証器の `nickname` とその `webauthnType` を含みます。
   * @type {ScreenMembersOnMfaWebAuthnEnrollmentSuccess}
   */
  screen: ScreenMembersOnMfaWebAuthnEnrollmentSuccess;

  /**
   * WebAuthn 登録が成功した後、ユーザーが認証フローを続行できるようにします。
   * このメソッドは `action: "default"` を指定して `/u/mfa-webauthn-enrollment-success` エンドポイントに POST リクエストを送信します。
   *
   * @param {ContinueOptions} [payload] - 省略可能。リクエストに追加のパラメーターを含める必要がある場合の `CustomOptions` オブジェクト。
   * @returns {Promise<void>} 続行アクションが正常に送信されると解決される Promise。
   *                          成功した場合、ブラウザは通常フローの次のステップにリダイレクトされます。
   * @throws {Error} フォームの送信に失敗した場合（例：ネットワークエラー、無効な状態）にエラーをスローします。
   *
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnEnrollmentSuccess のインスタンスであると想定します
   * try {
   *   await sdk.continue();
   *   // ユーザーは次のステップにリダイレクトされます。
   * } catch (error) {
   *   console.error("Failed to continue after WebAuthn enrollment:", error);
   *   // エラーを処理します。ページがエラーとともに再レンダリングされる場合は sdk.transaction.errors を確認してください。
   * }
   * ```
   */
  continue(payload?: ContinueOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnEnrollmentSuccess">ScreenMembersOnMfaWebAuthnEnrollmentSuccess</a></span>}>
  `mfa-webauthn-enrollment-success` 画面固有のプロパティとデータにアクセスできます。
  登録された認証器の `nickname` と `webauthnType` が含まれます。
</ParamField>

<ParamField body="tenant" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="continue" type="Promise<void>">
  WebAuthn の登録完了後に、ユーザーが認証フローを続行できるようにします。
  このメソッドは、`action: "default"` を指定して `/u/mfa-webauthn-enrollment-success` エンドポイントに POST リクエストを送信します。

  続行アクションが正常に送信されると解決される Promise です。
  成功すると、通常はブラウザがフローの次のステップにリダイレクトされます。

  #### 例外

  フォームの送信に失敗した場合 (例: ネットワークの問題、無効な状態) 、エラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnEnrollmentSuccess のインスタンスであるとします
  try {
    await sdk.continue();
    // ユーザーはフローの次のステップにリダイレクトされます。
  } catch (error) {
    console.error("Failed to continue after WebAuthn enrollment:", error);
    // エラーを処理します。ページがエラー付きで再描画された場合は、sdk.transaction.errors を確認することもできます。
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/WebAuthnEnrollSuccessContinueOptions">WebAuthnEnrollSuccessContinueOptions</a></span>}>
      任意。リクエストとともに追加の
      パラメーターを送信する必要がある場合に使用する `CustomOptions` オブジェクトです。
    </ParamField>
  </Expandable>
</ParamField>