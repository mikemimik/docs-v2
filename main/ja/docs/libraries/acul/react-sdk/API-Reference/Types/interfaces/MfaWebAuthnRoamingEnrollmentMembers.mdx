---
title: "MfaWebAuthnRoamingEnrollmentMembers"
---

mfa-webauthn-roaming-enrollment画面で使用できるメソッドとプロパティを定義するインターフェース。

````ts Example
export interface MfaWebAuthnRoamingEnrollmentMembers extends BaseMembers {
  screen: ScreenMembersOnMfaWebAuthnRoamingEnrollment;
  /**
   * WebAuthnの資格情報の作成を開始し、結果をサーバーに送信します。
   * これは、ユーザーがFIDO Security Keysのプロンプトを操作する場合に対応します。
   * @param payload リクエストに含めるカスタムオプション（省略可能）。
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * try {
   *   // ブラウザのWebAuthnプロンプトをトリガーします
   *   const credential = await navigator.credentials.create({ publicKey: webauthnEnrollment.screen.data.passkey.public_key });
   *   // 通常はここで資格情報のレスポンスをシリアライズします
   *   const response = JSON.stringify(credential);
   *   await webauthnEnrollment.enroll({ response });
   * } catch (error) {
   *   console.error('WebAuthn enrollment failed:', error);
   *   // エラーを処理します。例：ユーザーにエラーメッセージを表示するか、エラーの詳細を送信します
   *   // await webauthnEnrollment.showError({ error: { name: error.name, message: error.message } });
   * }
   * ```
   */
  enroll(payload: CustomOptions): Promise<void>;
  /**
   * WebAuthnブラウザーエラーの詳細をサーバーに送信します。
   * ブラウザのWebAuthn APIでエラーが発生した場合に使用します。
   * @param payload エラーの詳細を含むオプション。
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * // ブラウザのWebAuthn API呼び出し用エラーハンドラーの例
   * const handleError = async (error: any) => {
   *   console.error('WebAuthnエラー:', error);
   *   await webauthnEnrollment.showError({
   *     error: {
   *       name: error.name,
   *       message: error.message,
   *       // 必要に応じて他の関連するエラープロパティを含めます
   *     },
   *   });
   * };
   * // ... WebAuthn API呼び出しのcatchブロックでhandleErrorを使用します
   * ```
   */
  showError(payload: ShowErrorOptions): Promise<void>;
  /**
   * ユーザーが別のMFA方式を試せるようにします。
   * これは「Try Another Method」ボタンに対応します。
   * @param payload リクエストに含めるカスタムオプション（省略可能）。
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * await webauthnEnrollment.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnRoamingEnrollment">ScreenMembersOnMfaWebAuthnRoamingEnrollment</a></span>} />

<ParamField body="tenant" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="enroll" type="Promise<void>">
  WebAuthn 資格情報の作成を開始し、その結果をサーバーに送信します。
  これは、ユーザーが FIDO Security Keys 画面を操作することに対応します。

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  try {
    // これによりブラウザーの WebAuthn ダイアログが表示されます
    const credential = await navigator.credentials.create({ publicKey: webauthnEnrollment.screen.data.passkey.public_key });
    // 通常はここで credential のレスポンスをシリアライズします
    const response = JSON.stringify(credential);
    await webauthnEnrollment.enroll({ response });
  } catch (error) {
    console.error('WebAuthn enrollment failed:', error);
    // エラーを処理します。たとえば、ユーザーにエラーメッセージを表示したり、エラーの詳細を送信したりします
    // await webauthnEnrollment.showError({ error: { name: error.name, message: error.message } });
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="showError" type="Promise<void>">
  WebAuthnブラウザーエラーの詳細をサーバーに送信します。
  これは、ブラウザーの WebAuthn API でエラーが発生したときに使用されます。

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  // ブラウザーの WebAuthn API 呼び出し用のエラーハンドラーの例
  const handleError = async (error: any) => {
    console.error('WebAuthn error:', error);
    await webauthnEnrollment.showError({
      error: {
        name: error.name,
        message: error.message,
        // 利用可能であれば、関連する他のエラーのプロパティも含めます
      },
    });
  };
  // ... WebAuthn API 呼び出しの catch ブロックで handleError を使用します
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ShowErrorOptions">ShowErrorOptions</a></span>}>
      エラーの詳細を含むオプション。

      **プロパティ**

      <ParamField body="error" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        WebAuthn エラーの詳細。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  ユーザーが別の MFA方式を試せるようにします。
  これは、「Try Another Method」ボタンに対応します。

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  await webauthnEnrollment.tryAnotherMethod();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/MfaWebAuthnRoamingEnrollmentTryAnotherMethodOptions">MfaWebAuthnRoamingEnrollmentTryAnotherMethodOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>