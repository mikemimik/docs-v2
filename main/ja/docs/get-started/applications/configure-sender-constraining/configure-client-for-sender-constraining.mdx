---
description: Auth0テナント向けに 送信者拘束 を設定する方法の概要。
title: 送信者拘束 を設定する
---

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Demonstrating Proof-of-Possession (DPoP) を使用した 送信者拘束 トークンは現在、Early Access 段階です。この機能へのアクセスをリクエストするには、Auth0 の担当者にお問い合わせください。
</Callout>

[送信者拘束](/ja/docs/secure/sender-constraining) は、アクセス トークンと <Tooltip tip="" cta="用語集を見る" href="/ja/docs/glossary?term=refresh+tokens">リフレッシュ トークン</Tooltip> を、それらを取得した特定のクライアント アプリケーションに暗号学的に関連付けることで、トークンの盗難や不正利用を防ぐ <Tooltip tip="" cta="用語集を見る" href="/ja/docs/glossary?term=OAuth+2.0">OAuth 2.0</Tooltip> および <Tooltip tip="" cta="用語集を見る" href="/ja/docs/glossary?term=OpenID">OpenID</Tooltip> Connect (OIDC) のセキュリティ メカニズムです。

Auth0 は、mTLS 送信者拘束 と Demonstrating Proof-of-Possession (DPoP) をサポートしています。クライアント アプリケーションで 送信者拘束 を有効にする場合は、API 呼び出し先の <Tooltip tip="" cta="用語集を見る" href="/ja/docs/glossary?term=resource+server">リソース サーバー</Tooltip> に対しても適用する必要があります。

Auth0 で 送信者拘束 を設定するには、次の設定が必要です。

* [クライアント アプリケーションの 送信者拘束 を設定する](#configure-sender-constraining-for-a-client-application)。
* [リソース サーバーの 送信者拘束 を設定する](#configure-sender-constraining-for-a-resource-server)。

<div id="how-it-works">
  ## 仕組み
</div>

<Tooltip tip="" cta="用語集を見る" href="/ja/docs/glossary?term=Access+tokens">アクセストークン</Tooltip> が Auth0 で送信者拘束されるかどうかは、クライアントアプリケーションとリソースサーバーの送信者拘束をどのように設定しているかによって決まります。

1. **要求されたオーディエンス:** トークンリクエストで、要求されたオーディエンスが `/userinfo` のみであるか、[`/userinfo`](https://auth0.com/docs/api/authentication/user-profile/get-user-info) エンドポイントでのみ使用することを想定しているか、または `openid` スコープも要求した場合に `/userinfo` を含む可能性があるカスタム API であるかによって、アクセストークンが送信者拘束されるかどうかが変わります。
2. **クライアントアプリケーション:** クライアントアプリケーションで送信者拘束を `required` に設定しているかどうか。
3. **リソースサーバー:** リソースサーバーに対して送信者拘束を設定しているかどうか。

   1. `none`: リソースサーバーに対して送信者拘束を設定していません。
   2. `allowed`: 送信者拘束方式を設定することで、リソースサーバーに対して送信者拘束を設定しています。
   3. `required`: リソースサーバーに対して送信者拘束を必須として設定しています。つまり、アクセストークンはアプリケーションに送信者拘束されている必要があります。送信者拘束方式の設定が必要です。

* **所有証明:** クライアントアプリケーションがトークンリクエストで所有証明アサーションを送信したかどうか。

  * mTLS 送信者拘束: 所有証明は、TLS ハンドシェイク中にクライアントが特定の秘密鍵 (クライアント証明書に関連付けられたもの) を正常に提示することで実証されます。
  * DPoP: 所有証明は、クライアントが DPoP Proof JWT を秘密鍵で暗号学的に署名して作成し、関連するアクセストークンが使用されるすべてのリクエストの DPoP HTTP ヘッダーに `DPoP` Proof JWT を含めることで成立します。

次の表は、さまざまなクライアントリクエストパラメーターと Auth0 リソースサーバーの設定に基づいて、アクセストークンがどのように発行されるか、および送信者拘束されるかどうかを示しています。

| 要求されたオーディエンスの種類                | クライアントで PoP が必須か | クライアントが所有証明 (PoP) を送信したか | Auth0 リソースサーバーポリシー: None | Auth0 リソースサーバーポリシー: Allowed (必須ではない) | Auth0 リソースサーバーポリシー: Required |
| ------------------------------ | ---------------- | ------------------------ | ------------------------ | ------------------------------------ | ---------------------------- |
| Userinfo のみ                    | いいえ              | いいえ                      | 発行される、送信者拘束されない          | 該当なし                                 | 該当なし                         |
| Userinfo のみ                    | いいえ              | はい                       | 発行される、送信者拘束される           | 該当なし                                 | 該当なし                         |
| Userinfo のみ                    | はい               | いいえ                      | 発行されない                   | 該当なし                                 | 該当なし                         |
| Userinfo のみ                    | はい               | はい                       | 発行される、送信者拘束される           | 該当なし                                 | 該当なし                         |
| カスタムオーディエンス (Userinfo を含む場合あり) | いいえ              | いいえ                      | 発行される、送信者拘束されない          | 発行される、送信者拘束されない                      | 発行されない                       |
| カスタムオーディエンス (Userinfo を含む場合あり) | いいえ              | はい                       | 発行される、送信者拘束されない          | 発行される、送信者拘束される                       | 発行される、送信者拘束される               |
| カスタムオーディエンス (Userinfo を含む場合あり) | はい               | いいえ                      | 発行されない                   | 発行されない                               | 発行されない                       |
| カスタムオーディエンス (Userinfo を含む場合あり) | はい               | はい                       | 発行されない                   | 発行される、送信者拘束される                       | 発行される、送信者拘束される               |

<div id="configure-sender-constraining-for-a-client-application">
  ## クライアントアプリケーションの 送信者拘束 を設定する
</div>

クライアントアプリケーションで 送信者拘束 を必須にすると、アクセストークンはそのアプリケーションに制限されます。Auth0 はリクエストを検証し、トークンを要求したアプリケーションのみが、そのトークンを使用して関連リソースにアクセスできることを保証します。

クライアントアプリケーションで 送信者拘束 を必須に設定すると、リソースサーバーの設定時に、送信者拘束 の方式として mTLS または DPoP を指定できます。

クライアントアプリケーションの 送信者拘束 は、<Tooltip tip="" cta="用語集を表示" href="/ja/docs/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip> または <Tooltip tip="" cta="用語集を表示" href="/ja/docs/glossary?term=Management+API">Management API</Tooltip> で設定できます。

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. [Dashboard &gt; Applications &gt; Applications](https://manage.auth0.com/#/applications) に移動し、設定するアプリケーションを選択します。
    2. **Settings** で、**Token Sender-Constraining** までスクロールします。
    3. **Require Sender Constraining** をオンにします。アプリケーションの 送信者拘束 要件を解除するには、オフにします。

    <Frame>![](/docs/images/cdy7uua7fh8z/3BoGW9NEYKPBkfk2tPBL7l/0f884ac3f64f17b23fd6a0e9436bb472/Screenshot_2024-07-23_at_1.30.19_PM.png)</Frame>
  </Tab>

  <Tab title="Management API">
    クライアントで 送信者拘束 を設定するには、[Management API](https://auth0.com/docs/api/management/v2) を使用します。

    クライアントで 送信者拘束 を必須にするには、[クライアント設定を更新する](https://auth0.com/docs/api/management/v2/clients/patch-clients-by-id) PATCH リクエストを送信します。`require_proof_of_possession` パラメーターを `true` に設定します。

    ```json lines
    "require_proof_of_possession": true
    ```

    送信者拘束 要件を解除するには、`require_proof_of_possession` パラメーターを `false` に設定します。

    ```json lines
    "require_proof_of_possession": false
    ```
  </Tab>
</Tabs>

<div id="configure-sender-constraining-for-a-resource-server">
  ## リソースサーバーの送信者拘束を設定する
</div>

Auth0 が発行するアクセストークンは、リソースサーバー上の API にアクセスする送信者 (つまりクライアントアプリケーション) に紐付けることができます。

リソースサーバーの送信者拘束は、Auth0 Dashboard または Management API で設定できます。

<Tabs>
  <Tab title="Auth0 Dashboard">
    Token Binding または送信者拘束を有効にするには、API の **API Settings** を設定します。

    1. [Auth0 Dashboard &gt; Applications &gt; APIs](https://manage.auth0.com/#/apis) に移動します。
    2. 設定する API を選択します。
    3. **Settings** タブで、**Token Sender-Constraining** セクションを探します。
    4. 次の項目を設定します。

       1. Sender Constraining Method:

          1. **None:** リソースサーバーで送信者拘束方式を有効にしません。
          2. **mTLS**: リソースサーバーの送信者拘束方式として mTLS を有効にします。
          3. **DPoP:** リソースサーバーの送信者拘束方式として DPoP を有効にします。

    B. **Require Token Sender Constraining** をオンにします。この API 用にアプリケーションへ発行されるすべてのアクセストークンは、そのアプリケーションに拘束されます。

    <Frame>![Auth0 Dashboard > APIs > Settings > トークンバインディング](/docs/images/cdy7uua7fh8z/3Dv98iZosdpJMcZXfXyurn/823fa368ab874218fbb737aacac9b262/Screenshot_2025-07-28_at_3.59.01_PM.png)</Frame>
  </Tab>

  <Tab title="Management API">
    Management API で送信者拘束を有効にするには、[リソースサーバーの更新](https://auth0.com/docs/api/management/v2/resource-servers/patch-resource-servers-by-id) に PATCH リクエストを送信します。`proof_of_possession` オブジェクトのパラメーターを次のように設定します。

    | Parameter   | Description                                                                                                     |
    | ----------- | --------------------------------------------------------------------------------------------------------------- |
    | `mechanism` | 送信者拘束方式を設定します: `none`、`mtls`、または `dpop`。                                                                        |
    | `required`  | `true` に設定すると、この API 用にアプリケーションへ発行されるすべてのアクセストークンは、そのアプリケーションに拘束されます。`false` に設定すると、そのアプリケーションで送信者拘束は必須ではありません。 |

    次のコードサンプルは、リソースサーバーに mTLS の送信者拘束を設定するリクエストボディの例です。

    ```json lines
    "proof_of_possession": {
        "mechanism": "mtls",
        "required": true
      }
    ```
  </Tab>
</Tabs>