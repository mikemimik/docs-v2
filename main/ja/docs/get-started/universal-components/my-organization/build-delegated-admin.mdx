---
title: My Organization API を使用して委任管理インターフェースを構築する
description: Universal Components を使用して、Auth0 の My Organization API で委任管理インターフェースを構築する方法を説明します。
sidebarTitle: 委任管理インターフェースを構築する
---

Universal Components を使用すると、自社のアプリケーション内にセルフサービス型の管理ダッシュボードを構築できます。これにより、[Auth0 Organization](/ja/docs/manage-users/organizations) の管理者として委任された B2B 顧客は、Auth0 Dashboard へアクセスしたり、基本的な管理作業のためにサポートチームへ依頼したりすることなく、自社のブランディング、アイデンティティプロバイダー、検証済みドメインを管理できます。

<div id="how-it-works">
  ## 仕組み
</div>

Universal Components は、ユーザースコープのトークンを使用する [My Organization API](/ja/docs/api/myorganization) を利用して、[セルフサービス SSO 管理ダッシュボード](/ja/docs/authenticate/enterprise-connections/self-service-enterprise-configuration) を作成します。管理者がアプリケーションに埋め込まれたダッシュボードにログインすると、Auth0 SDK は、管理者権限に必要な organization 固有のスコープを含むアクセストークンを取得します。コンポーネントはこのトークンを使って、ログインしているユーザーとして API を呼び出します。これにより、委任された管理者は、自分が所属する特定の Auth0 Organization のみを変更できます。

<div id="available-components">
  ### 利用可能なコンポーネント
</div>

各コンポーネントは、管理タスクを実行するために [My Organization API](/ja/docs/api/myorganization) の特定のエンドポイントに対応付けられています。

| **コンポーネント**                                                                                                           | **APIエンドポイント**                        |
| :-------------------------------------------------------------------------------------------------------------------- | :------------------------------------ |
| **OrganizationDetailsEdit** — 組織名、表示名、ブランドカラー、ロゴを編集します。                                                               | `/my-org/details`                     |
| **SsoProviderTable** — SSOアイデンティティプロバイダーの一覧表示、有効化、無効化、削除、関連付けの解除を行います。                                                | `/my-org/identity-providers`          |
| **SsoProviderCreate** — SSOプロバイダー (Okta、ADFS、SAML、OIDC、Google Workspace、Azure AD、Ping Federate) を作成するための複数ステップのウィザード。 | `/my-org/identity-providers`          |
| **SsoProviderEdit** — 認証設定の構成、プロビジョニングの有効化、SCIMトークンとドメインの関連付けの管理を行います。                                                | `/my-org/identity-providers/{idp_id}` |
| **DomainTable** — ドメインの作成、検証、削除、アイデンティティプロバイダーへの関連付けを行います。                                                            | `/my-org/domains`                     |

<div id="configure-auth0-dashboard">
  ## Auth0 Dashboard を設定する
</div>

My Organization コンポーネントを使用する前に、適切な [API](/ja/docs/api)、アプリケーション、[スコープ](/ja/docs/api/myorganization) を設定して、Auth0 テナントを構成しておく必要があります。

<Tabs>
  <Tab title="React">
    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      **Auth0 Dashboard の設定** - My
      Organization コンポーネントを使用する前に、以下の手順をすべて完了してください。
    </Callout>

    ## **アプリケーションの作成**

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      この例では、シングルページアプリケーション (SPA) を使用します。サーバーサイドレンダリングの React フレームワークについては、Next.js タブを参照してください。
    </Callout>

    1. [**Auth0 Dashboard &gt; アプリケーション &gt; アプリケーション**](https://manage.auth0.com/#/applications) を開き、**アプリケーションを作成** を選択します。

    2. **シングルページ Web アプリケーション** を選択します。

    3. **Settings**タブで、次の項目に `http://localhost:5173` を追加します。
       <br />A. Allowed Callback URLs
       <br />B. Allowed Logout URLs

    4. **Login Experience** で、次の項目を選択します。
       <br />A. Business users。
       <br />B.  (任意) Prompt for Organization。

    ## **My Organization APIを有効にする**

    1. [**Auth0 Dashboard &gt; アプリケーション &gt; APIs**](https://manage.auth0.com/#/applications) を開きます。
    2. **[My Organization API](/ja/docs/api/myorganization)** を選択し、その API がテナントで有効になっていることを確認します。

    ## **アプリケーションアクセスの設定**

    1. **Application Access** タブに移動します。
    2. 使用するアプリケーションの **Edit** を選択します。
    3. 次の設定を行います。
       <br />A. [接続プロファイル](/ja/docs/authenticate/enterprise-connections/connection-profile#connection-profile): 接続属性のマッピングを含むプロファイルを選択または作成します。
       <br />B. [ユーザー属性プロファイル](/ja/docs/authenticate/enterprise-connections/user-attribute-profile#user-attribute-profile): ユーザー属性のマッピングを含むプロファイルを選択または作成します。
       <br />C. サポートされている[IDプロバイダー](/ja/docs/authenticate/identity-providers): 顧客が利用できるプロバイダーを有効にします。
       <br />D. 接続削除時の動作: **Allow** または **Allow if Empty** を選択します。
       <br />    • **Allow**: ユーザーは接続を削除でき、その接続に含まれるすべてのユーザーも削除されます。
       <br />    • **Allow if Empty**: ユーザーが削除できるのは、ユーザーが存在しない接続のみです。
       <br />E. ユーザーアクセスの認可設定: **Unauthorized**、**Authorized**、または **All** を選択します。
       <br />    • **Unauthorized**: 権限は一切許可されません。
       <br />    • **Authorized**: 特定の権限を選択します。
       <br />    • **All**: 既存および今後追加されるすべての権限を含めます。
       <br />F. クライアント資格情報によるアクセスの認可設定: **Unauthorized**、**Authorized**、または **All** を選択します。
    4. **Save** を選択します。

    ## **データベースとユーザーのセットアップ**

    1. データベース接続を作成するには、[**Auth0 Dashboard &gt; 認証 &gt; Database**](https://manage.auth0.com/#/connections/database) に移動します。
    2. 接続の **アプリケーション** タブを選択し、新しく作成した SPA アプリケーションを有効化します。
    3. 初期テスト用に、このデータベースにテストユーザーを作成します。

    ## **ロールの設定**

    1. ロールを作成します (例: &quot;組織管理者&quot;) 。
    2. My Organization API の必要なスコープをロールに追加します。

    <Accordion title="必要なスコープ">
      ```
          read:my_org:details
          update:my_org:details
          create:my_org:identity_providers
          read:my_org:identity_providers
          update:my_org:identity_providers
          delete:my_org:identity_providers
          update:my_org:identity_providers_detach
          create:my_org:identity_providers_domains
          delete:my_org:identity_providers_domains
          read:my_org:domains
          delete:my_org:domains
          create:my_org:domains
          update:my_org:domains
          read:my_org:identity_providers_scim_tokens
          create:my_org:identity_providers_scim_tokens
          delete:my_org:identity_providers_scim_tokens
          create:my_org:identity_providers_provisioning
          read:my_org:identity_providers_provisioning
          delete:my_org:identity_providers_provisioning
          read:my_org:configuration
      ```
    </Accordion>

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      ユーザーのトークンには、割り当てられたロールと前の手順で設定した User Access Authorization の設定の両方に共通する権限のみが含まれます。
    </Callout>

    ## **組織を作成する**

    1. [**Organizations &gt; 組織を作成**](https://manage.auth0.com/#/organizations) に移動します。
    2. Auth0 Organizationを設定します:
       <br />A. **Members**: テストユーザーを追加し、「Organization Admin」ロールを割り当てます。
       <br />B. **Connections**: データベース接続を有効にします。

    ## **環境変数の設定**

    1. React プロジェクトのルートに `.env` ファイルを作成します:

    ```bash
    VITE_AUTH0_DOMAIN=your-domain.auth0.com
      VITE_AUTH0_CLIENT_ID=your-spa-client-id
    ```

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      お客様は、My Organization API および Embeddable UI Components の利用が、お客様のセキュリティポリシーおよび適用される
      法令 (エンドユーザーに付与した権限を含みます) に準拠するよう確保する責任を負います。
    </Callout>

    <Check>
      ユーザーは認証済みで、Auth0 Organization のメンバーである必要があります。Components は現在のユーザーコンテキストから自動的に組織データを読み込みます。詳しくは、インストール方法と `Auth0ComponentProvider` の設定について [Auth0 Universal Components](/ja/docs/get-started/universal-components/universal-components-overview) をご覧ください
    </Check>
  </Tab>

  <Tab title="Next.js">
    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      **Auth0 Dashboard の設定** - My Organization コンポーネントを使用する前に、以下の手順をすべて完了してください。
    </Callout>

    ## **アプリケーションの作成**

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      この例では、Next.js 用の Regular Web Application を使用します。Single Page Application も、同様の手順で設定できます。
    </Callout>

    1. [**Auth0 Dashboard &gt; アプリケーション &gt; アプリケーション**](https://manage.auth0.com/#/applications) を開き、**Create Application** を選択します。

    2. **Regular Web Application** を選択します。

    3. **Settings** タブで、次の項目に `http://localhost:5173` を追加します。
       <br />A. Allowed Callback URLs
       <br />B. Allowed Logout URLs

    4. **Login Experience** で、次を選択します。
       <br />A. ビジネスユーザー。
       <br />B.  (任意) 組織の入力を求める。

    ## **My Organization APIを有効にする**

    1. [**Auth0 Dashboard &gt; アプリケーション &gt; APIs**](https://manage.auth0.com/#/applications) に移動します。
    2. **[My Organization API](/ja/docs/api/myorganization)** を選択し、ご利用のテナントで有効になっていることを確認します。

    ## **アプリケーションアクセスの設定**

    1. **Application Access** タブを開きます。
    2. 使用するアプリケーションの **Edit** を選択します。
    3. 以下の設定を行います：
       <br />A. [接続プロファイル](/ja/docs/authenticate/enterprise-connections/connection-profile#connection-profile): 接続属性のマッピングが設定されたプロファイルを選択または作成します。
       <br />B. [ユーザー属性プロファイル](/ja/docs/authenticate/enterprise-connections/user-attribute-profile#user-attribute-profile): ユーザー属性のマッピングが設定されたプロファイルを選択または作成します。
       <br />C. サポートされている[IDプロバイダー](/ja/docs/authenticate/identity-providers): 顧客が利用できるプロバイダーを有効にします。
       <br />D. 接続の削除時の動作: **Allow** または **Allow if Empty** を選択します。
       <br />    • **Allow**: ユーザーは接続を削除でき、その接続内のすべてのユーザーも削除されます。
       <br />    • **Allow if Empty**: ユーザーが存在しない接続のみ削除できます。
       <br />E. ユーザーアクセスの認可: **Unauthorized**、**Authorized**、または **All** を選択します。
       <br />    • **Unauthorized**: 権限は一切付与されません。
       <br />    • **Authorized**: 特定の権限を選択します。
       <br />    • **All**: 現在および今後追加されるすべての権限を含みます。
       <br />F. クライアント資格情報アクセスの認可: **Unauthorized**、**Authorized**、または **All** を選択します。
    4. **保存** を選択します。

    ## **データベースとユーザーのセットアップ**

    1. データベース接続を作成するには、[**Auth0 Dashboard &gt; 認証 &gt; Database**](https://manage.auth0.com/#/connections/database) に移動します。
    2. 接続の**アプリケーション**タブを選択し、新しい SPA アプリケーションを有効にします。
    3. 最初のテスト用に、このデータベースにテストユーザーを作成します。

    ## **ロールの設定**

    1. ロール (例: &quot;Organization Admin&quot;) を作成します。
    2. My Organization API の必要なスコープをロールに追加します。

    <Accordion title="必要なスコープ">
      ```
          read:my_org:details
          update:my_org:details
          create:my_org:identity_providers
          read:my_org:identity_providers
          update:my_org:identity_providers
          delete:my_org:identity_providers
          update:my_org:identity_providers_detach
          create:my_org:identity_providers_domains
          delete:my_org:identity_providers_domains
          read:my_org:domains
          delete:my_org:domains
          create:my_org:domains
          update:my_org:domains
          read:my_org:identity_providers_scim_tokens
          create:my_org:identity_providers_scim_tokens
          delete:my_org:identity_providers_scim_tokens
          create:my_org:identity_providers_provisioning
          read:my_org:identity_providers_provisioning
          delete:my_org:identity_providers_provisioning
          read:my_org:configuration
      ```
    </Accordion>

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      ユーザーの`token`には、割り当てられた`role`と、前の手順で設定した User Access Authorization の設定の両方に含まれる権限のみが含まれます。
    </Callout>

    ## **組織を作成する**

    1. [**Organizations &gt; 組織を作成**](https://manage.auth0.com/#/organizations) を開きます。
    2. Auth0 Organization を設定します:
       <br />A. **Members** では、テストユーザーを追加し、&quot;Organization Admin&quot; ロールを割り当てます。
       <br />B. **Connections** では、データベース接続を有効にします。

    ## **環境変数の設定**

    1. React プロジェクトのルートディレクトリに `.env` ファイルを作成します:

    ```bash
    NEXT_PUBLIC_AUTH0_DOMAIN=your-domain.auth0.com
    NEXT_PUBLIC_AUTH0_CLIENT_ID=your-client-id
    AUTH0_SECRET=your-secret
    AUTH0_ISSUER_BASE_URL=https://your-domain.auth0.com
    ```

    @auth0/nextjs-auth0 SDKを使用したNext.jsの場合は、[Auth0 Next.js SDKドキュメント](https://auth0.com/docs/quickstart/webapp/nextjs)を参照して、完全なセットアップ手順を確認してください。

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      お客様は、My Organization API および Embeddable UI Components の利用が、自社のセキュリティポリシーおよびエンドユーザーに付与されるあらゆる権限を含む適用
      法令に準拠していることを確保する責任を負います。
    </Callout>

    <Check>
      ユーザーは認証されており、Auth0 Organization のメンバーである必要があります。コンポーネントは、現在のユーザーコンテキストから組織データを自動的に読み込みます。詳しくは、インストール方法と `Auth0ComponentProvider` の設定について [Auth0 Universal Components](/ja/docs/get-started/universal-components/universal-components-overview) を参照してください
    </Check>
  </Tab>

  <Tab title="shadcn">
    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      **Auth0 Dashboard の設定** - My Organization コンポーネントを使用する前に、以下の手順をすべて完了してください。
    </Callout>

    ## **アプリケーションの作成**

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      この例では Single Page Application を使用していますが、同様の手順で Regular Web Application (RWA) を設定することも可能です。
    </Callout>

    1. [**Auth0 Dashboard &gt; アプリケーション &gt; アプリケーション**](https://manage.auth0.com/#/applications) を開き、**アプリケーションを作成** を選択します。

    2. **シングルページ Web アプリケーション** を選択します。

    3. **Settings** タブで、以下のフィールドに `http://localhost:5173` を追加します。
       <br />A. Allowed Callback URLs
       <br />B. Allowed Logout URLs

    4. **ログイン体験** で、以下を選択します:
       <br />A. ビジネスユーザー。
       <br />B.  (任意) 組織の入力を求める。

    ## **My Organization APIを有効にする**

    1. [**Auth0 Dashboard &gt; アプリケーション &gt; APIs**](https://manage.auth0.com/#/applications) を開きます。
    2. **[My Organization API](/ja/docs/api/myorganization)** を選択し、お使いのテナントで有効になっていることを確認します。

    ## **アプリケーションアクセスの設定**

    1. **Application Access** タブを開きます。
    2. 使用するアプリケーションの **Edit** を選択します。
    3. 次の設定を行います。
       <br />A. [接続プロファイル](/ja/docs/authenticate/enterprise-connections/connection-profile#connection-profile): 接続属性のマッピングを含むプロファイルを選択または作成します。
       <br />B. [ユーザー属性プロファイル](/ja/docs/authenticate/enterprise-connections/user-attribute-profile#user-attribute-profile): ユーザー属性のマッピングを含むプロファイルを選択または作成します。
       <br />C. サポートされている[IDプロバイダー](/ja/docs/authenticate/identity-providers): 顧客が使用できるプロバイダーを有効にします。
       <br />D. 接続の削除時の動作: **Allow** または **Allow if Empty** を選択します。
       <br />    • **Allow**: ユーザーは接続を削除でき、その接続内のすべてのユーザーも削除されます。
       <br />    • **Allow if Empty**: ユーザーが存在しない接続のみ削除できます。
       <br />E. ユーザーアクセスの認可: **Unauthorized**、**Authorized**、または **All** を選択します。
       <br />    • **Unauthorized**: 権限は付与されません。
       <br />    • **Authorized**: 特定の権限を選択します。
       <br />    • **All**: 既存および今後追加されるすべての権限を含めます。
       <br />F. クライアント認証情報アクセスの認可: **Unauthorized**、**Authorized**、または **All** を選択します。
    4. **Save** を選択します。

    ## **データベースとユーザーのセットアップ**

    1. データベース接続を作成するには、[**Auth0 Dashboard &gt; 認証 &gt; Database**](https://manage.auth0.com/#/connections/database) に移動します。
    2. 接続の **アプリケーション** タブを選択し、新しい SPA アプリケーションを有効化します。
    3. 初期テスト用に、このデータベースにテストユーザーを作成します。

    ## **ロールの設定**

    1. ロールを作成します (例: &quot;組織管理者&quot;) 。
    2. My Organization API に必要なスコープをそのロールに追加します。

    <Accordion title="必要なスコープ">
      ```
          read:my_org:details
          update:my_org:details
          create:my_org:identity_providers
          read:my_org:identity_providers
          update:my_org:identity_providers
          delete:my_org:identity_providers
          update:my_org:identity_providers_detach
          create:my_org:identity_providers_domains
          delete:my_org:identity_providers_domains
          read:my_org:domains
          delete:my_org:domains
          create:my_org:domains
          update:my_org:domains
          read:my_org:identity_providers_scim_tokens
          create:my_org:identity_providers_scim_tokens
          delete:my_org:identity_providers_scim_tokens
          create:my_org:identity_providers_provisioning
          read:my_org:identity_providers_provisioning
          delete:my_org:identity_providers_provisioning
          read:my_org:configuration
      ```
    </Accordion>

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      ユーザーのトークンには、割り当てられたロールと、前の手順で設定した User Access Authorization の設定の両方に含まれる権限のみが含まれます。
    </Callout>

    ## **組織を作成する**

    1. [**Organizations &gt; Create Organization**](https://manage.auth0.com/#/organizations) に移動します。
    2. Auth0 Organization を設定します。
       <br />A. **Members**: テストユーザーを追加し、「Organization Admin」ロールを割り当てます。
       <br />B. **Connections**: データベース接続を有効にします。

    ## **環境変数の設定**

    <Warning>
      環境変数の設定は、フレームワークや構成によって異なります。具体的な実装例については、リポジトリ内の`examples/`ディレクトリを参照してください。
    </Warning>

    Reactプロジェクトのルートに`.env`ファイルを作成します：

    ```bash
    VITE_AUTH0_DOMAIN=your-domain.auth0.com
    VITE_AUTH0_CLIENT_ID=your-spa-client-id
    ```

    <Warning>
      お客様は、My Organization API および Embeddable UI Components の利用が、エンドユーザーに付与した権限を含め、ご自身のセキュリティポリシーおよび適用される法令に準拠していることを確保する責任を負います。
    </Warning>

    <Callout icon="file-lines" color="#0EA5E9" iconType="regular">
      ユーザーは認証されており、Auth0 Organization のメンバーである必要があります。コンポーネントは、現在のユーザーコンテキストから organization データを自動的に読み込みます。詳しくは、インストール方法と `Auth0ComponentProvider` の設定について [Auth0 Universal Components](/ja/docs/get-started/universal-components/universal-components-overview) を参照してください
    </Callout>
  </Tab>
</Tabs>