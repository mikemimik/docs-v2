---
description: Auth0 App for Splunk を使用して、テナントのトラフィックを可視化する Splunk の Auth0 ダッシュボードを作成する方法を説明します。
title: Auth0 App for Splunk を使用する
---

<Card title="始める前に">
  [Splunk へのログストリーミングを設定する](https://marketplace.auth0.com/integrations/splunk-log-streaming)
</Card>

**Auth0 App for Splunk** を使用すると、Auth0 テナントのデータを Splunk ダッシュボードで可視化できます。このダッシュボードでは、テナントのログイントラフィックの稼働状況を監視できます。また、このアプリでは、Auth0 が推奨する集計をそのまま使用することも、それらを出発点として独自のカスタム可視化を作成することもできます。

<div id="install-the-auth0-app-for-splunk">
  ## Auth0 App for Splunk をインストールする
</div>

1. [Splunk](https://www.splunk.com/) にログインします。
2. [Auth0 App for Splunk](https://splunkbase.splunk.com/app/5193) のアプリページを開きます。
3. 緑色の **Install** ボタンを選択します。
4. ユーザー名とパスワードを入力し、利用規約に同意して、**Login and Install** を選択します。
5. 表示された **Complete** ウィジェットで、**Open the App** ボタンを選択します。
   デフォルトのフィルターが適用された Auth0 Dashboard が表示されます。

<div id="dashboard-filters">
  ## ダッシュボードのフィルター
</div>

以下のフィルターを使用すると、トラフィックの詳細を絞り込めます。各フィールドのすべての値を対象に検索するには、`*` を入力します。

| Filter      | Description                                                                                                  |
| ----------- | ------------------------------------------------------------------------------------------------------------ |
| Time Range  | イベントを表示する期間を選択できる Splunk の時間入力要素です。                                                                          |
| Index       | 検索対象の Splunk `index` です。`Http Event Collector Token` の作成時に特定のインデックスを指定している場合は、ドロップダウンからその値を選択できます。           |
| HTTP Source | Splunk の `source` 名です。                                                                                       |
| IP          | 確認したいトラフィックの IP アドレスです。ログイベント内の `data.ip` に対応します。                                                            |
| Client      | 確認したいトラフィックのクライアントです。ログイベント内の `data.client_name` に対応します。                                                     |
| Country     | 確認したいトラフィックの国です。これは、クエリ内で `iplocation data.ip` 検索コマンドを使用して取得されるフィールドです。その結果、各ログイベントに `Country` フィールドが追加されます。 |
| Username    | 確認したいログイントラフィックのメールアドレスです。ログイベント内の `data.user_name` に対応します。                                                  |

<div id="customize-dashboard">
  ## ダッシュボードをカスタマイズする
</div>

Splunk Auth0 セキュリティダッシュボードは、カスタムデータウィジェットを追加できるようにカスタマイズできます。

1. Auth0 セキュリティダッシュボードに移動し、右上にある **edit** を選択します。
2. Edit Dashboard パネルで **add panel** を選択し、コンテンツタイプを選びます。たとえば、折れ線グラフ、イベント、エリアチャートなどです。
3. データの可視化に必要な期間、コンテンツタイトル、検索文字列を入力します。
4. **Add to dashboard** を選択して、新しいデータウィジェットを追加します。

<div id="learn-more">
  ## 詳細はこちら
</div>

* [攻撃対策のログイベントを表示する](/ja/docs/secure/attack-protection/view-attack-protection-events)