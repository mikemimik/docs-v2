---
#
# このファイルは自動生成されています。
# 手動で編集しないでください。
#
description: post-login Action トリガーのイベントオブジェクトについて説明します。このオブジェクトは、トリガー実行時のコンテキスト情報を提供します。
title: 'Actions Triggers: post-login - イベントオブジェクト'
validatedOn: 2026-05-25
---

post-login Actions トリガー用の `event` オブジェクトは、トリガー実行時のコンテキスト情報を提供します。

<div id="eventauthentication">
  ## `event.authentication`
</div>

<ResponseField name="event.authentication" type="dictionary" post={["optional"]}>
  ログインフロー中に取得された認証シグナルの詳細。

  <Expandable title="認証のプロパティ" defaultOpen>
    <ResponseField name="認証方式" type="array of objects">
      ユーザーがセッション中に完了した認証方法が含まれます。

      配列の要素には、次のいずれかのスキーマを指定できます。

      <Tabs>
        <Tab title="第1認証要素">
          <ResponseField name="name" type="string">
            完了した第1認証要素の名前です。指定できる値は次のとおりです。

            * `federated` ソーシャル接続またはエンタープライズ接続を使用して、ユーザーが第1認証要素として認証されたことを示します。
            * `pwd` パスワードを使用して、データベース接続のユーザーが第1認証要素として認証されたことを示します。
            * `passkey` パスキーを使用して、データベース接続のユーザーが第1認証要素として認証されたことを示します。
            * `sms` Passwordless SMS 接続を使用して、ユーザーが第1認証要素として認証されたことを示します。
            * `email` Passwordless Email 接続を使用して、ユーザーが第1認証要素として認証された、またはパスワードリセットのためにメールアドレスが確認されたことを示します。
            * `phone_number` パスワードリセットに電話番号が使用されたことを示します。
            * `mock` 内部テストで使用されます。
            * カスタム認証方法を示す URL (第2以降の認証要素) である場合もあります。
          </ResponseField>

          <ResponseField name="timestamp" type="string" />
        </Tab>

        <Tab title="多要素認証">
          <ResponseField name="name" type="string">
            ユーザーが多要素認証 (第2以降の認証要素) を完了したことを示します。

            使用可能な値: `mfa`
          </ResponseField>

          <ResponseField name="timestamp" type="string" />
        </Tab>
      </Tabs>
    </ResponseField>

    <ResponseField name="リスク評価" type="dictionary" post={["optional"]}>
      ログインまたはパスワードリセットフロー中に取得したリスク評価の詳細。

      <Expandable title="riskAssessment プロパティ" defaultOpen>
        <ResponseField name="評価" type="object">
          <Expandable title="評価プロパティ">
            <ResponseField name="ImpossibleTravel" type="object" post={["optional"]}>
              ユーザーが、通常ではありえない移動を示す場所からログインしているかどうかを判定します。

              <Expandable title="ImpossibleTravel プロパティ">
                <ResponseField name="code" type="string">
                  可能な値:

                  * `minimal_travel_from_last_login`
                  * `travel_from_last_login`
                  * `substantial_travel_from_last_login`
                  * `impossible_travel_from_last_login`
                  * `invalid_travel`
                  * `missing_geoip`
                  * `anonymous_proxy`
                  * `unknown_location`
                  * `initial_login`
                  * `location_history_not_found`
                  * `assessment_not_available`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  使用可能な値: `low`, `medium`, `high`, `neutral`
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="NewDevice" type="object" post={["optional"]}>
              ユーザーが既知のデバイスからログインしているかどうかを判定します。

              <Expandable title="NewDevice プロパティ">
                <ResponseField name="code" type="string">
                  可能な値:

                  * `initial_login`
                  * `assessment_not_available`
                  * `match`
                  * `partial_match`
                  * `no_match`
                  * `unknown_device`
                  * `no_device_history`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  使用可能な値: `low`, `medium`, `high`, `neutral`
                </ResponseField>

                <ResponseField name="details" type="object" post={["optional"]}>
                  <Expandable title="details プロパティ">
                    <ResponseField name="device" type="string" post={["optional"]}>
                      使用可能な値: `known`, `unknown`
                    </ResponseField>

                    <ResponseField name="useragent" type="string" post={["optional"]}>
                      使用可能な値: `known`, `unknown`
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="UntrustedIP" type="object" post={["optional"]}>
              IP が Auth0 の低評価 IP リポジトリで見つかったかどうかを示します。

              <Expandable title="UntrustedIP プロパティ">
                <ResponseField name="code" type="string">
                  使用可能な値: `assessment_not_available`, `not_found_on_deny_list`, `found_on_deny_list`, `invalid_ip_address`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  使用可能な値: `low`, `medium`, `high`, `neutral`
                </ResponseField>

                <ResponseField name="details" type="object" post={["optional"]}>
                  <Expandable title="details プロパティ">
                    <ResponseField name="category" type="string" post={["optional"]} />

                    <ResponseField name="ip" type="string" post={["optional"]}>
                      リクエストの送信元 IP アドレスです。
                    </ResponseField>

                    <ResponseField name="matches" type="string" post={["optional"]} />

                    <ResponseField name="source" type="string" post={["optional"]} />
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="信頼度" type="string">
          総合リスクスコア

          指定可能な値: `low`, `medium`, `high`, `neutral`
        </ResponseField>

        <ResponseField name="補足" type="dictionary" post={["optional"]}>
          &#91;早期アクセス&#93; 補足的なリスク評価。

          <Expandable title="補足プロパティ">
            <ResponseField name="akamai" type="辞書" post={["optional"]}>
              <Expandable title="akamai プロパティ">
                <ResponseField name="akamaiBot" type="オブジェクト" post={["optional"]}>
                  Akamai Bot Manager から転送されたボット検出結果。

                  <Expandable title="akamaiBot プロパティ">
                    <ResponseField name="type" type="文字列" post={["optional"]}>
                      Akamai Bot Manager の結果のタイプ。
                    </ResponseField>

                    <ResponseField name="action" type="文字列" post={["optional"]}>
                      Akamai Bot Manager の結果のアクション。
                    </ResponseField>

                    <ResponseField name="botCategory" type="文字列の配列" post={["optional"]}>
                      Akamai Bot Manager の結果のボットカテゴリ。
                    </ResponseField>

                    <ResponseField name="botScore" type="数値" post={["optional"]}>
                      Akamai Bot Manager の結果のボットスコア。
                    </ResponseField>

                    <ResponseField name="botScoreResponseSegment" type="文字列" post={["optional"]}>
                      Akamai Bot Manager の結果のボットスコア応答セグメント。
                    </ResponseField>

                    <ResponseField name="botnetId" type="文字列" post={["optional"]}>
                      Akamai Bot Manager の結果のボットネット ID。
                    </ResponseField>
                  </Expandable>
                </ResponseField>

                <ResponseField name="akamaiUserRisk" type="辞書" post={["optional"]}>
                  Akamai Account Protector から転送されたユーザーリスク検出結果。

                  <Expandable title="akamaiUserRisk プロパティ">
                    <ResponseField name="action" type="文字列" post={["optional"]}>
                      Akamai ユーザーリスク評価のアクション。
                    </ResponseField>

                    <ResponseField name="allow" type="数値" post={["optional"]}>
                      Akamai ユーザーリスク評価の許可ステータス。
                    </ResponseField>

                    <ResponseField name="emailDomain" type="文字列" post={["optional"]}>
                      ユーザーのメールアドレスのドメイン。
                    </ResponseField>

                    <ResponseField name="general" type="辞書" post={["optional"]}>
                      Akamai ユーザーリスク評価における全般的なリスク。
                    </ResponseField>

                    <ResponseField name="ouid" type="文字列" post={["optional"]}>
                      ユーザーの OUID。
                    </ResponseField>

                    <ResponseField name="requestid" type="文字列" post={["optional"]}>
                      ユーザーのリクエスト ID。
                    </ResponseField>

                    <ResponseField name="risk" type="辞書" post={["optional"]}>
                      Akamai ユーザーリスク評価のリスク。
                    </ResponseField>

                    <ResponseField name="score" type="数値" post={["optional"]}>
                      Akamai ユーザーリスク評価のスコア。
                    </ResponseField>

                    <ResponseField name="status" type="数値" post={["optional"]}>
                      Akamai ユーザーリスク評価のステータス。
                    </ResponseField>

                    <ResponseField name="trust" type="辞書" post={["optional"]}>
                      Akamai ユーザーリスク評価の信頼度。
                    </ResponseField>

                    <ResponseField name="username" type="文字列" post={["optional"]}>
                      ユーザーのユーザー名。
                    </ResponseField>

                    <ResponseField name="uuid" type="文字列" post={["optional"]}>
                      Akamai ユーザーリスク評価の UUID。
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="バージョン" type="string" />
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventauthorization">
  ## `event.authorization`
</div>

<ResponseField name="event.authorization" type="object" post={["optional"]}>
  ログインしているユーザーに付与された認可に関する情報を含むオブジェクトです。

  <Expandable title="authorization のプロパティ" defaultOpen>
    <ResponseField name="roles" type="array of strings">
      ユーザーに割り当てられているロール名を含む配列です。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventclient">
  ## `event.client`
</div>

<ResponseField name="event.client" type="dictionary">
  このログイントランザクションの開始元となったクライアントに関する情報です。

  <Expandable title="クライアントのプロパティ" defaultOpen>
    <ResponseField name="client_id" type="string">
      ユーザーのログイン先アプリケーションのクライアント ID です。
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      アプリケーションのその他のプロパティを保持するためのオブジェクトです。
    </ResponseField>

    <ResponseField name="name" type="string">
      アプリケーションの名前です (Auth0 Dashboard で定義) 。
    </ResponseField>

    <ResponseField name="refresh_token" type="object" post={["optional"]}>
      &#91;早期アクセス&#93; リフレッシュトークンの設定プロパティを保持するためのオブジェクトです。

      <Expandable title="refresh_token のプロパティ" defaultOpen>
        <ResponseField name="policies" type="array of objects" post={["optional"]}>
          &#91;早期アクセス&#93; マルチリソース リフレッシュトークン交換 (MRRT) を制御するポリシーのコレクションで、異なるリソースサーバー間でリフレッシュトークンをどのように使用できるかを定義します。

          <Expandable title="policies のプロパティ">
            <ResponseField name="audience" type="string" post={["optional"]}>
              &#91;早期アクセス&#93; この MRRT ポリシーが適用される特定のリソースサーバー (audience) です。
            </ResponseField>

            <ResponseField name="scope" type="array of strings" post={["optional"]}>
              リソースサーバー (audience) に対して認可されるアクセススコープです。
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventconnection">
  ## `event.connection`
</div>

<ResponseField name="event.connection" type="dictionary">
  ユーザーの認証に使用された接続の詳細です。

  <Expandable title="connection properties" defaultOpen>
    <ResponseField name="id" type="string">
      接続の一意の識別子です。
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      接続に関連付けられたメタデータです。
    </ResponseField>

    <ResponseField name="name" type="string">
      ユーザーの認証に使用された接続の名前です (`twitter` や `some-g-suite-domain` など) 。
    </ResponseField>

    <ResponseField name="strategy" type="string">
      接続の種類です。ソーシャル接続では、`event.connection.strategy === event.connection.name` となります。エンタープライズ接続では、strategy は `waad` (Windows Azure AD) 、`ad` (Active Directory/LDAP) 、`auth0` (データベース接続) などになります。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventcustom_domain">
  ## `event.custom_domain`
</div>

<ResponseField name="event.custom_domain" type="dictionary" post={["optional"]}>
  現在のトランザクションに関連付けられているカスタムドメインの詳細。

  <Expandable title="カスタムドメインのプロパティ" defaultOpen>
    <ResponseField name="domain" type="string">
      カスタムドメイン名。
    </ResponseField>

    <ResponseField name="domain_metadata" type="dictionary">
      キーと値のペア形式のカスタムドメインのメタデータ。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventorganization">
  ## `event.organization`
</div>

<ResponseField name="event.organization" type="dictionary" post={["optional"]}>
  現在のトランザクションに関連する組織の詳細。

  <Expandable title="organization のプロパティ" defaultOpen>
    <ResponseField name="id" type="string">
      組織ID。
    </ResponseField>

    <ResponseField name="display_name" type="string">
      組織の表示名。
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      組織に関連付けられたメタデータ。
    </ResponseField>

    <ResponseField name="name" type="string">
      組織名。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventprompt">
  ## `event.prompt`
</div>

<ResponseField name="event.prompt" type="dictionary" post={["optional"]}>
  レンダリングされたカスタムプロンプトで収集されたデータ。

  <Expandable title="プロンプトのプロパティ" defaultOpen>
    <ResponseField name="id" type="string">
      プロンプトのID。
    </ResponseField>

    <ResponseField name="fields" type="dictionary" post={["optional"]}>
      フィールドと非表示フィールドのデータ。
    </ResponseField>

    <ResponseField name="vars" type="dictionary" post={["optional"]}>
      共有変数のデータ。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrefresh_token">
  ## `event.refresh_token`
</div>

<ResponseField name="event.refresh_token" type="dictionary" post={["optional"]}>
  &#91;Enterprise のお客様&#93; 現在のリフレッシュトークン。

  <Expandable title="refresh_token プロパティ" defaultOpen>
    <ResponseField name="id" type="string">
      &#91;Enterprise のお客様&#93; リフレッシュトークンの ID。
    </ResponseField>

    <ResponseField name="client_id" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンに関連付けられたクライアントの ID。
    </ResponseField>

    <ResponseField name="created_at" type="string">
      &#91;Enterprise のお客様&#93; リフレッシュトークンが作成された日時のタイムスタンプ。
    </ResponseField>

    <ResponseField name="device" type="object" post={["optional"]}>
      <Expandable title="device プロパティ" defaultOpen>
        <ResponseField name="initial_asn" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このリフレッシュトークンに最初に関連付けられた自律システム番号。
        </ResponseField>

        <ResponseField name="initial_ip" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このリフレッシュトークンに最初に関連付けられた IP アドレス。
        </ResponseField>

        <ResponseField name="initial_user_agent" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このリフレッシュトークンに関連付けられたデバイスの最初のユーザーエージェント。
        </ResponseField>

        <ResponseField name="last_asn" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このリフレッシュトークンが最後に交換された際の自律システム番号。
        </ResponseField>

        <ResponseField name="last_ip" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このリフレッシュトークンが最後に交換された際の IP アドレス。
        </ResponseField>

        <ResponseField name="last_user_agent" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このリフレッシュトークンが最後に交換された際のデバイスのユーザーエージェント。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="expires_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンが完全に失効する日時のタイムスタンプ。
    </ResponseField>

    <ResponseField name="idle_expires_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンがアイドル状態により失効する日時のタイムスタンプ。
    </ResponseField>

    <ResponseField name="last_exchanged_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンが最後に正常に交換された日時のタイムスタンプ。
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      リフレッシュトークンのメタデータ
    </ResponseField>

    <ResponseField name="resource_servers" type="array of objects" post={["optional"]}>
      <Expandable title="resource_servers プロパティ" defaultOpen>
        <ResponseField name="audience" type="string">
          &#91;Enterprise のお客様&#93; リフレッシュトークンの audience。
        </ResponseField>

        <ResponseField name="scopes" type="string">
          &#91;Enterprise のお客様&#93; リフレッシュトークンのスコープ。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="rotating" type="boolean" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンがローテーション型かどうか。
    </ResponseField>

    <ResponseField name="session_id" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンに紐付けられたセッションの ID。
    </ResponseField>

    <ResponseField name="session_transfer" type="object" post={["optional"]}>
      &#91;Enterprise のお客様&#93; このオブジェクトは、セッションがセッション転送トークン (ネイティブからWebへのSSO) から作成された場合に定義され、それ以外の場合は未定義です。

      <Expandable title="session_transfer プロパティ" defaultOpen>
        <ResponseField name="parent_refresh_token" type="object" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このオブジェクトは、リフレッシュトークンがセッション転送 (ネイティブからWebへのSSO) の結果として開始されたセッションから作成された場合に定義され、それ以外の場合は未定義です。

          <Expandable title="parent_refresh_token プロパティ">
            <ResponseField name="id" type="string" post={["optional"]}>
              &#91;Enterprise のお客様&#93; このセッション / リフレッシュトークンがセッション転送 (ネイティブからWebへのSSO) の結果として作成される元になった、親リフレッシュトークンの ID。
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="user_id" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; リフレッシュトークンに紐付けられたユーザーの ID。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrequest">
  ## `event.request`
</div>

<ResponseField name="event.request" type="dictionary">
  トランザクションを開始したリクエストの詳細です。

  <Expandable title="リクエストのプロパティ" defaultOpen>
    <ResponseField name="asn" type="string" post={["optional"]}>
      リクエストを行うユーザーエージェントの ASN (自律システム番号) です。
    </ResponseField>

    <ResponseField name="body" type="dictionary">
      POST リクエストの本文です。このデータは、リフレッシュトークン、Client Credential Exchange フロー、および Post Login Action でのみ使用できます。
    </ResponseField>

    <ResponseField name="geoip" type="dictionary">
      <Expandable title="geoip のプロパティ" defaultOpen>
        <ResponseField name="cityName" type="string" post={["optional"]} />

        <ResponseField name="continentCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode3" type="string" post={["optional"]} />

        <ResponseField name="countryName" type="string" post={["optional"]} />

        <ResponseField name="latitude" type="number" post={["optional"]} />

        <ResponseField name="longitude" type="number" post={["optional"]} />

        <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

        <ResponseField name="subdivisionName" type="string" post={["optional"]} />

        <ResponseField name="timeZone" type="string" post={["optional"]} />
      </Expandable>
    </ResponseField>

    <ResponseField name="hostname" type="string" post={["optional"]}>
      認証フローで使用されるホスト名です。
    </ResponseField>

    <ResponseField name="ip" type="string">
      リクエストの送信元 IP アドレスです。
    </ResponseField>

    <ResponseField name="language" type="string" post={["optional"]}>
      ブラウザーが要求した言語です。
    </ResponseField>

    <ResponseField name="method" type="string">
      リクエストに使用される HTTP メソッドです
    </ResponseField>

    <ResponseField name="query" type="dictionary">
      認可リクエストに送信されるクエリ文字列パラメーターです。
    </ResponseField>

    <ResponseField name="user_agent" type="string" post={["optional"]}>
      トランザクションの開始時に受信した `User-Agent` ヘッダーの値です。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventresource_server">
  ## `event.resource_server`
</div>

<ResponseField name="event.resource_server" type="object" post={["optional"]}>
  アクセスの要求先であるリソースサーバーの詳細。

  <Expandable title="resource_server プロパティ" defaultOpen>
    <ResponseField name="identifier" type="string">
      リソースサーバーの識別子。例: `https://your-api.example.com`。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsecrets">
  ## `event.secrets`
</div>

<ResponseField name="event.secrets" type="secrets">
  このActionに安全に関連付けられているSecretの値です。
</ResponseField>

<div id="eventsecurity_context">
  ## `event.security_context`
</div>

<ResponseField name="event.security_context" type="object" post={["optional"]}>
  フィンガープリント署名を含むオブジェクトです。これは、クライアントが Cloudflare を使用している場合にのみ利用できます。JA3/JA4 フィンガープリントは、状況によっては null または空になることがあります。最も一般的なのは HTTP リクエストの場合で、JA3 と JA4 は TLS で計算されるためです。また、Worker が同じゾーン内、またはプロキシされていないゾーン (あるいはサードパーティ) にリクエストを送信している場合も、空になることがあります。

  <Expandable title="security_context のプロパティ" defaultOpen>
    <ResponseField name="ja3" type="string" post={["optional"]}>
      JA3 フィンガープリント署名です。これは、クライアントが TLS 接続を使用している場合にのみ利用できます。
    </ResponseField>

    <ResponseField name="ja4" type="string" post={["optional"]}>
      JA4 フィンガープリント署名です。これは、クライアントが TLS 接続を使用している場合にのみ利用できます。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsession">
  ## `event.session`
</div>

<ResponseField name="event.session" type="dictionary" post={["optional"]}>
  現在のログインセッション。

  <Expandable title="セッションのプロパティ" defaultOpen>
    <ResponseField name="id" type="string">
      現在のセッションのID。
    </ResponseField>

    <ResponseField name="authenticated_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションが最後に認証された日時。
    </ResponseField>

    <ResponseField name="クライアント" type="array of objects" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションのクライアント詳細一覧。

      <Expandable title="クライアントのプロパティ" defaultOpen>
        <ResponseField name="client_id" type="string">
          &#91;Enterprise のお客様&#93; セッションのクライアント ID。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="Cookie" type="object" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションの Cookie 設定です。これにより、ユーザーエージェントがセッション Cookie をどのように扱うかが決まります。

      <Expandable title="Cookie のプロパティ" defaultOpen>
        <ResponseField name="mode" type="string">
          &#91;Enterprise のお客様&#93; セッション Cookie の永続化モードです。&#39;non-persistent&#39; (一時的) に設定すると、ブラウザーを閉じたときに Cookie は削除されます。&#39;persistent&#39; に設定すると、Cookie は有効期限が切れるか、ユーザーが削除するまで保存されます。

          指定可能な値: `persistent`, `non-persistent`
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="created_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションの作成日時。
    </ResponseField>

    <ResponseField name="デバイス" type="object" post={["optional"]}>
      &#91;Enterprise のお客様&#93; このセッションで使用されたデバイスに関するメタデータ。

      <Expandable title="デバイスのプロパティ" defaultOpen>
        <ResponseField name="initial_asn" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このセッションに関連付けられた最初の自律システム番号。
        </ResponseField>

        <ResponseField name="initial_ip" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このセッションに関連付けられた最初の IP アドレス。
        </ResponseField>

        <ResponseField name="initial_user_agent" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このセッションに関連付けられたデバイスの最初のユーザーエージェント。
        </ResponseField>

        <ResponseField name="last_asn" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このユーザーが最後にログインした際の自律システム番号。
        </ResponseField>

        <ResponseField name="last_ip" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このユーザーが最後にログインした際の IP アドレス。
        </ResponseField>

        <ResponseField name="last_user_agent" type="string" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このユーザーが最後にログインした際に使用したデバイスのユーザーエージェント。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="expires_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションの有効期限が切れる日時。
    </ResponseField>

    <ResponseField name="idle_expires_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションが一定時間操作されない場合に期限切れになる日時。
    </ResponseField>

    <ResponseField name="last_interacted_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションが最後に正常に使用された日時。
    </ResponseField>

    <ResponseField name="メタデータ" type="dictionary" post={["optional"]}>
      &#91;Enterprise のお客様&#93; &#91;早期アクセス&#93; セッションメタデータ
    </ResponseField>

    <ResponseField name="session_transfer" type="dictionary" post={["optional"]}>
      &#91;Enterprise のお客様&#93; &#91;早期アクセス&#93; このオブジェクトは、セッションがセッション転送トークン (ネイティブからWebへのSSO) から作成された場合に定義され、それ以外では undefined です。

      <Expandable title="session_transfer プロパティ" defaultOpen>
        <ResponseField name="parent_refresh_token" type="dictionary" post={["optional"]}>
          &#91;Enterprise のお客様&#93; このオブジェクトは、リフレッシュトークンがセッション転送 (ネイティブからWebへのSSO) によって開始されたセッションから作成された場合に定義され、それ以外では undefined です。

          <Expandable title="parent_refresh_token プロパティ">
            <ResponseField name="id" type="string" post={["optional"]}>
              &#91;Enterprise のお客様&#93; このセッション/リフレッシュトークンがセッション転送 (ネイティブからWebへのSSO) の結果として作成された際の、親リフレッシュトークンの ID。
            </ResponseField>

            <ResponseField name="metadata" type="dictionary" post={["optional"]}>
              &#91;Enterprise のお客様&#93; このセッション/リフレッシュトークンがセッション転送 (ネイティブからWebへのSSO) の結果として作成された際の、親リフレッシュトークンのメタデータ。
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="updated_at" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; セッションの最終更新日時。
    </ResponseField>

    <ResponseField name="user_id" type="string" post={["optional"]}>
      &#91;Enterprise のお客様&#93; 他の API を利用する際に使用できるユーザー ID。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsession_transfer_token">
  ## `event.session_transfer_token`
</div>

<ResponseField name="event.session_transfer_token" type="dictionary" post={["optional"]}>
  &#91;早期アクセス&#93; ネイティブアプリケーションから Web アプリケーションへのシングルサインオン (SSO) を確立するために使用されている、現在のセッション転送トークンの詳細です。

  <Expandable title="session_transfer_token プロパティ" defaultOpen>
    <ResponseField name="client_id" type="string">
      &#91;早期アクセス&#93; トークンを発行したアプリケーションのクライアント ID です。
    </ResponseField>

    <ResponseField name="request" type="dictionary">
      &#91;早期アクセス&#93; トークンの発行元となったリクエストの詳細です。

      <Expandable title="request プロパティ" defaultOpen>
        <ResponseField name="asn" type="string" post={["optional"]}>
          &#91;早期アクセス&#93; トークンの発行元となったリクエストに関連付けられた自律システム番号 (ASN) です。
        </ResponseField>

        <ResponseField name="geoip" type="dictionary" post={["optional"]}>
          <Expandable title="geoip プロパティ">
            <ResponseField name="cityName" type="string" post={["optional"]} />

            <ResponseField name="continentCode" type="string" post={["optional"]} />

            <ResponseField name="countryCode" type="string" post={["optional"]} />

            <ResponseField name="countryCode3" type="string" post={["optional"]} />

            <ResponseField name="countryName" type="string" post={["optional"]} />

            <ResponseField name="latitude" type="number" post={["optional"]} />

            <ResponseField name="longitude" type="number" post={["optional"]} />

            <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

            <ResponseField name="subdivisionName" type="string" post={["optional"]} />

            <ResponseField name="timeZone" type="string" post={["optional"]} />
          </Expandable>
        </ResponseField>

        <ResponseField name="ip" type="string">
          &#91;早期アクセス&#93; トークンの発行元となったリクエストに関連付けられた IP アドレスです。
        </ResponseField>

        <ResponseField name="user_agent" type="string" post={["optional"]}>
          &#91;早期アクセス&#93; トークンを発行したデバイスの User-Agent 文字列です。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="scope" type="array of strings">
      &#91;早期アクセス&#93; トークンの発行時に要求されたスコープです。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventstats">
  ## `event.stats`
</div>

<ResponseField name="event.stats" type="object">
  現在のユーザーのログインに関する統計情報です。

  <Expandable title="stats のプロパティ" defaultOpen>
    <ResponseField name="logins_count" type="number">
      このユーザーがログインした回数です。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtenant">
  ## `event.tenant`
</div>

<ResponseField name="event.tenant" type="object">
  現在のトランザクションに関連するテナントの詳細。

  <Expandable title="テナントのプロパティ" defaultOpen>
    <ResponseField name="id" type="string">
      テナントの名前。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtransaction">
  ## `event.transaction`
</div>

<ResponseField name="event.transaction" type="dictionary" post={["optional"]}>
  現在のトランザクションに関する詳細情報。

  <Expandable title="トランザクションのプロパティ" defaultOpen>
    <ResponseField name="id" type="string" post={["optional"]}>
      トランザクションの一意の識別子。すべてのブラウザベースのログインフローで設定されます。
    </ResponseField>

    <ResponseField name="acr_values" type="array of strings" post={["optional"]}>
      元の認証リクエストで指定された acr&#95;values。
    </ResponseField>

    <ResponseField name="linking_id" type="string" post={["optional"]}>
      開発者がこのトランザクションを参照するための Dynamic Linking ID。
    </ResponseField>

    <ResponseField name="ロケール" type="string" post={["optional"]}>
      ブラウザーから要求された言語とテナントの言語設定を比較して決定される、このトランザクションで使用されるロケール。
    </ResponseField>

    <ResponseField name="login_hint" type="string" post={["optional"]}>
      必要に応じて、エンドユーザーがログイン時に使用する可能性のある識別子を認可サーバーに示すためのヒントです。
    </ResponseField>

    <ResponseField name="画面" type="array of strings" post={["optional"]}>
      ユーザーに再認証や同意を求めるかどうかを示す指示の一覧。
    </ResponseField>

    <ResponseField name="プロトコル" type="string" post={["optional"]}>
      指定できる値:

      * `oidc-basic-profile`
      * `oidc-ciba`
      * `oidc-ciba-web-link`
      * `oidc-implicit-profile`
      * `oauth2-device-code`
      * `oauth2-resource-owner`
      * `oauth2-resource-owner-jwt-bearer`
      * `oauth2-password`
      * `oauth2-webauthn`
      * `oauth2-access-token`
      * `oauth2-refresh-token`
      * `oauth2-token-exchange`
      * `oidc-hybrid-profile`
      * `samlp`
      * `wsfed`
      * `wstrust-usernamemixed`
    </ResponseField>

    <ResponseField name="redirect_uri" type="string" post={["optional"]}>
      トランザクション完了後に、Auth0 がブラウザーをリダイレクトする先の URL。
    </ResponseField>

    <ResponseField name="要求された認可の詳細" type="array of objects" post={["optional"]}>
      Rich Authorization Requests 仕様のセクション 2 (https://datatracker.ietf.org/doc/html/draft-ietf-oauth-rar#section-2) で定義されている、rich authorization request の詳細です。

      <Expandable title="requested_authorization_details プロパティ" defaultOpen>
        <ResponseField name="type" type="string">
          認可の詳細の種類を表す文字列です。type フィールドの値によって、そのフィールドを含むオブジェクトに指定できる内容が決まります。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="リクエストされたスコープ" type="array of strings" post={["optional"]}>
      この認証フローの開始時に要求されたスコープ (存在する場合) 。
    </ResponseField>

    <ResponseField name="response_mode" type="string" post={["optional"]}>
      認可エンドポイントからパラメータを返すために使用する方式を認可サーバーに通知します。

      使用できる値: `query`, `fragment`, `form_post`, `web_message`
    </ResponseField>

    <ResponseField name="response_type" type="array of strings" post={["optional"]}>
      Auth0 が返す認証情報の種別を示します。
    </ResponseField>

    <ResponseField name="state" type="string" post={["optional"]}>
      アプリが最初のリクエストに追加する、不透明な任意の英数字文字列で、Auth0 がアプリケーションにリダイレクトして戻す際に含まれます。
    </ResponseField>

    <ResponseField name="ui_locales" type="array of strings" post={["optional"]}>
      元の認証リクエストで指定されたui&#95;locales。
    </ResponseField>

    <ResponseField name="実行主体" type="dictionary" post={["optional"]}>
      トークン交換リクエスト内のアクター。

      <Expandable title="actor のプロパティ" defaultOpen>
        <ResponseField name="act" type="dictionary" post={["optional"]}>
          委譲チェーン内の次のアクターであり、このアクターが別のプリンシパルに代わってリクエストを行っていることを示します。

          <Expandable title="act のプロパティ">
            <ResponseField name="act" type="dictionary" post={["optional"]}>
              委譲チェーン内の次のアクターであり、このアクターが別のプリンシパルに代わってリクエストを行っていることを示します。

              <Expandable title="act のプロパティ">
                <ResponseField name="act" type="dictionary" post={["optional"]}>
                  委譲チェーン内の次のアクターであり、このアクターが別のプリンシパルに代わってリクエストを行っていることを示します。

                  <Expandable title="act のプロパティ">
                    <ResponseField name="act" type="dictionary" post={["optional"]}>
                      委譲チェーン内の次のアクターであり、このアクターが別のプリンシパルに代わってリクエストを行っていることを示します。

                      <Expandable title="act のプロパティ">
                        <ResponseField name="act" type="undefined" post={["optional"]} />

                        <ResponseField name="sub" type="string">
                          アクターのサブジェクト識別子です。この役割を担うエンティティの一意の識別子です。
                        </ResponseField>
                      </Expandable>
                    </ResponseField>

                    <ResponseField name="sub" type="string">
                      アクターのサブジェクト識別子です。この役割を担うエンティティの一意の識別子です。
                    </ResponseField>
                  </Expandable>
                </ResponseField>

                <ResponseField name="sub" type="string">
                  アクターのサブジェクト識別子です。この役割を担うエンティティの一意の識別子です。
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="sub" type="string">
              アクターのサブジェクト識別子です。この役割を担うエンティティの一意の識別子です。
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="sub" type="string">
          アクターのサブジェクト識別子です。この役割を担うエンティティの一意の識別子です。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="actor_token_type" type="string" post={["optional"]}>
      トークン交換リクエストにおけるアクタートークンの種類。
    </ResponseField>

    <ResponseField name="correlation_id" type="string" post={["optional"]}>
      アプリケーションが Universal Login にリダイレクトされる際、Correlation ID は初回認証リクエストで指定できます。この値を使うと、Action コードからのログやリクエストをユーザーフローに関連付けることができます。
    </ResponseField>

    <ResponseField name="メタデータ" type="dictionary">
      トランザクションの実行中に、カスタム Actions 全体で共有されるデータを含むオブジェクト。
    </ResponseField>

    <ResponseField name="subject_token_type" type="string" post={["optional"]}>
      トークン交換リクエストにおけるサブジェクトトークンの種類。
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventuser">
  ## `event.user`
</div>

<ResponseField name="event.user" type="dictionary">
  現在のトランザクションが開始されたユーザーを表すオブジェクト。

  <Expandable title="ユーザープロパティ" defaultOpen>
    <ResponseField name="app_metadata" type="dictionary">
      ユーザーのアクセス権に影響する、サポートプラン、セキュリティロール、アクセス制御グループなどの情報を保存するカスタムフィールド。
    </ResponseField>

    <ResponseField name="created_at" type="string">
      ユーザープロファイルが初めて作成された日時を示すタイムスタンプ。
    </ResponseField>

    <ResponseField name="メール" type="string" post={["optional"]}>
      (一意の) ユーザーのメールアドレス。
    </ResponseField>

    <ResponseField name="email_verified" type="boolean">
      ユーザーのメールアドレスが確認済みかどうかを示します。
    </ResponseField>

    <ResponseField name="姓" type="string" post={["optional"]}>
      ユーザーの姓。
    </ResponseField>

    <ResponseField name="given_name" type="string" post={["optional"]}>
      ユーザーの名。
    </ResponseField>

    <ResponseField name="last_password_reset" type="string" post={["optional"]}>
      ユーザーのパスワードが最後にリセットまたは変更された日時を示すタイムスタンプです。ユーザー作成時点では、このフィールドは存在しません。このプロパティは、データベース接続でのみ使用できます。
    </ResponseField>

    <ResponseField name="名前" type="string" post={["optional"]}>
      ユーザーの氏名 (フルネーム) 。
    </ResponseField>

    <ResponseField name="ニックネーム" type="string" post={["optional"]}>
      ユーザーのニックネーム。
    </ResponseField>

    <ResponseField name="phone_number" type="string" post={["optional"]}>
      ユーザーの電話番号。
    </ResponseField>

    <ResponseField name="phone_verified" type="boolean" post={["optional"]}>
      ユーザーが電話番号の確認を完了しているかどうかを示します。
    </ResponseField>

    <ResponseField name="プロフィール画像" type="string" post={["optional"]}>
      ユーザーの[プロフィール画像](https://auth0.com/docs/users/change-user-picture)のURL。
    </ResponseField>

    <ResponseField name="updated_at" type="string">
      ユーザーのプロファイルが最後に更新または変更された日時を示すタイムスタンプ。
    </ResponseField>

    <ResponseField name="user_id" type="string">
      (unique) ユーザーの一意の識別子。
    </ResponseField>

    <ResponseField name="user_metadata" type="dictionary">
      勤務先住所、自宅住所、ユーザー設定など、ユーザーがアクセスできる内容に影響しない情報を保存するCustom Fields。
    </ResponseField>

    <ResponseField name="ユーザー名" type="string" post={["optional"]}>
      (一意の) ユーザー名。
    </ResponseField>

    <ResponseField name="enrolledFactors" type="array of objects" post={["optional"]}>
      ユーザーが登録済みの認証要素の配列です。

      <Expandable title="enrolledFactors のプロパティ" defaultOpen>
        <ResponseField name="type" type="string">
          `push-notification`、`phone`、`email`、`otp`、`webauthn-roaming`、`webauthn-platform` などの認証要素の種類です。
        </ResponseField>

        <ResponseField name="options" type="dictionary" post={["optional"]}>
          この登録済み認証要素に関する追加オプションです。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="多要素認証" type="array of strings" post={["optional"]}>
      ユーザーが登録している多要素認証 (MFA) プロバイダーの一覧です。この配列は、ユーザーがMFAに登録したとき、および管理者がユーザーのMFA登録をリセットしたときに更新されます。
    </ResponseField>

    <ResponseField name="アイデンティティ情報" type="array of objects">
      ユーザーが最初に認証したアイデンティティプロバイダーから取得した情報が含まれます。ユーザーは自身のプロファイルを複数のアイデンティティプロバイダーにリンクすることもでき、その場合はそれらのアイデンティティもこの配列に含まれます。各アイデンティティプロバイダーオブジェクトの内容は、プロバイダーによって異なります。

      <Expandable title="identities 要素のプロパティ" defaultOpen>
        <ResponseField name="connection" type="string" post={["optional"]}>
          ユーザーの認証に使用される Auth0 接続の名前。
        </ResponseField>

        <ResponseField name="isSocial" type="boolean" post={["optional"]}>
          この接続がソーシャル接続かどうかを示します。
        </ResponseField>

        <ResponseField name="profileData" type="dictionary" post={["optional"]}>
          接続に関連付けられたユーザー情報。プロファイルがリンクされている場合は、関連付けられたセカンダリアカウントのユーザー情報が格納されます。
        </ResponseField>

        <ResponseField name="provider" type="string" post={["optional"]}>
          Facebook、Google、SAML、または独自のプロバイダーなど、ユーザーを認証するエンティティの名前。
        </ResponseField>

        <ResponseField name="user_id" type="string" post={["optional"]}>
          この接続/プロバイダーにおけるユーザー固有の識別子。
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>