---
description: Describe cómo los tokens de ID se ajustan al estándar JWT y contienen claims JWT sobre el propio token, claims OIDC estándar sobre el usuario autenticado y claims personalizados que defines, controlas y agregas a un token mediante una rule.
title: Estructura del token de ID
---

<Tooltip tip="Token de ID: credencial destinada al propio cliente, en lugar de para acceder a un recurso." cta="Ver glosario" href="/es/docs/glossary?term=ID+tokens">Los tokens de ID</Tooltip> siguen el estándar [JSON Web Token (JWT)](/es/docs/secure/tokens/json-web-tokens), lo que significa que su estructura básica se ajusta a la [estructura típica de un JWT](/es/docs/secure/tokens/json-web-tokens/json-web-token-structure) y que contienen [claims JWT](/es/docs/secure/tokens/json-web-tokens/json-web-token-claims) estándar sobre el propio token.

Además de lo que requiere <Tooltip tip="Token de ID: credencial destinada al propio cliente, en lugar de para acceder a un recurso." cta="Ver glosario" href="/es/docs/glossary?term=JWT">JWT</Tooltip>, los tokens de ID también contienen claims sobre el usuario autenticado, predefinidos por el protocolo <Tooltip tip="OpenID: estándar abierto de autenticación que permite a las aplicaciones verificar las identidades de los usuarios sin recopilar ni almacenar información de inicio de sesión." cta="Ver glosario" href="/es/docs/glossary?term=OpenID">OpenID</Tooltip> Connect (OIDC), y por ello se conocen como claims OIDC estándar. Algunos claims OIDC estándar incluyen:

* `name`
* `nickname`
* `picture`
* `email`
* `email_verified`

Para ver la lista completa de claims OIDC estándar, consulta [OIDC specification: Standard Claims](https://openid.net/specs/openid-connect-core-1_0.html#StandardClaims). También puedes crear [claims personalizados](/es/docs/secure/tokens/json-web-tokens/json-web-token-claims), es decir, claims que defines, controlas y agregas a un token mediante Auth0 Actions.

Puedes controlar qué claims OIDC se incluyen en el token de ID que consume tu aplicación incluyendo scopes específicos de [OpenID Connect](/es/docs/get-started/apis/scopes/openid-connect-scopes) en un parámetro al solicitar tokens durante la autenticación de usuarios. Para obtener más información sobre cómo solicitar un token de ID, consulta [Obtener tokens de ID](/es/docs/secure/tokens/id-tokens/get-id-tokens).

<div id="sample-id-token">
  ## Ejemplo de token de ID
</div>

Este ejemplo muestra el contenido de un token de ID. Ten en cuenta que el valor de <Tooltip tip="Audiencia: identificador único de la audiencia de un token emitido. Se denomina aud en un token y su valor contiene el ID de una aplicación (ID de cliente) para un token de ID o de una API (identificador de API) para un token de acceso." cta="Ver glosario" href="/es/docs/glossary?term=audience">audiencia</Tooltip> (ubicado en el claim `aud`) se establece en el identificador de la aplicación, lo que significa que solo esta aplicación específica debe consumir el token. Para obtener más información sobre los claims de este ejemplo, consulta [JSON Web Token Claims](/es/docs/secure/tokens/json-web-tokens/json-web-token-claims).

```json lines
{
  "iss": "http://my-domain.auth0.com",
  "sub": "auth0|123456",
  "aud": "my_client_id",
  "exp": 1311281970,
  "iat": 1311280970,
  "name": "Jane Doe",
  "given_name": "Jane",
  "family_name": "Doe",
  "gender": "female",
  "birthdate": "0000-10-31",
  "email": "janedoe@example.com",
  "picture": "http://example.com/janedoe/me.jpg"
}
```

<div id="learn-more">
  ## Más información
</div>

* [Obtener tokens de ID](/es/docs/secure/tokens/id-tokens/get-id-tokens)
* [Validar tokens de ID](/es/docs/secure/tokens/id-tokens/validate-id-tokens)
* [Actualizar el tiempo de vida del token de ID](/es/docs/secure/tokens/id-tokens/update-id-token-lifetime)