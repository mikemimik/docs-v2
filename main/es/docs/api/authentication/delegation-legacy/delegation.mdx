---
title: "Delegación"
description: "Endpoint obsoleto que genera un token de delegación para transferir la identidad del usuario desde una aplicación a un add-on de API."
---

<div id="endpoint">
  ## Endpoint
</div>

`POST /delegation`

<Note>
  De forma predeterminada, esta funcionalidad está deshabilitada para los tenants que no tenían ningún add-on en uso a fecha del 8 de junio de 2017. Los tenants heredados que actualmente usan un add-on que requiere delegación pueden seguir usando esta funcionalidad. Si la funcionalidad de delegación se modifica o se elimina del servicio en algún momento, se notificará con antelación a los clientes que la usan actualmente y se les dará tiempo suficiente para migrar.
</Note>

Se puede obtener y usar un token de delegación cuando una aplicación necesita llamar a la API de un add-on de aplicación, como Firebase o SAP, registrado y configurado en Auth0, en el mismo tenant que el programa que realiza la llamada.

A partir de un token existente, este endpoint generará un nuevo token firmado con el secreto de la app `target`. Esto permite transferir la identidad del usuario desde la aplicación a una API.

<div id="remarks">
  ### Observaciones
</div>

* El valor de `scope` `profile` solicita acceso a los Claims predeterminados del perfil del usuario final, que son: `name`, `family_name`, `given_name`, `middle_name`, `nickname`, `preferred_username`, `profile`, `picture`, `website`, `gender`, `birthdate`, `zoneinfo`, `locale` y `updated_at`.

* El valor de `scope` `email` solicita acceso a los Claims `email` y `email_verified`.

* La delegación **no es compatible** con la versión 8 de [auth0.js](https://auth0.com/docs/libraries/auth0js). Para ver un ejemplo en la versión 7 de la biblioteca, consulta la [guía de referencia de auth0.js v7](https://auth0.com/docs/libraries/auth0js/v7).

* Este endpoint permite hasta 10 solicitudes por minuto desde la misma dirección IP con el mismo `user_id`.

* Este endpoint devolverá tres encabezados de respuesta HTTP que proporcionan datos relevantes sobre sus límites de solicitudes:
  * `X-RateLimit-Limit`: Número de solicitudes permitidas por minuto.
  * `X-RateLimit-Remaining`: Número de solicitudes disponibles. Cada nueva solicitud reduce este número en 1. Por cada minuto que transcurre, se vuelve a añadir una solicitud, por lo que este número aumenta en 1 cada vez.
  * `X-RateLimit-Reset`: Tiempo restante hasta que se restablezca el límite de solicitudes (`X-RateLimit-Limit`). El valor se expresa en [segundos de la época UTC](https://en.wikipedia.org/wiki/Unix_time).

<div id="learn-more">
  ### Más información
</div>

* [Tokens de delegación](https://auth0.com/docs/secure/tokens/delegation-tokens)
* [Política de límites de solicitudes de la API de Auth0](https://auth0.com/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy)

<div id="response-messages">
  ## Mensajes de respuesta
</div>

| Estado | Descripción                                |
| ------ | ------------------------------------------ |
| 200    | Obtención correcta del token de delegación |