---
title: "ScreenMembersOnEmailVerificationResult"
---

ScreenMembersOnEmailVerificationResult

```ts Example
export interface ScreenMembersOnEmailVerificationResult extends ScreenMembers {
  /**
   * Datos específicos de la pantalla que contienen el estado de la verificación de correo electrónico.
   * @type {{ status: string; } | null}
   */
  data: {
    /**
     * El estado del proceso de verificación de correo electrónico.
     * Los valores posibles pueden incluir "success", "failure", "already_verified", etc.
     * Este estado debe mostrarse al usuario para informarle del resultado.
     */
    status: string;
  } | null;

  /**
   * Enlaces de navegación disponibles en esta pantalla.
   * @type {string | null}
   */
  loginLink: string | null;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/docs/es/libraries/acul/react-sdk/API-Reference/Types/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="status">
  Datos específicos de esta pantalla.

  #### Declaración de tipo

  &#123; `status`: `string`; &#125;

  #### status

  El estado del proceso de verificación del correo electrónico.
  Entre los valores posibles se incluyen &quot;success&quot;, &quot;failure&quot;, &quot;already&#95;verified&quot;, etc.
  Este estado debe mostrarse al usuario para informarle del resultado.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record">
  Enlaces de navegación disponibles en esta pantalla.
</ParamField>

<ParamField body="loginLink" type="string">
  Enlace de navegación disponible en esta pantalla.
</ParamField>

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />