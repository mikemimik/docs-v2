---
title: "MfaWebAuthnRoamingEnrollmentMembers"
---

Interfaz que define los métodos y propiedades disponibles para la pantalla mfa-webauthn-roaming-enrollment.

````ts Example
export interface MfaWebAuthnRoamingEnrollmentMembers extends BaseMembers {
  screen: ScreenMembersOnMfaWebAuthnRoamingEnrollment;
  /**
   * Inicia la creación de credenciales WebAuthn y envía el resultado al servidor.
   * Esto corresponde al usuario interactuando con el prompt de FIDO Security Keys.
   * @param payload Opciones personalizadas opcionales para incluir con la solicitud.
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * try {
   *   // Esto activará el prompt WebAuthn del navegador
   *   const credential = await navigator.credentials.create({ publicKey: webauthnEnrollment.screen.data.passkey.public_key });
   *   // Normalmente aquí se serializa la respuesta de la credencial
   *   const response = JSON.stringify(credential);
   *   await webauthnEnrollment.enroll({ response });
   * } catch (error) {
   *   console.error('WebAuthn enrollment failed:', error);
   *   // Maneja el error, p. ej., muestra un mensaje de error al usuario o envía los detalles del error
   *   // await webauthnEnrollment.showError({ error: { name: error.name, message: error.message } });
   * }
   * ```
   */
  enroll(payload: CustomOptions): Promise<void>;
  /**
   * Envía al servidor los detalles sobre un error de WebAuthn en el navegador.
   * Se utiliza cuando la API de WebAuthn del navegador encuentra un error.
   * @param payload Las opciones que contienen los detalles del error.
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * // Ejemplo de manejador de errores para la llamada a la API de WebAuthn del navegador
   * const handleError = async (error: any) => {
   *   console.error('WebAuthn error:', error);
   *   await webauthnEnrollment.showError({
   *     error: {
   *       name: error.name,
   *       message: error.message,
   *       // Incluye otras propiedades de error relevantes si están disponibles
   *     },
   *   });
   * };
   * // ... usa handleError en el bloque catch de tu llamada a la API de WebAuthn
   * ```
   */
  showError(payload: ShowErrorOptions): Promise<void>;
  /**
   * Permite al usuario probar otro método de MFA.
   * Esto corresponde al botón "Try Another Method".
   * @param payload Opciones personalizadas opcionales para incluir con la solicitud.
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * await webauthnEnrollment.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnRoamingEnrollment">ScreenMembersOnMfaWebAuthnRoamingEnrollment</a></span>} />

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="enroll" type="Promise<void>">
  Inicia la creación de la credencial de WebAuthn y envía el resultado al servidor.
  Esto corresponde a la interacción del usuario con el prompt FIDO Security Keys.

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  try {
    // Esto activará el prompt de WebAuthn del navegador
    const credential = await navigator.credentials.create({ publicKey: webauthnEnrollment.screen.data.passkey.public_key });
    // Normalmente, aquí serializarías la respuesta de la credencial
    const response = JSON.stringify(credential);
    await webauthnEnrollment.enroll({ response });
  } catch (error) {
    console.error('WebAuthn enrollment failed:', error);
    // Gestiona el error; por ejemplo, muestra un mensaje de error al usuario o envía los detalles del error
    // await webauthnEnrollment.showError({ error: { name: error.name, message: error.message } });
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="showError" type="Promise<void>">
  Envía al servidor detalles sobre un error del navegador relacionado con WebAuthn.
  Se usa cuando la API de WebAuthn del navegador encuentra un error.

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  // Ejemplo de controlador de errores para la llamada a la API de WebAuthn del navegador
  const handleError = async (error: any) => {
    console.error('WebAuthn error:', error);
    await webauthnEnrollment.showError({
      error: {
        name: error.name,
        message: error.message,
        // Incluye otras propiedades de error relevantes, si están disponibles
      },
    });
  };
  // ... usa handleError en el bloque catch de tu llamada a la API de WebAuthn
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ShowErrorOptions">ShowErrorOptions</a></span>}>
      Las opciones que contienen los detalles del error.

      **Propiedades**

      <ParamField body="error" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        Los detalles del error de WebAuthn.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permite al usuario probar otro método de MFA.
  Esto corresponde al botón &quot;Try Another Method&quot;.

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  await webauthnEnrollment.tryAnotherMethod();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/MfaWebAuthnRoamingEnrollmentTryAnotherMethodOptions">MfaWebAuthnRoamingEnrollmentTryAnotherMethodOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>