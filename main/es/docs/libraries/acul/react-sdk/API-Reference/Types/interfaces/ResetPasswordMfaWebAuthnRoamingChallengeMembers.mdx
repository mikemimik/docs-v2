---
title: "ResetPasswordMfaWebAuthnItineranteDesafíoMiembros"
---

ResetPasswordMfaWebAuthnItineranteDesafíoMiembros

```ts Example
export interface ResetPasswordMfaWebAuthnRoamingChallengeMembers extends BaseMembers {

  /**
 * Acceso a las propiedades y datos específicos de la pantalla `reset-password-mfa-webauthn-roaming-challenge`,
 * incluidas las opciones de desafío `publicKey` de WebAuthn y el indicador `showRememberDevice`.
 * @type {ScreenMembersOnResetPasswordMfaWebAuthnRoamingChallenge}
 */
  screen: ScreenMembersOnResetPasswordMfaWebAuthnRoamingChallenge;

  /**
   * Inicia el proceso de aserción de WebAuthn (desafío de clave de seguridad).
   * Este método llamará internamente a `navigator.credentials.get()` usando el desafío
   * proporcionado en `screen.publicKey`.
   * Tras una interacción exitosa con la clave de seguridad, envía el
   * `PublicKeyCredential` resultante a Auth0 con `action: "default"`.
   *
   * @param {UseSecurityKeyOptions} [options] - Opcional. Parámetros para la operación,
   * como `rememberDevice` (si `screen.showRememberDevice` es true) y otras opciones personalizadas.
   * El campo `response` (la credencial de WebAuthn) es gestionado internamente por el SDK.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se envía el intento de verificación.
   * Una operación exitosa generalmente resulta en una redirección por parte de Auth0.
   * @throws {Error} Lanza un error si falta `screen.publicKey`, si `navigator.credentials.get()`
   * falla (p. ej., cancelación por parte del usuario, no se encontró ningún autenticador), o si el envío del formulario a Auth0 falla.
   * Se recomienda capturar estos errores y, si corresponde, usar `showError()` para notificar problemas específicos de la API de WebAuthn.
   */
  useSecurityKey(options?: UseSecurityKeyOptions): Promise<void>;

  /**
   * Notifica a Auth0 un error de la API de WebAuthn del lado del cliente (proveniente de `navigator.credentials.get()`).
   * Este método debe utilizarse cuando `useSecurityKey()` (o una llamada manual a `navigator.credentials.get()`)
   * falla debido a un problema de WebAuthn en el navegador (p. ej., `NotAllowedError` si el usuario cancela el prompt,
   * `NotFoundError` si no se encuentra ningún autenticador coincidente, o un timeout).
   * Envía los detalles del error con `action: "showError::{errorDetailsJsonString}"` y un `response` vacío.
   *
   * @param {ShowErrorOptions} options - Contiene el objeto `error` (con `name` y `message`
   * de la DOMException de la API de WebAuthn), un indicador opcional `rememberDevice` y cualquier otra opción personalizada.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se envía el reporte de error.
   * Auth0 puede volver a renderizar la página con información del error o redirigir al usuario.
   * @throws {Error} Lanza un error si el envío del formulario falla (p. ej., error de red, estado inválido).
   */
  showError(options: ShowErrorOptions): Promise<void>;

  /**
   * Permite al usuario omitir el desafío WebAuthn roaming y seleccionar un método de MFA diferente.
   * Esta acción envía `action: "pick-authenticator"` a Auth0, lo que debería dirigir
   * al usuario a una pantalla de selección de factor de MFA.
   *
   * @param {TryAnotherMethodOptions} [options] - Opcional. Parámetros para la operación,
   * como `rememberDevice` (si `screen.showRememberDevice` es true) y otras opciones personalizadas.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se envía la acción 'pick-authenticator'.
   * @throws {Error} Lanza un error si el envío del formulario falla.
   */
  tryAnotherMethod(options?: TryAnotherMethodOptions): Promise<void>;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnResetPasswordMfaWebAuthnRoamingChallenge">ScreenMembersOnResetPasswordMfaWebAuthnRoamingChallenge</a></span>}>
  Acceso a las propiedades y los datos específicos de la pantalla `reset-password-mfa-webauthn-roaming-challenge`,
  incluidas las opciones de desafío `publicKey` de WebAuthn y el indicador `showRememberDevice`.
</ParamField>

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="showError" type="Promise<void>">
  Informa a Auth0 de un error client-side de la API de WebAuthn (de `navigator.credentials.get()`).
  Este método debe usarse cuando `useSecurityKey()` (o una llamada manual a `navigator.credentials.get()`)
  falla debido a un problema de WebAuthn en el navegador (p. ej., `NotAllowedError` si el usuario cancela el prompt,
  `NotFoundError` si no se encuentra ningún autenticador compatible, o un timeout).
  Envía los detalles del error con `action: "showError::{errorDetailsJsonString}"` y una `response` vacía.

  Una promesa que se resuelve cuando se envía el informe del error.
  Auth0 puede volver a renderizar la página con información sobre el error o redirigir.

  #### Lanza

  Lanza un error si falla el envío del formulario (p. ej., por un error de red o un estado no válido).

  <Expandable title="Parámetros">
    <ParamField body="options" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaWebAuthnRoamingChallengeShowErrorOptions">ResetPasswordMfaWebAuthnRoamingChallengeShowErrorOptions</a></span>}>
      Contiene el objeto `error` (con `name` y `message`
      de la DOMException de la API de WebAuthn), un indicador `rememberDevice` opcional y cualquier otra opción personalizada.

      **Propiedades**

      <ParamField body="error" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        El objeto de error de la API de WebAuthn (`navigator.credentials.get()`)
        que contiene el `name` y el `message` de la DOMException.
      </ParamField>

      <ParamField body="rememberDevice?" type="boolean">
        Opcional. Si es `true` y `screen.showRememberDevice` también es `true`,
        el SDK incluirá `rememberBrowser=true` en el envío del formulario.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permite al usuario omitir la verificación roaming de WebAuthn y seleccionar otro método de MFA.
  Esta acción envía `action: "pick-authenticator"` a Auth0, lo que debería llevar
  al usuario a una pantalla de selección de factor de MFA.

  Una promesa que se resuelve cuando se envía la acción &#39;pick-authenticator&#39;.

  #### Throws

  Genera un error si falla el envío del formulario.

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaWebAuthnRoamingChallengeTryAnotherMethodOptions">ResetPasswordMfaWebAuthnRoamingChallengeTryAnotherMethodOptions</a></span>}>
      Opcional. Parámetros de la operación,
      como `rememberDevice` (si `screen.showRememberDevice` es `true`) y otras opciones personalizadas.

      **Propiedades**

      <ParamField body="rememberDevice?" type="boolean">
        Opcional. Si `true` y `screen.showRememberDevice` también es `true`,
        el SDK incluirá `rememberBrowser=true` en el envío del formulario.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="useSecurityKey" type="Promise<void>">
  Inicia el proceso de aserción de WebAuthn (desafío de clave de seguridad).
  Este método llamará internamente a `navigator.credentials.get()` usando el desafío
  proporcionado en `screen.publicKey`.
  Si la interacción con la clave de seguridad se completa correctamente, envía la
  `PublicKeyCredential` resultante a Auth0 con `action: "default"`.

  Una Promise que se resuelve cuando se envía el intento de verificación.
  Normalmente, una operación correcta da lugar a una redirección por parte de Auth0.

  #### Lanza

  Lanza un error si falta `screen.publicKey`, si `navigator.credentials.get()`
  falla (p. ej., cancelación del usuario o no se encuentra ningún autenticador) o si falla el envío del formulario a Auth0.
  Se recomienda capturar estos errores y, si procede, usar `showError()` para informar de problemas específicos de la API de WebAuthn.

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/es/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaWebAuthnRoamingChallengeUseSecurityKeyOptions">ResetPasswordMfaWebAuthnRoamingChallengeUseSecurityKeyOptions</a></span>}>
      Opcional. Parámetros de la operación,
      como `rememberDevice` (si `screen.showRememberDevice` es `true`) y otras opciones personalizadas.
      El SDK gestiona internamente el campo `response` (la credencial de WebAuthn).

      **Propiedades**

      <ParamField body="rememberDevice?" type="boolean">
        Opcional. Si es `true` y `screen.showRememberDevice` también es `true`,
        el SDK incluirá `rememberBrowser=true` al enviar el formulario, lo que indica la
        elección del usuario de recordar este dispositivo.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>