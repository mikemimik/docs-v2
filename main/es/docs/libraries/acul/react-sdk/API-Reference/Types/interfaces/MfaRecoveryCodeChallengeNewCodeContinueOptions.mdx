---
title: "MfaCódigoDeRecuperaciónDesafíoNuevoCódigoOpcionesContinuar"
---

OpcionesContinuar

```ts Example
export interface ContinueOptions extends CustomOptions {
  // No se requieren propiedades específicas más allá de CustomOptions.
  // El parámetro 'saved' es gestionado internamente por el método continue.
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`