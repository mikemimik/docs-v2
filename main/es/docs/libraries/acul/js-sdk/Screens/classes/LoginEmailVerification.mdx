---
title: "LoginEmailVerification"
description: "Describe todas las propiedades y métodos disponibles para personalizar la pantalla `login-email-verification` de Universal Login."
---

La clase LoginEmailVerification implementa la funcionalidad de la pantalla `login-email-verification`. Esta pantalla solicita al usuario que introduzca un código de un solo uso enviado a su dirección de correo electrónico para verificar su identidad durante el proceso de inicio de sesión.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/2kIZmIlrAHoSTyaD8thnIL/515c6f940036953d2989c7b37b7f1fe2/Screenshot_2025-05-15_at_21.18.53.png" alt="" />
</Frame>

<div id="constructors">
  ## Constructores
</div>

Crea una instancia del gestor de pantalla LoginEmailVerification:

```typescript Example
import LoginEmailVerification from '@auth0/auth0-acul-js/login-email-verification';
const loginEmailVerificationManager = new LoginEmailVerification();
loginEmailVerificationManager.continueWithCode({
  code: '123456',
});
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Proporciona configuraciones relacionadas con la marca, como el tema de marca y otros ajustes.
</ParamField>

<ParamField body="client" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `login-email-verification`.
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Proporciona información sobre la organización del usuario, como `id` y `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contiene datos sobre el prompt actual en el flujo de autenticación.
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ScreenMembers">ScreenMembers</a></span>}>
  Contiene detalles específicos de la pantalla `login-email-verification`, incluida su configuración y contexto.
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contiene datos relacionados con el tenant, como `id` y sus metadatos asociados.
</ParamField>

<ParamField body="transaction" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Proporciona datos específicos de la transacción para la pantalla `login-email-verification`, como identificadores activos y estados del flujo.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gestiona datos no confiables que se pasan al SDK, como la entrada del usuario durante el inicio de sesión.
</ParamField>

<ParamField body="user" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Detalles del usuario activo, incluidos `username`, `email` y `roles`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Este método cambia el idioma de visualización de la página de Universal Login.

  ```typescript Example
  import LoginEmailVerification from '@auth0/auth0-acul-js/login-email-verification';
  const loginEmailVerificationManager = new LoginEmailVerification();
  loginEmailVerificationManager.changeLanguage({
    language: 'fr',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [LanguageChangeOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      El código de locale del idioma deseado (por ejemplo, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Cuando se establece en `'session'`, el idioma seleccionado se conserva durante toda la sesión.
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22; | &#x22;undefined&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="continueWithCode" type="Promise<void>">
  Este método envía el código de verificación introducido por el usuario.

  ```typescript Example
  import LoginEmailVerification from '@auth0/auth0-acul-js/login-email-verification';
  const loginEmailVerificationManager = new LoginEmailVerification();
  loginEmailVerificationManager.continueWithCode({
    code: '123456',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [ContinueWithCodeOptionPayload](/docs/es/libraries/acul/js-sdk/Screens/interfaces/ContinueWithCodeOptionPayload).
    </ParamField>

    <ParamField body="code" type="string" required>
      El código de verificación enviado al correo electrónico del usuario.
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22; | &#x22;undefined&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Este método obtiene el arreglo de errores de la transacción desde el contexto, o un arreglo vacío si no existe ninguno. Un arreglo de objetos de error del contexto de la transacción.
</ParamField>

<ParamField body="resendCode" type="Promise<void>">
  Este método solicita que se envíe un nuevo código de verificación a la dirección de correo electrónico del usuario.

  ```typescript Example
  import LoginEmailVerification from '@auth0/auth0-acul-js/login-email-verification';
  const loginEmailVerificationManager = new LoginEmailVerification();
  loginEmailVerificationManager.resendCode();
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [CustomOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/CustomOptions).
    </ParamField>

    <ParamField body="[`key`: `string`]" type="&#x22;string&#x22; | &#x22;number&#x22; | &#x22;boolean&#x22; | &#x22;undefined&#x22;">
      Campos personalizados adicionales con el prefijo `ulp-` (por ejemplo, `'ulp-custom-field'`).
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendManager" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/interfaces/ResendControl">ResendControl</a></span>}>
  Este método ofrece la funcionalidad de reenvío con gestión del tiempo de espera. Devuelve un objeto [`ResendControl`](/docs/es/libraries/acul/js-sdk/Screens/interfaces/ResendControl) con un método `startResend()` que inicia el reenvío y aplica un período de espera antes de que el usuario pueda volver a reenviar.

  ```typescript Example
  import LoginEmailVerification from '@auth0/auth0-acul-js/login-email-verification';

  const loginEmailVerificationManager = new LoginEmailVerification();
  const { startResend } = loginEmailVerificationManager.resendManager({
    timeoutSeconds: 15,
    onStatusChange: (remainingSeconds, isDisabled) => {
      console.log(`Resend available in ${remainingSeconds}s, disabled: ${isDisabled}`);
    },
    onTimeout: () => {
      console.log('Resend is now available');
    }
  });

  // Llama a startResend cuando el usuario haga clic en el botón de reenvío
  startResend();
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [StartResendOptions](/docs/es/libraries/acul/js-sdk/Screens/interfaces/StartResendOptions).
    </ParamField>

    <ParamField body="timeoutSeconds?" type="number">
      La duración del período de espera, en segundos, antes de que el usuario pueda volver a enviar el código.
    </ParamField>

    <ParamField body="onStatusChange?" type={<span><a href="/docs/es/libraries/acul/js-sdk/Screens/type-aliases/OnStatusChangeCallback">OnStatusChangeCallback</a></span>}>
      Devolución de llamada que se invoca cada vez que cambia el estado del reenvío; devuelve los segundos restantes e indica si la funcionalidad de reenvío está deshabilitada en ese momento.
    </ParamField>

    <ParamField body="onTimeout?" type="() => void">
      Devolución de llamada que se invoca cuando expira el período de espera y el reenvío vuelve a estar disponible.
    </ParamField>
  </Expandable>
</ParamField>