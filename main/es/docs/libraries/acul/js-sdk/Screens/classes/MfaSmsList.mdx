---
title: "MfaSmsList"
description: "Describe todas las propiedades y los métodos disponibles para personalizar la pantalla `mfa-sms-list` de Universal Login."
---

La clase MfaSmsList implementa la funcionalidad de la pantalla `mfa-sms-list`. Esta pantalla se muestra cuando el usuario necesita seleccionar un número de teléfono ya registrado para MFA mediante SMS.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/5WKzkwCtJn5ZViHcl9xATn/6147dac2ff98da4ab190922ced35a1f2/Screenshot_2025-02-21_at_12.29.36.png" alt="MfaSmsList" />
</Frame>

<div id="constructors">
  ## Constructores
</div>

Crea una instancia del gestor de la pantalla de lista de SMS de MFA:

```typescript Example
import MfaSmsList from '@auth0/auth0-acul-js/mfa-sms-list';

const mfaSmsList = new MfaSmsList();
await mfaSmsList.selectPhoneNumber({ index: 0 });
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Proporciona configuraciones de branding, como el tema de branding y otros ajustes.
</ParamField>

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `mfa-sms-list`.
</ParamField>

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Proporciona información sobre la organización del usuario, como `id` y `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contiene datos sobre el prompt actual dentro del flujo de autenticación.
</ParamField>

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembers">ScreenMembers</a></span>}>
  Contiene detalles específicos de la pantalla `mfa-sms-list`, incluida su configuración y contexto.
</ParamField>

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contiene datos relacionados con el tenant, como `id` y los metadatos asociados.
</ParamField>

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Proporciona datos específicos de la transacción para la pantalla `mfa-sms-list`, como identificadores activos y estados del flujo.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Gestiona datos no confiables que se pasan al SDK, como la entrada del usuario durante la selección en la lista SMS de MFA.
</ParamField>

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Detalles del usuario activo, incluidos `username`, `email` y `roles`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="backAction" type="Promise<void>">
  Este método vuelve a la pantalla anterior.

  ```typescript Example
  import MfaSmsList from '@auth0/auth0-acul-js/mfa-sms-list';

  const mfaSmsList = new MfaSmsList();
  await mfaSmsList.backAction();
  ```
</ParamField>

<ParamField body="changeLanguage" type="Promise<void>">
  Este método cambia el idioma de visualización de la página de Universal Login.

  ```typescript Example
  import MfaSmsList from '@auth0/auth0-acul-js/mfa-sms-list';

  const mfaSmsList = new MfaSmsList();
  mfaSmsList.changeLanguage({
    language: 'fr',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [LanguageChangeOptions](/es/docs/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      El código de configuración regional del idioma deseado (por ejemplo, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Cuando se establece en `'session'`, el idioma seleccionado se conserva durante la sesión.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Este método recupera del contexto el arreglo de errores de la transacción, o un arreglo vacío si no existe ninguno.
</ParamField>

<ParamField body="selectPhoneNumber" type="Promise<void>">
  Este método selecciona un número de teléfono de la lista de números de teléfono registrados.

  ```typescript Example
  import MfaSmsList from '@auth0/auth0-acul-js/mfa-sms-list';

  const mfaSmsList = new MfaSmsList();
  await mfaSmsList.selectPhoneNumber({ index: 0 });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [MfaSmsListOptions](/es/docs/libraries/acul/js-sdk/Screens/interfaces/MfaSmsListOptions).
    </ParamField>

    <ParamField body="index" type="number" required>
      El índice del número de teléfono que se va a seleccionar.
    </ParamField>
  </Expandable>
</ParamField>