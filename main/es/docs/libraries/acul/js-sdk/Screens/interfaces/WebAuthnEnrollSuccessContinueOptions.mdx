---
title: "WebAuthnEnrollSuccessContinueOptions"
---

ContinueOptions

```ts Example
export interface ContinueOptions extends CustomOptions {
  // No se requieren propiedades específicas de pantalla para la Action 'continue' más allá de CustomOptions.
  // El servidor utiliza `action: "default"` para esta operación.
}
```