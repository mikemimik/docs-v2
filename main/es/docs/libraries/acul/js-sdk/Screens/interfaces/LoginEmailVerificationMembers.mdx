---
title: "LoginEmailVerificationMembers"
---

LoginEmailVerificationMembers
description Define los miembros (propiedades y métodos) disponibles para interactuar con la pantalla de verificación de Email durante el login.
Esta pantalla es una parte fundamental de los procesos de verificación de email, normalmente durante el login, en los que un usuario debe demostrar
que es titular de una dirección de email proporcionando un código de un solo uso. El SDK facilita el envío de este código
o la solicitud de uno nuevo.

El `universal_login_context` para esta pantalla (`window.universal_login_context`) contendrá:

* `client`: Información sobre la aplicación de Auth0.
* `organization` (opcional): Detalles si la autenticación es para una organización específica.
* `prompt`: Contexto del prompt actual de autenticación (p. ej., &#39;login&#39;).
* `screen`: Textos de la IU e información general de la pantalla. No se define ningún campo `data` específico de esta pantalla de forma exclusiva para `login-email-verification`, más allá de los estándar.
* `transaction`: Detalles de la transacción en curso, incluido `state` y cualquier error de intentos anteriores (p. ej., &quot;invalid-code&quot;).

````ts Example
export interface LoginEmailVerificationMembers extends BaseMembers {
  /**
   * Envía el código de verificación de correo electrónico introducido por el usuario.
   * Esta acción corresponde al usuario introduciendo el código que recibió por correo electrónico y
   * haciendo clic en un botón "Continuar" o "Verificar". El SDK realizará entonces un POST de este código
   * al endpoint `/u/login-email-verification` de Auth0.
   *
   * Si el código es válido, Auth0 normalmente redirigirá al usuario al siguiente paso en el
   * flujo de autenticación. Si el código no es válido, ha expirado o se produce otro error,
   * Auth0 generalmente volverá a renderizar la pantalla login-email-verification, y el
   * array `transaction.errors` en el contexto del SDK se actualizará con detalles
   * sobre el fallo (p. ej., código de error `invalid-code`).
   *
   * @param {ContinueWithCodeOptions} payload - Un objeto que contiene la cadena `code` introducida por el usuario.
   *                                            También puede incluir cualquier `CustomOptions` para extensibilidad.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se inicia el envío del formulario.
   *                          No devuelve datos directamente al resolverse, ya que una redirección o
   *                          un re-renderizado de la página es el resultado habitual.
   * @throws {Error} Lanza un error si `payload.code` no se proporciona o no es una cadena de texto,
   *                 o si el `FormHandler` encuentra un problema irrecuperable durante el envío (p. ej., error de red).
   *                 Los errores de validación de Auth0 (como un código inválido) no se lanzan como errores de JavaScript,
   *                 sino que se reflejan en `this.transaction.errors` tras la operación.
   *
   * @example
   * ```typescript
   * // Se asume que 'manager' es una instancia de LoginEmailVerification
   * const userInputCode = "123456";
   * try {
   *   await manager.continueWithCode({ code: userInputCode });
   *   // Si tiene éxito, la página redirige. No se requiere ninguna acción adicional del lado del cliente aquí.
   * } catch (error) {
   *   // Este catch es para errores inesperados, no para errores de validación de Auth0.
   *   console.error("Failed to submit the verification code:", error);
   * }
   * // Tras el await, comprueba siempre manager.transaction.errors para detectar problemas de validación del servidor.
   * if (manager.transaction.errors && manager.transaction.errors.length > 0) {
   *   manager.transaction.errors.forEach(err => {
   *     if (err.code === 'invalid-code') {
   *       // Mostrar "El código que introdujiste no es válido" al usuario.
   *     }
   *   });
   * }
   * ```
   */
  continueWithCode(payload: ContinueWithCodeOptions): Promise<void>;

  /**
   * Solicita que se envíe un nuevo código de verificación a la dirección de correo electrónico del usuario.
   * Esta acción se invoca normalmente cuando el usuario hace clic en un botón "Reenviar código", quizás porque
   * no recibió el primer correo, el código expiró o sospecha que hay algún problema.
   * El SDK realizará un POST al endpoint `/u/login-email-verification` de Auth0 con una acción que indica
   * una solicitud de reenvío.
   *
   * Tras el envío exitoso de esta solicitud, Auth0 intenta enviar un nuevo correo electrónico.
   * La página puede volver a renderizarse. Si hay problemas (p. ej., demasiados intentos de reenvío para el mismo correo,
   * identificados por el código de error `too-many-emails`), `transaction.errors` se actualizará.
   *
   * @param {ResendCodeOptions} [payload] - Opcional. Un objeto para `CustomOptions` si se necesita enviar
   *                                        parámetros adicionales con la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se inicia la solicitud de reenvío.
   *                          Al igual que `continueWithCode`, esto generalmente resulta en un re-renderizado de la página o una actualización de estado,
   *                          en lugar de datos directos en la resolución de la promesa.
   * @throws {Error} Lanza un error si el `FormHandler` encuentra un problema irrecuperable (p. ej., error de red).
   *                 Los errores del lado del servidor (como los límites de frecuencia) se reflejan en `this.transaction.errors`.
   *
   * @example
   * ```typescript
   * // Se asume que 'manager' es una instancia de LoginEmailVerification
   * try {
   *   await manager.resendCode();
   *   // Opcionalmente, actualiza la UI para informar al usuario de que se ha enviado un nuevo código.
   *   alert("A new verification code has been dispatched to your email.");
   * } catch (error) {
   *   console.error("Failed to request a new code:", error);
   * }
   * // Tras el await, comprueba manager.transaction.errors para detectar problemas del lado del servidor.
   * if (manager.transaction.errors && manager.transaction.errors.length > 0) {
   *   manager.transaction.errors.forEach(err => {
   *     if (err.code === 'too-many-emails') {
   *       // Mostrar "Has solicitado demasiados correos. Por favor, espera unos minutos."
   *     }
   *   });
   * }
   * ```
   */
  resendCode(payload?: ResendCodeOptions): Promise<void>;

  /**
   * Obtiene la funcionalidad de reenvío con gestión de tiempo de espera para esta pantalla
   * @param options Opciones de configuración para la funcionalidad de reenvío
   */
  resendManager(options?: StartResendOptions): ResendControl;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembers">ScreenMembers</a></span>} />

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="continueWithCode" type="Promise<void>">
  Envía el código de verificación de correo electrónico introducido por el usuario.
  Esta acción corresponde a cuando el usuario introduce el código que recibió por correo electrónico y
  hace clic en un botón &quot;Continuar&quot; o &quot;Verificar&quot;. Luego, el SDK hará un POST con este código
  al endpoint `/u/login-email-verification` de Auth0.

  Si el código es válido, Auth0 normalmente redirigirá al usuario al siguiente paso del
  flujo de autenticación. Si el código no es válido, ha caducado o se produce otro error,
  Auth0 normalmente volverá a renderizar la pantalla login-email-verification, y el
  array `transaction.errors` en el contexto del SDK se actualizará con detalles
  sobre el error (p. ej., el código de error `invalid-code`).

  Una promesa que se resuelve cuando se inicia el envío del formulario.
  No devuelve datos directamente al resolverse, ya que una redirección o el
  volver a renderizar la página es el resultado habitual.

  #### Genera

  Genera un error si no se proporciona `payload.code` o no es una cadena,
  o si `FormHandler` encuentra un problema irrecuperable durante el envío (p. ej., un error de red).
  Los errores de validación de Auth0 (como un código no válido) no se generan como errores de JavaScript,
  sino que se reflejan en `this.transaction.errors` después de la operación.

  ```typescript Example
  // Suponiendo que 'manager' es una instancia de LoginEmailVerification
  const userInputCode = "123456";
  try {
    await manager.continueWithCode({ code: userInputCode });
    // Si se realiza correctamente, la página redirige. Aquí no se necesita ninguna otra acción del lado del cliente.
  } catch (error) {
    // Este catch es para errores inesperados, no para errores de validación de Auth0.
    console.error("Failed to submit the verification code:", error);
  }
  // Después del await, comprueba siempre manager.transaction.errors para detectar problemas de validación del lado del servidor.
  if (manager.transaction.errors && manager.transaction.errors.length > 0) {
    manager.transaction.errors.forEach(err => {
      if (err.code === 'invalid-code') {
        // Muestra "The code you entered is invalid" al usuario.
      }
    });
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ContinueWithCodeOptionPayload">ContinueWithCodeOptionPayload</a></span>}>
      Un objeto que contiene la cadena `code` introducida por el usuario.
      También puede incluir cualquier `CustomOptions` para la extensibilidad.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendCode" type="Promise<void>">
  Solicita que se envíe un nuevo código de verificación a la dirección de correo electrónico del usuario.
  Esta acción normalmente se invoca cuando el usuario hace clic en un botón &quot;Reenviar código&quot;, quizá porque
  no recibió el primer correo electrónico, el código caducó o sospecha que hay algún problema.
  El SDK hará un POST al endpoint `/u/login-email-verification` de Auth0 con una acción que indica
  una solicitud de reenvío.

  Tras enviar correctamente esta solicitud, Auth0 intenta enviar un nuevo correo electrónico.
  La página puede volver a renderizarse. Si hay problemas (p. ej., demasiados intentos de reenvío para el mismo correo electrónico,
  identificados por el código de error `too-many-emails`), `transaction.errors` se actualizará.

  Una promesa que se resuelve cuando se inicia la solicitud de reenvío.
  Al igual que `continueWithCode`, esto normalmente da como resultado un nuevo renderizado de la página o una actualización de estado
  en lugar de datos directos en la resolución de la promesa.

  #### Genera

  Genera un error si `FormHandler` encuentra un problema irrecuperable (p. ej., un error de red).
  Los errores del lado del servidor (como los límites de frecuencia) se reflejan en `this.transaction.errors`.

  ```typescript Example
  // Suponiendo que 'manager' es una instancia de LoginEmailVerification
  try {
    await manager.resendCode();
    // Opcionalmente, actualiza la UI para informar al usuario de que se ha enviado un nuevo código.
    alert("A new verification code has been dispatched to your email.");
  } catch (error) {
    console.error("Failed to request a new code:", error);
  }
  // Después del await, comprueba manager.transaction.errors para detectar problemas del lado del servidor.
  if (manager.transaction.errors && manager.transaction.errors.length > 0) {
    manager.transaction.errors.forEach(err => {
      if (err.code === 'too-many-emails') {
        // Muestra "You have requested too many emails. Please wait a few minutes."
      }
    });
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ResendCodeOptionsPayload">ResendCodeOptionsPayload</a></span>}>
      Opcional. Un objeto para `CustomOptions` si es necesario enviar
      parámetros adicionales con la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="resendManager" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ResendControl">ResendControl</a></span>}>
  Obtiene la funcionalidad de reenvío con control del tiempo de espera para esta pantalla

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/StartResendOptions">StartResendOptions</a></span>}>
      Opciones de configuración para la funcionalidad de reenvío

      **Propiedades**

      <ParamField body="onStatusChange?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/type-aliases/OnStatusChangeCallback">OnStatusChangeCallback</a></span>} />

      <ParamField body="onTimeout" type="void" />

      <ParamField body="timeoutSeconds?" type="number" />
    </ParamField>
  </Expandable>
</ParamField>