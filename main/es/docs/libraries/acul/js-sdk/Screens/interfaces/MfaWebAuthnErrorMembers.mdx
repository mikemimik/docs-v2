---
title: "MfaWebAuthnErrorMembers"
---

MfaWebAuthnErrorMembers

````ts Example
export interface MfaWebAuthnErrorMembers extends BaseMembers {
  /**
   * El objeto de pantalla con la estructura de datos específica de MFA WebAuthn Error.
   */
  screen: ScreenMembersOnMfaWebAuthnError;
  
  /**
   * Permite al usuario volver a intentar la operación WebAuthn anterior.
   * Por lo general, redirige al usuario a la pantalla donde se inició la operación WebAuthn.
   *
   * @param {CustomOptions} [payload] - Opciones personalizadas opcionales para incluir en la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la acción se envía correctamente.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
   * await sdk.tryAgain();
   * ```
   */
  tryAgain(payload?: CustomOptions): Promise<void>;

  /**
   * Permite al usuario optar por la autenticación mediante contraseña como alternativa, si está configurada.
   * Por lo general, está disponible si la autenticación principal o un paso anterior involucró una contraseña.
   *
   * @param {CustomOptions} [payload] - Opciones personalizadas opcionales para incluir en la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la acción se envía correctamente.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
   * await sdk.usePassword();
   * ```
   */
  usePassword(payload?: CustomOptions): Promise<void>;

  /**
   * Permite al usuario seleccionar un método de MFA diferente si hay otros factores registrados o disponibles.
   * Dirige al usuario a una pantalla de selección de autenticador.
   *
   * @param {CustomOptions} [payload] - Opciones personalizadas opcionales para incluir en la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la acción se envía correctamente.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
   * await sdk.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: CustomOptions): Promise<void>;

  /**
   * Permite al usuario rechazar la adición de un dispositivo o la finalización del paso WebAuthn, declinando así el flujo WebAuthn actual.
   * Esta acción se utiliza normalmente en escenarios de registro donde el usuario decide no continuar.
   *
   * @param {CustomOptions} [payload] - Opciones personalizadas opcionales para incluir en la solicitud.
   * @returns {Promise<void>} Una promesa que se resuelve cuando la acción se envía correctamente.
   * @throws {Error} Lanza un error si el envío falla.
   * @example
   * ```typescript
   * // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
   * await sdk.noThanks();
   * ```
   */
  noThanks(payload?: CustomOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnError">ScreenMembersOnMfaWebAuthnError</a></span>}>
  El objeto de pantalla con la estructura de datos específica para el error de WebAuthn de MFA.
</ParamField>

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="noThanks" type="Promise<void>">
  Permite al usuario rechazar añadir un dispositivo o completar el paso de WebAuthn, lo que equivale a rechazar el flujo actual de WebAuthn.
  Esta acción suele usarse en escenarios de registro en los que el usuario decide no continuar.

  Una promesa que se resuelve cuando la acción se envía correctamente.

  #### Lanza

  Lanza un error si el envío falla.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
  await sdk.noThanks();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAgain" type="Promise<void>">
  Permite al usuario volver a intentar la operación anterior de WebAuthn.
  Normalmente, esto redirige al usuario a la pantalla donde se inició la operación de WebAuthn.

  Una promesa que se resuelve cuando la acción se envía correctamente.

  #### Lanza

  Lanza un error si el envío falla.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
  await sdk.tryAgain();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permite al usuario seleccionar otro método de MFA si hay otros factores registrados o disponibles.
  Esto lleva al usuario a una pantalla de selección de autenticador.

  Una promesa que se resuelve cuando la acción se envía correctamente.

  #### Lanza

  Lanza un error si el envío falla.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
  await sdk.tryAnotherMethod();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="usePassword" type="Promise<void>">
  Permite al usuario optar por la autenticación con contraseña como alternativa, si está configurada.
  Normalmente, esta opción está disponible si la autenticación principal o un paso anterior implicaba una contraseña.

  Una promesa que se resuelve cuando la acción se envía correctamente.

  #### Lanza

  Lanza un error si el envío falla.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnError
  await sdk.usePassword();
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      Opciones personalizadas opcionales para incluir en la solicitud.
    </ParamField>
  </Expandable>
</ParamField>