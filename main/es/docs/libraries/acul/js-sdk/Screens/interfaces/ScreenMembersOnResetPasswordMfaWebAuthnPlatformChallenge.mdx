---
title: "ScreenMembersOnResetPasswordMfaWebAuthnPlatformChallenge"
---

ScreenMembersOnResetPasswordMfaWebAuthnPlatformChallenge

```ts Example
export interface ScreenMembersOnResetPasswordMfaWebAuthnPlatformChallenge extends ScreenMembers {
  /**
   * Getter de conveniencia para las opciones de solicitud de credencial de clave pública (desafío) necesarias para WebAuthn.
   * Se deriva de `screen.data.passkey.public_key`.
   * Si `screen.data.passkey` o `screen.data.passkey.public_key` no está disponible, será `null`.
   * @type {PasskeyRead['public_key'] | null}
   */
  publicKey: PasskeyRead['public_key'] | null;

  /**
   * Getter de conveniencia para determinar si se debe mostrar la opción "Recordar este dispositivo".
   * Se deriva de `screen.data.show_remember_device`. El valor predeterminado es `false` si no se especifica.
   * @type {boolean}
   */
  showRememberDevice: boolean;
}
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="captcha" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PasskeyCreate">PasskeyCreate</a></span>}>
  Datos específicos de la pantalla.
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="publicKey" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/AllowCredential">AllowCredential</a></span>}>
  Un acceso directo a `data.passkey.public_key`.
  Proporciona el desafío y otros parámetros para la
  llamada a la API de WebAuthn `navigator.credentials.get()`.
</ParamField>

<ParamField body="showRememberDevice" type="boolean">
  Un acceso directo a `data.show_remember_device`.
  Indica si debe mostrarse la opción &quot;Recordar este dispositivo&quot;.
  De forma predeterminada, es `false` si no está presente en el contexto.
</ParamField>

<ParamField body="texts" type="Record" />