---
description: Aprende a crear Organizations con Auth0 Dashboard y Management API.
title: Crear Organizations
---

import {AuthCodeBlock} from "/snippets/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/AuthCodeGroup.jsx";

Puedes crear [organizaciones](/es/docs/manage-users/organizations/organizations-overview) desde el <Tooltip tip="Auth0 Dashboard: el producto principal de Auth0 para configurar tus servicios." cta="Ver glosario" href="/es/docs/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip> o la <Tooltip tip="Auth0 Dashboard: el producto principal de Auth0 para configurar tus servicios." cta="Ver glosario" href="/es/docs/glossary?term=Management+API">Management API</Tooltip>.

<Card title="La disponibilidad varía según el plan de Auth0">
  Tu plan de Auth0 o tu acuerdo personalizado determina si esta función está disponible. Para obtener más información, consulta [Precios](https://auth0.com/pricing).
</Card>

<div id="auth0-dashboard">
  ## Auth0 Dashboard
</div>

Para crear una organización desde el Auth0 Dashboard:

1. Vaya a [Auth0 Dashboard &gt; Organizations](https://manage.auth0.com/#/organizations).
2. Seleccione **Create Organization**.
3. Introduzca la información básica de su organización y seleccione **Add Organization**:

   | **Campo**        | **Descripción**                                                                                                                                                                                                                                                                                                                                                           |
   | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   | **Name**         | Nombre de la organización que desea crear. Este es el nombre que un usuario final escribiría en el prompt de pre-login para identificar a través de qué organización quiere iniciar sesión. Identificador lógico único. Puede contener letras minúsculas, números, guiones bajos (`_`) y guiones (`-`). Puede comenzar con un número. Debe tener entre 1 y 50 caracteres. |
   | **Display Name** | Nombre descriptivo que se mostrará.                                                                                                                                                                                                                                                                                                                                       |
4. Busque la sección **Branding** y personalice su organización; luego, seleccione **Save changes**:

   | **Campo**                 | **Descripción**                                                                                              |
   | ------------------------- | ------------------------------------------------------------------------------------------------------------ |
   | **Organization Logo**     | Logotipo que se mostrará. La resolución mínima recomendada es de 200 píxeles (ancho) por 200 píxeles (alto). |
   | **Primary Color**         | Color de los elementos principales.                                                                          |
   | **Page Background Color** | Color de fondo.                                                                                              |
5. Busque la sección **Metadata** y añada a la organización los pares clave-valor de metadatos necesarios; luego, seleccione **Add**.

<div id="management-api">
  ## Management API
</div>

Para crear una organización mediante la Management API:

Haz una solicitud `POST` al endpoint `Create Organizations`. Asegúrate de reemplazar los valores de marcador de posición por los valores correspondientes de tu tenant. Para obtener más detalles, consulta la tabla de parámetros a continuación.

<AuthCodeGroup>
  ```bash cURL
  curl --request POST \
    --url 'https://{yourDomain}/api/v2/organizations' \
    --header 'authorization: Bearer {MGMT_API_ACCESS_TOKEN}' \
    --header 'cache-control: no-cache' \
    --header 'content-type: application/json' \
    --data '{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations");
  var request = new RestRequest(Method.POST);
  request.AddHeader("content-type", "application/json");
  request.AddHeader("authorization", "Bearer {MGMT_API_ACCESS_TOKEN}");
  request.AddHeader("cache-control", "no-cache");
  request.AddParameter("application/json", "{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }", ParameterType.RequestBody);
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"strings"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations"

  	payload := strings.NewReader("{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }")

  	req, _ := http.NewRequest("POST", url, payload)

  	req.Header.Add("content-type", "application/json")
  	req.Header.Add("authorization", "Bearer {MGMT_API_ACCESS_TOKEN}")
  	req.Header.Add("cache-control", "no-cache")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.post("https://{yourDomain}/api/v2/organizations")
    .header("content-type", "application/json")
    .header("authorization", "Bearer {MGMT_API_ACCESS_TOKEN}")
    .header("cache-control", "no-cache")
    .body("{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'POST',
    url: 'https://{yourDomain}/api/v2/organizations',
    headers: {
      'content-type': 'application/json',
      authorization: 'Bearer {MGMT_API_ACCESS_TOKEN}',
      'cache-control': 'no-cache'
    },
    data: '{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }'
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "POST",
    CURLOPT_POSTFIELDS => "{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer {MGMT_API_ACCESS_TOKEN}",
      "cache-control: no-cache",
      "content-type: application/json"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  payload = "{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }"

  headers = {
      'content-type': "application/json",
      'authorization': "Bearer {MGMT_API_ACCESS_TOKEN}",
      'cache-control': "no-cache"
      }

  conn.request("POST", "/{yourDomain}/api/v2/organizations", payload, headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Post.new(url)
  request["content-type"] = 'application/json'
  request["authorization"] = 'Bearer {MGMT_API_ACCESS_TOKEN}'
  request["cache-control"] = 'no-cache'
  request.body = "{ "name": "ORG_NAME", "display_name": "ORG_DISPLAY_NAME", "branding": [ { "logo_url": "{orgLogo}", "colors": [ { "primary": "{orgPrimaryColor}", "page_background": "{orgPageBackground}" } ] } ], "metadata": [ { "{key}": "{value}", "{key}": "{value}", "{key}": "{value}" } ] }, "enabled_connections": [ { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" }, { "connection_id": "{connectionId}", "assign_membership_on_login": "{assignMembershipOption}" } ] }"

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Encuentra tu dominio de Auth0**

  Tu dominio de Auth0 está formado por el nombre de tu tenant, tu subdominio regional (a menos que tu tenant esté en la región de EE. UU. y se haya creado antes de junio de 2020) y `.auth0.com`. Por ejemplo, si el nombre de tu tenant fuera `travel0`, tu dominio de Auth0 sería `travel0.us.auth0.com`. (Si tu tenant estuviera en EE. UU. y se hubiera creado antes de junio de 2020, entonces tu nombre de dominio sería `https://travel0.auth0.com`).

  Si usas dominios personalizados, este debe ser el nombre de tu dominio personalizado.
</Callout>

| **Valor**                  | **Descripción**                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MGMT_API_ACCESS_TOKEN`    | [Token de acceso para la Management API](/es/docs/secure/tokens/access-tokens/management-api-access-tokens) con el scope `create:organizations`.                                                                                                                                                                                                                                     |
| `ORG_NAME`                 | Nombre de la organización que quieres crear. Este es el nombre que un usuario final escribiría en el prompt de pre-login para identificar a través de qué organización quiere iniciar sesión. Es un identificador lógico único. Puede contener letras minúsculas, números, guiones bajos (`_`) y guiones (`-`). Puede comenzar con un número. Debe tener entre 1 y 50 caracteres. |
| `ORG_DISPLAY_NAME`         | Opcional. Nombre descriptivo de la organización que puede mostrarse en el flujo de inicio de sesión y en las plantillas de correo electrónico.                                                                                                                                                                                                                                    |
| `ORG_LOGO`                 | Opcional. URL del logotipo de la organización.                                                                                                                                                                                                                                                                                                                                    |
| `ORG_PRIMARY_COLOR`        | Opcional. Código de color HEX para los elementos principales.                                                                                                                                                                                                                                                                                                                     |
| `ORG_BACKGROUND_COLOR`     | Opcional. Código de color HEX para el fondo.                                                                                                                                                                                                                                                                                                                                      |
| `KEY`/`VALUE`              | Opcional. Pares clave/valor de tipo String que representan los metadatos de la organización. Máximo de 255 caracteres cada uno. Máximo de 25 pares de metadatos.                                                                                                                                                                                                                  |
| `CONNECTION_ID`            | Opcional. ID de la conexión que quieres habilitar para la organización especificada. Las conexiones habilitadas se muestran en el prompt de inicio de sesión de la organización para que los usuarios puedan acceder a tu(s) aplicación(es) a través de ellas.                                                                                                                    |
| `ASSIGN_MEMBERSHIP_OPTION` | Opcional. Indica si quieres que a los usuarios que inicien sesión con la conexión habilitada se les otorgue automáticamente membresía en la organización especificada. Cuando se establece en `true`, a los usuarios se les otorgará membresía automáticamente. Cuando se establece en `false`, no se les otorgará membresía automáticamente.                                     |

<div id="response-status-codes">
  ### Códigos de estado de la respuesta
</div>

Los posibles códigos de estado de la respuesta son los siguientes:

| **Código de estado** | **Código de error**     | **Mensaje**                                                                                                  | **Causa**                                                                                                |
| -------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| `201`                |                         | Organización creada correctamente.                                                                           |                                                                                                          |
| `400`                | `invalid_body`          | Cuerpo de la solicitud no válido. El mensaje variará según la causa.                                         | El payload de la solicitud no es válido.                                                                 |
| `400`                | `invalid_query_string`  | Query string de la solicitud no válido. El mensaje variará según la causa.                                   | El query string no es válido.                                                                            |
| `401`                |                         | Token no válido.                                                                                             |                                                                                                          |
| `401`                |                         | Se recibió una firma no válida para la validación de JSON Web Token.                                         |                                                                                                          |
| `401`                |                         | El cliente no es global.                                                                                     |                                                                                                          |
| `403`                | `insufficient_scope`    | Scope insuficiente; se esperaba alguno de los siguientes: `create:organizations`.                            | Se intentó leer o escribir un campo que no está permitido con los scopes del bearer token proporcionado. |
| `409`                | `organization_conflict` | Ya existe una organización con el mismo nombre.                                                              | Ya existe una organización con el mismo nombre.                                                          |
| `429`                |                         | Demasiadas solicitudes. Revisa los encabezados X-RateLimit-Limit, X-RateLimit-Remaining y X-RateLimit-Reset. |                                                                                                          |