---
description: Describe cómo configurar la duración de la sesión de un tenant con Auth0 Dashboard, la Management API y Actions.
title: Configurar la duración de la sesión
---

Para configurar la duración de una sesión, puedes usar el [Auth0 Dashboard](/es/docs/get-started/auth0-overview/dashboard), la [Management API](https://auth0.com/docs/api/management/v2) o una [Post Login Action](/es/docs/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger).

<div id="auth0-dashboard">
  ## Auth0 Dashboard
</div>

Para configurar la duración de la sesión en Auth0 Dashboard:

1. Ve a **Dashboard &gt; Configuración del tenant** y selecciona la vista **Advanced**.

2. En **Expiración de la sesión**, puedes configurar lo siguiente:

| Política de sesión                                  | Descripción                                                                                              |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| **Duración de la sesión inactiva (Persistente)**    | Tiempo máximo (en minutos) de inactividad antes de que expire una sesión persistente.                    |
| **Duración de la sesión inactiva (No persistente)** | Tiempo máximo (en minutos) de inactividad antes de que expire una sesión no persistente.                 |
| **Duración máxima de la sesión (Persistente)**      | Tiempo máximo (en minutos) que puede durar una sesión persistente, incluso si el usuario está activo.    |
| **Duración máxima de la sesión (No persistente)**   | Tiempo máximo (en minutos) que puede durar una sesión no persistente, incluso si el usuario está activo. |

<Frame>![Dashboard > tenant > Configuración > Advanced > Expiración de la sesión](/docs/images/cdy7uua7fh8z/session-lifetime/Dashboard-Tenant-Settings-Advanced-Session-expiration.png)</Frame>

<div id="auth0-management-api">
  ## Auth0 Management API
</div>

Para configurar la duración de la sesión con la Auth0 Management API:

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Las llamadas al endpoint `/api/v2/tenants/settings` requieren un [token de acceso de la Management API](/es/docs/secure/tokens/access-tokens) con el scope `update:tenant_settings`.
</Callout>

Realiza una solicitud `PATCH` al endpoint [/api/v2/tenants/settings](/es/docs/api/management/v2/tenants/patch-settings):

```bash cURL
curl --request PATCH \
  --url 'https://<your-domain>/api/v2/tenants/settings' \
  --header 'Authorization: Bearer MGMT_API_ACCESS_TOKEN' \
  --header 'Content-Type: application/json' \
  --data '{\
    "session_lifetime": SESSION_LIFETIME_VALUE,\
    "idle_session_lifetime": IDLE_SESSION_LIFETIME_VALUE,\
    "ephemeral_session_lifetime": EPHEMERAL_SESSION_LIFETIME_VALUE,\
    "idle_ephemeral_session_lifetime": EPHEMERAL_IDLE_SESSION_LIFETIME_VALUE\
  }'
```

| Parámetro                         | Descripción                                                                |
| --------------------------------- | -------------------------------------------------------------------------- |
| `session_lifetime`                | Duración máxima (en horas) del tiempo de espera absoluto.                  |
| `idle_session_lifetime`           | Duración máxima (en horas) antes de que una sesión expire por inactividad. |
| `ephemeral_session_lifetime`      | Duración máxima (en horas) del tiempo de espera absoluto.                  |
| `idle_ephemeral_session_lifetime` | Duración máxima (en horas) antes de que una sesión expire por inactividad. |

<div id="auth0-post-login-actions">
  ## Auth0 Actions de Post Login
</div>

Puede configurar dinámicamente el comportamiento de la sesión mediante métodos de `api.session` con una [Action de Post Login](/es/docs/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger). Esto le permite anular la configuración predeterminada de sesión del tenant en cada inicio de sesión, según la lógica específica del usuario o del contexto.

Los casos de uso incluyen:

* Acortar los tiempos de espera para inicios de sesión de alto riesgo

* Extender los tiempos de espera para usuarios u organizaciones de confianza

* Ajustar la persistencia de la cookie según el tipo de aplicación

Los métodos de `api.session` disponibles para configurar la duración de la sesión son:

* `api.session.setExpiresAt`

* `api.session.setIdleExpiresAt`

* `api.session.setCookieMode`

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Los métodos de `api.session` solo se aplican a la sesión actual y deben llamarse durante la transacción de inicio de sesión. Estos métodos requieren que la propiedad `event.session.id` del evento esté disponible.
</Callout>

Para obtener más información, consulte [Sesiones con Actions](/es/docs/manage-users/sessions/manage-sessions-actions).